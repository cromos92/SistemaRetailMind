Django==4.2.2
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
gunicorn==21.2.0
whitenoise==6.5.0
dj-database-url==2.1.0
python-dotenv==1.1.1
PyJWT==2.3.0
psycopg2-binary==2.9.10
pytz==2024.1
tzdata==2023.3
Pillow==11.1.0
openpyxl==3.1.5
reportlab==4.4.3
num2words==0.5.14
# Evidencia de requerimientos en DigitalOcean Spaces: el disco del contenedor
# es efímero y las fotos se perdían en cada deploy. Solo se activa si están
# definidas las env vars SPACES_* (si no, sigue guardando en MEDIA_ROOT).
django-storages==1.14.4
boto3==1.35.36
transbank-pos-sdk==1.0.1
pyserial==3.5
websockets==11.0.3
requests==2.30.0

# Dependencias solo de dev/migración (mysql-connector-python, statsmodels,
# numpy) se movieron a requirements-dev.txt para no cargarlas en runtime
# del servidor que atiende el POS.
