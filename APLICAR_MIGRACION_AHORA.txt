===============================================
EJECUTAR MIGRACIÓN - INSTRUCCIONES EXACTAS
===============================================

La migración YA EXISTE (0044_historialcambioprecio.py)
Solo falta APLICARLA a la base de datos.

PASO 1: Asegúrate de estar en el directorio correcto
C:\DjangoProyects\retailmind\SistemaRetailMind\retailmind

PASO 2: Ejecuta EXACTAMENTE esto (copia y pega):

venv\Scripts\python.exe manage.py migrate

O si tienes venv activado:

python manage.py migrate

O:

py manage.py migrate

(Sin el .\)

===============================================
SALIDA ESPERADA:
===============================================

Operations to perform:
  Apply all migrations: admin, app, auth, contenttypes, empresa_management, sessions, users
Running migrations:
  Applying app.0044_historialcambioprecio... OK

===============================================
DESPUÉS DE EJECUTAR:
===============================================

1. Reiniciar servidor Django (Ctrl+C y volver a correr)

2. Ir a: http://localhost:8000/app/gestion-precios/edicion-rapida/

3. Buscar productos - AHORA SIN ERROR

4. Probar edición rápida

5. ✓ Todo funcional

===============================================
SI SIGUE SIN FUNCIONAR:
===============================================

Verifica el archivo de migración existe:
C:\DjangoProyects\retailmind\SistemaRetailMind\retailmind\app\migrations\0044_historialcambioprecio.py

Si existe → Solo ejecuta: python manage.py migrate
Si NO existe → Hay un problema con el guardado del modelo

