## Familia: VENTAS

### Ventas por Sucursal / Vendedor (con comparativa mensual y drill-down) — `MEJORABLE`

- **Ruta**: /app/reportes/ventas-sucursal/ → app/views_modulo_reportes.py:63 ver_reporte_ventas_sucursal; APIs: app/views_modulo_reportes.py:84 obtener_ventas_por_vendedor_reporte, app/views_modulo_reportes.py:1149 obtener_ventas_por_sucursal_reporte
- **Mide**: Facturación histórica del período (mes / día / rango) partida en dos pestañas: por vendedor y por sucursal. Suma SOLO DTEs (Dte.monto_con_iva) con tipo_transaccion en VENTA/VENTA_PUBLICO y resta todas las NC (DEVOLUCION+ANULACION) emitidas en el mismo rango. Entrega ventas netas, brutas, descuentos, IVA, documentos, UPT, %descuento, participación y ticket promedio.
- **Filtros**: Ofrece: mes (YYYY-MM), fecha puntual, rango fecha_inicio/fecha_fin, sucursal y vendedor. TODOS se aplican server-side: las fechas contra Dte.fecha_emision (líneas 1188-1204), el vendedor con .filter(vendedor_id=) (línea 134) y la sucursal vía filtrar_queryset_por_sucursal, que lee request.GET['sucursal_id'] (app/utils_permisos.py:220). NO hay buscador de texto libre en este reporte (verificado: no existe ningún input type=text en reporte_ventas_sucursal.html), así que no promete nada que no cumpla. OJO con el fallback silencioso de utils_permisos.py:228-233: si el usuario pide una sucursal a la que no tiene acceso, en vez de error se le devuelven los datos de la sucursal de su sesión — el título dice una sucursal y los números son de otra.
- **Fidelidad**: Fecha: usa Dte.fecha_emision (DateField), correcto para un reporte de facturación — este reporte NO toca Ticket.fecha, así que no le aplica el bug de auto_now. Anulados: los INCLUYE a propósito (estado_dte in EMITIDO/ACEPTADO/ANULADO, línea 1213) porque es facturación histórica, no caja; está documentado y es defendible, pero significa que NUNCA cuadra con cuadratura-caja ni con ventas-global. NC: sí las resta (ambas modalidades). descartado=False: sí lo aplica (línea 1214) — es el ÚNICO reporte de ventas que lo hace. Agujero real de cobertura: el universo son solo DTEs; las ventas por ticket POS que no generaron documento (Ticket.dte_generado=False, tipo_dte='TICKET') NO existen en este reporte, mientras que ventas-global y ventas-comparativo sí las suman. Dos reportes del mismo menú responden distinto a la misma pregunta ('cuánto vendió NICK1 en junio') y ninguno avisa por qué. Además incluye tipo_transaccion='VENTA' genérico, que según la regla del proyecto arrastra facturas por concepto/despacho/compensación (no son venta de público).
- **Rendimiento**: Sano. ~8 queries agregadas con values().annotate(), sin N+1. La única consulta pesada es Dte_Productos.objects.filter(dte__in=queryset_ventas) (línea 181) que genera un subquery IN sobre el queryset de DTEs; con rangos anuales puede degradar, pero para mes/día está bien. Hay índices dte_suc_fecha_idx y dte_suc_tt_fecha_idx que cubren el filtro.

**Problemas:**

- [alta] **El KPI 'Total Ventas' se escribe dos veces con dos definiciones distintas: gana el que responda último** — cargarDatos() dispara en paralelo cargarDatosVendedores() y cargarDatosSucursales(). La primera escribe totalVentas/totalDocumentos/ticketPromedio con el total del endpoint de vendedores (que EXCLUYE la fila 'Sin vendedor asignado' y sus NC, ver views_modulo_reportes.py:232-236). La segunda los pisa con la suma de la tabla de sucursales (que SÍ incluye todo, líneas 1359-1362). Como son dos fetch concurrentes, el valor final depende de cuál responde primero: el mismo filtro puede mostrar dos totales distintos en dos refrescos seguidos. En sucursales con DTEs sin vendedor la diferencia es exactamente el monto de esos documentos.
  - *Fix*: Definir UNA fuente de verdad para la cabecera de KPIs (la de sucursales, que es la que cuadra con la facturación total) y que cargarDatosVendedores() solo pinte su tabla. Alternativamente exponer ambos con etiqueta explícita: 'Facturado total' vs 'Atribuido a vendedor'.
  - *Ref*: `app/templates/vistas/modulo_reportes/reporte_ventas_sucursal.html:807 y :878`
- [media] **La comparativa mensual embebida ignora el filtro de período elegido** — El gráfico se pide con cargarComparativaMensual(sucursal) pasando SOLO la sucursal; el endpoint obtiene siempre los últimos 180 días desde timezone.now() (fecha_inicio = fecha_fin - timedelta(days=180)). Si el usuario está mirando marzo, el gráfico igual muestra feb-jul. Peor: 180 días corta el mes más antiguo por la mitad, así que la primera barra siempre aparece artificialmente baja y se lee como 'caída de ventas'.
  - *Fix*: Aceptar mes/fecha_inicio/fecha_fin en obtener_comparativa_mensual y anclar el inicio al día 1 del mes más antiguo (fecha_fin.replace(day=1) - relativedelta(months=5)) para que ningún mes salga parcial.
  - *Ref*: `app/views_modulo_reportes.py:1850-1851 y reporte_ventas_sucursal.html:912`
- [baja] **Falta filtro de empresa y el selector de vendedores lista vendedores sin ventas** — En un sistema multi-empresa el reporte solo permite acotar por sucursal. Y obtener_vendedores_reporte, cuando no hay sucursal seleccionada, devuelve Vendedor.objects.filter(nombre__isnull=False) — TODOS los vendedores históricos de todas las empresas, sin filtrar por actividad ni por permisos del usuario.
  - *Fix*: Agregar empresa_id (sucursal__empresa_id) al filtro y, en obtener_vendedores_reporte sin sucursal, restringir a los vendedores de obtener_sucursales_usuario(request.user) con ventas en los últimos N meses.
  - *Ref*: `app/views_modulo_reportes.py:1690-1694`
- [media] **El endpoint de datos no está protegido por el middleware de permisos, solo la página** — URL_PERMISO_MAP mapea '/app/reportes/ventas-sucursal/' → 'reporte_ventas_sucursal', pero obtener_codigo_opcion hace match por substring y '/app/api/reportes/ventas-por-vendedor/' NO contiene esa cadena. Un usuario sin el permiso puede llamar el endpoint JSON directo y obtener los montos (acotados a su sucursal por filtrar_queryset_por_sucursal, pero igualmente datos de comisiones/ventas que el rol tiene vedados).
  - *Fix*: Agregar al mapa las claves '/app/api/reportes/ventas-por-vendedor/', '/app/api/reportes/ventas-por-sucursal/', '/app/api/reportes/comparativa-mensual/' y '/app/api/reportes/documentos-vendedor/' apuntando a 'reporte_ventas_sucursal'.
  - *Ref*: `app/middleware_permisos.py:83 y app/middleware_permisos.py:246-260`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Margen bruto $ y % por sucursal y por vendedor | margen = Σ(Dte_Productos.monto_item o precio*stock) − Σ(Dte_Productos.costo * stock), agrupado por dte__sucursal_id / dte__vendedor_id; margen_pct = margen / venta_neta * 100 | Dte_Productos.costo, Dte_Productos.monto_item, Dte_Productos.precio, Dte_Productos.stock (app/models/dte.py:448-455); para tickets sin DTE, Ticket_Productos.costo_fifo (app/models/ventas.py:361) | Hoy el ranking premia al que más factura, no al que más gana. Con el catálogo en liquidación (memoria del proyecto: 70% del stock >180 días) vender volumen a descuento puede destruir margen y el reporte lo aplaude. | medio |
| Tasa de devolución (NC / ventas brutas) por sucursal y vendedor | tasa = Σ NC (tipo_documento='NOTA DE CREDITO') / Σ ventas brutas * 100, con desglose DEVOLUCION vs ANULACION | Ya calculado internamente: queryset_ncs / queryset_ncs_anulacion (app/views_modulo_reportes.py:138-139, 1226-1228); solo falta exponerlo como KPI | Los datos ya están en la respuesta (devoluciones, cantidad_devoluciones, nc_anulacion_total) pero no hay un ratio. Una tasa de ANULACION alta en un vendedor es señal de error de digitación o de fraude; una de DEVOLUCION alta es problema de producto o de venta forzada. | bajo |
| Venta por franja horaria y día de semana (por sucursal) | agrupar por EXTRACT(hour) sobre Dte.hora (o TruncHour de Ticket.created_at) y por ExtractWeekDay(fecha_emision), sumando monto_con_iva y contando documentos | Dte.hora (app/models/dte.py, campo TimeField), Dte.fecha_emision, Ticket.created_at | Es la única forma de dimensionar turnos y decidir dotación por tienda. Hoy el reporte agrega todo el día y no se puede saber si la tienda vende a las 12 o a las 19. | medio |
| Mix boleta vs factura y ticket promedio por tipo de documento | por sucursal: Σ monto_con_iva y Count(id) agrupado por tipo_documento; % factura = venta_factura / venta_total | Dte.tipo_documento (ya está en el queryset base, no requiere joins nuevos) | Distingue venta de público de venta mayorista/empresa dentro del mismo total. Hoy ambas se suman sin distinción y una factura grande distorsiona el ticket promedio de la tienda. | bajo |

### Ventas Global por Empresa (consolidado) — `ROTO`

- **Ruta**: /app/reportes/ventas-global/ → app/views_modulo_reportes.py:7295 ver_reporte_ventas_global; API: app/views_modulo_reportes.py:7303 obtener_ventas_global_por_empresa
- **Mide**: Consolidado multi-empresa: agrupa las ventas de todas las sucursales visibles por empresa, con comparativo contra el período anterior de igual largo. Suma Tickets PAGADOS sin DTE + DTEs de venta, y pretende restar notas de crédito.
- **Filtros**: Solo mes o rango fecha_inicio/fecha_fin — la UI (reporte_ventas_global.html:59-67) no ofrece ni empresa ni sucursal ni vendedor ni buscador. El scoping por permisos SÍ está y es correcto: si el usuario no ve todas las sucursales, se acota con obtener_sucursales_usuario (líneas 7330-7333), que es el fix ya aplicado en la auditoría previa. Confirmado vigente.
- **Fidelidad**: Aquí está el problema más grave de la familia. (1) LAS DEVOLUCIONES SIEMPRE SALEN EN CERO: el bloque que suma NC filtra tipo_transaccion__in=['VENTA','VENTA_PUBLICO'] Y tipo_documento='NOTA DE CREDITO' a la vez, pero TODAS las NC de venta se crean con tipo_transaccion='DEVOLUCION' o 'ANULACION' (app/views.py:29454, app/views_modulo_ventas.py:7808, :8410, :19641). El AND nunca se cumple → devoluciones=0, ventas netas=ventas brutas, y el KPI 'Total Devoluciones' de la cabecera es un cero decorativo. (2) Tickets: usa created_at (correcto) y dte_generado=False (anti-doble-conteo correcto). (3) No excluye descartado=True, a diferencia de ventas-sucursal. (4) No excluye autoventas (receptor=emisor), a diferencia de ventas-comparativo. Resultado: los tres reportes de ventas del menú miden tres universos distintos.
- **Rendimiento**: Aceptable: 6 queries agregadas (3 por período × 2 períodos). Sin N+1. El comparativo duplica el costo pero sigue siendo O(1) en queries.

**Problemas:**

- [alta] **Las notas de crédito nunca se restan: el filtro combinado es imposible** — El queryset de NC exige tipo_transaccion in ('VENTA','VENTA_PUBLICO') y tipo_documento='NOTA DE CREDITO' simultáneamente, pero el código que emite NC de venta setea tipo_transaccion='DEVOLUCION' (o 'ANULACION'). La intersección es vacía en la práctica, así que data[sid]['devoluciones'] queda en 0 para todas las sucursales. El reporte publica 'ventas netas' que en realidad son brutas, y sobreestima la venta consolidada exactamente en el monto de las NC del período.
  - *Fix*: Cambiar el filtro de NC a tipo_transaccion__in=['DEVOLUCION','ANULACION'] (o simplemente quitar la condición de tipo_transaccion y quedarse con tipo_documento='NOTA DE CREDITO'), replicando lo que sí hace bien obtener_ventas_por_sucursal_reporte en la línea 1226.
  - *Ref*: `app/views_modulo_reportes.py:7395-7402`
- [media] **El reporte es huérfano: no está en el menú ni en el mapa de permisos** — No existe ninguna referencia a ver_reporte_ventas_global en app/templates/ (grep sobre todo el árbol de templates: cero resultados fuera de su propio archivo), no hay flag permiso_reporte_ventas_global en menu.html y URL_PERMISO_MAP no lo incluye. Es una página viva, sin permiso alguno, alcanzable solo escribiendo la URL — o sea: mantenida a medias y usada por nadie, mientras el bug de las NC pasa inadvertido.
  - *Fix*: Decidir: o se publica en 'Reportes Ventas' con su OpcionMenu + entrada en URL_PERMISO_MAP, o se elimina y su función se absorbe en el comparativo (que ya agrupa por sucursal y tiene comparativo de período).
  - *Ref*: `app/middleware_permisos.py:82-93 (sección Reportes, sin la ruta) y app/templates/layout/menu.html:2536-2582`
- [media] **No excluye DTEs descartados ni autoventas, a diferencia de sus reportes hermanos** — El queryset de DTEs (líneas 7369-7376) no aplica descartado=False ni el exclude(receptor_id=F('emisor_id')). Un DTE anulado lógicamente (descartado por gestión de documentos) sigue sumando aquí y no en ventas-sucursal; una factura entre sucursales de la misma empresa suma aquí y no en ventas-comparativo. Consolidar dos empresas del holding con facturas cruzadas infla el consolidado.
  - *Fix*: Alinear el queryset base con el de comparativo: agregar .filter(descartado=False) y .exclude(receptor__isnull=False, receptor_id=F('emisor_id')). Idealmente extraer un helper único _queryset_ventas_dte(fi, ff) que usen los tres reportes.
  - *Ref*: `app/views_modulo_reportes.py:7369-7376`
- [baja] **La variación % miente cuando el período anterior es 0** — variacion = 100 si ventas_ant == 0 y hay ventas actuales. Una sucursal nueva o que no operó el mes pasado aparece con '+100%' en vez de 'sin base comparable', y entra al ranking de crecimiento como si hubiera duplicado.
  - *Fix*: Devolver None y que el frontend pinte '—' o 'nuevo' cuando ventas_ant == 0.
  - *Ref*: `app/views_modulo_reportes.py:7433 y :7463`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Same-store sales (venta comparable) del consolidado | variación % calculada SOLO sobre las sucursales con ventas > 0 en ambos períodos; reportar aparte 'aporte de sucursales nuevas' y 'pérdida por sucursales cerradas' | Los dicts actual/anterior que ya construye _sumar_periodo (app/views_modulo_reportes.py:7409-7410); es aritmética sobre datos ya disponibles | Con multi-empresa y aperturas/cierres, el crecimiento consolidado hoy mezcla crecimiento real con efecto de red. Sin comparable no se sabe si el negocio crece o solo abrió tiendas. | bajo |
| Comparativo YoY (mismo mes del año anterior) además del período inmediato anterior | tercer llamado a _sumar_periodo con fi.replace(year=fi.year-1), ff.replace(year=ff.year-1) | Misma función _sumar_periodo, sin datos nuevos | El retail de calzado es estacional; comparar julio contra junio no dice nada. El comparativo mes-a-mes actual induce a error justo en los cambios de temporada. | bajo |
| Participación del canal internet dentro del consolidado por empresa | venta_ecommerce = Σ Ticket.total con modulo_origen='ECOMMERCE' del período / venta total de la empresa * 100 | Ticket.modulo_origen, Ticket.total, Ticket.sucursal__empresa_id (app/models/ventas.py:222-228) | Hoy el ecommerce está diluido dentro del total de la sucursal que despacha; a nivel holding no se puede ver si internet crece o se estanca sin abrir otro reporte. | bajo |

### Comparativo de Ventas (período actual vs anterior) — `MEJORABLE`

- **Ruta**: /app/reportes/ventas-comparativo/ → app/views_modulo_reportes.py:7932 ver_reporte_ventas_comparativo; API: app/views_modulo_reportes.py:7940 obtener_ventas_comparativo (motor: _sumar_ventas_comparativo, línea 7651)
- **Mide**: Comparación de dos períodos equivalentes (hoy/ayer, semana, MTD, mes completo, últimos 7/30, YoY, trimestre YoY, YTD, año completo, custom) con desglose por sucursal, vendedor y canal (Ecommerce / POS presencial / DTE). Es el reporte más elaborado de la familia y el único con lógica anti-doble-conteo explícita bien resuelta.
- **Filtros**: tipo_flujo + fechas custom + sucursal_id (aplicado con filtrar_queryset_por_sucursal en las líneas 7720 y 7850, server-side, respetando permisos). Sin buscador de texto ni filtro de vendedor/empresa. Los 11 flujos temporales están bien construidos (_calcular_rangos_comparativo, líneas 7509-7648); reviso 'semana' y 'mes_mtd' y comparan rangos de igual largo, que es lo correcto.
- **Fidelidad**: Fechas correctas: Ticket por created_at__date (línea 7715), DTE por fecha_emision. Anti-doble-conteo bien hecho y documentado: tickets solo si dte_generado=False, ecommerce sumado por ticket y restado del canal DTE. Excluye ANULADO/CANCELADO/RECHAZADO y autoventas. PERO arrastra el mismo bug fatal que ventas-global: qs_nc se deriva de qs_dtes_base, que ya filtró tipo_transaccion in ('VENTA','VENTA_PUBLICO'), y las NC viven con tipo_transaccion 'DEVOLUCION'/'ANULACION' → total_devoluciones = 0 SIEMPRE. Eso deja tres cosas falsas: 'ventas netas' (= brutas), el KPI 'tasa de devolución' (siempre 0,0%) y la columna devoluciones_actual de cada sucursal. Además no filtra descartado=False.
- **Rendimiento**: ~14 queries por período × 2 períodos = ~28 queries agregadas, todas con values().annotate(). Sin N+1. Es aceptable, pero las dos llamadas a _sumar_ventas_comparativo son secuenciales y con 'anio_full' recorren dos años completos de Dte + Ticket_Productos: ahí sí puede tardar decenas de segundos. No hay caché.

**Problemas:**

- [alta] **Tasa de devolución y ventas netas son estructuralmente cero** — qs_nc = qs_dtes_base.filter(tipo_documento='NOTA DE CREDITO'), pero qs_dtes_base ya restringió tipo_transaccion a VENTA/VENTA_PUBLICO. Como toda NC de venta se crea con tipo_transaccion='DEVOLUCION'/'ANULACION', el queryset es vacío: total_devoluciones=0, cantidad_devoluciones=0, y por lo tanto total_ventas_netas == total_ventas_brutas. El KPI 'tasa_devolucion_actual' que muestra el tablero es literalmente siempre 0,0% y su delta en puntos porcentuales también.
  - *Fix*: Construir el queryset de NC aparte del base: Dte.objects.filter(fecha_emision entre fi y ff, tipo_documento='NOTA DE CREDITO', descartado=False) con el mismo filtrado de sucursal, sin condicionar tipo_transaccion (o condicionándolo a DEVOLUCION/ANULACION). Mismo fix que ventas-global; conviene hacerlo en un helper compartido.
  - *Ref*: `app/views_modulo_reportes.py:7907`
- [baja] **Las unidades de tickets ecommerce se calculan y se botan** — El bloque de las líneas 7784-7796 consulta Ticket_Productos para los tickets ECOMMERCE, itera el resultado y dentro del if termina en 'pass' con un comentario. La query se ejecuta, se paga su costo y no alimenta ningún acumulador de sucursal; solo suma al canal ECOMMERCE. Es código muerto a medias que confunde a quien lo lea y cuesta una query por período.
  - *Fix*: Reescribir el bloque para que sume las unidades ecommerce al canal en una sola pasada y eliminar el bucle con 'pass'.
  - *Ref*: `app/views_modulo_reportes.py:7789-7796`
- [media] **No excluye DTEs descartados** — qs_dtes_base no aplica descartado=False. Los documentos descartados lógicamente desde gestión de documentos siguen sumando al comparativo, y como el descarte suele hacerse después, un mes ya reportado cambia de valor hacia abajo cuando se descarta un DTE... salvo aquí, donde no cambia nunca.
  - *Fix*: Agregar .filter(descartado=False) al queryset base (línea 7840-7849).
  - *Ref*: `app/views_modulo_reportes.py:7840-7849`
- [baja] **'Top crecimiento' se lo lleva cualquier sucursal marginal** — top_suc/top_vend se eligen con max() sobre variacion_pct sin umbral de volumen. Una sucursal que pasó de $50.000 a $200.000 (+300%) desplaza a la que creció $40 millones. El único guardarraíl es ventas_actual > 0.
  - *Fix*: Exigir un piso de participación (p.ej. ventas_actual >= 1% del total) o rankear por variación absoluta $ y mostrar el % como dato secundario.
  - *Ref*: `app/views_modulo_reportes.py:8118-8127`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Descomposición del crecimiento: tráfico × ticket × UPT | Δventa = (docs_act − docs_ant) × ticket_ant + (ticket_act − ticket_ant) × docs_act; y UPT = unidades / documentos por período | Ya están total_documentos, total_unidades y total_ventas_netas en el dict que devuelve _sumar_ventas_comparativo (líneas 7680-7695) | Responde la pregunta que el reporte hoy deja abierta: ¿vendí más porque entró más gente o porque cada boleta fue más grande? Son acciones opuestas (marketing vs venta cruzada) y los datos ya están calculados. | bajo |
| Margen bruto comparado entre períodos | margen = Σ(monto_item − costo*stock) de Dte_Productos + Σ(subtotal − costo_fifo*stock) de Ticket_Productos sin DTE, por período; mostrar Δ margen % en puntos | Dte_Productos.costo/monto_item/stock, Ticket_Productos.costo_fifo/subtotal/stock | El comparativo mide solo facturación. En plena liquidación (campañas NxM y descuentos por año de ingreso ya implementados) la venta puede subir mientras el margen cae, y este reporte lo celebraría. | medio |
| Índice de concentración de vendedores (top 20% sobre el total) | participación acumulada de los vendedores del primer quintil sobre ventas_actual, comparada contra el período anterior | vendedores_rows que ya arma el endpoint (línea 8059-8091) | Detecta dependencia de una o dos personas por tienda; si el índice sube, el riesgo operativo por vacaciones/renuncia sube con él. | bajo |

### Comparativa Mensual por Sucursal (gráfico embebido en Ventas Sucursal) — `MEJORABLE`

- **Ruta**: /app/api/reportes/comparativa-mensual/ → app/views_modulo_reportes.py:1847 obtener_comparativa_mensual (consumido por reporte_ventas_sucursal.html:912)
- **Mide**: Serie de 6 meses de ventas por sucursal para el gráfico de líneas del reporte de ventas-sucursal. Suma Tickets PAGADOS sin DTE + DTEs VENTA_PUBLICO no anulados, y pretende restar las NC del mes.
- **Filtros**: Solo sucursal_id (vía filtrar_queryset_por_sucursal, líneas 1868 y 1899). El rango es fijo y NO configurable: siempre los últimos 180 días desde now(). El frontend solo lo llama cuando el filtro activo es 'mes' y le pasa la sucursal, nunca el mes elegido.
- **Fidelidad**: El fix anti-doble-conteo de la auditoría previa está VIGENTE y correcto (dte_generado=False en la línea 1864, con el comentario de los $180M/mes duplicados). Fechas correctas (created_at para tickets, fecha_emision para DTEs). Pero: (1) el bloque de resta de NC es el MISMO patrón roto — queryset_dtes ya filtró tipo_transaccion='VENTA_PUBLICO' y luego se le pide tipo_documento='NOTA DE CREDITO': nunca hay filas, las NC no se restan; (2) ventana de 180 días → el mes más antiguo sale truncado y se lee como caída; (3) sin cota superior de fecha: un DTE con fecha_emision futura (se puede digitar) crea una barra en un mes que no existe todavía; (4) excluye ANULADO mientras la tabla de arriba en la misma pantalla los incluye, así que gráfico y tabla no cuadran; (5) excluye tipo_transaccion='VENTA' (mayorista/concepto) que la tabla sí incluye.
- **Rendimiento**: 3 queries agregadas con TruncMonth. Sin problemas.

**Problemas:**

- [alta] **La resta de notas de crédito es código muerto** — nc_mensuales = queryset_dtes.filter(tipo_documento='NOTA DE CREDITO') donde queryset_dtes tiene tipo_transaccion='VENTA_PUBLICO'. Las NC llevan tipo_transaccion='DEVOLUCION'/'ANULACION' → cero filas. Las 20 líneas del bloque (1922-1941) no hacen nada y el gráfico muestra ventas brutas mientras el comentario del código promete netas.
  - *Fix*: Consultar las NC con un queryset independiente por tipo_documento (y descartado=False), igual que el fix de ventas-global y comparativo.
  - *Ref*: `app/views_modulo_reportes.py:1923-1941`
- [media] **El primer mes de la serie siempre sale parcial** — fecha_inicio = now() − 180 días cae a mitad de mes (hoy, 25-jul → 26-ene), así que enero muestra ~5 días de ventas junto a 5 meses completos. La lectura visual inevitable es 'veníamos de un mes malísimo'.
  - *Fix*: Anclar al día 1: fecha_inicio = (hoy.replace(day=1) − 5 meses). Y agregar cota superior fecha_emision__lte=hoy para no graficar documentos con fecha futura.
  - *Ref*: `app/views_modulo_reportes.py:1850-1851`
- [media] **El gráfico y la tabla de la misma pantalla usan universos distintos** — El gráfico excluye estado ANULADO y excluye tipo_transaccion='VENTA'; la tabla de sucursales de la misma página incluye ambos. Para el mismo mes y sucursal, la barra del gráfico y la fila de la tabla no coinciden, sin explicación en la UI.
  - *Fix*: O alinear filtros con obtener_ventas_por_sucursal_reporte, o rotular el gráfico como 'Venta de público, sin anulados' para que la diferencia sea intencional y visible.
  - *Ref*: `app/views_modulo_reportes.py:1889-1897 vs :1209-1215`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Variación mes/mes y acumulado móvil 3 meses por sucursal | var_mm = (mes_n − mes_n-1)/mes_n-1; MM3 = promedio de los últimos 3 meses cerrados | La misma serie sucursales_dict que ya arma el endpoint (línea 1853) | El gráfico obliga a comparar barras a ojo. El promedio móvil quita el ruido de meses con feriados y hace visible la tendencia real. | bajo |
| Participación mensual de cada sucursal (share stack 100%) | share = venta_sucursal_mes / venta_total_mes * 100 | Misma serie, sin queries nuevas | En términos absolutos todas las tiendas suben y bajan juntas con la estacionalidad; el share revela cuál está ganando o perdiendo terreno dentro de la cadena. | bajo |

### Productos Vendidos — `UTIL`

- **Ruta**: /app/reportes/productos-vendidos/ → app/views_modulo_reportes.py:8653 ver_reporte_productos_vendidos; API: app/views_modulo_reportes.py:8661 obtener_productos_vendidos (motor: _agregar_productos_vendidos, línea 8209)
- **Mide**: Ranking de productos vendidos en el período con unidades netas de devolución, monto, costo, margen $ y %, documentos, nº de sucursales, stock actual, sell-through y cobertura en días; más agregaciones por marca, categoría v1.2 (Padre › Hijo), sexo, especialidad multi-etiqueta y heatmap Sucursal × Categoría.
- **Filtros**: Los más completos de la familia y TODOS server-side: marca (atributo1), color (atributo2, sin control en la UI), sexo (atributo3), categoría con expansión de hijas (líneas 8178-8189), especialidad v1.2 vía ProductoAtributoValor, temporada, año temporada, rango de precio, orden, top_n, incluir_excluidos y búsqueda de texto. La búsqueda hace lo que dice a medias: filtra por Producto.articulo y Producto.descripcion con icontains (líneas 8200-8205) — o sea busca código y modelo, pero NO marca ni SKU (Producto_Talla.sku es BigInteger y no matchea letras, gotcha ya conocido en el proyecto). El filtro de sucursal se aplica en las tres ramas (tickets, dtes, devoluciones) con el campo correcto.
- **Fidelidad**: Lo bueno: usa created_at para tickets, aplica excluir_de_analitica por defecto (líneas 8166-8168), anti-doble-conteo correcto (tickets solo con dte_generado=False), excluye NC y autoventas, y aplica la recategorización v1.2 con label Padre › Hijo. Lo que compromete las cifras: (1) MEZCLA BASES TRIBUTARIAS: en boletas monto_item viene con IVA y en facturas viene NETO (así está documentado en el propio modelo: Dte_Productos.precio_unitario 'neto (factura) o IVA-inclusive (boleta)'), y ambos se suman en el mismo campo 'monto' junto con Ticket_Productos.subtotal (con IVA). Para un producto que se vende por ambos canales el monto está subestimado y el margen % queda deformado. (2) El stock que usa para sell-through y cobertura es el stock ACTUAL de TODAS las sucursales, aunque el usuario haya filtrado una: Producto_Talla.objects.filter(producto_id__in=...) sin scoping por sucursal. (3) La columna 'Género' es atributo4, que en este proyecto está 0% poblado — sale '-' en todas las filas. (4) Las devoluciones se netean solo desde CambioDevolucionDetalle: una devolución hecha vía NC sobre factura (ajustar DTE emitido) no descuenta unidades. (5) No excluye descartado=True.
- **Rendimiento**: ~12 queries agregadas, sin N+1 real. Las dos consultas .values(...).distinct() para contar sucursales por producto (líneas 8325 y 8372) son las más caras (agrupan sobre todas las líneas del período). Con rango 'anio_full' y sin filtros puede pesar, pero está lejos de los casos patológicos de compras-integral. La agregación por especialidad carga ProductoAtributoValor de todos los productos vendidos en una query (línea 8527), correcto.

**Problemas:**

- [alta] **El monto suma facturas en neto con boletas y tickets en bruto** — agg_dp suma monto_item (neto en facturas 33/34, con IVA en boletas 39) y agg_tp suma Ticket_Productos.subtotal (con IVA). Todo cae en p['monto'], que después alimenta margen, margen_pct, participación, ticket_promedio_unid y el heatmap. Un producto vendido 50/50 boleta-factura queda ~9,5% bajo el valor real y su margen % aparece artificialmente bajo frente a uno que solo se vende por boleta.
  - *Fix*: Normalizar a una sola base al agregar: si dte.tipo_documento empieza con 'FACTURA', multiplicar la línea por 1,19 (o guardar todo neto y dividir las boletas por 1,19). Se puede resolver con un Case/When sobre dte__tipo_documento dentro del mismo annotate, sin queries extra.
  - *Ref*: `app/views_modulo_reportes.py:8348-8367 y app/models/dte.py:451`
- [alta] **Sell-through y cobertura ignoran el filtro de sucursal** — stock_por_producto se calcula con Producto_Talla.objects.filter(producto_id__in=...) sin ningún filtro de sucursal, mientras las ventas SÍ están filtradas. Al mirar una tienda, el denominador incluye el stock del centro de distribución y de las demás tiendas: el sell-through se hunde y la cobertura se dispara. Justo el escenario del proyecto (bodegas EDEL/IMP concentran stock) hace que el indicador sea inutilizable a nivel tienda.
  - *Fix*: Aplicar el mismo scoping a Producto_Talla (Producto tiene FK a sucursal: filtrar producto__sucursal_id o la sucursal del request), y cuando no hay sucursal, excluir los centros de distribución como ya hace obtener_indicador_compra_categoria (app/views_modulo_ventas.py:18710).
  - *Ref*: `app/views_modulo_reportes.py:8425-8430`
- [baja] **La columna 'Género' está muerta (atributo4, 0% poblado)** — El backend expone prod_genero/prod_genero_id desde atributo4 y arma por_genero; el template ya no grafica esa dimensión (renderSexoGenero recibe por_especialidad en su lugar) pero la tabla sigue mostrando la columna 'Género' vacía y el backend sigue pagando el join y el filtro genero_id.
  - *Fix*: Eliminar prod_genero/prod_genero_id/por_genero y el filtro genero_id del backend, y quitar la columna del template (o renombrar la de atributo3 a 'Género', que es lo que realmente contiene según la convención del proyecto).
  - *Ref*: `app/views_modulo_reportes.py:8303-8304, :8517 y reporte_productos_vendidos.html:689`
- [media] **Las devoluciones vía nota de crédito no descuentan unidades** — El neteo sale exclusivamente de CambioDevolucionDetalle con fecha_ejecucion en el rango. Una devolución de factura mayorista resuelta con NC (flujo 'ajustar DTE emitido' / devolución garantía) no aparece: las líneas de la NC están excluidas del queryset y no hay CambioDevolucion asociado. Las unidades de ese SKU quedan sobrecontadas.
  - *Fix*: Sumar una tercera fuente de neteo: Dte_Productos de DTEs con tipo_documento='NOTA DE CREDITO' en el rango, restando stock y monto por producto (con el mismo criterio de no crear filas negativas fantasma).
  - *Ref*: `app/views_modulo_reportes.py:8384-8420`
- [media] **Margen sin control de calidad del costo** — El margen usa Dte_Productos.costo y Ticket_Productos.costo_fifo, ambos IntegerField default=0. Para líneas migradas o vendidas por el fallback sin FIFO el costo es 0 y el margen aparece como 100%. El reporte no informa qué porcentaje de las unidades tiene costo > 0, así que un margen consolidado puede estar inflado sin que nadie lo note.
  - *Fix*: Agregar un KPI de cobertura de costo (unidades con costo>0 / unidades totales) y marcar en la tabla las filas con costo 0 para que no ensucien el ranking por margen.
  - *Ref*: `app/views_modulo_reportes.py:8309-8311 y :8352-8354`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| GMROI (retorno del margen sobre el inventario invertido) | GMROI = margen_bruto_período / (Σ Producto_Talla.stock × Producto.costo) del mismo scope | margen ya calculado en el reporte; Producto_Talla.stock y Producto.costo (usados igual en app/views_modulo_ventas.py:18719) | Es EL indicador de retail para decidir recompra: un producto de margen 60% que rota una vez al año rinde menos que uno de 30% que rota seis. Con 70% del stock sobre 180 días, el ranking por margen % que hay hoy está mandando la señal equivocada. | medio |
| Quiebre de curva de tallas por producto | por producto: nº de tallas con stock=0 y ventas>0 en el período / nº total de tallas del producto; alertar cuando la talla core (mayor participación de venta) está en 0 | Producto_Talla.stock y talla + Ticket_Productos.ProductoTalla / Dte_Productos.productoTalla (ya son el grano de las líneas de venta) | En calzado un producto con stock 'suficiente' pero sin 38-39-40 está muerto comercialmente. Hoy el reporte agrega a nivel producto y esconde el quiebre de talla, que es la causa #1 de venta perdida. | alto |
| Descuento efectivo promedio por producto / marca | descuento_pct = 1 − Σ(Ticket_Productos.precio × stock) / Σ(Ticket_Productos.precio_original × stock) | Ticket_Productos.precio, precio_original y porcentaje_descuento (app/models/ventas.py:338-344) | Los campos ya existen y nadie los reporta. Con las campañas de liquidación NxM activas, saber qué marcas solo venden con descuento profundo define qué se deja de comprar. | bajo |
| SKUs con stock y cero venta en el período (dead stock del catálogo) | universo = Producto_Talla con stock>0 en el scope; restar los producto_id presentes en productos_acum; reportar unidades y $ a costo | Producto_Talla.stock, Producto.costo, Producto.excluir_de_analitica | El reporte hoy solo ve lo que SÍ se vendió; lo que no se vendió — que es donde está el capital inmovilizado — es invisible. Es la contracara necesaria del mismo reporte. | medio |

### Documentos Emitidos — `MEJORABLE`

- **Ruta**: /app/reportes/documentos-emitidos/ → app/views_modulo_reportes.py:1973 ver_documentos_emitidos; API: app/views_modulo_reportes.py:1986 obtener_documentos_emitidos; Excel: app/views_modulo_reportes.py:2452
- **Mide**: Listado documento a documento de boletas/facturas emitidas en un rango, con cliente, monto, descuento, pagado, vendedor, voucher y desglose de medios de pago; más un resumen por método de pago y un bloque de diagnóstico para cuadrar contra /app/ventas/documentos/.
- **Filtros**: Fechas desde/hasta, tipo de documento, método de pago y sucursal (con semántica explícita 'all' / id / vacío=sesión, líneas 2026-2069, validando permisos con puede_ver_sucursal). Todos server-side. El filtro de método de pago usa icontains sobre Dte_Detalle_Pago.metodo_pago con distinct(): filtra bien, pero trae el DTE COMPLETO aunque solo una de sus formas de pago coincida, y el resumen suma el monto entero a esa categoría. No hay buscador por folio, cliente ni RUT — que es justo lo que se busca en un listado de documentos. La paginación real (fix previo) está VIGENTE y correcta (líneas 2129-2140 + navegación en el template).
- **Fidelidad**: El punto ciego más serio: NO filtra estado_dte NI descartado. Entran documentos ANULADOS, RECHAZADOS por el SII, CANCELADOS y descartados lógicamente, todos presentados como 'emitidos' y sumando al resumen y al total_global. Segundo: el bucket 'notas_credito' del resumen es código muerto — el queryset filtra tipo_transaccion in ('VENTA','VENTA_PUBLICO') y las NC llevan 'DEVOLUCION'/'ANULACION', así que nunca entran; total_global = ventas_brutas − 0 − descuentos. Tercero: cuando el DTE no tiene detalle de pago, el fallback busca el ticket por Ticket.correlativo == Dte.numero_documento en la misma sucursal — son dos secuencias distintas (folio SII vs correlativo de ticket) y existe Ticket.folio_dte para hacer el join correcto; cualquier coincidencia numérica atribuye el medio de pago de OTRA venta. Cuarto: si hay pagos parciales registrados, la diferencia se clasifica como 'descuento' (líneas 2346-2351), inflando descuentos y desinflando total_global en toda venta a crédito.
- **Rendimiento**: Riesgo medio-alto de memoria: dtes_todos = list(queryset) materializa TODOS los documentos del rango con prefetch de pagos antes de paginar (línea 2103) — la paginación es solo del slice visible; el resumen necesita el total, pero eso debería hacerse con agregación en DB, no cargando 20-30k objetos. El Excel es peor: hace Ticket.objects.filter(...).first() y TicketDetallePago.filter(...).first() POR CADA documento sin detalle de pago, más un .exists() por fila que anula el prefetch → N+1 clásico de 2-3 queries por documento.

**Problemas:**

- [alta] **Incluye documentos anulados, rechazados y descartados sin distinguirlos** — El queryset base solo filtra tipo_transaccion y rango de fechas: no hay condición sobre estado_dte ni sobre descartado. Un DTE RECHAZADO por el SII (que no es un documento válido) o uno descartado aparece en la lista y suma a 'ventas brutas' y al 'total global' del resumen, que es el número que se usa para cuadrar contra caja.
  - *Fix*: Agregar .filter(descartado=False) y exponer un selector de estado con default estado_dte__in=['EMITIDO','ACEPTADO']; si se quiere mantener la visión completa, mostrar los otros estados con badge y excluirlos del resumen monetario.
  - *Ref*: `app/views_modulo_reportes.py:2008-2018`
- [alta] **El resumen nunca resta notas de crédito (bucket muerto)** — resumen['notas_credito'] solo se alimenta dentro del bucle si tipo_documento contiene 'NOTA'+'CREDITO', pero esos documentos jamás entran al queryset por el filtro de tipo_transaccion. total_global = ventas_brutas − notas_credito(0) − descuentos, y el diagnóstico reporta cantidad_notas_credito=0 dando falsa tranquilidad de que no hubo NC en el período.
  - *Fix*: Incluir las NC en el queryset (ampliando tipo_transaccion a DEVOLUCION/ANULACION) y mostrarlas como filas negativas + restarlas del total; o dejar el filtro y eliminar el bucket para no publicar un cero engañoso.
  - *Ref*: `app/views_modulo_reportes.py:2015 y :2321-2324 y :2385`
- [alta] **El medio de pago de fallback se resuelve con un join incorrecto** — tickets_map se llena con Ticket.objects.filter(correlativo__in=correlativos) donde 'correlativos' son NÚMEROS DE FOLIO DTE, y se indexa por (str(correlativo), sucursal_id). El folio de una boleta electrónica y el correlativo interno de un ticket son secuencias independientes: cualquier colisión numérica dentro de la misma sucursal asigna el método de pago, el voucher y el tipo de tarjeta de una venta ajena. El modelo ya tiene Ticket.folio_dte justamente para esto.
  - *Fix*: Reemplazar el join por Ticket.objects.filter(folio_dte__in=folios, sucursal_id__in=...) indexando por (folio_dte, sucursal_id). Aplicar el mismo cambio en el Excel (línea 2585).
  - *Ref*: `app/views_modulo_reportes.py:2114-2116 y app/models/ventas.py:247`
- [media] **Los pagos parciales se contabilizan como descuento** — Si pagado_sum > 0 y < total, el código asume descuento = total − pagado_sum. En una factura a crédito con abono parcial registrado, el saldo pendiente se convierte en 'descuento', infla resumen['descuentos'] y reduce total_global. La misma lógica está en la fila de la tabla (líneas 2213-2218), así que la columna Descuento del listado muestra saldos impagos.
  - *Fix*: Usar Dte.descuento como única fuente de descuento y exponer aparte una columna 'Saldo pendiente' = monto_con_iva − pagos, en vez de mezclar ambos conceptos.
  - *Ref*: `app/views_modulo_reportes.py:2346-2353`
- [media] **El Excel no coincide con la pantalla y tiene N+1** — En el Excel 'Pagado' = total − descuento (línea 2610), mientras en pantalla 'pagado' = suma de los pagos registrados. Para el mismo documento las dos vistas muestran cifras distintas. Además el export hace detalles_pago.exists() (query extra que ignora el prefetch) más un Ticket y un TicketDetallePago por fila sin detalle de pago.
  - *Fix*: Extraer la construcción de filas a un helper compartido por pantalla y Excel, y reutilizar los mapas precalculados (tickets_map / pagos_ticket_map) en el export.
  - *Ref*: `app/views_modulo_reportes.py:2571-2610`
- [media] **Materializa todo el rango en memoria y filtra un tipo de documento inexistente** — list(queryset) carga todos los DTEs del rango con sus pagos para poder calcular el resumen, aunque solo se muestren 100. Y la opción 'Boleta Manual' del selector filtra tipo_documento='BOLETA', valor que no está en TIPO_DOCUMENTO_CHOICES (los válidos son 'BOLETA ELECTRONICA' y 'BOLETA PAPEL'), por lo que devuelve siempre vacío.
  - *Fix*: Calcular el resumen con .aggregate()/values().annotate() sobre el queryset y paginar en DB. Y corregir o eliminar la opción 'BOLETA' del selector (los DTE manuales usan el tipo real que eligió el operador).
  - *Ref*: `app/views_modulo_reportes.py:2103 y :2081-2082 y documentos_emitidos.html:62`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Documentos sin medio de pago registrado (calidad de datos) | dtes_sin_pagos_registrados / cantidad_dtes * 100, por sucursal | Ya se calcula en el bloque diagnóstico (app/views_modulo_reportes.py:2292 y :2339) pero no se publica como KPI | Cada documento sin pago registrado es una venta que cae a 'Efectivo' por default y ensucia la cuadratura de caja. Tener el % por tienda permite atacar el problema donde está. | bajo |
| Continuidad y salud de folios por serie | por sucursal y tipo_documento: min(folio), max(folio), cantidad emitida, folios faltantes = (max−min+1) − cantidad, y % anulados/rechazados | Dte.numero_documento, Dte.tipo_documento, Dte.sucursal, Dte.estado_dte | Riesgo SII directo y con antecedente real en este sistema (368 boletas de PAO4 reusando folios de PAO3). Un salto o un duplicado de folio hoy no lo detecta ningún reporte. | medio |
| Ticket promedio y participación por medio de pago | por categoría del resumen: monto / nº de documentos que la usaron; participación = monto_categoría / ventas_brutas | Dte_Detalle_Pago.metodo_pago y monto, ya recorridos en el bucle del resumen | El resumen da montos absolutos pero no cuántos documentos hay detrás. Saber que el efectivo son muchas boletas chicas y la transferencia pocas grandes cambia decisiones de caja y de riesgo. | bajo |

### Documentos por Vendedor (drill-down del reporte de ventas-sucursal) — `ROTO`

- **Ruta**: /app/api/reportes/documentos-vendedor/ → app/views_modulo_reportes.py:1745 obtener_documentos_vendedor_reporte (invocado desde reporte_ventas_sucursal.html:1721 verDocumentosVendedor)
- **Mide**: Detalle de los documentos de un vendedor en el período: mezcla DTEs de venta al público con Tickets POS pagados, mostrando tipo, número, fecha, cliente, sucursal, monto y estado, más un total y un conteo.
- **Filtros**: vendedor_id (obligatorio), sucursal_id (vía filtrar_queryset_por_sucursal, líneas 1788 y 1811) y los mismos modos de fecha del reporte padre. Sin paginación ni límite: devuelve todos los documentos del rango de una sola vez.
- **Fidelidad**: Es el hallazgo más embarazoso de la familia: el drill-down NO reproduce la fila desde la que se abre. La tabla de vendedores se construye con estado_dte in ('EMITIDO','ACEPTADO','ANULADO') y tipo_transaccion in (VENTA, VENTA_PUBLICO, DEVOLUCION, ANULACION), incluyendo autoventas; el detalle usa estado_dte in ('EMITIDO','PAGADO') — y 'PAGADO' NO es un valor válido de ESTADO_DTE_CHOICES (es un estado de PAGO), así que la mitad de la condición es inerte y además se pierden todos los DTEs 'ACEPTADO' —, restringe a tipo_transaccion='VENTA_PUBLICO' y excluye autoventas. Encima suma Tickets PAGADO sin filtrar dte_generado=False, o sea vuelve a contar como documento aparte el ticket cuya boleta ya está listada. El total del modal puede quedar por encima o por debajo del de la fila según qué predomine, y las NC no se restan en ninguna parte del detalle.
- **Rendimiento**: 2 queries + iteración en Python. Sin paginación: para un vendedor con 3.000 boletas al mes el JSON y el modal se vuelven pesados, pero no hay N+1.

**Problemas:**

- [alta] **El detalle usa filtros distintos a la fila que explica y el total no cuadra** — Fila padre: estados EMITIDO/ACEPTADO/ANULADO, tipos VENTA+VENTA_PUBLICO+NC (restadas), incluye autoventas. Detalle: estados EMITIDO/'PAGADO', solo VENTA_PUBLICO, excluye autoventas, sin NC. Un vendedor cuyos DTEs quedaron en estado ACEPTADO ve un modal casi vacío frente a una fila con millones. Es el clásico 'el sistema no cuadra' reportado por el usuario final.
  - *Fix*: Reutilizar exactamente el queryset de obtener_ventas_por_vendedor_reporte (extraer un helper _queryset_dtes_ventas(fi, ff, request)) y listar también las NC como filas negativas para que la suma del modal sea idéntica a la columna 'Ventas' de la fila.
  - *Ref*: `app/views_modulo_reportes.py:1777-1786`
- [alta] **Cuenta dos veces las ventas POS que generaron boleta** — qs_tickets filtra estado='PAGADO' sin dte_generado=False, mientras qs_dtes ya trae la boleta electrónica de ese mismo ticket. Cada venta POS documentada aparece dos veces en la lista (una como 'DTE' y otra como 'TICKET') y su monto se suma dos veces en el total del modal. Es exactamente el bug que ya se corrigió en comparativa-mensual y ventas-global, pero aquí quedó vivo.
  - *Fix*: Agregar dte_generado=False al filtro de tickets (línea 1804-1808), igual que en app/views_modulo_reportes.py:7345-7349.
  - *Ref*: `app/views_modulo_reportes.py:1804-1808`
- [media] **Filtra por 'PAGADO', un estado que no existe en ESTADO_DTE_CHOICES** — estado_dte__in=['EMITIDO','PAGADO']: 'PAGADO' pertenece a ESTADO_PAGO_CHOICES, no a ESTADO_DTE_CHOICES (app/models/dte.py:25-41). La condición no aporta nada y sí deja fuera 'ACEPTADO', que es un estado real y frecuente. Es un bug de copiar/pegar entre dos campos distintos.
  - *Fix*: Reemplazar por estado_dte__in=['EMITIDO','ACEPTADO','ANULADO'] alineado con el reporte padre.
  - *Ref*: `app/views_modulo_reportes.py:1782`
- [baja] **Sin paginación ni tope de filas** — Construye la lista completa en memoria y la serializa entera; con un rango anual y un vendedor activo puede devolver decenas de miles de filas al navegador.
  - *Fix*: Paginar (mismo patrón que documentos-emitidos: page/per_page con tope 500) o limitar a los 500 más recientes con aviso.
  - *Ref*: `app/views_modulo_reportes.py:1829-1838`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Composición del documento del vendedor: unidades por boleta y ticket promedio | UPT = Σ Dte.unidades_productos / nº documentos; ticket promedio = venta neta / nº documentos, con la mediana además del promedio | Dte.unidades_productos (ya está en el modelo y poblado al emitir) y los documentos que el endpoint ya lista | El modal hoy solo lista. Con UPT y mediana por vendedor se distingue al que hace pocas ventas grandes del que hace muchas chicas, que es lo que define coaching de venta cruzada. | bajo |
| Devoluciones y anulaciones atribuidas al vendedor, con el documento original | listar las NC del período donde Dte.documento_afectado pertenece al vendedor, con monto y motivo_nc | Dte.documento_afectado, Dte.motivo_nc, Dte.es_nota_credito (app/models/dte.py, sección Notas de Crédito) | Hoy la NC se atribuye al vendedor que la emitió, no al que hizo la venta original. Rastrear al vendedor de origen es lo que permite detectar ventas forzadas y errores sistemáticos de digitación. | medio |

### Ventas por Categoría / Especialidad + Indicador de Compra (dashboard de ventas) — `UTIL`

- **Ruta**: /app/api/ventas/por-categoria/, /app/api/ventas/por-especialidad/, /app/api/ventas/indicador-compra/ → app/views_modulo_ventas.py:18597 obtener_ventas_por_categoria, :18636 obtener_ventas_por_especialidad, :18670 obtener_indicador_compra_categoria (consumidos por el dashboard de ventas, no por el módulo de reportes)
- **Mide**: Ventas ($ y unidades) por categoría v1.2 (Padre › Hijo) y por especialidad multi-etiqueta, más un indicador de compra que cruza ventas del período contra stock actual para dar cobertura en días, sell-through y un veredicto (QUIEBRE / BAJO / SANO / SOBRE-STOCK / SIN ROTACIÓN).
- **Filtros**: Período (_rango_periodo), estado del ticket, sucursal_id, empresa_id, y filtros cruzados especialidad_id ↔ categoria_id (con expansión de hijas). Se aplican server-side. PERO el scoping por permisos NO existe: _scope_suc_emp solo mira los parámetros GET; si el usuario no manda sucursal_id ni empresa_id, el endpoint agrega TODAS las sucursales de todas las empresas. Contrasta con obtener_mix_por_sucursal (línea 18809), que sí parte de obtener_sucursales_usuario. La expansión de categorías padre→hijas está bien resuelta y la recategorización v1.2 está correctamente aplicada.
- **Fidelidad**: Correcto: created_at como fecha (documentado explícitamente en el helper), exclusión de excluir_de_analitica en las tres funciones, especialidad tratada como atribución (no partición) con la advertencia en el docstring, y el indicador de compra excluye centros de distribución cuando no hay sucursal — que es exactamente la regla del proyecto (bodegas EDEL/IMP no venden a público). Limitación de universo: SOLO mira Ticket_Productos. Toda venta documentada que no pasó por ticket (facturas emitidas desde emisionDTE, mayorista) queda fuera, así que estas cifras no cuadran con productos-vendidos ni con ventas-sucursal, y la categoría con más venta mayorista aparece subrepresentada. Además no aplica dte_generado (aquí no hace falta porque no suma DTEs, pero explica la brecha).
- **Rendimiento**: Bien resuelto: agregaciones puras, y en el mix se calculan las unidades totales ANTES del join con atributos para evitar el fan-out de la multi-etiqueta (comentario explícito en la línea 18856). El indicador de compra hace 2 queries agregadas. Sin N+1.

**Problemas:**

- [alta] **Los endpoints no aplican el scoping por permisos del usuario** — _scope_suc_emp devuelve el queryset sin filtrar cuando no vienen sucursal_id ni empresa_id en la URL. Un usuario asignado a una sola tienda que llame /app/api/ventas/por-categoria/ sin parámetros obtiene las ventas consolidadas de TODAS las empresas del holding. El middleware tampoco lo cubre: URL_PERMISO_MAP mapea '/app/ventas/dashboard' pero '/app/api/ventas/por-categoria/' no contiene esa cadena.
  - *Fix*: Que _scope_suc_emp aplique siempre un filtro base sucursal_id__in=obtener_sucursales_usuario(request.user) y valide que el sucursal_id/empresa_id pedido esté dentro de ese universo, como ya hace obtener_mix_por_sucursal.
  - *Ref*: `app/views_modulo_ventas.py:18562-18581`
- [media] **El universo son solo tickets: la venta facturada queda fuera** — _tickets_pagados_periodo parte de Ticket y las tres funciones agregan sobre Ticket_Productos. Las ventas emitidas directamente como factura (sin ticket POS) no existen para estos gráficos. En una empresa con venta mayorista relevante, la participación por categoría está sesgada y el indicador de compra subestima la demanda de las categorías que se venden por factura.
  - *Fix*: Sumar Dte_Productos de DTEs de venta sin ticket asociado (o reutilizar el motor _agregar_productos_vendidos, que ya resuelve el anti-doble-conteo), y rotular claramente si el gráfico es 'venta de público' o 'venta total'.
  - *Ref*: `app/views_modulo_ventas.py:18584-18592`
- [media] **La cobertura se calcula con el stock de hoy contra ventas de un período pasado** — cobertura_dias = stock_actual / (unidades del período / días). Si se consulta un período histórico (mayo, por ejemplo), el numerador es el stock de hoy: el resultado no es la cobertura que hubo en mayo ni la de hoy. El veredicto SOBRE-STOCK/QUIEBRE que se muestra hereda esa mezcla.
  - *Fix*: Restringir el veredicto a períodos que terminan hoy (o advertirlo en la UI), y para históricos usar Movimientos_Producto para reconstruir el stock a la fecha de corte.
  - *Ref*: `app/views_modulo_ventas.py:18730-18749`
- [baja] **Los umbrales del veredicto son fijos y no distinguen temporada** — QUIEBRE <15 días, BAJO <30, SANO ≤120, SOBRE-STOCK >120, hardcodeados. Con la línea base del catálogo en 641 días de cobertura, prácticamente todo cae en SOBRE-STOCK y el semáforo pierde poder discriminante — el propio comentario del código lo reconoce y por eso agregó el sell-through.
  - *Fix*: Parametrizar los umbrales por categoría/temporada en ConfiguracionPermisoGlobal o una tabla de parámetros, y ordenar el listado por sell-through cuando la cobertura satura.
  - *Ref*: `app/views_modulo_ventas.py:18740-18749`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Share de venta vs share de inventario por categoría | share_venta = venta_categoría / venta_total; share_stock = (Σ stock × costo de la categoría) / capital total en stock; brecha = share_venta − share_stock | Ticket_Productos.subtotal/stock + Producto_Talla.stock × Producto.costo (ambos ya usados en obtener_indicador_compra_categoria) | Es la lectura más directa de dónde sobra y falta plata: una categoría con 5% de la venta y 25% del inventario es exactamente el capital muerto que hay que liquidar. Los dos numeradores ya se calculan por separado en este mismo endpoint. | bajo |
| Margen bruto por categoría y por especialidad | margen = Σ(Ticket_Productos.subtotal − costo_fifo × stock) agrupado por categoría/especialidad | Ticket_Productos.costo_fifo y subtotal (app/models/ventas.py:340 y :361) | El indicador de compra decide qué reponer mirando solo rotación. Reponer lo que rota rápido pero deja poco margen es una decisión distinta a reponer lo que rota bien y deja margen; hoy no se puede distinguir. | medio |
| Cobertura de etiquetado de especialidad | unidades vendidas de productos con al menos una etiqueta 'Especialidad' / unidades vendidas totales | ProductoAtributoValor filtrado por atributo__nombre='Especialidad' vs las líneas de venta del período | El gráfico de especialidad es una vista de atribución y hoy no dice qué proporción del catálogo está etiquetada. Sin ese dato, una especialidad que 'no vende' puede simplemente estar sin etiquetar. (El mix por sucursal ya expone cobertura_pct; falta replicarlo aquí.) | bajo |

### Ventas Internet — `MEJORABLE`

- **Ruta**: /app/reportes/ventas-internet/ → app/views_modulo_reportes.py:7146 ver_reporte_ventas_internet; API: app/views_modulo_reportes.py:7153 obtener_reporte_ventas_internet (motor: _construir_reporte_ventas_internet, línea 6632); Excel: línea 7163
- **Mide**: Venta del canal internet reconstruida desde dos fuentes: tickets con modulo_origen='ECOMMERCE' y tickets con un pago de método 'VENTA_INTERNET' (venta telefónica/web cobrada en POS). Desglosa por empresa, sucursal, plataforma/marketplace, origen, vendedor, producto y día, con alertas de calidad de datos.
- **Filtros**: Rango de fechas (default: año a la fecha) o todo_historico=1, empresa, sucursal (validando contra las sucursales permitidas, líneas 6667-6671), plataforma/canal, origen ECOMMERCE|POS, vendedor y buscador de texto. El buscador es el mejor de la familia y busca donde dice: correlativo, nombre y RUT de cliente, voucher, tipo de tarjeta y los identificadores del PedidoEcommerce (numero_ticket_rm, numero_pedido_canal, correlativo, cliente_nombre, cliente_documento), todo con icontains y distinct. El scoping por permisos parte de obtener_sucursales_usuario, correcto.
- **Fidelidad**: Fecha correcta y con el comentario explícito de por qué no se usa Ticket.fecha (auto_now). La lógica de proporción para tickets mixtos (parte internet, parte efectivo) está bien pensada y las alertas (ecommerce_sin_pago_internet, tickets_mixtos, sin_plataforma) son un acierto poco común: el reporte se autodiagnostica. Riesgos: (1) el fallback cuando un ticket ECOMMERCE no tiene pago VENTA_INTERNET asigna el TOTAL del ticket al canal internet, lo que puede sobreestimar (queda registrado en monto_fallback_ecommerce, bien); (2) no descuenta devoluciones ni NC en ningún punto — la venta internet siempre es bruta; (3) se solapa conceptualmente con el canal ECOMMERCE del reporte comparativo, que calcula lo mismo con otra definición (solo modulo_origen='ECOMMERCE', sin los pagos VENTA_INTERNET): los dos reportes darán cifras distintas de 'ventas internet' para el mismo mes.
- **Rendimiento**: Es el reporte más caro de la familia por diseño: itera el queryset COMPLETO en Python (for ticket in queryset, línea 6737) con prefetch de pagos, ticket_productos→ProductoTalla→producto y pedidos_ecommerce→dte; la paginación se aplica solo a la lista 'detalle' al final, no a la carga. Con todo_historico=1 carga todos los tickets de internet de la historia con sus líneas y productos en memoria de una vez. Además el Q(modulo_origen=...)|Q(pagos__metodo_pago=...) con distinct() sobre un join a pagos degrada en rangos amplios.

**Problemas:**

- [alta] **Todo el reporte se calcula iterando el queryset completo en Python** — El motor recorre ticket por ticket para acumular empresas, plataformas, orígenes, vendedores, productos, boletas, ventas diarias y alertas. La paginación (Paginator) se aplica solo sobre la lista 'detalle' YA construida, así que no ahorra nada: el costo es proporcional al total de tickets del rango, no a la página. Con todo_historico=1 es un full scan con tres niveles de prefetch.
  - *Fix*: Migrar los agregados (por empresa, plataforma, origen, vendedor, día) a values().annotate() en DB y dejar el bucle Python solo para la página visible del detalle. Como mínimo, poner un tope duro al rango y desactivar todo_historico salvo para el export.
  - *Ref*: `app/views_modulo_reportes.py:6637-6649 y :6737`
- [media] **No descuenta devoluciones: la venta internet siempre es bruta** — No hay ninguna referencia a notas de crédito ni a CambioDevolucionDetalle en las ~500 líneas del motor. El canal con mayor tasa de devolución del retail (internet) se reporta sin netear, y el 'ticket promedio' y la participación por plataforma quedan sobreestimados.
  - *Fix*: Restar las NC asociadas a los tickets del período (vía Ticket.folio_dte → Dte.documento_afectado) o los CambioDevolucionDetalle de esos tickets, y exponer la tasa de devolución por plataforma.
  - *Ref*: `app/views_modulo_reportes.py:6737-6800`
- [media] **Define 'venta internet' distinto que el reporte comparativo** — Aquí venta internet = ECOMMERCE ∪ pagos VENTA_INTERNET. En _sumar_ventas_comparativo el canal ECOMMERCE = solo tickets con modulo_origen='ECOMMERCE' (línea 7725). Los KPIs 'pct_internet' del comparativo y el 'total ventas' de este reporte no van a coincidir nunca, y ninguna de las dos pantallas lo advierte.
  - *Fix*: Unificar la definición en un helper compartido (o al menos documentar en ambas UIs qué incluye cada una) y hacer que el comparativo use la misma regla ampliada.
  - *Ref*: `app/views_modulo_reportes.py:6640-6641 vs :7725`
- [media] **La página y su API no están en el mapa de permisos** — El menú la muestra bajo permiso_reporte_ventas_internet, pero URL_PERMISO_MAP no tiene '/app/reportes/ventas-internet/' ni su endpoint. Como obtener_codigo_opcion devuelve None para rutas no mapeadas y el middleware permite por defecto, cualquier usuario autenticado accede escribiendo la URL (los datos sí quedan acotados a sus sucursales).
  - *Fix*: Agregar '/app/reportes/ventas-internet/', '/app/reportes/ventas-comparativo/' y '/app/reportes/productos-vendidos/' (más sus endpoints /app/api/reportes/...) al URL_PERMISO_MAP.
  - *Ref*: `app/middleware_permisos.py:82-93 y :224-229`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Tasa de devolución y de anulación por plataforma/marketplace | NC o CambioDevolucion asociadas a tickets de la plataforma / venta bruta de la plataforma * 100 | PedidoEcommerce.canal_origen, Ticket.folio_dte → Dte.documento_afectado, CambioDevolucionDetalle | Cada marketplace tiene comportamiento de devolución distinto y eso define su rentabilidad real. Hoy el reporte compara plataformas solo por venta bruta, que es la mitad de la historia. | medio |
| Lead time de despacho: días entre pedido y emisión del documento | promedio y p90 de (Dte.fecha_emision − PedidoEcommerce.fecha_pedido) por canal | PedidoEcommerce (fecha del pedido y FK a dte, ya prefetcheado en la línea 6647), Dte.fecha_emision | Es el KPI operacional que falta: el reporte mide cuánto se vendió pero no cuánto se demora en salir, que es lo que penalizan los marketplaces con rating. | medio |
| Cobertura de identificación de plataforma | tickets con plataforma identificada / tickets internet totales (ya existe el conteo crudo en alertas['sin_plataforma']) | TicketDetallePago.tipo_tarjeta y PedidoEcommerce.canal_origen | La alerta ya cuenta los casos pero no los expresa como porcentaje sobre el total. Si el 30% de la venta internet no tiene canal identificado, todo el análisis por plataforma del reporte es una muestra sesgada y hay que decirlo arriba, no en una alerta al pie. | bajo |

## Familia: EXISTENCIAS / INVENTARIO

### Resumen de Existencias (por sucursal / por categoría, con corte histórico) — `MEJORABLE`

- **Ruta**: /app/reportes/resumen-existencias/ — app/views_resumen_existencias.py:24 ver_resumen_existencias, :468 obtener_resumen_existencias, :621 _resumen_por_categoria, :126 _aggregar_por_sucursal_historico, :812/:1191 exports Excel/PDF
- **Mide**: Pares (unidades) y valorización del inventario agregados por sucursal o por categoría raíz v1.2, con opción de reconstruir el stock a una fecha de corte pasada. Tres valorizaciones en paralelo: costo (producto.costo), precio interno (costo+sobreprecio) y precio venta (precioventa). Permite excluir artículos puntuales del análisis y muestra explícitamente cuánto se dejó fuera.
- **Filtros**: REALES server-side: marca (producto__atributo1_id, :96-99), categoría (producto__categoria_id exacto, :99 — OJO: aquí NO usa _expandir_categoria_ids, así que filtrar por un padre v1.2 devuelve solo los productos colgados literalmente del padre y pierde toda la rama, a diferencia de los otros reportes), fecha_corte (:481-486), agrupar_por sucursal|categoria, excluir_articulos (CSV de IDs, :354). El scoping multi-empresa sí se aplica (EmpresaUser, :489-492) y se propaga a los helpers. NO hay filtro por sucursal individual, ni por especialidad, ni por género, ni separación CD vs tienda. Los exports Excel/PDF reutilizan la misma API (:826), así que respetan exactamente los mismos filtros — bien.
- **Fidelidad**: Aciertos: excluye excluir_de_analitica en todas las ramas (:93, :133, :242, :274); el bucket 'Sin categoría' y 'Sin recategorizar' evitan que se pierda stock invisible (:677-686); los excluidos se reportan aparte (:365). Riesgos reales: (1) el corte histórico revierte ingresos buscando la clave (talla, sucursal_destino_id) (:164-167), pero la migración Laravel dejó sucursal_destino=NULL en los ingresos y guardó la sucursal en sucursal_origen — el propio proyecto ya lo documentó y lo corrigió en movimientos-sucursal con COALESCE(destino, origen) (views_modulo_reportes.py:5415-5450), pero aquí NO: todo ingreso migrado queda sin revertir y el stock histórico sale SOBREVALORADO para cortes anteriores a la migración. (2) La valorización histórica usa el costo/precio de HOY (:195-206), no el costo vigente a la fecha ni el costo de lote: 'valor del inventario al 31/12' es una cifra fabricada; solo las UNIDADES históricas son defendibles. (3) El filtro de ingresos usa tipo_movimiento='INGRESO' (:163) en vez del signo de cantidad; con la data legacy que quedó con egresos etiquetados INGRESO (documentado en models/inventario.py:208-215) esos egresos se restan como si fueran ingresos e inflan aún más el histórico. (4) abs() se aplica al Sum agregado (:191), no fila a fila: si un grupo mezcla signos, el neto se suma con signo equivocado. (5) Todo sale del stock plano Producto_Talla.stock, que tiene drift conocido contra los lotes FIFO; ningún KPI advierte el descalce. (6) verificar_disponibilidad_historico (:2021-2025) cuenta movimientos de TODA la base sin filtrar por empresa: informa un rango histórico que puede no existir para el tenant y filtra un conteo global.
- **Rendimiento**: Es el mejor del grupo: agregación SQL pura para el modo actual (1 query, :104-113) y 3 queries para el histórico (:143-181). Sin N+1. El punto débil es el histórico: materializa TODAS las tallas del universo en Python (list(talla_qs.values(...)), :143) y pasa esa lista de IDs como IN gigante a dos queries de movimientos (:159, :162) — con ~200k tallas eso es una sentencia enorme y varios cientos de MB de dicts. calcular_stock_historico (:30-83) es código muerto: no lo llama nadie (grep confirma una sola ocurrencia, su propia definición); borrar.

**Problemas:**

- [alta] **El corte histórico ignora los ingresos migrados (destino NULL) y sobrevalora el stock pasado** — _aggregar_por_sucursal_historico indexa los ingresos por (ProductoTalla_id, sucursal_destino_id) y los busca con sid=producto.sucursal_id. La migración Laravel guardó la sucursal SIEMPRE en sucursal_origen dejando sucursal_destino=NULL, incluso para INGRESO_INICIAL y RECEPCION_COMPRA. Esos ingresos quedan en la clave (talla, None), nunca hacen match, y por tanto NO se restan del stock actual. Resultado: para cualquier fecha de corte anterior a la migración, el reporte declara que en esa fecha ya existía stock que en realidad entró después. El mismo bug está en _aggregar_por_categoria_historico (:302), _calcular_totales_excluidos (:402) y _calcular_detalle_top_por_sucursal (:1751, que además fija sucursal_destino_id=sucursal_id explícitamente).
  - *Fix*: Reusar exactamente la lógica ya escrita y probada en views_modulo_reportes.py:5415-5450 (_mapa_entrada): sumar por destino cuando sucursal_destino no es NULL y por origen cuando es NULL (conjuntos disjuntos, sin doble conteo). Idealmente extraer ese helper a app/constants_kardex.py o a un utils_inventario.py y que los cuatro puntos lo importen.
  - *Ref*: `app/views_resumen_existencias.py:156-181 (y :294-318, :398-414, :1747-1765)`
- [alta] **La valorización histórica usa costos y precios de hoy** — En el modo histórico el stock se reconstruye a la fecha de corte pero costo, sobreprecio y precioventa se leen del maestro Producto actual (:195-206). Si un producto se recosteó o entró en liquidación, el 'inventario valorizado al 30/06' se calcula con el precio de julio. Los tres totales monetarios del cierre histórico (costo, precio interno, precio venta) no son auditables ni sirven para conciliar con contabilidad.
  - *Fix*: Para el modo histórico valorizar a costo de lote: reconstruir con LoteProducto (cantidad_disponible/costo_unitario y consumos) o, como mínimo viable, usar Movimientos_Producto.costo del último ingreso anterior a la fecha de corte por talla. Si no se hace, etiquetar la columna en UI/Excel como 'valorizado a precios de hoy' para no inducir a error.
  - *Ref*: `app/views_resumen_existencias.py:195-206`
- [media] **El filtro de categoría no expande la rama v1.2** — Se filtra con producto__categoria_id=categoria_id exacto (:99, :138) mientras que existencias-marca, movimientos-sucursal y reporte-existencias usan _expandir_categoria_ids (padre + hijas). Al elegir 'Calzado' el usuario ve casi cero stock porque los productos cuelgan de las hijas, y el mismo filtro en otro reporte da un número distinto.
  - *Fix*: Importar _expandir_categoria_ids desde views_modulo_reportes y aplicar categoria_id__in en :99 y :138 (el agrupado por categoría ya sube a raíz, así que solo cambia el filtro).
  - *Ref*: `app/views_resumen_existencias.py:99`
- [media] **Ingresos/egresos se clasifican por tipo_movimiento en vez del signo de cantidad** — Q(tipo_movimiento='INGRESO') | Q(concepto='TRASPASO_ENTRADA'). El save() del modelo solo empezó a derivar el tipo del signo después del fix documentado en models/inventario.py:208-215; la data anterior tiene egresos etiquetados INGRESO. Esos movimientos entran a la bolsa de ingresos con cantidad negativa y, al restarse, SUMAN stock histórico.
  - *Fix*: Sustituir el criterio por cantidad__gt=0 / cantidad__lt=0 (el signo es la convención canónica del kardex) y sumar por fila, no aplicar abs() sobre el Sum agregado.
  - *Ref*: `app/views_resumen_existencias.py:163 y :177`
- [baja] **Código muerto: calcular_stock_historico** — Función de 54 líneas con 2 queries por talla, documentada como la fórmula oficial del corte histórico, pero ya no la invoca nadie (fue reemplazada por los agregados). Es una trampa: quien la lea creerá que ahí vive la lógica y la corregirá sin efecto.
  - *Fix*: Borrarla o convertirla en docstring del módulo.
  - *Ref*: `app/views_resumen_existencias.py:30-83`
- [baja] **verificar-disponibilidad-historico consulta la tabla completa sin scoping** — Movimientos_Producto.objects.count() y el primer/último movimiento globales, sin filtrar por las empresas del usuario. Un usuario de una empresa ve el volumen total del sistema y un rango de fechas que puede no corresponder a sus datos.
  - *Fix*: Filtrar por ProductoTalla__producto__sucursal__empresa_id__in=empresas_usuario y cachear el resultado (es una consulta cara para un mensaje informativo).
  - *Ref*: `app/views_resumen_existencias.py:2021-2025`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Cobertura en días por sucursal (días de stock) | pares_sucursal / (unidades_vendidas_ultimos_90d / 90); marcar 'sin rotación' cuando el denominador es 0 | pares ya calculados en _aggregar_por_sucursal_actual; ventas = Movimientos_Producto con concepto__in=CONCEPTOS_VENTA, estado='COMPLETADO', sucursal_origen_id=suc, fecha>=hoy-90 → abs(Sum(cantidad)) | Hoy el reporte dice cuánto hay pero no si sobra. Con la línea base conocida (~641 días de cobertura) este KPI es el que decide congelar compra por sucursal y disparar traspasos desde el CD. | bajo |
| Capital inmovilizado >180 días por sucursal y por categoría v1.2 | Sum(cantidad_disponible * costo_unitario) sobre LoteProducto activo, no agotado, fecha_ingreso <= hoy-180, agrupado por producto.sucursal y por categoria.padre›categoria | LoteProducto + Producto.categoria/atributo1 (misma agregación ya escrita en views.py:22891-22909 para el dashboard FIFO) | Convierte el resumen de 'cuántos pares tengo' en 'cuánta plata tengo detenida y dónde', que es lo que alimenta el Plan de Liquidación. Requiere haber corrido corregir_fecha_ingreso_lotes. | medio |
| Split CD/bodega vs tienda vendedora | Dos subtotales de pares y costo: sucursal.es_centro_distribucion=True vs False (o tipo_sucursal) | Sucursal.es_centro_distribucion / tipo_sucursal (organizacion.py:105-116) | Hoy el total mezcla bodegas mayoristas (EDEL/GILD/IMP) con tiendas; el promedio resultante no significa nada. Separarlos es lo mínimo para leer el número. | bajo |
| Margen potencial del inventario | (Sum(precioventa*stock) - Sum((costo+sobreprecio)*stock)) / Sum(precioventa*stock), por sucursal y por categoría | columnas total_p_venta y total_p_interno que la API YA calcula (:110-111) — solo falta exponer el ratio | Las tres valorizaciones ya están ahí pero el usuario tiene que dividir a mano; el margen por sucursal muestra dónde el sobreprecio del CD se comió la rentabilidad. | bajo |
| Delta vs cierre anterior (unidades) | pares_hoy - pares_a_fecha_corte, por sucursal, usando la misma reconstrucción histórica | _aggregar_por_sucursal_actual vs _aggregar_por_sucursal_historico en una sola respuesta | El corte histórico existe pero se usa como foto aislada; comparado contra hoy responde '¿la sucursal está acumulando o drenando stock?' sin exportar dos veces y restar en Excel. Solo válido en unidades, no en pesos (ver problema de valorización). | medio |

### Reporte de Existencias (detalle SKU-talla) — `UTIL`

- **Ruta**: /app/reportes/existencias/ — app/views.py:31897 ver_reporte_existencias, :31904 obtener_existencias_reporte, :32177 exportar_existencias_excel; template vistas/reporte_existencias.html
- **Mide**: Listado paginado a nivel SKU-talla con stock, costo maestro, costo FIFO derivado de lotes y PVP, más KPIs globales del universo filtrado (stock total, SKUs sin stock, SKUs bajo stock, valor FIFO, valor venta).
- **Filtros**: REALES server-side y aplicados sobre el dataset completo antes de paginar: sucursal (:31957), categoría con expansión de rama v1.2 (:31962-31966), búsqueda sobre sku/artículo/descripción/talla (:31970-31976), estado_stock con 4 cortes en DB (:31980-31987). Paginación real (:32046-32048) y los KPIs se calculan sobre TODO el filtrado, no sobre la página (:32005-32033) — hecho correctamente. El combo de categorías se arma como 'Padre (toda la rama)' + 'Padre › Hija' ocultando las _ZZ_ (:32118-32141), coherente con la recategorización v1.2. FALTA el filtro por marca (atributo1), que es el eje principal del negocio, y no hay filtro por género (atributo3) ni por especialidad.
- **Fidelidad**: Scoping multi-empresa correcto por queryset base (:31936-31951): pedir una sucursal ajena devuelve vacío, no fuga. Excluye excluir_de_analitica (:31951). Riesgos: (1) la columna costo_fifo cae silenciosamente a producto.costo cuando el SKU no tiene lotes (:32077-32081) — la columna mezcla dos fuentes sin bandera, y con el drift conocido entre stock plano y lotes el usuario no puede saber cuál está viendo; (2) valor_inventario_fifo (KPI global, :32022-32033) suma lotes mientras total_stock suma el campo plano: los dos KPIs de la misma tarjeta describen inventarios distintos y su cociente (costo medio implícito) es inconsistente; (3) sin_stock/bajo_stock cuentan PRODUCTOS agregados mientras total_registros cuenta tallas — mezcla de granularidad en la misma tarjeta; (4) el filtro de lotes usa activo=True y cantidad_disponible>0 pero no agotado=False, criterio distinto al del dashboard FIFO (views.py:22872) — en la práctica coincide, pero conviene unificar.
- **Rendimiento**: Sano: 4 agregados + 1 count + 1 página de 100 filas + 1 query de lotes para la página. El punto caro es valor_fifo_global, que hace LoteProducto.filter(producto_talla__in=productos_talla) con el queryset completo como subconsulta (:32023-32024): sobre el universo sin filtros son cientos de miles de filas y ese agregado se recalcula en CADA cambio de página. Cachearlo o calcularlo solo cuando pagina==1.

**Problemas:**

- [media] **No existe filtro por marca** — El reporte más granular del módulo no permite filtrar por atributo1 (marca). El buscador cubre sku/artículo/descripción/talla pero no marca ni color, así que para 'todo NIKE en PAO3' hay que irse a otro reporte (existencias-marca) que a su vez trunca a 500 filas.
  - *Fix*: Añadir marca_id (producto__atributo1_id) y, ya que la query lo tiene barato, genero_id (producto__atributo3_id, 100% poblado) al bloque de filtros de :31954-31987 y al combo del frontend.
  - *Ref*: `app/views.py:31969-31977`
- [media] **costo_fifo mezcla costo de lote y costo maestro sin avisar** — Si el SKU tiene lotes se muestra el costo promedio ponderado de los lotes; si no, se muestra producto.costo con el mismo formato y encabezado. Dado el drift documentado entre stock plano y lotes, una columna que no distingue la fuente induce a decisiones de precio sobre datos heterogéneos.
  - *Fix*: Devolver un campo extra tiene_lotes (bool) y unidades_en_lotes, y en la tabla marcar visualmente las filas sin lotes o con |unidades_en_lotes - stock| > 0.
  - *Ref*: `app/views.py:32077-32081`
- [media] **Los KPIs globales mezclan stock plano y lotes en la misma tarjeta** — total_stock sale de Producto_Talla.stock y valor_inventario_fifo de LoteProducto.cantidad_disponible. Con drift, el 'costo unitario medio' implícito (valor/stock) es falso y el usuario no tiene forma de detectarlo.
  - *Fix*: Agregar un KPI 'unidades en lotes' junto a 'stock total' y mostrar el descalce en % (drift). Es la misma resta que ya hace el comando read-only reconciliar_stock_lotes.
  - *Ref*: `app/views.py:32005-32041`
- [baja] **valor_fifo_global se recalcula en cada página** — La subconsulta de lotes sobre el queryset completo se ejecuta en todas las peticiones de paginación, aunque el resultado no cambia entre páginas del mismo filtro.
  - *Fix*: Calcular los KPIs globales solo cuando pagina==1 o cuando el frontend mande kpis=1, y cachearlos en el cliente mientras el filtro no cambie (mismo patrón que ya se usa con cargar_filtros).
  - *Ref*: `app/views.py:32022-32033`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Días sin venta por SKU (edad comercial) | hoy - MAX(Movimientos_Producto.fecha) con concepto__in=CONCEPTOS_VENTA y estado='COMPLETADO' por ProductoTalla; NULL = nunca vendido | Movimientos_Producto + constants_kardex.CONCEPTOS_VENTA (una anotación Max sobre la página actual, barata) | Es el corte que distingue 'stock' de 'stock muerto'. Ordenando la tabla por días sin venta desc, el comprador obtiene directamente la lista de liquidación sin cruzar reportes. | bajo |
| Drift lotes vs stock plano por SKU | stock (Producto_Talla.stock) - Sum(LoteProducto.cantidad_disponible activo y disponible>0) | Producto_Talla + LoteProducto (la query de lotes por página ya existe en :32057-32064, solo falta devolver total_cantidad) | Expone el problema de integridad que hoy solo se ve corriendo un comando por consola, y en el mismo lugar donde el usuario mira el stock. Sin esto, la valorización FIFO del reporte no es auditable. | bajo |
| Antigüedad del stock (días del lote más antiguo) por SKU | hoy - MIN(LoteProducto.fecha_ingreso) con activo=True, agotado=False, cantidad_disponible>0 | LoteProducto.fecha_ingreso (requiere haber aplicado corregir_fecha_ingreso_lotes) | Permite filtrar el detalle por bucket de antigüedad (0-90/90-180/180-365/>365) con los mismos cortes del dashboard FIFO, y hace que el listado detallado sirva para armar campañas, no solo para consultar. | medio |
| Cobertura por SKU en la sucursal filtrada | stock / (unidades vendidas del SKU en esa sucursal últimos 90d / 90) | Producto_Talla.stock + Movimientos_Producto (CONCEPTOS_VENTA, sucursal_origen) | Complementa 'días sin venta' para los SKUs que sí rotan: distingue el que tiene 3 meses de stock del que tiene 5 años. Es el input directo de reposición/traspaso. | medio |

### Existencias por Marca (matriz SKU × sucursal) — `ROTO`

- **Ruta**: /app/reportes/existencias-marca/ — app/views_modulo_reportes.py:2660 ver_reporte_existencias_marca, :2680 obtener_reporte_existencias_marca, :2913 exportar_existencias_marca_excel
- **Mide**: Pivot de stock por artículo/marca/color/departamento contra todas las sucursales del usuario, pensado para ver dónde está repartida una marca. Añade un bloque 'salud' con cobertura en días y % de stock viejo del universo filtrado.
- **Filtros**: Server-side: marca (:2756), departamento con expansión de rama v1.2 (:2759-2761), búsqueda sobre artículo/marca/color (:2763-2769), sucursal ('todas' o una, con fallback a la sucursal de sesión, :2701-2708), solo_con_stock (:2778, y se reaplica sobre el total agrupado en :2843-2847), límite configurable (default 500). Exige al menos un filtro salvo sin_filtro=true (:2710-2719), lo cual es razonable. El export Excel reutiliza la API (:2926) así que respeta los filtros. El input 'Buscar Marca' del template es solo un filtro del combo en el navegador (filtrarMarcas), no un filtro de datos — no confundir.
- **Fidelidad**: Excluye excluir_de_analitica (:2744). Dos defectos graves: (1) sucursal_id NO se valida contra las empresas del usuario (:2727-2728: Sucursal.objects.filter(id=sucursal_id) a secas), así que cualquier usuario autenticado puede pedir el stock, costo y PVP de una sucursal de otra empresa pasando el ID por querystring — es la misma clase de fuga que ya se corrigió en ventas-global; (2) el límite se aplica a filas Producto (una por sucursal) ANTES de agrupar por SKU (:2782 seguido de :2803-2841), de modo que con 'todas las sucursales' y 12 sucursales las 500 filas equivalen a ~40 artículos y, peor, los totales por fila quedan truncados: un mismo SKU puede mostrar stock de 3 sucursales y omitir las otras 9 sin ninguna advertencia visible. Además costo y precio_venta se toman del PRIMER producto del grupo (:2821-2822) aunque cada sucursal tenga su propio costo/sobreprecio. El bloque 'salud' (:2852-2878) divide stock viejo medido en LOTES por stock total medido en el campo PLANO, así que pct_stock_viejo puede pasar de 100% o quedar subestimado según el drift.
- **Rendimiento**: Aceptable por el límite: 1 query de productos + 1 de tallas + 2 del bloque salud. El bloque salud pasa productos_ids como IN (hasta 500, tolerable). El riesgo está en sin_filtro=true con limite alto: la agrupación en Python es O(n) pero el JSON de salida crece sin techo. La anotación total_stock_anotado (:2773-2775) se usa solo para el filtro y luego se descarta: el stock real se recalcula en una segunda query (:2796-2801) — trabajo duplicado.

**Problemas:**

- [alta] **Fuga entre empresas: sucursal_id no se valida contra EmpresaUser** — Cuando llega sucursal_id la vista hace Sucursal.objects.filter(id=sucursal_id) sin cruzar con las empresas del usuario, y esa lista alimenta el filtro de productos. Un usuario de la empresa A que pase ?sucursal_id=<id de B>&sin_filtro=true obtiene artículos, stock, costo y PVP de la empresa B. El fallback a la sucursal de sesión tampoco se valida. El mismo endpoint alimenta el export a Excel.
  - *Fix*: Resolver siempre las sucursales permitidas primero (empresas_usuario) y luego intersectar: sucursales = Sucursal.objects.filter(id=sucursal_id, empresa_id__in=empresas_usuario); si queda vacío, devolver 403. Es el patrón que ya usa obtener_reporte_existencias_sucursal en :3186-3190.
  - *Ref*: `app/views_modulo_reportes.py:2727-2728`
- [alta] **El límite trunca filas-producto antes de agrupar por SKU: los totales que muestra son falsos** — El slice [:limite] se aplica sobre Producto (1 registro por sucursal) y solo después se agrupa por (artículo, marca, color, categoría). Con N sucursales, el reporte muestra ~limite/N artículos y, para los artículos que sí aparecen, el total_stock y la fila de sucursales están recortados arbitrariamente por el orden alfabético. La respuesta no marca truncamiento (solo un debug.limite_aplicado que la UI no muestra), así que el usuario lee totales incompletos como si fueran el inventario de la marca.
  - *Fix*: Limitar por SKU agrupado, no por fila: obtener primero los N artículos distintos (values('articulo','atributo1_id','atributo2_id','categoria_id').distinct()[:limite]) y luego traer TODAS sus filas producto de todas las sucursales. Y devolver truncado=True + total_disponible para que la UI avise.
  - *Ref*: `app/views_modulo_reportes.py:2782 y :2803-2841`
- [media] **El bloque 'salud' divide lotes por stock plano** — pct_stock_viejo = 100 * Sum(LoteProducto.cantidad_disponible con fecha_ingreso<=hoy-181) / Sum(Producto_Talla.stock). Numerador y denominador vienen de fuentes que no cuadran entre sí (drift documentado), y el numerador usa 181 días mientras el resto del sistema usa 180.
  - *Fix*: Calcular numerador y denominador ambos sobre lotes (y exponer aparte el stock plano), o ambos sobre stock plano usando la antigüedad del lote más antiguo por SKU. Unificar el corte en 180 días con los cortes del dashboard FIFO.
  - *Ref*: `app/views_modulo_reportes.py:2867-2875`
- [media] **Costo y PVP del grupo se toman de una sucursal arbitraria** — Al fusionar variantes de distintas sucursales en una fila, costo y precio_venta se fijan con el primer producto que aparece (orden por marca/artículo). Sucursales con sobreprecio de CD distinto quedan representadas por un costo que no es el suyo, y el Excel exporta ese valor único.
  - *Fix*: O bien mostrar rango (min-max) por SKU, o bien devolver el costo/PVP dentro de cada celda de stock_por_sucursal (ya es un dict por sucursal).
  - *Ref*: `app/views_modulo_reportes.py:2821-2822`
- [baja] **Doble cálculo del stock por producto** — annotate(total_stock_anotado=Sum('producto_talla__stock')) se usa solo para filtrar y luego se recalcula el mismo Sum en una query aparte para armar tallas_por_producto.
  - *Fix*: Reusar la anotación (producto.total_stock_anotado) y eliminar la segunda query, o mover el filtro solo_con_stock al final sobre el agrupado.
  - *Ref*: `app/views_modulo_reportes.py:2773-2801`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Índice de dispersión / concentración por SKU | nº de sucursales con stock>0 sobre el total de sucursales activas, y % del stock que está en la sucursal top (max(stock_suc)/total_stock) | el dict stock_por_sucursal que la API ya construye (:2823) | Este reporte existe para decidir traspasos. 'El 80% del stock de la marca está en una sola bodega' es la señal accionable; hoy el usuario tiene que leerlo a ojo en una matriz de 12 columnas. | bajo |
| Quiebre de talla por artículo y sucursal | tallas con stock>0 / tallas del curvado del artículo, por sucursal; alertar bajo 60% | Producto_Talla (talla, stock) agrupado por producto — ya se consultan todas las tallas en :2796 | En calzado el stock total engaña: 40 pares en 2 tallas no venden. Es el único KPI que convierte esta matriz en decisión de reposición real y los datos ya están en la query. | medio |
| Venta 90d y cobertura por marca y sucursal | abs(Sum(cantidad)) de Movimientos_Producto con concepto__in=CONCEPTOS_VENTA, estado='COMPLETADO', fecha>=hoy-90, agrupado por sucursal_origen; cobertura = stock_sucursal / (venta_90/90) | Movimientos_Producto + constants_kardex.CONCEPTOS_VENTA (el bloque salud ya hace la mitad de esta query, pero solo a 30 días y sin abrir por sucursal) | La matriz muestra dónde está la mercadería pero no dónde se vende. Con la venta al lado, el traspaso deja de ser intuición: se mueve de la sucursal con cobertura 900 días a la de 40. | medio |
| Capital detenido por marca (>180d, a costo) | Sum(cantidad_disponible*costo_unitario) de lotes >180d agrupado por atributo1 | LoteProducto + Producto.atributo1 (agregación idéntica a la de views.py:22902-22909) | Es el número con el que se negocia con el proveedor de esa marca antes de recomprar. Ya está calculado en el dashboard FIFO pero solo para la sucursal de sesión; aquí corresponde a nivel marca. | medio |

### Existencias por Sucursal (detalle SKU-talla de una sucursal) — `REDUNDANTE`

- **Ruta**: /app/reportes/existencias-sucursal/ — app/views_modulo_reportes.py:3168 ver_reporte_existencias_sucursal, :3175 obtener_reporte_existencias_sucursal, :3334 Excel, :3454 PDF; template modulo_reportes/reporte_existencias_sucursal.html
- **Mide**: Listado talla a talla de una sucursal con 'stock inicial' (todo lo recibido históricamente), stock actual, costo, sobreprecio y PVP, más KPIs de la sucursal: valor inventario, valor venta potencial, cobertura, % stock viejo, vendidas 30d.
- **Filtros**: Server-side: sucursal_id obligatorio y VALIDADO contra EmpresaUser con get_object_or_404 (:3186-3190, correcto), marca (:3200), incluir_sin_stock (:3180). El filtro de fechas no existe. El buscador de la tabla y la paginación son 100% CLIENT-SIDE (template :798-808 y :813-818): el backend devuelve TODAS las filas de la sucursal en un solo JSON y el navegador filtra/pagina. No hay filtro por categoría, ni por género (aunque la columna Género sí se muestra), ni por antigüedad.
- **Fidelidad**: Excluye excluir_de_analitica (:3195). El 'recibido' usa los sets canónicos de constants_kardex y estado='COMPLETADO' (:3215-3233), coherente con movimientos-sucursal. Problemas de fidelidad: (1) la columna se rotula 'Stock Inicial' en pantalla y en el Excel (:3387) pero es el acumulado histórico de TODO lo que entró alguna vez a ese SKU — no un stock a una fecha; leerla como saldo inicial de período es un error garantizado, y como no filtra por sucursal receptora suma también ingresos registrados con esa talla en otra bodega; (2) valor_inventario se calcula con costo+sobreprecio (:3248) mientras el Resumen de Existencias reporta el costo puro en su columna 'costo': dos reportes de la misma familia dan cifras distintas para 'valor del inventario' de la misma sucursal sin que nada lo explique; (3) pct_stock_viejo vuelve a dividir lotes por stock plano (:3303-3312) con corte 181 días; (4) el resumen llama 'total_productos' a un conteo de TALLAS con stock (:3283) y 'sin_stock' a un conteo de tallas sin stock (:3244), granularidad mezclada respecto de los demás reportes que cuentan artículos.
- **Rendimiento**: Es el peor patrón del grupo en volumen de salida: sin paginación server-side, una sucursal grande devuelve decenas de miles de filas JSON (una por talla) que el navegador debe renderizar y filtrar. Además construye una lista con TODOS los talla_ids de la sucursal (:3205-3210) y la manda como IN a la query de movimientos (:3225-3233): sentencia SQL de cientos de miles de parámetros. Las exportaciones Excel y PDF vuelven a ejecutar la API completa (:3325).

**Problemas:**

- [media] **Redundante con /app/reportes/existencias/ filtrado por sucursal** — El Reporte de Existencias (views.py:31904) entrega el mismo detalle SKU-talla con MÁS filtros (categoría con rama v1.2, búsqueda server-side, estado de stock), paginación real y KPIs globales bien calculados. Este reporte solo aporta tres cosas propias: la columna 'stock inicial', la columna Género y el export PDF. Mantener dos listados divergentes garantiza que den números distintos (de hecho ya valorizan distinto).
  - *Fix*: Consolidar: añadir a /reportes/existencias/ el filtro por marca, la columna Género (atributo3), la columna 'recibido histórico' y el export PDF, y dejar esta URL como redirect con el filtro de sucursal preaplicado. Si se prefiere conservarlo, al menos unificar la fórmula de valorización con el Resumen.
  - *Ref*: `app/views_modulo_reportes.py:3175 vs app/views.py:31904`
- [alta] **La columna 'Stock Inicial' no es un stock inicial** — stocks_iniciales_map suma TODOS los movimientos de entrada del SKU desde siempre (abastecimiento + traspasos de entrada + ajustes positivos + cambios), sin fecha de corte y sin filtrar por sucursal receptora. Se presenta al lado de 'Stock Actual' con el rótulo 'Stock Inicial', lo que invita a interpretar la resta como consumo del período. La diferencia real incluye traspasos salientes, mermas, devoluciones y años de historia.
  - *Fix*: Renombrar a 'Total recibido (histórico)' en JSON, tabla, Excel y PDF; o convertirla en un verdadero saldo inicial exigiendo un rango de fechas y calculando saldo_al_inicio = stock_actual - Sum(cantidad de movimientos dentro del rango). Y acotar por sucursal receptora con COALESCE(destino, origen) como hace _mapa_entrada.
  - *Ref*: `app/views_modulo_reportes.py:3218-3237 y :3387`
- [alta] **Sin paginación server-side: todo el inventario de la sucursal viaja en un JSON** — La vista itera el queryset completo con prefetch de tallas y arma una fila por talla; el template carga todo en memoria del navegador y pagina en JS. En una sucursal con decenas de miles de SKU-talla esto produce respuestas de varios MB, congela el navegador y hace que el Excel/PDF repitan el trabajo completo.
  - *Fix*: Paginar en servidor (pagina/por_pagina como en obtener_existencias_reporte), mover el buscador de la tabla a un parámetro server-side, y calcular el resumen con .aggregate() en vez de acumular en el loop de Python. Para el export, mantener la ruta sin paginar pero con streaming.
  - *Ref*: `app/views_modulo_reportes.py:3246-3288 y templates/vistas/modulo_reportes/reporte_existencias_sucursal.html:798-808`
- [media] **Lista completa de talla_ids como parámetro IN** — talla_ids materializa todos los IDs de tallas de la sucursal (con filtro de marca opcional) y se usa como ProductoTalla_id__in. Sin filtro de marca es toda la sucursal.
  - *Fix*: Reemplazar el IN por un JOIN: filtrar Movimientos_Producto por ProductoTalla__producto__sucursal_id=sucursal_id (y atributo1 si aplica) y agregar por ProductoTalla_id. Elimina la lista intermedia por completo.
  - *Ref*: `app/views_modulo_reportes.py:3205-3233`
- [media] **Valorización incoherente con el Resumen de Existencias** — Aquí valor_inventario = Sum((costo+sobreprecio)*stock); en resumen-existencias esa misma cifra se llama 'precio interno' y 'costo' es solo Sum(costo*stock). Un gerente que compare ambos reportes para la misma sucursal ve dos valores distintos sin explicación.
  - *Fix*: Devolver las tres valorizaciones con los mismos nombres en ambos reportes (costo, precio_interno, precio_venta) y documentar en la UI cuál usa el CD y cuál la tienda.
  - *Ref*: `app/views_modulo_reportes.py:3248 vs app/views_resumen_existencias.py:109-111`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Curva de tallas / quiebre por artículo | por artículo en la sucursal: tallas con stock>0 / tallas totales del artículo; listar los artículos con curva rota (<60%) y unidades atrapadas | Producto_Talla (producto, talla, stock) — el reporte ya recorre exactamente estos datos | Es el KPI natural de un reporte talla a talla y hoy no existe: identifica el stock que no va a vender por curva incompleta y que debe consolidarse por traspaso o liquidarse. | bajo |
| Días de inventario y venta 90d por SKU en la sucursal | stock / (abs(Sum(cantidad)) de CONCEPTOS_VENTA con sucursal_origen=suc en 90d / 90) | Movimientos_Producto + constants_kardex.CONCEPTOS_VENTA (ya se hace a 30 días agregado en :3296-3302; falta abrirlo por SKU y ampliar la ventana) | La cobertura agregada de la sucursal ya se muestra, pero no sirve para actuar: la decisión es por SKU. 90 días amortigua la estacionalidad semanal del retail. | medio |
| Stock muerto de la sucursal (unidades y $ sin venta en 180d) | Sum(stock) y Sum(stock*costo) de los SKU cuya MAX(fecha de movimiento de venta) es anterior a hoy-180 o es NULL | Producto_Talla + Movimientos_Producto (CONCEPTOS_VENTA) | Traduce el % de stock viejo (que hoy se calcula mal, mezclando lotes con stock plano) a un número que el jefe de local puede accionar, y se calcula con una sola fuente coherente. | medio |
| Descalce lotes vs stock de la sucursal | Sum(Producto_Talla.stock) - Sum(LoteProducto.cantidad_disponible activo) para la sucursal, y nº de SKU descalzados | Producto_Talla + LoteProducto (mismo cálculo del comando read-only reconciliar_stock_lotes) | Es la condición de validez de todos los KPIs de antigüedad y valorización FIFO de esta pantalla. Si el descalce es alto, el % de stock viejo que se muestra no significa nada y el usuario debe saberlo. | bajo |

### Inicial vs Restante / Movimientos por Sucursal — `UTIL`

- **Ruta**: /app/reportes/movimientos-sucursal/ — app/views_modulo_reportes.py:5481 ver_reporte_movimientos_sucursal, :5508 obtener_reporte_movimientos_sucursal, :5390 _mapas_movimientos_sucursal, :5731 export Excel
- **Mide**: Por artículo y sucursal: compras/abastecimiento recibido, traspasos recibidos, traspasos enviados, unidades vendidas y stock restante. Es el reporte de trazabilidad de flujo del módulo.
- **Filtros**: Server-side y correctos: marca (:5560), departamento con expansión v1.2 (:5563-5565), búsqueda artículo/marca/color (:5567-5572), fecha_desde/fecha_hasta (:5591-5602) y límite opcional. Exige al menos un filtro salvo sin_filtro=true (:5526-5534). Sucursales acotadas a las empresas del usuario (:5537-5543) y usadas como whitelist en los mapas (sucursales_ids). El export Excel repite la lógica con los mismos helpers. El filtro de fechas se aplica SOLO a los movimientos, nunca al stock restante.
- **Fidelidad**: Es el reporte mejor construido del grupo en cuanto a reglas de dominio: separa compras de traspasos (antes se mezclaban), cuenta las entradas por sucursal receptora con COALESCE(destino, origen) documentando el problema de la migración Laravel (:5415-5450), incluye los conceptos de traspaso legacy, filtra estado='COMPLETADO' y excluir_de_analitica, usa el set canónico CONCEPTOS_VENTA (incluye VENTA_DIRECTA/VENTA_TICKET) y separa entradas por signo (cantidad__gt=0) en vez de fiarse de tipo_movimiento. Fallas: (1) mezcla de períodos — al filtrar fechas, 'inicial' y 'vendido' son del período pero 'restante' es el stock de HOY, y la UI presenta las columnas como si fueran conciliables; (2) la ecuación no cierra nunca porque faltan flujos: no se cuentan reingresos (DEVOLUCION_CLIENTE, ANULACION, CAMBIO_PRODUCTO_ENTRADA está mal ubicado como 'compra'), ni egresos por pérdida/merma/ajuste negativo/devolución a proveedor; el usuario ve inicial - vendido ≠ restante sin explicación; (3) 'vendido' no descuenta las anulaciones/devoluciones, así que sobreestima la venta neta; (4) los ajustes positivos y las entradas por inventario físico se suman dentro de 'compras', inflando el abastecimiento aparente.
- **Rendimiento**: Bueno: 5 queries agregadas totales, sin N+1 (el rediseño ya está hecho). Riesgo: con limite vacío (default None, :5520-5521) y sin_filtro=true se materializan TODOS los productos del usuario y sus IDs viajan como IN a 5 agregaciones — ahí sí se cae. La vista de página tampoco tiene límite defensivo.

**Problemas:**

- [alta] **Falta @login_required en la vista de página** — ver_reporte_movimientos_sucursal no tiene decorador de autenticación; y en el middleware de permisos la ruta mapeada ('/app/reporte-movimientos-sucursal/') no coincide con la real ('/app/reportes/movimientos-sucursal/'), así que tampoco se valida el permiso de menú. Con usuario anónimo la vista revienta al filtrar EmpresaUser por AnonymousUser (500 en vez de redirect al login), y con cualquier usuario autenticado se sirve aunque su rol no tenga el permiso.
  - *Fix*: Agregar @login_required a :5481 y corregir las claves del mapa de permisos (ver problema en el bloque de Kardex/permisos).
  - *Ref*: `app/views_modulo_reportes.py:5481`
- [alta] **El filtro de fechas no aplica al 'restante': se comparan períodos distintos** — filtro_fecha se pasa solo a _mapas_movimientos_sucursal (:5609-5611); el stock restante sale de Producto_Talla.stock, que es siempre HOY (:5614-5620). Filtrando 'enero 2026' se ven las compras y ventas de enero junto al stock de julio, y la UI las muestra en la misma fila como si se restaran.
  - *Fix*: Cuando hay fecha_hasta, reconstruir el saldo a esa fecha (stock_hoy - Sum(cantidad de movimientos posteriores a fecha_hasta), con la corrección COALESCE ya presente en _mapa_entrada) y rotular la columna 'Saldo al DD/MM'. Si no se implementa, deshabilitar el filtro de fechas o marcar la columna 'restante (hoy)' de forma explícita.
  - *Ref*: `app/views_modulo_reportes.py:5591-5620`
- [alta] **Faltan reingresos y egresos no comerciales: la ecuación de flujo no cierra** — Los mapas cubren abastecimiento, traspasos in/out y ventas. Quedan fuera CONCEPTOS_REINGRESO (DEVOLUCION_CLIENTE, DEVOLUCION_NC, ANULACION) y CONCEPTOS_PERDIDA (PERDIDA_ROBO, PERDIDA_DETERIORO, DEVOLUCION_PROVEEDOR, DESPACHO_COTIZACION), además de AJUSTE_NEGATIVO / AJUSTE_INVENTARIO_SALIDA. Como consecuencia inicial + reingresos - vendido - salidas ≠ restante y el usuario no puede auditar la diferencia. Encima CAMBIO_PRODUCTO_ENTRADA y AJUSTE_POSITIVO están dentro de 'compras' (:5397-5400), inflando el abastecimiento.
  - *Fix*: Agregar dos columnas usando los sets ya definidos en constants_kardex: 'reingresos' (CONCEPTOS_REINGRESO, cantidad>0) y 'otros egresos' (CONCEPTOS_PERDIDA + ajustes negativos, cantidad<0); mover CAMBIO_PRODUCTO_ENTRADA a reingresos y AJUSTE_POSITIVO/AJUSTE_INVENTARIO_ENTRADA a una columna 'ajustes'. Cerrar con una columna 'descuadre' = inicial + reingresos + ajustes - vendido - traspasos_out - otros_egresos - restante.
  - *Ref*: `app/views_modulo_reportes.py:5397-5400 y :5452-5477`
- [media] **'Vendido' no descuenta anulaciones ni devoluciones** — ventas suma solo CONCEPTOS_VENTA. Un ticket anulado genera su movimiento de reingreso con otro concepto, que no se resta. En sucursales con alta tasa de cambio/devolución, 'vendido' queda por encima de la venta neta y no cuadra con los reportes de ventas.
  - *Fix*: Calcular vendido_neto = ventas - reingresos ligados a venta (DEVOLUCION_CLIENTE, ANULACION, ANULACION_TICKET, DEVOLUCION_NC) y mostrar ambas cifras.
  - *Ref*: `app/views_modulo_reportes.py:5471-5477`
- [media] **Sin límite por defecto** — limite = None cuando el parámetro viene vacío; con sin_filtro=true se cargan todos los productos del usuario y sus IDs se usan como IN en 5 agregaciones más la query de stock.
  - *Fix*: Default de 500-1000 como en existencias-marca, devolviendo truncado=True y el total disponible.
  - *Ref*: `app/views_modulo_reportes.py:5520-5521 y :5576-5577`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Sell-through del período por artículo y sucursal | vendido / (saldo_inicial + entradas del período); expresado en % | los mapas ya calculados (compras, traspasos_in, ventas) + saldo inicial reconstruido con la fórmula de reversión de movimientos | Es LA métrica de retail que este reporte casi construye y no entrega. Con la línea base conocida (ST 90 días ~12%) permite ordenar artículos por eficacia de la compra y separar 'no se vendió' de 'nunca llegó'. | medio |
| Descuadre de flujo (unidades sin explicación) | saldo_inicial + entradas + reingresos + ajustes - ventas - traspasos_out - pérdidas - restante | Movimientos_Producto usando los sets completos de constants_kardex | Convierte el reporte en una herramienta de auditoría: cualquier valor distinto de 0 es merma no registrada, movimiento mal clasificado o edición directa de stock. Hoy esa diferencia existe y está oculta. | medio |
| Tasa de retorno de traspaso (mercadería que vuelve o no se vende en destino) | por artículo y sucursal destino: vendido / traspasos_in del período | traspasos_in_map y ventas_map ya existentes | Mide si el CD está despachando a la sucursal correcta. Un ratio bajo sostenido indica push de mercadería a tiendas que no la venden, que es el origen del sobre-stock detectado en la auditoría. | bajo |
| Días desde el último abastecimiento por artículo y sucursal | hoy - MAX(fecha) de movimientos con concepto__in=CONCEPTOS_ABASTECIMIENTO+CONCEPTOS_TRASPASO_ENTRADA y cantidad>0, por sucursal receptora | Movimientos_Producto (misma base del helper, cambiando Sum por Max) | Distingue el artículo que no rota del que simplemente lleva dos años sin reposición. Barato: es un Max sobre una query que ya se ejecuta. | bajo |

### Kardex / Tarjeta de Movimiento (4 vistas del mismo dato) — `MEJORABLE`

- **Ruta**: /app/tarjeta-movimiento/ + /app/api/tarjeta-movimiento/ — app/views_modulo_existencias_nuevo.py:42 y :82; /app/reporte_movimientos_kardex/ — app/views.py:7930; /app/reporte_kardex_agrupado/ — app/views.py:8003; /app/verMovimientosProducto/ + /app/obtener_movimientos_producto/ — app/views.py:20574 y :20582
- **Mide**: La vida de un producto/SKU: entradas, salidas y saldo acumulado. Hay cuatro implementaciones distintas con reglas de saldo distintas: la Tarjeta de Movimiento (mejor diseñada: agrupa por artículo en todas las bodegas de la empresa, kardex por serie bodega+talla, timeline con ventas agregadas por mes), el kardex por SKU, el kardex agrupado por producto y el listado de movimientos de la sucursal activa.
- **Filtros**: Tarjeta: sku o código de artículo (:105-118), fecha_desde/fecha_hasta (:146-149), y filtros de talla/bodega que se resuelven en el frontend con las listas que entrega la API. Kardex por SKU: producto_talla_id + rango de fechas + paginación real server-side (:7934-7960). Kardex agrupado: producto_id + fechas, pero la paginación es en Python DESPUÉS de traer todos los movimientos (:8108-8111). Listado de sucursal: fechas (con default de 30 días, :20629-20635), tipo, artículo, sku (Cast a texto, :20670-20673), responsable, concepto — todos server-side con paginación real. Ninguna de las cuatro filtra por estado del movimiento.
- **Fidelidad**: Ninguna de las cuatro excluye estado ANULADO/PENDIENTE/RECHAZADO: el saldo del kardex incluye movimientos que el resto del sistema (todos los reportes agregados usan estado='COMPLETADO') descarta, de modo que el saldo final del kardex puede no coincidir con Producto_Talla.stock. La Tarjeta además deriva el signo del movimiento de tipo_movimiento con abs() (:167-173): para la data legacy que quedó con egresos etiquetados INGRESO (defecto documentado en models/inventario.py:208-215) convierte una salida en entrada, invirtiendo el saldo. El kardex por SKU reinicia el saldo en cada página y en cada rango de fechas (:7962-7964), así que la columna 'saldo' solo es correcta en la página 1 sin filtros. El orden por ('fecha','hora') sin desempate por id (:7951) es no determinista con la data legacy que tiene hora 00:00 masiva, lo que con LIMIT/OFFSET puede repetir u omitir filas entre páginas. El kardex agrupado agrupa por fecha+concepto+responsable+destino y muestra la referencia del PRIMER movimiento del grupo (:8056, :8068-8077): dos facturas del mismo día quedan en una fila atribuida a un solo folio. Positivo: la Tarjeta documenta y aplica bien la regla de traspasos (dos movimientos sobre dos SKUs, detección por concepto, :50-66) y excluye los traspasos de los totales de entradas/salidas para no inflarlos (:271-292).
- **Rendimiento**: Tarjeta: carga TODOS los movimientos del artículo en todas las bodegas sin paginación (:138-151); un artículo con años de venta genera miles de filas en un JSON (la timeline sí está capada a 150 hitos, la tabla no). Kardex agrupado: trae todos los movimientos del producto y pagina en Python (:8027-8036 + :8108-8111). Kardex por SKU y listado de sucursal: paginación real, sin problemas. Ninguno tiene N+1 grave gracias a los select_related.

**Problemas:**

- [alta] **api_tarjeta_movimiento no valida a qué empresa pertenece el SKU consultado** — Resuelve el SKU con Producto_Talla.objects.filter(sku=...).first() sobre toda la base y luego expande a todas las sucursales de la empresa DUEÑA de ese producto, no de las empresas del usuario. Cualquier usuario autenticado que pruebe un SKU obtiene kardex completo, costos, precios y distribución de stock de otra empresa. Los otros dos kardex sí verifican con puede_ver_sucursal (views.py:7943-7948 y :8018-8024). Además el middleware de permisos no cubre la ruta de la API: la clave del mapa es '/app/tarjeta-movimiento/' y el endpoint real es '/app/api/tarjeta-movimiento/', que no la contiene como substring.
  - *Fix*: Acotar la resolución del SKU a las sucursales del usuario (EmpresaUser → Sucursal) antes de buscar, devolver 404 si no está en su ámbito, y usar sucursal_ids del usuario (no de la empresa del producto) para expandir las variantes. Añadir '/app/api/tarjeta-movimiento/' al URL_PERMISO_MAP.
  - *Ref*: `app/views_modulo_existencias_nuevo.py:105-134`
- [alta] **Las rutas de los reportes de existencias en el middleware de permisos están obsoletas: el permiso de menú no se aplica** — URL_PERMISO_MAP declara '/app/reporte-existencias/', '/app/reporte-existencias-marca/', '/app/reporte-existencias-sucursal/', '/app/resumen-existencias/' y '/app/reporte-movimientos-sucursal/', pero las URLs reales registradas son '/app/reportes/existencias/', '/app/reportes/existencias-marca/', '/app/reportes/existencias-sucursal/', '/app/reportes/resumen-existencias/' y '/app/reportes/movimientos-sucursal/'. El match del middleware es exacto o por substring (middleware_permisos.py:253-259) y ninguna clave es substring de la ruta real, así que obtener_codigo_opcion devuelve None y el acceso se permite siempre. El menú oculta el enlace, pero la URL directa (y las APIs) quedan abiertas a cualquier usuario autenticado, sin importar su rol ni su PermisoSucursal.
  - *Fix*: Actualizar las 5 claves a las rutas reales con prefijo '/app/reportes/'. Como el match es por substring, conviene además mapear los endpoints AJAX asociados ('/app/api/reporte-existencias-marca/', '/app/api/resumen-existencias/', etc.) o cambiar el matcher a resolve() sobre el url name, que no se rompe al renombrar rutas.
  - *Ref*: `app/middleware_permisos.py:85-89 vs app/urls.py:1216, 1288, 1293, 1299, 1308`
- [alta] **El saldo del kardex por SKU se reinicia en cada página y con cada filtro de fecha** — saldo = 0 antes del loop y solo se acumulan los movimientos de la página. La página 2 arranca en 0 y con un rango de fechas el saldo no parte del saldo previo al rango. La columna 'saldo' —el corazón de un kardex— es incorrecta en todos los casos salvo página 1 sin filtros.
  - *Fix*: Calcular el saldo de arranque con una agregación: saldo_inicial = Sum(cantidad) de los movimientos del SKU anteriores al primer registro de la página (por fecha_inicio y por offset, con el mismo order_by), e iniciar el acumulador ahí. Mostrar la fila 'Saldo anterior' en la tabla.
  - *Ref*: `app/views.py:7958-7964`
- [alta] **La Tarjeta invierte el signo de los movimientos legacy mal etiquetados** — delta = abs(cantidad) cuando tipo_movimiento está en ('INGRESO','DEVOLUCION') y -abs(cantidad) cuando está en ('EGRESO','PERDIDA'). El propio comentario del modelo indica que el autoclasificador estuvo muerto y dejó egresos con tipo='INGRESO'; para esas filas la Tarjeta muestra una entrada donde hubo una salida y arrastra el error en el saldo acumulado y en la timeline.
  - *Fix*: Usar el signo de cantidad como fuente de verdad (delta = m.cantidad) y recurrir a tipo_movimiento únicamente cuando cantidad == 0. Es exactamente la convención que el save() del modelo ya impone para los registros nuevos.
  - *Ref*: `app/views_modulo_existencias_nuevo.py:167-173`
- [media] **Ningún kardex filtra el estado del movimiento** — Las cuatro vistas consultan Movimientos_Producto sin estado='COMPLETADO', mientras todos los reportes agregados sí lo hacen. Los movimientos ANULADO/PENDIENTE/PENDIENTE_RECEPCION entran en el saldo y en las listas, de modo que el kardex no cuadra contra el stock ni contra los reportes.
  - *Fix*: Filtrar estado='COMPLETADO' por defecto en las cuatro y ofrecer un toggle 'ver también anulados/pendientes' que los muestre marcados y excluidos del saldo.
  - *Ref*: `app/views.py:7949-7951, :8027-8029, :20641-20653; app/views_modulo_existencias_nuevo.py:138-145`
- [media] **Orden no determinista y paginación en Python** — El kardex por SKU ordena por ('fecha','hora') sin desempate por id, con miles de filas legacy compartiendo hora 00:00; con LIMIT/OFFSET eso puede duplicar u omitir registros entre páginas. El kardex agrupado y la Tarjeta traen todo el historial a memoria antes de paginar/serializar.
  - *Fix*: Añadir 'id' como último criterio de orden en las tres (la Tarjeta ya lo hace en :151, replicarlo) y paginar en base de datos en el kardex agrupado y en la tabla de la Tarjeta.
  - *Ref*: `app/views.py:7951 y :8108-8111`
- [media] **Cuatro implementaciones del mismo kardex con reglas distintas** — Tarjeta, kardex por SKU, kardex agrupado y listado de movimientos calculan el saldo y clasifican entradas/salidas con criterios diferentes (signo vs tipo, con/sin estado, con/sin traspasos). Para el mismo producto entregan cifras distintas.
  - *Fix*: Extraer un único helper de kardex (filtro base + regla de signo + saldo inicial) a app/services o a utils_inventario, y que las cuatro vistas lo consuman. La Tarjeta es la mejor base salvo por el abs().
  - *Ref*: `app/views_modulo_existencias_nuevo.py:82 vs app/views.py:7930 vs app/views.py:8003`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Saldo anterior y saldo final del rango | saldo_inicial = Sum(cantidad) de movimientos COMPLETADOS anteriores a fecha_inicio; saldo_final = saldo_inicial + Sum(cantidad) del rango; validar saldo_final == Producto_Talla.stock cuando no hay fecha_hasta | Movimientos_Producto (una agregación adicional por consulta) | Es el requisito básico de un kardex auditable y hoy no existe en ninguna de las cuatro vistas. Además la comparación contra el stock plano detecta ediciones directas de stock sin movimiento. | bajo |
| Días entre recepción y primera venta (time-to-first-sale) por SKU | MIN(fecha de CONCEPTOS_VENTA) - MIN(fecha de CONCEPTOS_ABASTECIMIENTO/TRASPASO_ENTRADA) por Producto_Talla y bodega | Movimientos_Producto + constants_kardex — la Tarjeta ya tiene todos los movimientos en memoria | La Tarjeta ya cuenta la historia del producto; este número la resume en una métrica comparable entre artículos y responde si el problema fue la compra o la exhibición. | bajo |
| Descuadre kardex vs stock por SKU | Sum(cantidad de movimientos COMPLETADOS) - Producto_Talla.stock | Movimientos_Producto + Producto_Talla | Detecta stock editado fuera del kardex (sincronizaciones MySQL, correcciones manuales). Es la métrica que justifica la existencia del kardex como control interno y hoy no se calcula en ninguna parte de la UI. | bajo |
| Rotación del SKU en su bodega (unidades vendidas / stock promedio 12m) | unidades vendidas 12m / ((stock actual + stock hace 12m)/2), con stock hace 12m reconstruido revirtiendo movimientos | Movimientos_Producto (CONCEPTOS_VENTA) + Producto_Talla | Da a la Tarjeta una conclusión cuantitativa además del relato. Es la única rotación real calculable con estos datos (a diferencia de la 'rotación estimada' inventada en el dashboard FIFO). | medio |

### Dashboard FIFO / Lotes (+ reporte_fifo_general) — `MEJORABLE`

- **Ruta**: /app/dashboard_fifo/ — app/views.py:22580 dashboard_fifo, :22601 obtener_datos_dashboard_fifo, :22989 exportar_dashboard_fifo, :22532 reporte_fifo_general (JSON usado desde verGestionProductos.html:14547 y :14666); helpers :22287-22331
- **Mide**: Estado de los lotes FIFO de la sucursal activa: valor de inventario a costo de lote, diferencia contra la valorización con el costo maestro, antigüedad del lote más antiguo por SKU-talla, aging del capital en 4 tramos (0-90/90-180/180-365/>365) y top 10 de capital viejo por categoría v1.2 y por marca, con alertas en pesos.
- **Filtros**: Server-side: periodo (hoy/semana/mes/trimestre/semestre/anio/todo) o rango fecha_desde/fecha_hasta aplicados a lotes__fecha_ingreso (:22662-22669), y filtro_producto/filtro_talla (:22691-22699). Se aplican en PYTHON, sobre las filas ya traídas: filtro_antiguedad (:22759-22768), filtro_diferencia (:22771-22781), filtro_stock (:22783-22791), mostrar_solo_diferencias y el ordenamiento (:22839-22852). El export CSV reutiliza la misma query anotada y respeta los filtros (corregido). No hay selector de sucursal: siempre la de sesión (:22607), así que no se puede mirar la empresa completa ni comparar bodegas. El aging del capital deliberadamente IGNORA el filtro de período (:22867-22870), lo cual está bien pero conviene que la UI lo diga (el template lo aclara en la línea 1028).
- **Fidelidad**: El N+1 histórico ya está resuelto: todo el detalle sale de una query anotada con filter= y HAVING sobre n_lotes_agg (:22677-22688), y excluye excluir_de_analitica. El aging usa agregación en DB (:22871-22909). Riesgos vigentes: (1) toda la antigüedad depende de LoteProducto.fecha_ingreso, que es auto_now_add y que generar_lotes_faltantes.py pobló con la fecha de generación (~abril 2026); existe corregir_fecha_ingreso_lotes para repararlo — si no se corrió con --apply en producción, TODO el aging dice 'stock fresco' y las alertas de capital viejo son falsas (verificar antes de decidir liquidaciones sobre este panel); (2) 'diferencia_valor' compara valor de lotes contra stock_plano*costo_maestro (:22731-22732), así que mezcla dos causas incomparables: descalce de UNIDADES entre lotes y stock, y cambio del costo maestro; un SKU con unidades perfectas pero recosteado aparece como 'diferencia'; (3) 'rotacion_estimada' = 365/antiguedad_promedio (:22859) no es rotación: la rotación es COGS/inventario promedio, y aquí el denominador es el promedio SIN ponderar de la edad del lote más antiguo por SKU; 'dias_inventario' es la misma cifra con otro nombre y 'eficiencia_fifo' (:22865) no mide cumplimiento FIFO sino el % de SKUs con lote reciente; (4) los contadores se llaman lotes_frescos/normales/antiguos/criticos pero cuentan SKU-tallas, no lotes (:22750-22757); (5) al filtrar por período, valor_total_inventario deja de ser el inventario y pasa a ser solo la porción ingresada en ese período, con el mismo rótulo.
- **Rendimiento**: El dashboard está corregido (1 query anotada + 6 agregados del aging; el registro previo de la auditoría era ~600s → 39s). Queda pendiente el volumen de salida: productos_data no tiene límite y devuelve una fila por SKU-talla con lotes de la sucursal. En cambio reporte_fifo_general (:22532-22576) sigue con el N+1 completo confirmado: por cada Producto_Talla ejecuta obtener_valor_inventario_fifo (1 query, :22293), obtener_costo_promedio_fifo (exists() + iteración = 2 queries, :22312-22319) y accede a producto_talla.producto sin select_related (1 query, :22556) → ~4 queries por fila; con ~740 SKU-talla con lotes eso da los ~2.968 queries y los ~9,9 min del backlog F-14, y la BD corta la conexión antes de responder. Está enganchado en verGestionProductos.html en dos puntos.

**Problemas:**

- [alta] **reporte_fifo_general sigue con N+1 y hace caer la conexión (F-14 confirmado)** — El loop llama dos helpers que cada uno vuelve a consultar LoteProducto para el mismo Producto_Talla, y además el queryset no tiene select_related('producto') pese a leer producto.id, producto.articulo y producto.costo dentro del loop. Son ~4 queries por SKU-talla; el dashboard equivalente ya resolvió esto en una sola query anotada.
  - *Fix*: Reescribir con la misma anotación del dashboard (views.py:22677-22688): Sum(cantidad_disponible*costo_unitario, filter=...) y Sum(cantidad_disponible, filter=...) sobre Producto_Talla con select_related('producto'), y paginar. Los helpers obtener_valor_inventario_fifo / obtener_costo_promedio_fifo deberían aceptar una lista de tallas y devolver un dict, o eliminarse.
  - *Ref*: `app/views.py:22551-22566 y :22287-22331`
- [alta] **'diferencia_valor' mezcla descalce de unidades con cambio de costo y no es interpretable** — valor_sistema = producto_talla.stock * producto.costo (costo maestro de hoy) frente a valor_inventario = Sum(cantidad_disponible*costo_unitario) de los lotes. Un SKU cuyas unidades cuadran perfectamente pero cuyo costo maestro se actualizó aparece con 'diferencia' y dispara la alerta de descalce >20%; y un SKU con unidades descuadradas pero costos iguales puede compensarse y no aparecer. El KPI principal del panel no distingue el problema de integridad del problema de costeo.
  - *Fix*: Separar en dos métricas, ambas ya disponibles: drift_unidades = disp_total_agg - producto_talla.stock (la anotación disp_total_agg ya se calcula en :22684 y hoy solo se usa para el costo promedio) y delta_costo = costo_promedio_fifo - producto.costo. Basar la alerta de integridad en drift_unidades y dejar la de valorización aparte.
  - *Ref*: `app/views.py:22728-22737`
- [media] **KPIs inventados: 'rotación estimada', 'días de inventario' y 'eficiencia FIFO'** — rotacion_estimada = 365 / antiguedad_promedio, donde antiguedad_promedio es el promedio aritmético (sin ponderar por unidades ni por valor) de la edad del lote MÁS ANTIGUO de cada SKU-talla. No es rotación de inventario ni se aproxima a ella; dias_inventario es literalmente la misma variable renombrada; eficiencia_fifo es el % de SKUs cuyo lote más viejo tiene menos de 180 días, y no mide en absoluto si las ventas consumieron primero el lote más antiguo (que es lo que significa cumplir FIFO). Tres cifras que aparentan rigor y que no se pueden defender frente a un contador.
  - *Fix*: Eliminar rotacion_estimada y dias_inventario y reemplazarlos por la rotación real (unidades vendidas 12m / stock promedio) o por cobertura (stock / venta diaria). Ponderar la antigüedad por unidades: Sum(dias*cantidad_disponible)/Sum(cantidad_disponible). Renombrar eficiencia_fifo a '% de SKU con stock fresco (<180d)' o calcular el cumplimiento FIFO real comparando el lote consumido en cada venta contra el más antiguo disponible.
  - *Ref*: `app/views.py:22855-22865`
- [media] **Toda la antigüedad depende de que se haya corrido corregir_fecha_ingreso_lotes** — LoteProducto.fecha_ingreso es auto_now_add y generar_lotes_faltantes.py creó los lotes del histórico migrado sin pasar fecha, dejando la fecha de generación (~abril 2026). El comando de corrección existe y es dry-run por defecto. El dashboard no valida ni informa el estado de esa corrección, así que puede estar mostrando aging plausible y falso.
  - *Fix*: Añadir al panel un indicador de calidad de dato: nº y % de lotes cuya fecha_ingreso coincide (día) con la fecha de generación masiva o que no tienen movimiento de ingreso asociado, con la leyenda 'antigüedad no verificable'. Y correr/verificar corregir_fecha_ingreso_lotes en producción antes de usar el aging para decidir liquidaciones.
  - *Ref*: `app/management/commands/corregir_fecha_ingreso_lotes.py:1-28 y app/models/inventario.py:249`
- [media] **El filtro de período redefine silenciosamente el 'valor total del inventario'** — periodo/fecha_desde se aplican a lotes__fecha_ingreso dentro del filter= de las anotaciones, de modo que al elegir 'mes' el panel muestra únicamente los lotes ingresados en los últimos 30 días, pero los rótulos siguen diciendo valor total, total de SKUs y diferencia total. El aging sí ignora el filtro (correcto y documentado), lo que produce dos secciones de la misma pantalla hablando de universos distintos.
  - *Fix*: Renombrar la métrica a 'Valor de los lotes ingresados en el período' cuando hay filtro activo, o separar en dos bloques: 'Inventario total (snapshot)' siempre completo y 'Ingresos del período' filtrado.
  - *Ref*: `app/views.py:22662-22669 y :22939-22946`
- [media] **Sin selector de sucursal y sin límite de filas** — La sucursal sale exclusivamente de la sesión (:22607): no se puede ver la empresa completa ni comparar CD vs tiendas, que es donde el capital viejo importa. Y productos_data devuelve una fila por SKU-talla con lotes sin tope ni paginación.
  - *Fix*: Añadir selector de sucursal validado contra EmpresaUser (con opción 'todas') y paginar el detalle en servidor, manteniendo los KPIs y el aging sobre el universo completo.
  - *Ref*: `app/views.py:22607 y :22726-22836`
- [baja] **Los contadores de antigüedad se llaman 'lotes' pero cuentan SKU-tallas** — lotes_frescos/normales/antiguos/criticos y total_lotes_analizados se incrementan una vez por Producto_Talla usando la fecha del lote MÁS antiguo, no una vez por lote. Un SKU con 10 lotes cuenta 1, y un SKU con un lote viejo residual de 1 unidad pesa igual que uno con 500 unidades viejas.
  - *Fix*: Renombrar a skus_frescos/… (cambio de rótulo, coste cero) y agregar la distribución ponderada por unidades y por valor, que ya está calculada en aging_capital.
  - *Ref*: `app/views.py:22719-22757`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Drift de unidades lotes vs stock plano (sucursal y top SKU) | por SKU-talla: disp_total_agg - Producto_Talla.stock; agregado: Sum(abs(drift)) y nº de SKU con drift != 0 sobre el total | la anotación disp_total_agg ya existe en views.py:22684 junto a Producto_Talla.stock — cero queries nuevas | Es el verdadero indicador de salud FIFO y el que valida o invalida todo lo demás del panel (valorización, aging, liquidaciones). Hoy está oculto detrás de una diferencia en pesos contaminada por los cambios de costo. | bajo |
| Antigüedad ponderada por unidades y por capital | Sum(dias_lote * cantidad_disponible)/Sum(cantidad_disponible) y Sum(dias_lote * cantidad_disponible * costo_unitario)/Sum(cantidad_disponible*costo_unitario) | LoteProducto (fecha_ingreso, cantidad_disponible, costo_unitario) — misma base que aging_capital | Reemplaza el promedio simple actual, que un SKU con 1 unidad vieja distorsiona. La versión ponderada por capital es la que se lleva al comité de compras. | bajo |
| Cumplimiento FIFO real | % de salidas de venta que consumieron el lote más antiguo disponible en la fecha del movimiento (movimientos ligados a lote vs MIN(fecha_ingreso) de lotes con disponibilidad en esa fecha) | LoteProducto.movimiento (FK ya existente en models/inventario.py:238) + Movimientos_Producto | El panel se llama FIFO y no mide FIFO. Este porcentaje distingue un sistema que aplica la política de uno que solo guarda lotes; si sale bajo, la valorización a costo de lote no es confiable. Requiere que la FK movimiento esté poblada — verificar cobertura antes de comprometerlo. | alto |
| Capital viejo vs velocidad de venta (candidatos a liquidación priorizados) | por SKU o por marca: capital >180d (Sum(cantidad_disponible*costo_unitario)) dividido por unidades vendidas 90d; ordenar desc y cortar el top que concentre el 80% del capital | LoteProducto + Movimientos_Producto (CONCEPTOS_VENTA, sucursal_origen) — el top por marca/categoría ya está en :22891-22909, falta el denominador de venta | El aging actual dice cuánto capital es viejo pero no cuál liquidar primero. Cruzarlo con la velocidad separa 'viejo pero rota' de 'viejo y muerto', que es la lista que alimenta las Campañas de Liquidación. | medio |
| Margen implícito sobre costo de lote | (precioventa - costo_unitario_promedio_lote) / precioventa, ponderado por unidades disponibles, por marca y por tramo de antigüedad | LoteProducto.costo_unitario + Producto.precioventa | Antes de fijar el descuento de liquidación hay que saber cuánto colchón hay sobre el costo REAL del lote (no el costo maestro). Es el input directo del piso de precio que ya usa el motor de descuentos del ecommerce. | medio |

## Familia: Compras y Proveedores

### Reporte de Compras Integral (pestañas Evolución / Proveedores / Recepciones / Pagos) — `ROTO`

- **Ruta**: /app/reportes/compras/ (vista: retailmind/app/views_modulo_reportes.py:ver_reporte_compras) — datos: /app/api/reporte-compras/ (views_modulo_reportes.py:api_reporte_compras, línea 3726) — export: /app/api/exportar-reporte-compras-excel/ (línea 4647) — template: retailmind/app/templates/vistas/modulo_reportes/reporte_compras.html
- **Mide**: Pretende medir toda la compra a proveedores del año leyendo DTEs con tipo_transaccion='COMPRA' (emisor = proveedor): nº de documentos, inversión (monto_neto), unidades, ranking/Pareto de proveedores, 'cumplimiento', ROI, estado de recepciones y cuentas por pagar. Cambia de fuente automáticamente si la sucursal activa es VENDEDORA: ahí su 'compra' pasa a ser los DTE tipo TRASPASO recibidos desde EDEL (views_modulo_reportes.py:3866-3875).
- **Filtros**: La pantalla ofrece 4 filtros: Año, Período (Todo el año / Últimos 3 meses / Último mes / Última semana), Proveedor y Temporada (reporte_compras.html:477-505). Verificado en el código: SOLO Año y Proveedor se aplican server-side. 'Período' se usa para calcular fecha_inicio/fecha_fin en api_reporte_compras (líneas 3768-3779) y esas variables NUNCA se pasan a ningún helper — todo sigue siendo el año completo. 'Temporada' se recibe y se pasa como argumento a 8 helpers (calcular_metricas_compras, calcular_evolucion_mensual_compras, calcular_top_proveedores_compras, etc.) y NINGUNO la referencia en el cuerpo: es un parámetro muerto. Además el modelo Dte no tiene campo de temporada, así que sobre esta fuente el filtro es inimplementable tal cual. El desplegable de Año se llena dinámicamente (año actual y 7 hacia atrás) pero preselecciona el AÑO ANTERIOR por defecto (reporte_compras.html:1358-1366).
- **Fidelidad**: Baja. (1) Los KPIs de cabecera 'ROI Estimado', 'Margen Bruto Esperado' y 'Valor Venta' son aritmética del IVA: valor_venta_esperado = Sum(monto_con_iva) de las facturas de COMPRA, margen = monto_con_iva - monto_neto y roi_promedio = (con_iva - neto)/neto ≈ 19% siempre (líneas 4077-4079). No hay ningún dato de venta involucrado. (2) 'Cumplimiento' por proveedor = % de DOCUMENTOS con estado_pago='PAGADO' (4190-4194), pero el insight que se muestra al usuario dice 'problemas en las entregas' (4570-4575): se etiqueta pago como entrega. (3) En la pestaña Recepciones, para DTE en RECEPCIONADO_PARCIAL el sistema inventa el dato: recibidas = int(unidades * 0.5) con el comentario '# Estimación' (línea 4420). (4) Cero scoping por empresa: el queryset base es Dte.objects.filter(tipo_transaccion='COMPRA', fecha_emision__year=anio) sin filtro de receptor (3878-3881), así que mezcla las compras de todas las empresas del holding — al revés de la pantalla operativa de Gestión DTE Compras, que sí filtra por receptor_id=idEmpresaActual (views_modulo_compras.py:1535-1539). (5) No excluye notas de crédito de compra (Dte con tipo_documento='NOTA DE CREDITO' y tipo_transaccion='COMPRA' creado por crearDteCompras), ni descartado=True, ni estado_dte ANULADO/CANCELADO: las NC SUMAN inversión y deuda en vez de restarlas. (6) El estado de pago real puede ser 'Parcial' (lo escribe _recalcular_estado_pago en views_modulo_compras.py:4588) y también existe 'VENCIDO' en los choices, pero el reporte solo bucketea PAGADO/Pagado y PENDIENTE/Pendiente (4021-4026, 4444-4456): toda factura parcialmente pagada desaparece de ambos lados. (7) 'unidades_recepcionadas' del KPI en realidad es un CONTEO DE DOCUMENTOS con estado ACEPTADO/RECEPCIONADO_COMPLETO (4034-4039 → 4074), y se muestra al lado de 'unidades_esperadas' que sí son unidades. (8) El complemento con el modelo Compras es un fallback de tipo 'si el DTE dio 0 usa Compras' (4009-4017) y en el ranking mezcla dos bases incompatibles en la misma tabla: monto_neto de DTE para unos proveedores y costo×stock de la OC para otros (4210-4230), con lo que la columna 'participación %' compara peras con manzanas. Lo único bien hecho: _get_compras_base sí excluye ELIMINADA y CANCELADA (filtra estado__in=['ACTIVA','COMPLETADA'], línea 3891) y Dte_Productos sí filtra excluir_de_analitica.
- **Rendimiento**: Es el reporte de ~44 minutos y ~9.000 queries; confirmado y dimensionado leyendo el código. Causas, en orden de peso: (a) calcular_top_proveedores_compras se EJECUTA COMPLETO TRES VECES por request — para 'top', para 'pareto' (línea 4245) y para 'cumplimiento' (línea 4260) — y cada ejecución hace un loop por proveedor con ~6 queries (count, 2 aggregates, values_list de ids, aggregate de unidades, filtro de pagados) más un Empresa.objects.get() por proveedor; (b) dentro de cada una de esas 3 pasadas se vuelve a llamar _datos_compras_por_proveedor, que itera proveedores haciendo 4 queries cada uno (3897-3922) — se recalcula 4 veces en total contando la de métricas; (c) calcular_evolucion_mensual_compras hace 4 queries por mes y además _datos_compras_mensuales hace 3 queries por mes (48 + 36); (d) calcular_comparativa_anual_compras hace 2 queries por mes × 12 = 24 más 24 de construcción de queryset. El total escala como ~34·P + 150 queries con P = nº de proveedores emisores; con los ~250-260 proveedores del catálogo da del orden de 9.000 queries, que es exactamente lo reportado. Todos los loops son N+1 puros y son reescribibles como values('emisor_id').annotate(...) agrupado.

**Problemas:**

- [alta] **ROI, Margen Bruto y Valor de Venta son en realidad el IVA de la factura de compra** — valor_venta_esperado = Sum(monto_con_iva) de los DTE de COMPRA; margen_bruto_esperado = monto_con_iva - monto_neto; roi_promedio = (con_iva - neto)/neto. No interviene ningún precio de venta ni ninguna venta real: el 'ROI Estimado' que se muestra en la tarjeta grande del reporte es matemáticamente el 19% de IVA (o la mezcla de tasas si hay exentos), y el 'Margen Bruto Esperado' es el IVA soportado. Además dispara insights automáticos ('Excelente rentabilidad' / 'ROI bajo') basados en ese número inventado (4550-4561).
  - *Fix*: Sacar las 3 tarjetas de la pantalla o recalcularlas contra datos reales: margen = Sum(Ticket_Productos.subtotal - stock*costo_fifo) de los SKU recepcionados de esas compras, y ROI = margen/inversión. Si no se quiere cruzar con venta, reemplazar por 'Markup teórico de lista' usando Compras_Producto.precioSugerido vs costo (que sí existe en el modelo de OC) y etiquetarlo como teórico, tal como ya lo hace el Excel del dashboard mejorado (views_modulo_compras.py:1401).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4077-4079`
- [alta] **'Cumplimiento del proveedor' mide pagos, no entregas, y el texto al usuario dice lo contrario** — En calcular_top_proveedores_compras: pagados = dtes_prov.filter(estado_pago PAGADO).count(); cumplimiento = pagados/total_compras*100. Ese mismo número alimenta la pestaña 'Cumplimiento de proveedores' y el insight 'El cumplimiento de X% indica problemas en las entregas. Evalúe proveedores alternativos'. Un proveedor que entregó todo perfecto pero al que le debemos plata aparece como incumplidor, y viceversa.
  - *Fix*: Cumplimiento = unidades recepcionadas / unidades pedidas, que ya está calculado correctamente en el dashboard mejorado (Productos_Recepcionados.stockArribado vs Compras_Producto_Talla.stock, views_modulo_compras.py:3512-3524). Renombrar el KPI actual a '% facturas pagadas' y moverlo a la pestaña de Pagos.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4190-4194 y 4570-4575`
- [alta] **La pestaña Recepciones inventa las unidades recibidas de las recepciones parciales** — obtener_recepciones_pendientes_compras asigna literalmente 'recibidas = int(unidades * 0.5)  # Estimación' para todo DTE en RECEPCIONADO_PARCIAL, y 'recibidas = unidades' (100%) para los que están EN_REGULARIZACION o RECHAZADO. La tabla que ve el usuario con columnas Esperadas/Recibidas está mostrando un número fabricado, no el dato de Productos_Recepcionados, que sí existe.
  - *Fix*: Reemplazar por Productos_Recepcionados.objects.filter(dte_id=dte.id).aggregate(Sum('stockArribado')) (o vía compra_producto_talla cuando la recepción viene de OC), que es lo que ya usa calcular_metricas_recepcion_compras en la línea 4371.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4418-4423`
- [alta] **Las facturas con pago parcial desaparecen de la deuda; el estado VENCIDO tampoco se cuenta** — _recalcular_estado_pago (views_modulo_compras.py:4588) escribe tres valores: 'PAGADO', 'Parcial' y 'Pendiente'. El reporte solo suma Q(estado_pago='PAGADO')|Q('Pagado') y Q('PENDIENTE')|Q('Pendiente') en calcular_metricas_compras, calcular_estado_pagos_compras, calcular_vencimientos_compras y obtener_pagos_pendientes_compras. Resultado: una factura de $10M con $1 pagado queda en 'Parcial' y no aparece ni como pagada ni como pendiente ni como vencida. Además, el bucket 'vencidos' se deriva de PENDIENTE + fecha_vencimiento < hoy, ignorando las filas que literalmente tienen estado_pago='VENCIDO' (choice válido en app/models/dte.py:28). Y aun para las PENDIENTE se imputa el monto_con_iva COMPLETO, sin restar los abonos ya registrados en Dte_Detalle_Pago.
  - *Fix*: Dejar de leer estado_pago y calcular el saldo: annotate(pagado=Coalesce(Sum('dte_asociado__monto'),0)) sobre Dte y saldo = monto_con_iva - pagado; pendiente = saldo > 0; vencido = saldo > 0 y fecha_vencimiento < hoy. Cubre PAGADO, Parcial, Pendiente y VENCIDO con una sola regla.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4444-4456`
- [alta] **Sin scoping por empresa: mezcla las compras de todo el holding** — _get_dtes_base_compras en modo centro de distribución filtra solo por tipo_transaccion='COMPRA' y año, sin receptor_id. Cualquier usuario de cualquier empresa ve la inversión, la deuda y los proveedores de todas las empresas juntas. La pantalla operativa equivalente (obtener_resumen_pendientes_anio) sí filtra Q(receptor_id=idEmpresaActual)|Q(receptor__isnull=True), y el bloque de compras por concepto del dashboard mejorado también (views_modulo_compras.py:3032-3033): el reporte es el único que no lo hace, y por eso sus totales nunca cuadran con la gestión de DTE.
  - *Fix*: Aplicar el mismo filtro por receptor que usa obtener_resumen_pendientes_anio, o mejor el helper de EmpresaUser ya usado en ventas-global tras la auditoría previa.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:3876-3883`
- [alta] **Notas de crédito de compra, DTE descartados y anulados suman en vez de restar** — crearDteCompras permite crear un DTE con tipo_documento='NOTA DE CREDITO' y tipo_transaccion='COMPRA' (views.py:10194, choices en app/models/dte.py:12). El queryset base del reporte no excluye es_nota_credito=True, ni descartado=True, ni estado_dte in ('ANULADO','CANCELADO','RECHAZADO'). Por lo tanto cada NC del proveedor infla la inversión total, el ranking del proveedor, el Pareto y la deuda pendiente en lugar de rebajarlas. Nótese que el equivalente en el dashboard mejorado sí filtra descartado=False (views_modulo_compras.py:3030).
  - *Fix*: En _get_dtes_base_compras: .exclude(descartado=True).exclude(estado_dte__in=['ANULADO','CANCELADO','RECHAZADO']) y tratar las NC como signo negativo (monto neto en negativo) en vez de como una compra más.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:3877-3881`
- [alta] **Los filtros 'Período' y 'Temporada' de la pantalla no hacen absolutamente nada** — api_reporte_compras calcula fecha_inicio/fecha_fin según 'periodo' (líneas 3768-3779) y esas variables no se usan nunca más en la función. 'temporada' se pasa como 4º argumento a calcular_metricas_compras, calcular_evolucion_mensual_compras, calcular_top_proveedores_compras, calcular_pareto_proveedores_compras, calcular_cumplimiento_proveedores_compras, calcular_comparativa_anual_compras, calcular_estado_recepciones_compras, calcular_metricas_recepcion_compras y calcular_estado_pagos_compras, y ninguna lo menciona en su cuerpo. El usuario elige 'Última semana' + 'Invierno' y sigue viendo el año completo de todos los proveedores, sin ningún aviso.
  - *Fix*: O se implementan (Período: filtrar fecha_emision__range; Temporada: no es posible sobre Dte, habría que cruzar con Compras.temporada_familia) o se quitan del HTML. Dejarlos visibles sin efecto es peor que no tenerlos.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:3768-3779 y 4098 (firma con temporada nunca usada)`
- [alta] **9.000 queries / 44 minutos: el ranking de proveedores se recalcula 3 veces y los datos de OC 4 veces** — api_reporte_compras llama calcular_top_proveedores_compras (loop N+1 de ~6 queries por proveedor + un Empresa.objects.get por proveedor) y luego calcular_pareto_proveedores_compras y calcular_cumplimiento_proveedores_compras, que internamente vuelven a llamar a la misma función completa. Cada una de las 3 pasadas ejecuta además _datos_compras_por_proveedor (4 queries por proveedor). Sumando la evolución mensual (84 queries) y la comparativa anual (48), el costo escala ~34 queries por proveedor: con ~250 proveedores da ~9.000.
  - *Fix*: Paso 1 (5 minutos, quita 2/3 del tiempo): calcular top_proveedores UNA vez en api_reporte_compras y pasar la lista a pareto/cumplimiento como argumento, en lugar de que ellos la recalculen. Paso 2: reemplazar los loops por agregados agrupados — dtes_query.values('emisor_id','emisor__nombre','emisor__rut').annotate(docs=Count('id'), inversion=Sum('monto_neto'), con_iva=Sum('monto_con_iva')) y una sola query de unidades agrupada por dte__emisor_id; ídem los 12 meses con .annotate(mes=ExtractMonth('fecha_emision')).values('mes').annotate(...). Objetivo realista: <30 queries y <2 s.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4243-4265 (pareto y cumplimiento re-invocan el cálculo completo) y 4156-4208 (loop N+1)`
- [media] **El KPI 'Unidades recepcionadas' es un conteo de documentos, no de unidades** — recepcionados = dtes_query.filter(estado_dte__in=['ACEPTADO','RECEPCIONADO_COMPLETO']).count() y se devuelve como 'unidades_recepcionadas', que la pantalla muestra junto a 'unidades_esperadas' (que sí son pares). Se comparan documentos contra unidades, así que el cumplimiento visual siempre parece catastrófico.
  - *Fix*: Devolver la suma real de Productos_Recepcionados.stockArribado (ya se calcula en calcular_metricas_recepcion_compras línea 4371) y renombrar el conteo de documentos a 'documentos_recepcionados'.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4034-4039 y 4073-4075`
- [media] **El ranking mezcla dos bases de datos incompatibles en la misma tabla de participación** — Para los proveedores con DTE, la 'inversión' es Sum(Dte.monto_neto) (valor facturado). Para los proveedores que solo tienen OC sin DTE, se agrega con inversión = Sum(costo × stock) de Compras_Producto_Talla (líneas 4210-4230). Ambos montos se suman en inversion_total_general y de ahí sale la columna 'participación %'. Además, a esos proveedores de complemento se les asigna cumplimiento=100 si tienen alguna unidad recepcionada y roi=0 fijo.
  - *Fix*: Elegir una sola base por reporte (recomendado: OC/Compras para 'qué compramos' y DTE para 'qué debemos') y, si se insiste en unificar, agregar una columna 'fuente' visible y no calcular participación entre bases distintas.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4210-4235`
- [media] **El Excel exportado no respeta el modo vendedora ni el filtro de período** — exportar_reporte_compras_excel llama calcular_metricas_compras(anio,'anual',proveedor_id,temporada) SIN el modo_ctx. En una sucursal VENDEDORA la pantalla muestra los traspasos recibidos desde EDEL y el Excel descargado muestra las compras del centro de distribución: dos documentos distintos con el mismo título.
  - *Fix*: Reconstruir modo_ctx (o extraer un helper compartido) y pasarlo a los tres cálculos del export, igual que hace api_reporte_compras.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4658-4660`
- [media] **Vencimientos y pagos pendientes ignoran TODOS los filtros de la pantalla** — calcular_vencimientos_compras() y obtener_pagos_pendientes_compras() no reciben año, proveedor ni modo: consultan Dte.objects.filter(tipo_transaccion='COMPRA') histórico completo y de todas las empresas. Con el filtro 'Proveedor = X' aplicado, el resto del reporte muestra X y la tabla de vencimientos muestra a todo el mundo.
  - *Fix*: Pasarles el mismo queryset base ya filtrado (dtes_query) en vez de reconstruirlo desde cero. En rigor la deuda es un stock, no un flujo del año, así que conviene mantener el histórico pero SÍ filtrar por proveedor y empresa, y decirlo en el subtítulo.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4473-4542`
- [baja] **Código muerto que confunde el mantenimiento** — recepciones_sin_dte_anio se calcula con una query completa sobre Productos_Recepcionados y jamás se usa (líneas 3993-3998). La variable 'ctx_anterior' de la comparativa anual es una copia idéntica del contexto actual (4308).
  - *Fix*: Borrarlo al hacer la refactorización de performance.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:3991-3998`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Saldo real por pagar a proveedores (deuda neta) | Por cada Dte de compra vigente: saldo = monto_con_iva - COALESCE(Sum(Dte_Detalle_Pago.monto), 0); KPI = Sum(saldo) para saldo > 0, excluyendo es_nota_credito, descartado y ANULADO/CANCELADO, y restando el monto de las NC del proveedor | Dte.monto_con_iva, Dte.es_nota_credito, Dte.descartado, Dte.estado_dte, Dte_Detalle_Pago.monto (related_name 'dte_asociado') | Es el único número de este reporte que nadie más entrega y hoy está mal en 3 formas distintas (parciales invisibles, NC sumando, sin descontar abonos). Es la cifra con la que se decide cuánto efectivo dejar disponible esta semana. | bajo |
| Aging de cuentas por pagar (0-30 / 31-60 / 61-90 / +90 días vencidos) | Para cada DTE con saldo > 0: dias_mora = hoy - fecha_vencimiento; agrupar el SALDO (no el monto total) en los buckets | Dte.fecha_vencimiento, Dte.monto_con_iva, Dte_Detalle_Pago.monto | Reemplaza la tabla actual de 'vencimientos', que suma montos completos de facturas ya parcialmente pagadas y no distingue el grado de atraso. Permite priorizar qué proveedor se paga primero antes de que corte despacho. | bajo |
| DPO real (días promedio de pago efectivo) por proveedor | Promedio ponderado por monto de (Dte_Detalle_Pago.fecha_pago - Dte.fecha_emision) sobre los pagos del período; comparar contra Dte.diasCredito pactado | Dte_Detalle_Pago.fecha_pago, Dte_Detalle_Pago.monto, Dte.fecha_emision, Dte.diasCredito | Dice si la empresa realmente está usando el crédito que negoció o pagando antes de tiempo (costo financiero puro), y sirve como argumento duro para renegociar plazos con los 5 proveedores más grandes. | bajo |
| % de facturas pagadas dentro del plazo | Facturas cuyo último pago que completa el saldo tiene fecha_pago <= fecha_vencimiento / total de facturas pagadas en el período | Dte_Detalle_Pago.fecha_pago, Dte.fecha_vencimiento | Es el KPI que la gerencia cree estar viendo cuando lee 'cumplimiento' hoy. Mide nuestro comportamiento como pagadores, que es lo que determina si el proveedor nos sigue dando crédito. | bajo |
| Concentración de deuda: % del saldo en los 5 principales acreedores | Sum(saldo) de los 5 emisores con mayor saldo / Sum(saldo) total | Dte.emisor_id + saldo calculado | Riesgo de continuidad operacional: si un solo proveedor concentra el 60% de la deuda y corta el suministro, para la temporada. Hoy el Pareto existe pero es sobre inversión facturada, no sobre exposición viva. | bajo |
| Monto de compra no inventariable (compra por concepto) sobre el total | Sum(monto_con_iva) de Dte con tipo_transaccion='COMPRA' y es_por_concepto=True / Sum(monto_con_iva) de todas las compras del período | Dte.es_por_concepto (ya existe y ya se usa en views_modulo_compras.py:3028-3036) | Separa el gasto operativo (fletes, servicios, arriendos facturados por proveedores) de la compra de mercadería. Hoy ambos están sumados en 'Inversión total', lo que distorsiona el costo por unidad y el ROI de temporada. | bajo |

### Reporte de Compras Integral — pestaña 'Rendimiento' (embudo Entrada → Despacho → Venta) — `MEJORABLE`

- **Ruta**: /app/api/rendimiento-compras/ (retailmind/app/views_modulo_reportes.py:api_rendimiento_compras, línea 4824) — se renderiza dentro de reporte_compras.html:1043-1060, se pide en reporte_compras.html:2225
- **Mide**: Embudo anual de unidades: cuánto entró (RECEPCION_COMPRA / INGRESO_INICIAL / INGRESO_MANUAL), cuánto se despachó a tiendas (TRASPASO_SALIDA), cuánto se vendió (VENTA_PUBLICO / VENTA_MAYORISTA o tickets), cuánto salió por merma/ajuste/devolución a proveedor y cuánto queda sin vender; más rotación, ROI, evolución mensual, desglose por sucursal y top de productos. Tiene lógica dual-source: si el año tiene menos de 500 movimientos usa los DTE migrados de Laravel, si no usa Movimientos_Producto.
- **Filtros**: Año (se repuebla con anios_disponibles devueltos por el propio API), Sucursal y checkbox 'comparar con año anterior'. Los tres SÍ se aplican server-side. Ojo: el filtro de sucursal se aplica sobre sucursal_origen_id en los movimientos y sobre idTicket__sucursal_id en los tickets, que no son la misma dimensión (uno es dónde estaba el stock, otro dónde se cobró); y en el modo DTE se aplica sobre Dte.sucursal_id. Falta cualquier filtro por proveedor, marca o categoría.
- **Fidelidad**: Media, y bastante más honesta que el resto del módulo: aplica excluir_de_analitica, usa ids_producto_talla_activos para sacar el cementerio de SKU de Laravel, normaliza signos con abs() y trae una 'nota_metodologia' que explica al usuario de dónde salió cada número, incluyendo el caso rotación >100%. Problemas reales: (1) usa idTicket__fecha__year en vez de created_at para las ventas (líneas 4859, 4938, 5199, 5271), justo lo contrario de la regla del proyecto y de lo que sí hace bien el reporte de Rendimiento por Proveedor; (2) incluye tickets en estado PENDIENTE junto a PAGADO en el total de ventas; (3) la evolución mensual NO filtra por SKU activos (q_tk en línea 4938 omite el ProductoTalla_id__in=pt_ids que sí lleva el cálculo anual en la línea 4860), así que la suma de los 12 meses del gráfico no cuadra con el KPI de cabecera; (4) el costo de lo vendido se PRORRATEA (inversion × vendido/entrada, línea 5042) pese a que Ticket_Productos.costo_fifo tiene el costo real línea a línea — el mismo dato que sí usa calcular_rendimiento_sucursales_destino en views_modulo_compras.py:3939; (5) usa max(ventas_por_movimiento, ventas_por_ticket) como heurística de deduplicación (5039-5040): es defendible para no doblar, pero significa que si una fuente está incompleta el número simplemente es el mayor de dos cifras malas; (6) el umbral de 500 movimientos puede cambiar la metodología de un año de un día para otro sin que el usuario lo pida.
- **Rendimiento**: Aceptable en número de queries (~20-30 fijas, sin N+1 por proveedor), pero cada una arrastra la subconsulta de ids_producto_talla_activos, que es un DISTINCT sobre Movimientos_Producto desde 2024 (app/utils_analitica.py:33-45) contra una tabla de millones de filas, repetido en ~10 agregaciones. Con 'comparar=1' se duplica todo. Es la causa más probable de que esta pestaña sea lenta aunque no llegue a los 44 min de la pestaña principal. Fix: materializar los IDs una vez por request (o mejor, convertirlo en un flag denormalizado en Producto_Talla, actualizado por batch nocturno).

**Problemas:**

- [alta] **Ventas por Ticket.fecha en vez de created_at** — Ticket.fecha es auto_now (se reescribe en cada save), por lo que un ticket editado/reimpreso migra de mes o de año. Toda la pestaña de rendimiento —KPI anual, evolución mensual, por sucursal y top de productos— usa idTicket__fecha__year / __month.
  - *Fix*: Cambiar los 4 puntos a idTicket__created_at__year / created_at__month (y __date__range donde aplique), igual que ya se hizo en api_reporte_rendimiento_proveedor línea 6416-6418.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4859, 4938, 5199, 5271`
- [media] **La evolución mensual no aplica el filtro de SKU activos que sí aplica el total anual** — El cálculo anual filtra los tickets con 'ProductoTalla_id__in': pt_ids (línea 4860); el cálculo mensual construye q_tk sin ese filtro (4938-4940). El gráfico mensual, por tanto, suma más unidades e ingresos que el KPI de cabecera del mismo reporte, y el usuario no tiene forma de saber por qué no cuadra.
  - *Fix*: Añadir 'ProductoTalla_id__in': pt_ids al q_tk de _evolucion_desde_movimientos.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4938-4943`
- [media] **Margen y ROI prorratean el costo teniendo costo_fifo real disponible** — costo_vendido = inversion * (vendido/entrada) asume que lo vendido tiene el costo promedio de lo comprado ese año, cuando Ticket_Productos.costo_fifo guarda el costo real de cada línea vendida. El error es grande justamente en los casos que importan: liquidaciones de stock viejo (costo real de otro año) y productos que se venden desde stock de temporadas anteriores, que es el escenario que la propia nota de metodología reconoce como frecuente.
  - *Fix*: margen = Sum(Ticket_Productos.subtotal) - Sum(stock * costo_fifo) sobre el mismo queryset de tickets; mantener el prorrateo solo como fallback cuando costo_fifo=0 (líneas legacy) y reportar qué % del margen viene de cada método.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:5042-5046`
- [media] **Incluye tickets PENDIENTE como venta** — idTicket__estado__in=['PAGADO','PENDIENTE'] en todos los cálculos de venta de esta pestaña. Un ticket pendiente no es una venta cobrada; el reporte de Rendimiento por Proveedor, en cambio, usa estado='PAGADO' con un comentario explícito diciendo que un PENDIENTE aún no es venta (línea 6411).
  - *Fix*: Unificar el criterio con el resto del módulo (solo PAGADO) o, si se quiere mantener, mostrar las dos cifras separadas.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:4859-4860`
- [baja] **El cambio automático de fuente de datos (umbral 500 movimientos) no es determinista para el usuario** — Si un año cruza el umbral de 500 movimientos COMPLETADOS, el reporte pasa de leer DTE migrados a leer Movimientos_Producto y todos los KPIs cambian de magnitud sin que el usuario haya tocado nada. Además, en modo DTE el desglose por sucursal devuelve entradas y despachos en cero con la explicación 'Datos desde DTEs migrados de Laravel', dejando la tabla medio vacía.
  - *Fix*: Mostrar la fuente como un badge fijo junto al selector de año (el API ya devuelve 'fuente_datos') y permitir forzarla por parámetro para poder auditar el mismo año con ambas metodologías.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:5085-5105`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Sell-through por cohorte de ingreso a 30/60/90 días | Para cada SKU, fecha_cohorte = fecha del primer Movimientos_Producto con concepto RECEPCION_COMPRA; vendidas_90 = Sum(Ticket_Productos.stock) de tickets PAGADO con created_at entre fecha_cohorte y fecha_cohorte+90d; ST90 = vendidas_90 / unidades_ingresadas_cohorte | Movimientos_Producto (concepto, fecha, cantidad, ProductoTalla_id), Ticket_Productos.stock, Ticket.created_at | La rotación actual es 'vendido del año / entrado del año' y se contamina con stock de años anteriores (el propio reporte lo admite en su nota). El sell-through por cohorte es comparable entre temporadas y responde de verdad si la compra fue buena. La línea base conocida del negocio es ST90 ≈ 12%, así que hay margen enorme de decisión. | medio |
| Días de cobertura del stock que quedó de la compra | stock_actual_de_los_SKU_ingresados / (unidades vendidas de esos SKU en los últimos 90 días / 90) | Producto_Talla.stock, Ticket_Productos.stock, Ticket.created_at | Convierte el 'stock_sin_vender' (que hoy es un número absoluto sin contexto) en meses de inventario. Con la cobertura de referencia del negocio en ~641 días, es el KPI que justifica frenar una recompra. | medio |
| Margen bruto real FIFO del período | Sum(Ticket_Productos.subtotal) - Sum(Ticket_Productos.stock * Ticket_Productos.costo_fifo), y % sobre venta | Ticket_Productos.subtotal, stock, costo_fifo | Es el único margen no inventado que puede entregar este módulo, y el dato ya está persistido por línea. Reemplaza el ROI prorrateado de esta pestaña y el ROI-IVA de la pestaña principal. | bajo |
| Fuga de unidades: % de lo ingresado que salió sin venderse | (otros_egresos) / entrada, abierto por concepto (AJUSTE_NEGATIVO, PERDIDA_ROBO, PERDIDA_DETERIORO, DEVOLUCION_PROVEEDOR, CAMBIO_PRODUCTO_SALIDA) | Movimientos_Producto.concepto y cantidad (la constante CONCEPTOS_OTROS_EGRESOS ya existe en views_modulo_reportes.py:4816) | El total ya se calcula pero se muestra agregado; abrirlo por concepto distingue merma real de errores de inventario y de devoluciones al proveedor (que además deberían estar cruzadas con NC recibidas). | bajo |

### Rendimiento por Proveedor (Compra → Recepción → Venta a público) — `MEJORABLE`

- **Ruta**: /app/reportes/rendimiento-proveedor/ (retailmind/app/views_modulo_reportes.py:ver_reporte_rendimiento_proveedor, línea 6296) — datos: /app/api/reporte-rendimiento-proveedor/ (línea 6302) — export: /app/api/exportar-rendimiento-proveedor-excel/ (línea 6473) — template: retailmind/app/templates/vistas/modulo_reportes/reporte_rendimiento_proveedor.html
- **Mide**: Por proveedor: pares comprados (OC), pares recepcionados, pares vendidos a público, % recepción, % venta, inversión, venta total, margen, stock disponible, pendiente de recepción y, aparte, el monto de documentos de compra sin líneas inventariables (compra por concepto). Es el único reporte del módulo que cierra el ciclo completo de una OC.
- **Filtros**: Año, Proveedor, Desde/Hasta (fechas) y Sucursal (reporte_rendimiento_proveedor.html:78-98). Todos se aplican server-side y con bastante cuidado: el rango de fechas se aplica a los DTE por fecha_emision, a las recepciones por fecha_recepcion con fallback a fecha cuando es NULL (líneas 6330-6341 y 6380-6394) y a las ventas por idTicket__created_at__date__range. El filtro de Sucursal aplica SOLO a la venta, no a la compra ni a la recepción — razonable, pero no está explicado en pantalla, y hace que con sucursal seleccionada el '% venta' se desplome sin motivo aparente.
- **Fidelidad**: Mixta y claramente partida en dos. La mitad de compra/recepción es sólida: filtra estado__in=['ACTIVA','COMPLETADA'] (excluye ELIMINADA y CANCELADA), lee unidades reales de Compras_Producto_Talla.stock y Productos_Recepcionados.stockArribado, resuelve bien el caso de recepciones retroactivas con fecha_recepcion NULL, usa Ticket.created_at y solo tickets PAGADO, y separa honestamente el monto no inventariable en vez de esconderlo. La mitad de venta/margen NO es confiable: los SKU vendidos de un proveedor se obtienen uniendo los enlaces reales (recep_qs.producto_talla) con un match por TEXTO entre Compras_Producto.nombre y Producto.articulo (líneas 6402-6406). Como en este catálogo 'articulo' es el CÓDIGO y el nombre en la OC suele ser el modelo/descripción, ese match falla masivamente (proveedores con venta 0 pese a tener recepción) y cuando acierta atribuye TODAS las ventas de ese código a este proveedor, sin filtrar por proveedor ni por lote: si dos proveedores comparten un código, ambos se anotan la venta completa. Por eso los totales de la fila KPI (total_vendidos, pct_venta_global, total_margen) pueden superar el 100% y no son sumables. Además el match es case-sensitive (__in exacto), y el catálogo recién se normalizó a mayúsculas mientras Compras_Producto.nombre no, lo que empeora los falsos ceros. Otros detalles: dtes_compra_qs no excluye notas de crédito, así que 'monto no inventariable' puede incluir NC en positivo; y no se aplica excluir_de_analitica a los tickets.
- **Rendimiento**: ~91 s confirmado por diseño: el loop for prov in proveedores (línea 6358) recorre TODOS los proveedores (unión de los que tienen OC y los emisores de DTE de compra) y por cada uno ejecuta ~9 queries, dos de ellas caras: Producto_Talla.objects.filter(producto__articulo__in=articulos) con cientos de strings, y Ticket_Productos.objects.filter(ProductoTalla_id__in=pts) donde pts es un set de Python que puede tener decenas de miles de IDs y viaja como literal en el SQL. Con ~250 proveedores son ~2.200 queries, varias de ellas con IN gigantes. Fix: eliminar el match por nombre (que además es el que genera los IN gigantes) y agrupar en 3 queries: una por proveedor sobre Compras_Producto_Talla, una sobre Productos_Recepcionados y una sobre Ticket_Productos anotada por el proveedor de origen del SKU.

**Problemas:**

- [alta] **Las ventas por proveedor se atribuyen por match de texto entre el nombre de la OC y el código de artículo** — articulos = Compras_Producto.values_list('nombre') y luego Producto_Talla.filter(producto__articulo__in=articulos). En este catálogo 'articulo' es el código y 'descripcion' el modelo, mientras que Compras_Producto.nombre viene del formato de importación de la OC. Dos consecuencias opuestas y ambas malas: falsos CEROS (proveedor con recepción y sin venta porque ningún nombre matchea un código) y SOBRECONTEO (cuando matchea, se le atribuyen todas las ventas históricas de ese código, vengan del proveedor que vengan y del lote que sea, en cualquier sucursal si no se filtró). El propio código lo documenta en un TODO(negocio) en la línea 6398. Todos los KPI de venta, margen y % venta —incluidos los globales de la fila superior— heredan el problema.
  - *Fix*: Usar exclusivamente el enlace real: Productos_Recepcionados.producto_talla (pt_directos, línea 6401) y Compras_Producto_Talla.producto_talla, que es el mismo enlace que ya usa con éxito calcular_compras_por_categoria_marca. Para las recepciones antiguas sin enlace, no adivinar: reportarlas en una columna 'unidades sin trazabilidad de venta' (igual que ya se hace con el monto no inventariable) y ofrecer un comando de backfill que enlace por (articulo normalizado + marca + color + talla), que es la clave de identidad ya usada en la deduplicación de productos. Mientras no se arregle, ocultar las columnas Vendidos/% Venta/Margen o marcarlas como estimadas.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:6397-6406`
- [alta] **Los totales de venta y margen no son sumables (doble conteo entre proveedores)** — tv = sum(r['pares_vendidos']) y total_margen = sum(r['margen']) se calculan sumando filas que pueden compartir el mismo Producto_Talla por el match de nombre. El KPI 'pct_venta_global' = tv/tr puede pasar del 100% y el margen total puede exceder la venta real del período.
  - *Fix*: Además de arreglar la atribución, calcular los totales globales con una query única sobre el conjunto de SKU distintos, no sumando las filas por proveedor.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:6446-6453`
- [alta] **N+1 por proveedor con IN de decenas de miles de IDs: ~91 s** — Un loop sobre todos los proveedores con ~9 queries cada uno, incluyendo un filter(ProductoTalla_id__in=pts) donde pts es un set de Python construido en memoria. El Excel (exportar_rendimiento_proveedor_excel, línea 6479) reejecuta el API completo, así que exportar cuesta otros 91 s.
  - *Fix*: Eliminar el match por nombre (quita el IN gigante) y reemplazar el loop por agregados agrupados: values('compra_producto__compras__empresa_id').annotate(...) para compras y recepciones, y para ventas una anotación desde Ticket_Productos hacia el proveedor vía el enlace producto_talla → recepción → compra.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:6358-6423`
- [media] **Las notas de crédito del proveedor entran como monto no inventariable positivo** — dtes_compra_qs = Dte.objects.filter(tipo_transaccion='COMPRA') sin excluir es_nota_credito ni descartado ni ANULADO. Una NC no tiene líneas con producto_talla, así que cae directo en el bucket 'documentos no inventariables' y SUMA al monto en vez de restarlo, inflando la columna que el usuario lee como 'gasto por concepto de este proveedor'.
  - *Fix*: Excluir es_nota_credito=True y descartado=True del queryset, o restar su monto explícitamente y mostrar una columna 'NC recibidas'.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:6312`
- [media] **El filtro de Sucursal solo afecta la venta y eso no se dice en pantalla** — sucursal_id se aplica únicamente a los tickets (línea 6419-6420). Con una sucursal seleccionada, 'comprados' y 'recepcionados' siguen siendo globales, así que el '% venta' colapsa y parece que el proveedor no rota, cuando en realidad se está comparando la venta de una tienda contra la compra de toda la cadena.
  - *Fix*: O propagar el scope de sucursal también a la recepción (Productos_Recepcionados tiene el destino) o cambiar la etiqueta del filtro a 'Sucursal de venta' y añadir la advertencia en el subtítulo del reporte.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:6419-6420`
- [baja] **No aplica excluir_de_analitica a las ventas** — El filtro de tickets no excluye ProductoTalla__producto__excluir_de_analitica=True, a diferencia del resto de reportes del módulo. Productos de exhibición o consignación cuentan como venta del proveedor.
  - *Fix*: Agregar .exclude(ProductoTalla__producto__excluir_de_analitica=True) al queryset de Ticket_Productos.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_reportes.py:6413-6421`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Lead time real de entrega vs comprometido (días) | real = fecha_entrega_real - fecha_envio_proveedor; comprometido = fecha_entrega_estimada - fecha_envio_proveedor; desviación = real - comprometido; agregar mediana y P90 por proveedor | Compras.fecha_envio_proveedor, Compras.fecha_entrega_estimada, Compras.fecha_entrega_real, Empresa.lead_time_dias | Los tres campos ya existen en el modelo y NINGÚN reporte los usa hoy. Es la métrica de proveedor que realmente permite negociar y planificar la temporada; además valida si el lead_time_dias configurado por proveedor (que alimenta la predicción de compras) es realista. | bajo |
| Fill rate a nivel de línea (no de unidades agregadas) | % de Compras_Producto_Talla cuya recepción acumulada >= stock pedido; complementar con % de unidades = Sum(stockArribado)/Sum(stock) | Compras_Producto_Talla.stock, Productos_Recepcionados.stockArribado agrupado por compra_producto_talla_id | El % de recepción actual es agregado y esconde el patrón típico: llega el 95% de las unidades pero faltando justo las tallas centrales, que son las que venden. El fill rate por línea detecta al proveedor que 'completa con lo que tiene'. | medio |
| Tasa de incidencia en recepción (dañado + faltante + regularización) | Sum(Productos_Recepcionados.cantidad_danada) / Sum(stockArribado); y % de líneas cuyo estado quedó en FALTANTE / EN_REGULARIZACION / RECEPCIONADO_DANADO | Productos_Recepcionados.cantidad_danada, cantidad_esperada, y los estados de ESTADO_RECEPCION_PRODUCTO_CHOICES | Es el 'cumplimiento del proveedor' de verdad (calidad de la entrega), el que hoy se está confundiendo con el % de facturas pagadas en el reporte integral. Los campos ya se llenan en el flujo de recepción. | bajo |
| Sell-through a 90 días por proveedor, sobre SKU con enlace real | unidades vendidas (tickets PAGADO, created_at) en los 90 días posteriores a la recepción / unidades recepcionadas, considerando solo Productos_Recepcionados con producto_talla no nulo, e informando la cobertura del enlace (% de unidades con trazabilidad) | Productos_Recepcionados.producto_talla, Productos_Recepcionados.fecha_recepcion, Ticket_Productos.stock, Ticket.created_at | Sustituye el '% venta' actual por uno defendible y comparable entre proveedores, con su propio indicador de confiabilidad al lado. Es la base para decidir a quién recomprar la próxima temporada. | medio |
| Capital inmovilizado por proveedor (stock actual valorizado a costo) | Sum(Producto_Talla.stock * costo) de los SKU recepcionados de ese proveedor, abierto por antigüedad del ingreso (0-90 / 91-180 / 181-365 / +365 días) | Producto_Talla.stock, Compras_Producto.costo (o costo del movimiento de ingreso), Movimientos_Producto.fecha del primer ingreso | 'stock_disponible' hoy son unidades sin precio ni antigüedad. Valorizarlo y envejecerlo es lo que convierte el reporte en una herramienta de negociación (devolución, descuento, campaña de liquidación) y conecta con el módulo de Plan de Liquidación que ya existe. | medio |

### Dashboard de Compras Estratégico — `REDUNDANTE`

- **Ruta**: /app/dashboard_compras_estrategico/ (retailmind/app/views_modulo_compras.py:dashboard_compras_estrategico, línea 1006) — template huérfano: retailmind/app/templates/vistas/modulo_dashboards/dashboard_compras_estrategico.html (lo pide en la línea 561). El único render de ese template está en retailmind/app/views.py:23579, en una función verDashboardCompras que NO está registrada en urls.py (urls.py:479 apunta a views_modulo_compras.verDashboardCompras, que renderiza el dashboard MEJORADO).
- **Mide**: Sobre el modelo Compras/OC del año: cumplimiento general (recepcionado/esperado), ROI teórico (precioSugerido vs costo), 'rotación de inventario' (productos con recepción / productos totales), 'precisión de pronóstico' (que es literalmente una copia del cumplimiento, línea 1094), cumplimiento por proveedor, ROI por temporada normalizada con YoY, top 5 proveedores, top 10 productos por inversión, compras por mes, alertas y recomendaciones.
- **Filtros**: Año, Temporada (con normalización de acentos VERANO/OTONO/INVIERNO/PRIMAVERA sobre temporada_familia y fallback icontains), Proveedor y Responsable. Los cuatro se aplican server-side correctamente sobre el queryset base — de hecho es el único reporte de la familia cuyo filtro de temporada funciona de verdad, porque el modelo Compras sí tiene temporada_familia/temporada_anio. El problema es que esos filtros ya NO son alcanzables desde la navegación: el template que los dibuja quedó huérfano.
- **Fidelidad**: El cálculo del año en curso es correcto y aplica la exclusión de ELIMINADA y CANCELADA (línea 1020-1021). Pero: (a) la comparación con el año anterior NO excluye eliminadas (línea 1255: Compras.objects.filter(fecha__year=anio-1) sin exclude), así que trend_cumplimiento y trend_roi comparan una base limpia contra una base sucia y siempre sesgan; (b) 'ROI promedio' es markup de lista teórico (precioSugerido/costo), no ROI realizado — el Excel del dashboard mejorado sí lo etiqueta como 'markup lista, teórico' (línea 1401), la pantalla no; (c) 'rotación de inventario' = productos con alguna recepción / total de productos: no es rotación, es cobertura de recepción, y la recomendación automática 'Optimizar gestión de inventario para aumentar rotación' se dispara con ese número; (d) 'precisión de pronóstico' es el mismo valor que cumplimiento, duplicando una tarjeta.
- **Rendimiento**: N+1 en dos frentes: el top de proveedores itera proveedores haciendo 2 queries cada uno (líneas 1297-1307) y 'rendimiento detallado' hace 4 aggregates por compra sobre las 10 primeras (1140-1171); además top_productos hace un aggregate extra por producto dentro del loop (1329-1331) y compras_por_mes hace exists()+count()+aggregate por mes. Es mucho más rápido que el reporte integral (decenas a pocas centenas de queries) porque el universo de proveedores con OC es chico, pero el bloque de cumplimiento por proveedor ya fue optimizado a queries agrupadas (1100-1119) y el resto quedó sin migrar.

**Problemas:**

- [alta] **TRIPLICADO CONFIRMADO: este dashboard es un subconjunto del Dashboard de Compras Mejorado y ya está desconectado de la navegación** — Comparando cálculo a cálculo: cumplimiento general, ROI teórico, cumplimiento por proveedor, ROI por temporada (de hecho llama a la MISMA función calcular_roi_temporadas_mejorado, línea 1135), top proveedores por inversión, top productos por inversión, compras por mes, alertas y recomendaciones — todo eso lo entrega también dashboard_compras_mejorado_api, con la misma base (modelo Compras) y con extras que el estratégico no tiene (inversión por categoría v1.2, sell-through 90d por marca, distribución CD→tiendas, márgenes del CD, compras por concepto). Lo único exclusivo del estratégico es el filtro por 'responsable' y la tarjeta de 'precisión de pronóstico', que es una copia del cumplimiento. Encima ya está muerto: urls.py:479 hace que verDashboardCompras renderice el template MEJORADO, con lo que dashboard_compras_estrategico.html no lo muestra ninguna ruta, y las funciones dashboard_compras_estrategico/verDashboardCompras duplicadas en views.py (líneas 23136 y 23575) tampoco están registradas: son ~440 líneas de código muerto.
  - *Fix*: Eliminar el endpoint, el template dashboard_compras_estrategico.html y las funciones duplicadas de views.py; portar al dashboard mejorado el único aporte real (el filtro por 'responsable', que existe en Compras.responsable). Consolidación propuesta para toda la familia: sobrevive el Dashboard de Compras Mejorado como única vista de 'qué compramos' (base OC), Rendimiento por Proveedor como única vista de 'cómo respondió el proveedor y cómo rotó' (base OC→recepción→venta), y el Reporte de Compras Integral se reduce a lo único que nadie más cubre: Cuentas por Pagar sobre DTE.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:1006-1361 vs 2876-3075; ruta huérfana en app/urls.py:479 y render muerto en app/views.py:23575-23579`
- [alta] **El botón del Dashboard Home lleva a un JSON crudo** — dashboard_home.html:622 hace <a href="{% url 'dashboard_compras_estrategico' %}">, pero ese nombre de URL resuelve al ENDPOINT API (urls.py:477), no a una vista HTML. El usuario que entra por el home de dashboards ve una pantalla de texto JSON.
  - *Fix*: Cambiar el enlace a {% url 'verDashboardComprasMejorado' %}. De paso, corregir el tooltip 'Dashboard Estratégico' de gestionCompras.html:1472, que apunta a verDashboardCompras y en realidad abre el mejorado.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/templates/vistas/dashboard_home.html:622`
- [media] **La comparación YoY compara una base limpia contra una base con compras eliminadas** — El año en curso excluye ELIMINADA y CANCELADA (línea 1020) pero compras_anterior no lo hace (línea 1255). Todas las tendencias (trend_cumplimiento, trend_roi, trend_precision) quedan sesgadas hacia abajo por las compras borradas del año pasado.
  - *Fix*: Aplicar el mismo .exclude(estado__in=['ELIMINADA','CANCELADA']) al queryset del año anterior. Mismo defecto en el dashboard mejorado (views_modulo_compras.py:3131).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:1255`
- [media] **Dos KPIs de cabecera son etiquetas falsas: 'rotación de inventario' y 'precisión de pronóstico'** — rotacion_inventario = productos con alguna recepción / total de productos de la compra (línea 1090-1091): no mide rotación, mide cobertura de recepción, y su valor natural es ≤1. precision_pronostico = cumplimiento_general, asignado literalmente en la línea 1094. Ambos alimentan recomendaciones automáticas que el usuario lee como si fueran diagnósticos independientes.
  - *Fix*: Al eliminar el dashboard no hace falta arreglarlos; si se decidiera conservarlo, renombrar a '% de líneas con recepción' y borrar la tarjeta de precisión de pronóstico (o calcularla de verdad contra el módulo de predicción, que tiene Compras.generada_por_sistema para identificar las OC sugeridas).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:1089-1094`

### Dashboard de Compras Mejorado — `MEJORABLE`

- **Ruta**: /app/verDashboardComprasMejorado/ y /app/verDashboardCompras/ (ambas rutas renderizan el MISMO template) — retailmind/app/views_modulo_compras.py:verDashboardComprasMejorado línea 2869 y verDashboardCompras línea 1454 — datos: /app/dashboard_compras_mejorado_api/ (línea 2876) — export: /app/exportar_dashboard_compras/ (línea 1374) — template: retailmind/app/templates/vistas/modulo_dashboards/dashboard_compras_mejorado.html
- **Mide**: Sobre el modelo Compras/OC: inversión, unidades esperadas vs recepcionadas, cumplimiento, markup teórico, evolución mensual compras vs ventas, Pareto y top de proveedores, top de productos, ROI por temporada normalizada con YoY real (Invierno 2025 vs Invierno 2024), inversión por categoría v1.2 (Padre › Hija) y por marca cruzada con stock actual y venta 90d, cumplimiento por proveedor, rendimiento por compra, compras por concepto (no inventariables) y todo un bloque de distribución centro→tiendas con márgenes del CD.
- **Filtros**: Año, Período, Temporada y Proveedor. A diferencia del reporte integral, aquí SÍ están implementados: el período se aplica de verdad al queryset (líneas 2912-2915, con la salvedad explícita de que solo tiene sentido en el año en curso) y la temporada se resuelve contra temporada_familia con fold de acentos (2917-2927). Falta un filtro de sucursal/empresa, que sería relevante ahora que hay bloque de distribución.
- **Fidelidad**: La mejor de la familia, con reservas. Bien: excluye ELIMINADA y CANCELADA en el queryset base; usa unidades reales de OC y recepción; el bloque de categoría/marca usa el enlace real producto_talla → Producto (no texto libre), aplica la recategorización v1.2 y —muy importante— publica su propia cobertura (inversion_enlazada, inversion_sin_enlace, pct_enlace), que es honestidad de dato que el resto del módulo no tiene; la venta 90d por marca usa Ticket.created_at y estado PAGADO; el bloque de compras por concepto sí filtra por receptor_id=idEmpresaActual y descartado=False. Mal: (a) la evolución mensual y la comparativa anual NO excluyen ELIMINADA/CANCELADA (líneas 3185, 3284, 3293), así que los gráficos muestran más inversión que las tarjetas KPI del mismo dashboard; (b) las tendencias YoY tampoco excluyen eliminadas y además ignoran los filtros de temporada/proveedor aplicados en pantalla (3131-3134): con un proveedor filtrado, el 'vs año anterior' compara ese proveedor contra TODOS los proveedores del año pasado; (c) la serie 'ventas' del gráfico mensual es Sum(Ticket.total) de todos los tickets PAGADO del sistema, sin scoping de empresa ni sucursal, sin excluir excluir_de_analitica y sin reaccionar a los filtros de proveedor/temporada — o sea, se superpone una curva global fija contra una curva de compras filtrada; (d) el ROI por temporada compara contra un año anterior que excluye solo ELIMINADA, no CANCELADA (3377); (e) el bloque de distribución mezcla bases: eficiencia_distribucion = despachos globales del año / unidades recepcionadas de las compras FILTRADAS (3798-3823), así que con filtro de proveedor puede superar 100% sin sentido; y calcular_despachos_por_sucursal exige sucursal_destino__isnull=False, lo que descarta el histórico migrado donde el destino es NULL, además de no restringir el origen a centros de distribución (tienda→tienda cuenta como despacho del CD).
- **Rendimiento**: Sin los 44 minutos del reporte integral, pero con N+1 en cuatro loops sobre proveedores: pareto (3231-3241, 2 queries por proveedor), top proveedores (3435-3468, 5 por proveedor), cumplimiento (3505-3520, 4 por proveedor) y rendimiento detallado (3633-3652, 4 aggregates por cada una de las 20 primeras compras). Súmese evolución mensual (12 × 3) y comparativa anual (12 × 4) y el bloque de distribución, que hace un loop por sucursal destino con 3 queries cada una. Orden de magnitud: 300-600 queries. Todo el bloque de compras es agrupable con un solo values('empresa_id').annotate(...) porque el universo son proveedores con OC del año, no el catálogo completo de empresas.

**Problemas:**

- [alta] **Los gráficos mensuales incluyen compras ELIMINADAS que las tarjetas KPI excluyen** — El queryset base del dashboard excluye ELIMINADA y CANCELADA (línea 2906-2907), pero calcular_evolucion_mensual_mejorado construye su propio Compras.objects.filter(fecha__year, fecha__month) sin exclusión (3185) y calcular_comparativa_anual_mejorado hace lo mismo para ambos años (3284 y 3293). El usuario ve un total anual en la tarjeta y una suma de barras mensuales mayor, en el mismo dashboard.
  - *Fix*: Extraer un helper único _compras_base(anio, temporada, proveedor) que aplique SIEMPRE el .exclude(estado__in=['ELIMINADA','CANCELADA']) y usarlo en las 5 funciones (métricas, evolución, comparativa, ROI temporadas YoY y flujo de distribución mensual, esta última en la línea 3978, que también omite la exclusión).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3185, 3284, 3293, 3978`
- [alta] **La curva de 'ventas' del gráfico mensual es global y no responde a ningún filtro** — ventas_mes = Ticket.objects.filter(created_at año/mes, estado='PAGADO').aggregate(Sum('total')): todos los tickets de todas las empresas y sucursales, sin excluir excluir_de_analitica, sin distinguir VENTA_PUBLICO de facturas por concepto/despacho, y sin aplicar los filtros de proveedor ni temporada que sí se aplicaron a la serie de compras. El gráfico 'Compras vs Ventas' invita a leer una relación causal entre dos series que no comparten universo. Además está envuelto en un try/except: bare (3205-3212) que silencia cualquier error dejando ventas en 0.
  - *Fix*: O restringir la venta al mismo universo (SKU provenientes de esas compras, vía producto_talla, que es exactamente lo que ya sabe hacer calcular_compras_por_categoria_marca) o etiquetar la serie como 'Venta total de la cadena (referencia)' y deshabilitarla cuando haya filtros de proveedor/temporada activos. Quitar el except: desnudo.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3204-3212`
- [alta] **Las tendencias vs año anterior ignoran los filtros aplicados y no excluyen eliminadas** — En calcular_metricas_principales_mejorado el año anterior es Compras.objects.filter(fecha__year=anio-1) a secas: sin exclusión de estado, sin temporada y sin proveedor. Con 'Proveedor = X' seleccionado, trend_inversion compara la inversión en X este año contra la inversión en TODOS los proveedores el año pasado, dando siempre caídas catastróficas.
  - *Fix*: Aplicar al año anterior exactamente los mismos filtros y exclusiones que al año actual (pasar la función de filtrado, no el año).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3131-3138`
- [alta] **El bloque de distribución mezcla un numerador global con un denominador filtrado** — calcular_metricas_distribucion toma unidades_compradas de las compras filtradas (compras_ids) pero unidades_despachadas de TODOS los movimientos TRASPASO_SALIDA del año, y con eso calcula 'eficiencia_distribucion' y 'stock_centro_distribucion = compradas - despachadas'. Con cualquier filtro activo el porcentaje se dispara y el 'stock del CD' (que además no es un stock, es una resta de flujos) queda en 0 por el max(0, ...).
  - *Fix*: Restringir los despachos a los SKU provenientes de esas compras (vía Productos_Recepcionados.producto_talla) o desacoplar el bloque de distribución de los filtros de compra y rotularlo como vista independiente. Para el stock del CD usar Producto_Talla.stock de las sucursales con es_centro_distribucion=True, que es el dato real.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3797-3823`
- [media] **Los despachos por sucursal pierden el histórico migrado y cuentan traspasos tienda→tienda** — calcular_despachos_por_sucursal filtra sucursal_destino__isnull=False, pero en los datos migrados de Laravel el destino viene NULL y la sucursal va en sucursal_origen (regla ya establecida en el reporte de movimientos por sucursal). Además no restringe el origen a centros de distribución, así que un traspaso entre dos tiendas se contabiliza como despacho del CD.
  - *Fix*: Agrupar por COALESCE(sucursal_destino, sucursal_origen) para la pata de entrada y filtrar sucursal_origen__es_centro_distribucion=True para la de salida, consistente con la regla canónica ya usada en el dashboard de despachos.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3838-3849`
- [media] **'ROI Promedio' en pantalla es markup teórico de lista, y solo el Excel lo aclara** — roi_promedio = (Sum(precioSugerido × stock) - Sum(costo × stock)) / Sum(costo × stock): es el margen que se ESPERA si todo se vende a precio de lista, sin descuentos, sin liquidación y sin considerar lo que efectivamente se vendió. El Excel exportado lo rotula 'ROI Promedio % (markup lista, teórico)' (línea 1401) pero la tarjeta de la pantalla dice solo 'ROI'.
  - *Fix*: Renombrar la tarjeta a 'Markup teórico de lista' y añadir al lado el margen realizado FIFO (Sum(Ticket_Productos.subtotal - stock*costo_fifo) de los SKU de esas compras), que sí es calculable con los datos existentes.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3120-3128`
- [media] **Cuatro loops N+1 por proveedor recalculando lo mismo** — pareto, top proveedores y cumplimiento por proveedor recorren el mismo conjunto de proveedores y recalculan inversión, esperadas y recibidas por separado (3225-3252, 3429-3472, 3499-3535); rendimiento detallado hace 4 aggregates por compra. El bloque de cumplimiento del dashboard estratégico ya fue optimizado a 2 queries agrupadas (views_modulo_compras.py:1100-1119): esa misma técnica no se aplicó aquí.
  - *Fix*: Calcular una sola vez, con 3 queries agrupadas por empresa_id (inversión, esperadas, recibidas) y derivar pareto, top y cumplimiento en Python desde ese diccionario.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:3225-3252 y 3429-3472`
- [baja] **Dos entradas de menú distintas apuntan al mismo dashboard** — verDashboardCompras (línea 1454) y verDashboardComprasMejorado (línea 2869) renderizan literalmente el mismo template. El menú usa el permiso 'dashboard_compras_estrategico' para mostrar un enlace a verDashboardComprasMejorado (menu.html:2018-2020) y gestionCompras.html:1472 ofrece otro botón rotulado 'Dashboard Estratégico' hacia verDashboardCompras.
  - *Fix*: Dejar una sola ruta y una sola etiqueta ('Dashboard de Compras'); redirigir la otra con un RedirectView permanente y alinear el nombre del permiso.
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:1453-1456`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| OTIF (On Time In Full) por proveedor y por temporada | % de OC entregadas completas (Sum(stockArribado) >= Sum(stock) pedido) Y a tiempo (fecha_entrega_real <= fecha_entrega_estimada). Reportar además los dos componentes por separado (On Time y In Full) | Compras.fecha_entrega_estimada, Compras.fecha_entrega_real, Compras_Producto_Talla.stock, Productos_Recepcionados.stockArribado | Es el indicador estándar de compras y todos sus insumos ya están en el modelo sin usar. Reemplaza el 'cumplimiento' actual, que solo mira cantidad y no plazo, y permite condicionar la recompra de temporada a un umbral objetivo. | medio |
| Días de cobertura por categoría v1.2 y por marca | stock actual de la categoría / (unidades vendidas de esa categoría en 90 días / 90), usando la jerarquía Padre › Hija | Producto_Talla.stock, Producto.categoria (con padre), Ticket_Productos.stock, Ticket.created_at — el bloque calcular_compras_por_categoria_marca ya arma exactamente estos joins | El dashboard ya muestra sell-through 90d por marca; la cobertura en días es la mitad que falta y la que dice CUÁNTO no comprar. Con la línea base de la casa (cobertura ~641 días, 70% del stock con más de 180 días) es el KPI que frena compra. | bajo |
| Desviación de precio: markup teórico vs markup realizado | teórico = precioSugerido/costo - 1 (desde la OC); realizado = (Sum(Ticket_Productos.subtotal)/Sum(stock)) / costo - 1 para los SKU de esa compra; desviación = realizado - teórico, abierto por marca y categoría | Compras_Producto.precioSugerido y costo, Compras_Producto_Talla.producto_talla, Ticket_Productos.subtotal y stock | Muestra cuánto del margen planificado se destruye en descuentos, campañas y liquidación. Hoy el dashboard solo enseña el teórico, que es sistemáticamente optimista, y sin este cruce no se puede evaluar si el problema fue el precio de compra o la política de descuentos. | medio |
| % de inversión sin trazabilidad al catálogo (indicador de calidad del dato) | inversion_sin_enlace / inversion_total (ya calculado en calcular_compras_por_categoria_marca como pct_enlace) | Compras_Producto_Talla.producto_talla nulo vs no nulo | Ya está calculado pero enterrado en el payload; subirlo a tarjeta KPI hace visible cuánta plata del período no se puede analizar por categoría/marca, y presiona a corregir el enlace en la recepción, que es la raíz de los problemas del reporte de Rendimiento por Proveedor. | bajo |
| Compra por concepto (no inventariable) como % del gasto total del período | monto de Dte con es_por_concepto=True / (ese monto + inversión inventariable de OC) | Dte.es_por_concepto, Dte.monto_con_iva (bloque ya implementado en la línea 3028) y Compras_Producto costo×stock | El bloque ya calcula el monto absoluto; expresarlo como porcentaje del gasto separa mercadería de gasto operativo y evita que se lea la inversión total como si fuera todo inventario. | bajo |

### Diagnóstico de datos de compras — `ROTO`

- **Ruta**: /app/verDiagnosticoCompras/ (retailmind/app/views_modulo_compras.py:1460) — datos: /app/diagnostico_datos_compras/ (línea 1466) — template: retailmind/app/templates/vistas/modulo_compras/diagnostico_compras.html
- **Mide**: Pretende dar un 'score de calidad' de los datos de compras: compras sin proveedor, sin fecha y sin total, más DTE de compra sin productos y sin emisor.
- **Filtros**: Ninguno. No acepta año, empresa ni proveedor: cuenta sobre la tabla completa.
- **Fidelidad**: Nula: la vista nunca llega a devolver datos. Consulta Compras.objects.filter(fecha_compra__isnull=True) y Compras.objects.filter(total__isnull=True), pero el modelo Compras no tiene ningún campo llamado 'fecha_compra' ni 'total' (los campos reales son 'fecha', 'fecha_creacion' y el monto no se guarda en la cabecera, se deriva de costo × stock de las líneas). Django levanta FieldError, el try/except lo captura y la API responde siempre {'success': False, 'error': ...}. Es decir: el módulo está roto al 100% de las ejecuciones, en cualquier base de datos, desde que se escribió. Como bonus, el 'calidad_score' que pretendía calcular suma tres conteos de defectos que pueden solaparse en la misma fila, así que podría dar negativo si no fuera por el max(0, ...).
- **Rendimiento**: Irrelevante: falla antes de ejecutar la mayoría de las queries.

**Problemas:**

- [alta] **La API referencia dos campos que no existen en el modelo: falla siempre** — Compras.objects.filter(fecha_compra__isnull=True) y Compras.objects.filter(total__isnull=True). El modelo Compras (app/models/compras.py:7-77) define empresa, nombre, correlativo, responsable, temporada, fecha, fecha_creacion, fechaInicioTemporada, fechaTerminoTemporada, estado, fecha_eliminacion, eliminado_por, tipo, generada_por_sistema, fecha_envio_proveedor, fecha_entrega_estimada, fecha_entrega_real, temporada_familia, temporada_anio y es_historica. No hay 'fecha_compra' ni 'total'. El FieldError queda tapado por el except genérico y el usuario solo ve el mensaje de error en pantalla.
  - *Fix*: O se borra el módulo (recomendado: sus chequeos son un subconjunto pobre de lo que ya se puede ver en el dashboard mejorado) o se reescribe contra campos reales y chequeos que sí importan: compras sin líneas, líneas sin producto_talla enlazado, compras con costo=0, DTE de compra sin receptor, DTE duplicados por (emisor, número, fecha).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:1472-1473`
- [baja] **La vista no valida permisos de sucursal ni empresa y cuenta sobre toda la tabla** — diagnostico_datos_compras solo lleva @login_required (no @require_GET, no chequeo de empresa) y cuenta Compras.objects.count() global, sin ningún scoping.
  - *Fix*: Si se conserva, alinear con el resto del módulo (require_GET + scoping por empresa actual).
  - *Ref*: `C:/Users/cromo/Documents/DjangoProyects/SistemaRetailMind/retailmind/app/views_modulo_compras.py:1465-1470`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Cobertura de enlace OC → catálogo (líneas y $) | % de Compras_Producto_Talla con producto_talla no nulo, y % de la inversión que representan | Compras_Producto_Talla.producto_talla, costo × stock | Es la métrica de calidad de dato que realmente bloquea el análisis: sin ese enlace no hay categoría, ni marca, ni sell-through, ni rendimiento por proveedor confiable. Es la causa raíz del match por nombre del reporte de proveedores. | bajo |
| DTE de compra sin recepción asociada, con antigüedad | Conteo y monto de Dte tipo COMPRA con estado_dte='EMITIDO' y sin filas en Productos_Recepcionados, agrupado por días desde fecha_emision | Dte.estado_dte, Dte.fecha_emision, Productos_Recepcionados.dte_id | Detecta mercadería facturada que nunca entró al inventario — el agujero operativo concreto ya identificado en el flujo de compras (el stock entra al crear productos, no al recepcionar). Con antigüedad se prioriza qué reclamar. | bajo |
| Compras con costo 0 o precioSugerido 0 | % de Compras_Producto con costo=0 o precioSugerido=0 sobre el total de líneas del período, y unidades afectadas | Compras_Producto.costo, Compras_Producto.precioSugerido, Compras_Producto_Talla.stock | Cada línea con costo 0 desinfla la inversión total y el markup de todos los dashboards de esta familia; es el chequeo que explica por qué un ROI teórico se dispara sin razón. | bajo |

## Familia: Operaciones y Decisión (despachos, liquidación, inteligencia/predicción de compra, trazabilidad y cuadratura)

### Despachos a Tiendas — `MEJORABLE`

- **Ruta**: /app/reportes/despachos-tiendas/ — vista app/views_modulo_reportes.py:8806 ver_reporte_despachos_tiendas; API app/views_modulo_reportes.py:8833 obtener_reporte_despachos_tiendas; template app/templates/vistas/modulo_reportes/reporte_despachos_tiendas.html
- **Mide**: Pivote artículo x tienda destino de lo despachado (Movimientos_Producto concepto='TRASPASO_SALIDA', estado='COMPLETADO') en un rango, valorizado a costo origen / sobreprecio / costo destino, más un acumulado por proveedor con 'despachado vs recibido' y tasa_recepcion. Por defecto la semana en curso.
- **Filtros**: fecha_desde / fecha_hasta / sucursal_destino_id / proveedor_id: los cuatro SÍ se aplican server-side (8847-8888 para fechas y tienda, 8952-8953 y 8966 para proveedor). El buscador de texto 'Artículo, descripción o proveedor' del template (línea 647-648) es 100% client-side (filtrarLocal) sobre el payload completo, igual que la paginación: no hay LIMIT/OFFSET en el backend, se devuelven TODOS los artículos del rango. Falta filtro por marca, por categoría v1.2 y por CD origen. El filtro de tienda destino aplica al pivote y también a las entradas (8919-8920), lo que rompe la tasa de recepción (ver problemas).
- **Fidelidad**: Scoping OK: acota a sucursales de las empresas del usuario vía EmpresaUser (8871-8877). Usa Movimientos_Producto.fecha (DateField con default de negocio, no auto_now — app/models/inventario.py:172), que es la fecha correcta para kardex; no aplica aquí la regla de Ticket.fecha. NO excluye producto.excluir_de_analitica ni valida estado del DTE asociado (un traspaso de un DTE anulado sigue sumando si el movimiento quedó COMPLETADO). Sólo captura traspasos MODERNOS: los legacy migrados usan TRASPASO_SUCURSAL/BODEGA/VITRINA (app/constants_kardex.py:34-39), así que cualquier rango histórico anterior a la migración muestra 0. 'Proveedor' NO es un dato del traspaso: se deriva de la última recepción de compra del artículo (8925-8948), es una aproximación y el propio código lo documenta. Tampoco filtra que el destino sea tienda: un TRASPASO_SALIDA de tienda a CD entra igual y se rotula como 'despacho a tienda'.
- **Rendimiento**: 3 queries agregadas + 1 query sin límite que materializa en Python TODAS las Productos_Recepcionados históricas de los artículos del rango (8935-8948: .filter(producto_talla__producto__articulo__in=articulos).select_related(...).order_by('dte__fecha_emision') y luego un for que sobrescribe). Es 1 query pero de cardinalidad no acotada: 500 artículos x N recepciones de varios años = decenas de miles de filas ORM sólo para quedarse con la última de cada artículo. Debe ser un Subquery anotado. El payload al navegador tampoco está paginado.

**Problemas:**

- [alta] **La tasa de recepción se cae a 0 al filtrar por tienda destino en datos con destino nulo y no está pareada temporalmente** — 'recibido' se calcula con un queryset independiente de TRASPASO_ENTRADA en el MISMO rango de fechas, sin exigir que esas entradas correspondan a las salidas contadas. En los bordes del rango la tasa puede pasar de 100% (entran recepciones de despachos anteriores) o quedar muy baja (despachos de fin de semana que se reciben el lunes siguiente). Además el acumulado 'recibido' sólo existe a nivel proveedor: la tabla de artículos nunca muestra si ESE artículo llegó.
  - *Fix*: Parear por documento: agrupar entradas y salidas por (dte_id, ProductoTalla) y calcular recibido sobre el universo de despachos del rango, no sobre entradas del rango. Exponer 'recibido' y 'pendiente' también por fila de artículo x tienda.
  - *Ref*: `app/views_modulo_reportes.py:8912-8923 y 9016-9031`
- [media] **Derivación de proveedor por 'última recepción del artículo' carga todo el histórico en memoria** — El bucle 8943-8948 recorre todas las Productos_Recepcionados de los artículos del período para quedarse con la más reciente. Sin .distinct() ni Subquery, y sin filtrar por empresa del usuario ni por estado del DTE (incluye recepciones de DTEs anulados/descartados).
  - *Fix*: Reemplazar por Subquery(Productos_Recepcionados.filter(producto_talla__producto__articulo=OuterRef('articulo'), dte__tipo_transaccion='COMPRA').order_by('-dte__fecha_emision').values('dte__emisor__nombre')[:1]) anotado sobre Producto, y excluir dte__descartado=True.
  - *Ref*: `app/views_modulo_reportes.py:8935-8948`
- [media] **El reporte no distingue CD→tienda de tienda→CD ni de tienda→tienda** — Filtra TRASPASO_SALIDA con origen en las sucursales del usuario, sin exigir que el origen sea centro de distribución ni que el destino sea tienda vendedora. Devoluciones de tienda al CD se cuentan como 'despacho a tienda' e inflan las unidades y la valorización.
  - *Fix*: Agregar el predicado canónico de CD ya existente (_es_cd_q de app/views_dashboards_kpi.py:421) al origen y excluir destinos con es_centro_distribucion=True, o al menos exponer la dirección como columna.
  - *Ref*: `app/views_modulo_reportes.py:8880-8888`
- [media] **Vista sin decorador de permiso: sólo se oculta en el menú** — ver_reporte_despachos_tiendas y su API sólo llevan @login_required. El menú la esconde con {% if permiso_reporte_movimientos_sucursal %} pero cualquier usuario autenticado puede pegar la URL y ver costos y valorización de todas las sucursales de sus empresas.
  - *Fix*: Aplicar @requiere_permiso('reporte_despachos_tiendas','puede_ver') como ya hace plan_liquidacion (app/views_inteligencia_compra.py:478).
  - *Ref*: `app/views_modulo_reportes.py:8805-8806 y app/templates/layout/menu.html:2642-2653`
- [baja] **Sin paginación ni límite server-side: el JSON crece sin techo con el rango** — Se devuelve la lista completa de artículos y el navegador pagina en JS. Con el default semanal está bien; con un trimestre el endpoint devuelve miles de filas con su diccionario de tiendas.
  - *Fix*: Paginar en DB (page/page_size, patrón de obtener_plan_liquidacion_detalle) o topear el rango de fechas.
  - *Ref*: `app/views_modulo_reportes.py:9074-9084`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Sell-through post-despacho D+30 / D+60 / D+90 por tienda y artículo | unidades vendidas en la tienda destino en los N días posteriores a la entrada / unidades recibidas (TRASPASO_ENTRADA). Numerador = Σ\|cantidad\| de Movimientos_Producto con concepto in CONCEPTOS_VENTA sobre el mismo ProductoTalla y fecha entre fecha_entrada y fecha_entrada+N. | Movimientos_Producto (concepto, ProductoTalla, sucursal_destino, fecha, cantidad); CONCEPTOS_VENTA de app/constants_kardex.py:15 | Es literalmente la pregunta que hoy el reporte NO responde: '¿se vendió lo que mandé?'. Permite frenar el despacho a tiendas que acumulan y redirigir a las que rotan, y evaluar al que decide la distribución. | medio |
| Lead time de despacho (días salida→entrada) y unidades en tránsito envejecidas | días = fecha del TRASPASO_ENTRADA − fecha del TRASPASO_SALIDA del mismo dte_id + ProductoTalla. En tránsito >7d = Σ unidades de salidas sin entrada pareada con antigüedad > 7 días. | Movimientos_Producto.dte_id + fecha (los dos movimientos comparten el DTE de traspaso, ver app/views.py:857-870) | Convierte el 'pendiente' actual (una resta ciega) en una alerta accionable de mercadería perdida o no recepcionada, y da un SLA logístico medible por ruta CD→tienda. | medio |
| Índice de sobre-despacho: cobertura resultante en la tienda destino | (stock de la tienda tras el despacho) / (velocidad diaria de ese SKU en esa tienda, últimos 90d) → días de cobertura. Marcar rojo si > 120 días. | Producto_Talla.stock por sucursal + Movimientos_Producto de venta últimos 90d en esa sucursal | Con la línea base de la casa (cobertura ~641 días, 70% del stock >180d), el problema no es despachar poco sino despachar a donde ya sobra. Este KPI bloquea el despacho antes de que se convierta en capital inmovilizado del Plan de Liquidación. | medio |
| Fit de curva de tallas despachada vs demanda de la tienda | Σ\|% participación de cada talla en lo despachado − % participación de esa talla en la venta de esa tienda (24m)\| / 2 (distancia de variación total, 0=perfecto). | Producto_Talla.talla en los movimientos de despacho + ventas por talla de la sucursal destino; el normalizador de talla EU ya existe en app/views_inteligencia_compra.py:54 | Es la causa raíz del dead-stock por talla: se despacha el surtido completo y quedan varadas las puntas. Ya hay motor de curva en Inteligencia de Compra; falta aplicarlo al despacho. | alto |
| % de lo despachado que termina en liquidación o retorna al CD | unidades despachadas en el período T que a T+180d están (a) en campaña de liquidación activa o (b) devueltas al CD por traspaso inverso / unidades despachadas. | CampanaLiquidacionProducto (app/models/precios.py:465) + Movimientos_Producto TRASPASO_SALIDA con destino CD | Mide el costo real del error de asignación en pesos, cerrando el circuito despacho → liquidación que hoy son dos módulos que no se hablan. | medio |

### Dashboard de Despachos — zona Flujo (CD → sucursal) — `UTIL`

- **Ruta**: /app/dashboard-despachos/ — vista app/views_dashboards_kpi.py:417 dashboard_despachos; API app/views_dashboards_kpi.py:431 api_despachos_flujo; template app/templates/vistas/modulo_dashboards/dashboard_despachos.html
- **Mide**: Volumen y ruteo de la distribución: unidades y valor a costo despachadas desde CD hacia sucursales, árbol/Sankey CD→tienda, matriz origen×destino, pivote de marcas por sucursal, serie temporal y detalle por destino.
- **Filtros**: fecha_inicio/fecha_fin (server-side, _parse_date_range app/views_dashboards_kpi.py:41), g=dia|semana|mes (server-side), cd (server-side, línea 460-461), destino (soportado en backend línea 462-463 pero NO existe control en el template — sólo hay el select despCd, línea 148). El checkbox 'Todas las sucursales' del template (línea 132) sólo afecta a la otra API (datos), no a flujo. Falta filtro por marca y por categoría pese a que el dashboard grafica marcas.
- **Fidelidad**: La mejor pieza del grupo. Aplica correctamente las tres reglas duras del proyecto y lo documenta: detecta por concepto (no por tipo_movimiento, que el save() pisa), cuenta SOLO la pierna TRASPASO_SALIDA para no doble-contar la entrada, y exige sucursal_destino no nulo para excluir los traspasos legacy de una sola pierna. Usa el predicado canónico _es_cd_q (línea 421) que tolera la divergencia entre es_centro_distribucion y tipo_sucursal. Riesgo real: el scoping de empresa sale de request.session['idEmpresaActual'] sin validar contra EmpresaUser y si la sesión no lo trae, emp_id=None y el dashboard muestra TODAS las empresas. Al excluir los legacy sin destino, cualquier consulta a años previos a la migración devuelve casi cero sin avisarlo en pantalla.
- **Rendimiento**: Sano: ~6 agregaciones sobre el mismo queryset base, sin N+1 y sin materializar filas por movimiento. El único costo es que qs se re-evalúa 6 veces (KPIs, pares, marcas, serie, detalle) sobre un rango que por defecto es el mes en curso.

**Problemas:**

- [alta] **El scoping de empresa depende de una clave de sesión no validada; si falta, se ven todas las empresas** — _get_sucursal_empresa devuelve request.session.get('idEmpresaActual') tal cual. api_despachos_flujo sólo filtra si emp_id es truthy (línea 458-459). Ni valida que esa empresa pertenezca al usuario (EmpresaUser) ni cae a un default seguro.
  - *Fix*: Derivar las empresas desde EmpresaUser.objects.filter(user=request.user, status=True) como hace el resto del módulo de reportes (app/views_modulo_reportes.py:8871), e intersectar con la sesión en vez de confiar en ella.
  - *Ref*: `app/views_dashboards_kpi.py:35-38 y 458-459`
- [baja] **El filtro 'destino' existe en el backend pero no hay control en la UI** — El parámetro se lee y aplica (462-463) pero el template sólo expone el select de CD. El usuario no puede aislar una tienda desde el dashboard.
  - *Fix*: Agregar un <select id='despDestino'> poblado con detalle_sucursal (ya viene en la respuesta) y pasarlo como &destino=.
  - *Ref*: `app/views_dashboards_kpi.py:462-463 vs app/templates/vistas/modulo_dashboards/dashboard_despachos.html:148`
- [media] **Los traspasos legacy se descartan en silencio** — sucursal_destino__isnull=False (455) más el concepto exacto TRASPASO_SALIDA excluyen toda la data migrada (TRASPASO_SUCURSAL/BODEGA/VITRINA, destino NULL). Es la decisión correcta, pero al graficar un rango histórico el usuario ve un desplome que interpretará como caída operativa.
  - *Fix*: Devolver en la respuesta un contador de movimientos de traspaso legacy excluidos en el rango y mostrarlo como nota bajo el gráfico.
  - *Ref*: `app/views_dashboards_kpi.py:452-457 y app/constants_kardex.py:34-39`
- [media] **Cuarta implementación distinta de parseo de fechas en el módulo (F-15 confirmado)** — _parse_date_range (41-60) convive con el parseo inline de obtener_reporte_despachos_tiendas (8853-8862) y con los de api_reporte_recepciones_detallado (6003-6008) y api_reporte_despachos_detallado (6200-6205). Cada uno tiene un comportamiento distinto ante fecha inválida (unos caen al mes actual, otros a la semana, otros levantan ValueError y devuelven 500).
  - *Fix*: Un único helper _parse_rango_fechas_reporte (ya existe en app/views_modulo_reportes.py:360) reutilizado por todos, con default y tope de rango explícitos.
  - *Ref*: `app/views_dashboards_kpi.py:41-60`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Cobertura de la red: % de tiendas atendidas y días desde el último despacho por tienda | tiendas con al menos un despacho en el período / total de tiendas vendedoras de la empresa; y por tienda, hoy − Max(fecha de TRASPASO_SALIDA con ese destino). | Movimientos_Producto + Sucursal.es_centro_distribucion (el Max ya se calcula en el detalle, línea 553) | Hoy el dashboard muestra 'destinos_atendidos' pero no contra cuántos existen: no se ve la tienda olvidada, que es el hallazgo operativo más caro. | bajo |
| Concentración de la distribución (Gini/Pareto de unidades por tienda) | % de unidades acumuladas en el top 20% de tiendas destino. | el mismo agregado 'pares' de la matriz (línea 487-488) | Detecta si el CD está alimentando siempre a las mismas 3 tiendas por inercia y no por demanda; se calcula sin una sola query nueva. | bajo |
| Balance neto por tienda (despachado − devuelto al CD) | Σ\|cantidad\| de TRASPASO_SALIDA CD→tienda − Σ\|cantidad\| de TRASPASO_SALIDA tienda→CD, por tienda y período. | Movimientos_Producto invirtiendo el predicado _es_cd_q entre origen y destino | Un flujo de retorno alto es la señal temprana de sobre-despacho; hoy el dashboard sólo mira una dirección y no ve el rebote. | bajo |
| Valor despachado a costo por tienda vs venta generada en el período siguiente | Σ(\|cantidad\| × costo) despachado en el mes M / venta a costo de esa tienda en M+1. | Movimientos_Producto.costo (ya se agrega en línea 475) + ventas de la misma sucursal | Traduce el volumen del Sankey a eficiencia de capital por ruta: cuánto capital hay que empujar a cada tienda para generar un peso de venta. | medio |

### Dashboard de Despachos — zona Recepción y Regularizaciones — `UTIL`

- **Ruta**: /app/api/dashboard-despachos/datos/ — app/views_dashboards_kpi.py:588 api_dashboard_despachos (misma página /app/dashboard-despachos/)
- **Mide**: Calidad del proceso de recepción de traspasos: pipeline de estados del DTE (emitido, recepcionado completo/parcial, en regularización, rechazado, anulado), tasa de éxito de recepción, faltantes/dañados/sobrantes, tiempo promedio emisión→recepción, tendencia mensual de problemas, regularizaciones pendientes y DTEs incompletos más antiguos.
- **Filtros**: fecha_inicio/fecha_fin server-side; ?todas=1 server-side pero condicionado a is_superuser o PermisoUsuario.usuario_ve_todas_sucursales (596-600) — es el ÚNICO endpoint de esta familia que valida un permiso real. Falta filtro por CD origen, por tienda destino y por proveedor/marca. La lista de 'DTEs incompletos' ignora deliberadamente el rango de fechas (742-746), lo cual es correcto para un backlog pero no está señalizado en la UI.
- **Fidelidad**: Buena para su propósito. Excluye dte.descartado=True, acota a tipo_transaccion='TRASPASO' y trata bien las recepciones históricas sin fecha_recepcion con el fallback a dte__fecha_emision (639-642). Dos problemas de exactitud: (a) todos los conteos son de FILAS de Productos_Recepcionados, no de unidades, así que 'tasa_exito 92%' significa 92% de líneas, no de pares — una línea de 1 par pesa igual que una de 60; (b) avg_dias hace round(avg.days, 1) sobre un timedelta, truncando la parte de horas, y silenciosamente devuelve 0 cuando no hay recepciones, indistinguible de 'se recibe el mismo día'.
- **Rendimiento**: Aceptable pero con muchas pasadas: ~15 .count() independientes sobre rec_periodo (644-656) que podrían ser un solo aggregate con filter=Q(...). Los .distinct() sobre joins a dte_movimientos (614, 636, 753) fuerzan DISTINCT sobre un join grande. No hay N+1.

**Problemas:**

- [alta] **Todo se mide en líneas, no en unidades: la tasa de éxito no es fiel al volumen** — total_recepciones, total_ok, faltantes, dañados y sobrantes son Count('id') de Productos_Recepcionados. Un faltante de 100 pares pesa lo mismo que uno de 1 par. El KPI 'tasa_exito' que se muestra al gerente no refleja el impacto real.
  - *Fix*: Duplicar cada métrica con Sum('stockArribado') y Sum('cantidad_faltante')/Sum('cantidad_danada'), y mostrar la tasa por unidades como principal dejando las líneas como secundario.
  - *Ref*: `app/views_dashboards_kpi.py:644-656`
- [media] **15 conteos separados sobre el mismo queryset** — Cada .count() y .filter().count() dispara su propia query sobre rec_periodo, que ya arrastra joins y distinct.
  - *Fix*: Un solo rec_periodo.aggregate(total=Count('id'), ok=Count('id', filter=Q(estado='RECEPCIONADO_OK')), ...), patrón que el propio archivo ya usa en por_sucursal (688-692).
  - *Ref*: `app/views_dashboards_kpi.py:644-656`
- [baja] **avg_dias trunca a días enteros y confunde 'sin datos' con 'cero días'** — round(avg_dias_raw.days, 1) descarta las horas; y el else deja avg_dias=0, que se pinta igual que un proceso instantáneo.
  - *Fix*: Usar avg_dias_raw.total_seconds()/86400 y devolver None (mostrando '—') cuando no hay muestra.
  - *Ref*: `app/views_dashboards_kpi.py:659-670`
- [alta] **El scoping también depende de idEmpresaActual sin validar contra EmpresaUser** — Mismo _get_sucursal_empresa. Si la sesión no trae empresa, se consultan traspasos de todas las empresas del sistema.
  - *Fix*: Igual que en flujo: derivar de EmpresaUser e intersectar.
  - *Ref*: `app/views_dashboards_kpi.py:35-38 y 608-611`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Tasa de recepción perfecta por unidades y por CD origen | Σ stockArribado de líneas RECEPCIONADO_OK / Σ cantidad_esperada, agrupado por dte__sucursal (CD emisor). | Productos_Recepcionados.stockArribado, cantidad_esperada, cantidad_faltante, cantidad_danada + Dte.sucursal | Permite atribuir el problema al CD que arma mal el pedido en vez de a la tienda que reclama; hoy sólo se ve el agregado por sucursal receptora. | bajo |
| Antigüedad del backlog de regularizaciones (aging buckets) | unidades y $ a costo en estado EN_REGULARIZACION / FALTANTE / SOBRANTE, agrupadas en 0-7, 8-30, 31-90 y >90 días desde dte.fecha_emision. | Productos_Recepcionados.estado + Dte.fecha_emision + Producto.costo | Hoy sólo hay una lista top-20 sin aging: no se sabe si el backlog envejece o se drena, ni cuánto capital está atrapado en disputa. | bajo |
| Merma de traspaso valorizada | Σ (cantidad_faltante + cantidad_danada) × Producto.costo por mes y por ruta CD→tienda. | Productos_Recepcionados + Producto.costo | Convierte el conteo de incidencias en pesos: es el número que justifica invertir en control logístico y el que hoy nadie puede citar. | bajo |
| Ciclo P50/P90 emisión→recepción por ruta | percentiles 50 y 90 de (fecha_recepcion − fecha_emision) por par (CD origen, tienda destino). | Dte.fecha_emision y Dte.fecha_recepcion | El promedio actual esconde la cola: el P90 es el que define cuántos días de stock de seguridad necesita cada tienda, y alimenta directamente el lead time interno del motor de compras. | medio |

### Despachos Detallado (endpoint huérfano y mal nombrado) — `REDUNDANTE`

- **Ruta**: /app/api/reporte-despachos-detallado/ — app/views_modulo_reportes.py:6190 api_reporte_despachos_detallado
- **Mide**: Pese al nombre, NO mide despachos: itera DTEs de tipo_transaccion='COMPRA' y compara lo facturado por el proveedor (Dte_Productos.stock) contra lo recepcionado (Productos_Recepcionados.stockArribado), con desglose por sucursal destino y split reposición/nuevo. Es un reporte de cumplimiento de proveedor.
- **Filtros**: fecha_inicio/fecha_fin y proveedor_id, aplicados server-side (6207-6213). Sin filtro de sucursal, sin filtro de estado de DTE, sin paginación ni tope de resultados. Ningún template ni JS del proyecto lo consume: grep sobre app/templates y app/static devuelve cero referencias; sólo aparece en app/urls.py:1262.
- **Fidelidad**: Baja. No filtra por empresa del usuario en absoluto (fuga entre empresas por URL directa), no excluye dte.descartado ni estados ELIMINADA/ANULADO, y por tanto el 'porcentaje_recepcion' incluye documentos muertos. Se solapa casi por completo con dos reportes vivos: el % de recepción por documento ya está en Gestión de Compras y el cumplimiento por proveedor en el reporte de Rendimiento por Proveedor (app/views_modulo_reportes.py:6302). El bug de nombres de campo que devolvía 500 (productoTalla/stock vs producto_talla/cantidad) ya está corregido y documentado en el propio código (6217-6220).
- **Rendimiento**: N+1 confirmado (F-13): 2 queries por cada DTE de compra del rango dentro del bucle 6216-6226 (Dte_Productos y Productos_Recepcionados), más la query base. Con 300 facturas de compra en 30 días son ~601 queries; con un año, varios miles. No hay LIMIT en dtes_compra, así que el costo escala linealmente con el rango pedido.

**Problemas:**

- [media] **Endpoint huérfano: ninguna vista lo consume** — Sólo está registrado en urls.py. Es superficie de ataque y deuda de mantenimiento (ya generó un incidente de 500 detectado por la suite de regresión) sin ningún consumidor.
  - *Fix*: Borrar la ruta y la función, o cablearla a una pestaña real dentro de Rendimiento por Proveedor. Lo mismo aplica a api_reporte_recepciones_detallado (6-5989), su gemelo también huérfano.
  - *Ref*: `app/urls.py:1262 (única referencia en todo el repo)`
- [alta] **Sin scoping por empresa ni exclusión de DTEs anulados/descartados** — Dte.objects.filter(tipo_transaccion='COMPRA', fecha_emision__range=...) sin EmpresaUser, sin descartado=False y sin excluir estado_dte anulado. Cualquier usuario autenticado obtiene montos de compra de todas las empresas.
  - *Fix*: Si se conserva: filtrar receptor_id/sucursal por EmpresaUser, agregar descartado=False y excluir estados anulados, aplicando la misma regla ELIMINADA que ya usa el reporte de compras.
  - *Ref*: `app/views_modulo_reportes.py:6207-6213`
- [alta] **N+1 de 2 queries por documento sin tope de filas** — El bucle abre Dte_Productos y Productos_Recepcionados por cada DTE. Además suma en Python (sum(p.stock ...)) lo que la DB puede agregar.
  - *Fix*: Reemplazar por dos agregaciones con .values('dte_id').annotate(Sum(...)) y un merge por dict, y paginar la salida.
  - *Ref*: `app/views_modulo_reportes.py:6216-6229`
- [media] **El nombre miente sobre lo que mide** — 'despachos' en este ERP significa traspaso CD→tienda; este endpoint mide recepción de compras a proveedor. Cualquiera que lo cablee a una UI generará un reporte contradictorio con Despachos a Tiendas y con el Dashboard de Despachos.
  - *Fix*: Si se conserva, renombrar a api_cumplimiento_recepcion_compras.
  - *Ref*: `app/views_modulo_reportes.py:6190-6193`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Fill rate del proveedor por unidades y OTIF | Σ stockArribado / Σ Dte_Productos.stock por proveedor y período; OTIF = % de documentos con fill rate = 100% y recibidos dentro del lead_time_dias comprometido. | Dte_Productos.stock, Productos_Recepcionados.stockArribado, Empresa.lead_time_dias, Dte.fecha_emision/fecha_recepcion | Es la única métrica que este endpoint aporta y que no está en otro lado; pertenece a Rendimiento por Proveedor, no a un endpoint aparte. Consolidar ahí evita mantener dos verdades. | medio |

### Recepciones Detallado (endpoint huérfano) — `REDUNDANTE`

- **Ruta**: /app/api/reporte-recepciones-detallado/ — app/views_modulo_reportes.py:5989 api_reporte_recepciones_detallado
- **Mide**: Recepciones reales desde Productos_Recepcionados: totales de ítems/unidades, split reposición vs producto nuevo, recepciones históricas vinculadas retroactivamente, desglose por sucursal destino y por proveedor (rama DTE + rama compra), y hasta 50 cambios de precio detectados en la recepción.
- **Filtros**: fecha_inicio/fecha_fin, proveedor_id y sucursal_id, los tres server-side (6023-6030). El manejo del rango de fechas es el más cuidado del módulo: usa fecha_recepcion cuando existe y cae a fecha para registros legados (6013-6015). No lo consume ninguna vista.
- **Fidelidad**: La lógica es correcta y bien pensada (doble rama de proveedor para cubrir compras históricas sin DTE, 6070-6139). Falla en scoping: no filtra por EmpresaUser, así que sucursal_id acepta cualquier sucursal del sistema. Tampoco excluye DTEs descartados. El único dato de negocio que aporta y que no está en otra parte es 'cambios de precio detectados en la recepción' (precio_anterior vs precio_nuevo), que hoy nadie ve.
- **Rendimiento**: Correcto: agregaciones puras, sin N+1. Sólo el bloque de cambios_precio (6143-6159) itera 50 objetos con select_related ya cargado. El costo real es que se ejecutan ~6 counts separados sobre el mismo queryset (6033-6044).

**Problemas:**

- [media] **Endpoint huérfano: cero consumidores en templates o JS** — Sólo referenciado en app/urls.py:1261. El trabajo de la doble rama de proveedor y del fallback de fechas está bien hecho y desperdiciado.
  - *Fix*: Cablearlo como pestaña 'Recepciones' del reporte de Compras o de Rendimiento por Proveedor en vez de borrarlo — es el único lugar donde vive el split reposición/nuevo y la alerta de cambio de precio en recepción.
  - *Ref*: `app/urls.py:1261`
- [alta] **sucursal_id no valida pertenencia a las empresas del usuario** — recepciones_qs.filter(sucursal_destino_id=sucursal_id) sin cruzar con EmpresaUser.
  - *Fix*: Acotar el queryset base a sucursal_destino__empresa_id__in=empresas del usuario antes de aplicar el filtro.
  - *Ref*: `app/views_modulo_reportes.py:6029-6030`
- [baja] **6 counts separados donde cabe un aggregate** — total_items, total_unidades, reposicion, nuevo, historicas y con_cambio_precio son 6 pasadas a la DB sobre el mismo queryset.
  - *Fix*: Un solo .aggregate con Count(filter=Q(...)).
  - *Ref*: `app/views_modulo_reportes.py:6033-6044`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Alza de costo en recepción (drift de precio de compra) | Σ (precio_nuevo − precio_anterior) × stockArribado por proveedor y mes; y % de líneas con alza. | Productos_Recepcionados.precio_anterior, precio_nuevo, stockArribado | El dato ya se captura y hoy sólo se muestran 50 filas sueltas sin valorizar. Es el argumento cuantitativo para renegociar con el proveedor y explica silenciosamente la caída de margen. | bajo |
| Mix reposición vs producto nuevo por temporada | Σ stockArribado con es_reposicion=True / Σ total, por mes y por marca. | Productos_Recepcionados.es_reposicion + Producto.atributo1 | Con cobertura de 641 días, saber qué proporción de la compra es novedad y cuánto es reponer lo que ya no rota es la decisión de surtido más importante del año. | bajo |

### Plan de Liquidación (v2) — `UTIL`

- **Ruta**: /app/reportes/plan-liquidacion/ — vista app/views_inteligencia_compra.py:479 ver_plan_liquidacion; APIs :651 obtener_plan_liquidacion, :946 obtener_plan_liquidacion_detalle, :1089 obtener_plan_liquidacion_por_anio, :1168 exportar_plan_liquidacion_excel, :1308 imprimir_plan_liquidacion, :1422 importar_seleccion_liquidacion; template app/templates/vistas/modulo_reportes/plan_liquidacion.html
- **Mide**: Ranking de capital inmovilizado por 4 dimensiones (marca, categoría v1.2 Padre›Hijo, especialidad multi-etiqueta y sucursal): stock y $ a costo en tiendas, stock CD aparte, ventas TTM 365d, rotación, cobertura, GMROI, dead-stock 180d y una acción sugerida (Liquidar / Reponer / Depurar / Traspasar / Monitorear). Drill-down a producto con antigüedad FIFO, días sin venta, descuento sugerido por escala de antigüedad y precio de liquidación con piso en costo×1.1.
- **Filtros**: sucursal_id, marca_id, categoria_id (expande la rama del padre vía _expandir_categoria_ids), especialidad_id (subconsulta IN sin duplicar filas) e incluir_cd: los cinco aplicados server-side en _scope_plan (518-577). El detalle agrega q (artículo/descripción/marca), antiguedad por bucket, anio(s), orden por 7 columnas y paginación REAL en DB con LIMIT/OFFSET (976-983). Es el reporte mejor filtrado de toda la familia. Falta: no hay filtro por rango de fechas (las ventanas 365d/180d están hardcodeadas) ni filtro server-side por 'acción'.
- **Fidelidad**: Estructuralmente sólido: excluye excluir_de_analitica, restringe ventas SIEMPRE a tiendas vendedoras (mov_scope, 548-552), aísla el stock de CD de los denominadores de rotación/GMROI, usa recategorización v1.2 con Padre›Hijo y advierte que especialidad es atribución (el total puede exceder 100%). Dos defectos de fidelidad de fondo: (1) el dinero es FALSO — 'venta', 'margen' y GMROI se calculan con Movimientos_Producto.precio y .costo, que al registrar la venta se escriben con producto.precioventa y producto.costo de LISTA, no con el precio efectivamente cobrado ni el costo FIFO consumido (app/views.py:22266-22267 lo escribe explícitamente así, y el mismo bloque calcula costo_total_consumido FIFO y lo descarta). En una cadena que corre campañas de descuento, el margen y el GMROI quedan sistemáticamente sobrestimados, y el módulo nunca ve el efecto de sus propios markdowns. (2) las ventas son BRUTAS: CONCEPTOS_VENTA no resta devoluciones ni NC (esos conceptos viven en CONCEPTOS_REINGRESO, app/constants_kardex.py:42-50), así que rotación y cobertura están infladas por la tasa de devolución.
- **Rendimiento**: Bien diseñado: ~15 queries agregadas fijas para el ranking completo (3 por dimensión × 4 dimensiones + totales), sin N+1. El drill-down pagina en DB con Subquery para antigüedad FIFO y última venta — es el fix que llevó ese reporte de 136s a 5-6s. Riesgo restante: vend180 se usa como .exclude(id__in=subquery) cuatro veces sobre un conjunto de Producto_Talla que puede ser de decenas de miles; en Postgres un NOT IN de esa cardinalidad no siempre elige un anti-join eficiente.

**Problemas:**

- [alta] **Margen y GMROI se calculan a precio y costo de LISTA, no reales** — _fila_liquidacion recibe venta=Sum(|cantidad|×precio) y costo=Sum(|cantidad|×costo) del kardex, y esos campos se graban con producto.precioventa y producto.costo al momento del movimiento. Ignoran descuento_unitario, campañas NxM, gift cards y el costo FIFO real. El dato correcto existe: Ticket_Productos.subtotal, descuento_unitario, precio_original y costo_fifo (app/models/ventas.py:337-361). Un producto liquidado al 40% aparece con el margen del precio original, así que 'Liquidar' se sugiere sobre una rentabilidad ficticia.
  - *Fix*: Calcular venta y costo TTM desde Ticket_Productos (join por Movimientos_Producto.ticket_id o directo por ticket__sucursal e idTicket__created_at): venta = Σ subtotal, costo = Σ costo_fifo × stock. Mantener el cálculo actual como fallback etiquetado 'lista' igual que ya se hace con margen_src en Inteligencia de Compra.
  - *Ref*: `app/views_inteligencia_compra.py:686-687 y 617-626; escritura del movimiento en app/views.py:22266-22267`
- [media] **Las ventas son brutas: no se restan devoluciones ni notas de crédito** — mov_base usa CONCEPTOS_VENTA, que excluye DEVOLUCION_CLIENTE, DEVOLUCION_NC y ANULACION_TICKET. Un SKU con alta devolución aparece rotando mejor de lo que rota, y por tanto NO se propone para liquidación.
  - *Fix*: Restar Σ|cantidad| de CONCEPTOS_REINGRESO sobre el mismo scope y ventana en las anotaciones de u/venta/costo, o exponer 'devoluciones' como columna para que el ratio sea visible.
  - *Ref*: `app/views_inteligencia_compra.py:549-552 y app/constants_kardex.py:42-50`
- [media] **Elegir una bodega/CD como sucursal deja el reporte en ceros sin explicarlo** — _scope_plan pone mov_scope = sólo los ids de alcance que son tienda (548). Si sucursal_id es un CD, mov_scope queda vacío: todas las ventas son 0, rotación/cobertura/GMROI son None, valor_costo y dead_costo son 0 (porque ES_TIENDA_PT no matchea nada) y sólo se puebla stock_cd. El usuario ve un plan de liquidación de una bodega con $0 de capital inmovilizado y acción 'Traspasar' en todo.
  - *Fix*: Cuando el alcance sea sólo CD, devolver una bandera 'modo_bodega' y sustituir los KPIs de rotación por 'demanda de la red' (ventas de todas las tiendas de los mismos artículos), que es la métrica correcta para decidir qué sacar de bodega.
  - *Ref*: `app/views_inteligencia_compra.py:544-552 y 629-630`
- [media] **El descuento sugerido es una escala fija por antigüedad, ciega a la elasticidad y al margen** — ESCALA_DESCUENTO_LIQUIDACION es 15/25/40% a 180/365/730 días con piso costo×1.1, igual para toda marca, categoría y tienda. No considera cuánto descuento hizo falta históricamente para mover ese tipo de producto ni el margen disponible.
  - *Fix*: Calibrar el % con el histórico real de campañas: para cada categoría, el descuento mediano de las campañas cerradas que efectivamente vendieron >X% del stock marcado (CampanaLiquidacionProducto.precio_original/precio_liquidacion + Ticket_Productos.promo_campana). Mantener la escala como fallback cuando no haya historia.
  - *Ref*: `app/views_inteligencia_compra.py:493-515`
- [baja] **Cuatro exclude(id__in=vend180) sobre un subquery grande** — vend180 (666-667) se reusa en las tres dimensiones más especialidad más el total: cinco anti-joins sobre el mismo conjunto de ProductoTalla vendidos en 180 días.
  - *Fix*: Materializar una vez a lista de ids si el conjunto es acotado, o convertirlo en un LEFT JOIN anotado (Max de fecha de venta por talla) y filtrar por NULL/antigüedad, reutilizable en las cinco pasadas.
  - *Ref*: `app/views_inteligencia_compra.py:666-667, 690, 761, 786`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| GMROI y margen REALES (dinero cobrado, costo FIFO) | margen_real = Σ Ticket_Productos.subtotal − Σ (costo_fifo × stock) en 365d; GMROI_real = margen_real / valor del inventario a costo. | Ticket_Productos.subtotal, costo_fifo, stock (app/models/ventas.py:337-361) unidos por Movimientos_Producto.ticket_id; Producto.costo para el inventario | Es el fix del defecto central: hoy la decisión 'Liquidar vs Reponer' (umbral GMROI<1) se toma con un GMROI calculado a precio de lista. Cambiar la fuente puede reordenar por completo el ranking de acciones. | medio |
| Efectividad de liquidación: unidades y capital recuperado por campaña | unidades vendidas con promo_campana = C / unidades marcadas en C; capital_recuperado = Σ subtotal de esas ventas; sacrificio = Σ (precio_original − precio_liquidacion) × unidades vendidas. | CampanaLiquidacionProducto.precio_original/precio_liquidacion (app/models/precios.py:465-482) + Ticket_Productos.promo_campana (app/models/ventas.py:367) | Hoy el módulo recomienda liquidar pero NUNCA mide si liquidar funcionó. Sin este número no se puede calibrar la escala de descuento ni justificar la próxima campaña. Los dos modelos ya están enlazados: sólo falta el reporte. | medio |
| Uplift de velocidad post-markdown | (unidades/día en los 30 días posteriores al inicio de la campaña) / (unidades/día en los 90 días previos), por producto y por % de descuento aplicado. | Movimientos_Producto de venta + CampanaLiquidacion.fecha_inicio y el % de la regla | Da la curva de elasticidad empírica de la casa: cuánto descuento se necesita para mover cada categoría. Es el insumo que convierte ESCALA_DESCUENTO_LIQUIDACION de una constante inventada en un parámetro calibrado. | medio |
| Flujo de envejecimiento de capital (aging roll-forward mensual) | $ a costo que cruzó de 180-365 a 365+ durante el mes − $ que salió del stock (vendido o liquidado) en esos buckets. | la misma antigüedad FIFO ya calculada (_aging_year_expr, app/views_inteligencia_compra.py:1008-1014) tomada como snapshot mensual | El reporte hoy es una foto: dice cuánto capital viejo hay, no si mejora o empeora. La línea base de la casa ($490M en stock viejo en 5 marcas) sólo se puede gestionar con la derivada, no con el nivel. | alto |
| Días-peso: antigüedad promedio ponderada por capital | Σ (días_antiguedad × stock × costo) / Σ (stock × costo) por marca/categoría/sucursal. | campos ya presentes en el drill-down (dias_antiguedad, stock_u, costo) | Un único número comparable entre dimensiones que hoy no existe: 'dead_pct_valor' sólo cuenta el binario >180d y no distingue 200 días de 900. Se calcula sin queries nuevas. | bajo |

### Inteligencia de Compra — `MEJORABLE`

- **Ruta**: /app/reportes/inteligencia-compra/ — vista app/views_inteligencia_compra.py:142 ver_inteligencia_compra; API :163 obtener_inteligencia_compra; template app/templates/vistas/modulo_reportes/inteligencia_compra.html
- **Mide**: Análisis histórico + pronóstico de UNA marca para decidir cuánto comprar: venta pública por año y por tienda, distribución en bodegas, stock actual, serie mensual 36m, forecast 12m (seasonal-naive con crecimiento acotado a [−35%, +10%]), sell-through anual, velocidad 90d y YoY, curva de tallas EU (venta vs stock, con flags quebrada/gap/sobre-stock), finanzas (inventario a costo y a precio, margen, rotación, WOS, GMROI), salud (dead 90/180, ABC del motor batch, lead time del proveedor dominante), un bloque de liquidación y una recomendación de compra con curva de tallas y asignación por tienda.
- **Filtros**: Sólo DOS: marca_id (obligatorio) y sucursal_id ('todas' o una tienda). Ambos server-side (166-188). NO hay filtro de categoría, especialidad v1.2, género (atributo3), temporada ni rango de fechas — las ventanas 90d/365d/730d están hardcodeadas. Es una limitación de diseño importante: tras la inversión en recategorización v1.2, este reporte sigue siendo marca-only y no puede responder '¿cuánto compro de running de mujer?'.
- **Fidelidad**: Muy bien construido en lo estructural (excluye excluir_de_analitica, sólo estado COMPLETADO, demanda pública sólo en tiendas vendedoras, conceptos desde constants_kardex, Σ|cantidad| robusto al signo). Tres defectos reales: (1) el sell-through se rompe al filtrar por tienda — el numerador va scopeado a la tienda pero el denominador de abastecimiento se calcula sobre TODAS las sucursales del usuario (abast se define sobre all_ids, línea 199-201, y aa nunca se reescopea), así que elegir una tienda hunde el ST% a una fracción arbitraria; (2) mismo problema de dinero de lista que el Plan de Liquidación: monto, ttm_venta_m, margen 'realizado' y GMROI salen de Movimientos.precio/costo, que son precioventa/costo de lista; (3) ventas brutas sin restar devoluciones/NC. Además el lead time del proveedor se resuelve por Compras_Producto.nombre IN (artículos) — match por STRING de código de artículo (404-412), la misma clase de fragilidad que F-12.
- **Rendimiento**: Razonable: ~25 queries agregadas fijas por marca, todas con joins a Producto por atributo1_id (indexable). Puntos flojos: la lista de artículos para lead time se materializa con [:500] y luego un IN por strings (404-409); dead90/dead180 usan exclude(id__in=subquery) sobre ProductoTalla; y la sección ABC hace un aggregate + un values sobre ClasificacionABC filtrando por atributo1 sin más restricción. No es un reporte lento, pero tampoco está cacheado y se recalcula íntegro en cada cambio de select.

**Problemas:**

- [alta] **El sell-through se calcula con numerador de una tienda y denominador de toda la red** — va (ventas por año) sale de ventas_tienda, que respeta tienda_scope; aa (abastecimiento por año) sale de abast, definido sobre movs con producto__sucursal_id__in=all_ids y nunca reescopeado. Al elegir una tienda específica el ST% anual queda dividido por el ingreso de TODA la empresa: se lee como 3-8% y el usuario concluye que la marca no vende.
  - *Fix*: Escopear abast al mismo tienda_scope cuando hay tienda específica, o —mejor para el caso CD→tienda— usar como denominador el abastecimiento de la tienda incluyendo CONCEPTOS_TRASPASO_ENTRADA, que es como realmente se surte una tienda. Etiquetar en la respuesta qué denominador se usó.
  - *Ref*: `app/views_inteligencia_compra.py:199-216 y 261-266`
- [alta] **Margen, GMROI y montos a precio de lista** — ttm_venta_m=_um(tv,'precio') y ttm_costo_m=_um(tv,'costo') sobre Movimientos, cuyos campos precio/costo son producto.precioventa y producto.costo. Como además la condición 0<ttm_costo_m<ttm_venta_m casi siempre se cumple, el código se autodeclara margen_src='realizado' cuando en realidad es margen de lista disfrazado.
  - *Fix*: Igual que en Plan de Liquidación: calcular desde Ticket_Productos (subtotal y costo_fifo). Mientras tanto, cambiar la etiqueta de 'realizado' a 'lista' para no inducir a error.
  - *Ref*: `app/views_inteligencia_compra.py:353-367`
- [alta] **El abastecimiento de una tienda no incluye traspasos: sell-through por tienda es estructuralmente imposible hoy** — CONCEPTOS_ABASTECIMIENTO sólo tiene INGRESO_INICIAL, INGRESO_MANUAL, RECEPCION_COMPRA y REPOSICION_STOCK. Como el modelo operativo es comprar al CD y traspasar a tienda, una tienda vendedora casi no tiene movimientos de abastecimiento: su denominador natural es TRASPASO_ENTRADA.
  - *Fix*: Introducir un helper 'ingreso a la sucursal' = CONCEPTOS_ABASTECIMIENTO + CONCEPTOS_TRASPASO_ENTRADA cuando el scope es una tienda, y mantener sólo abastecimiento cuando el scope es la empresa completa (para no doble-contar la compra y su traspaso).
  - *Ref*: `app/constants_kardex.py:24-32 y app/views_inteligencia_compra.py:201`
- [media] **Sólo se puede analizar por marca: la recategorización v1.2 no está disponible aquí** — El template ofrece únicamente marca y sucursal. No hay categoría (Padre›Hijo), especialidad ni género (atributo3, 100% poblado), pese a que todo el resto del módulo ya migró a v1.2 y a que la decisión real de compra es por marca × categoría × género.
  - *Fix*: Añadir categoria_id (reutilizando _expandir_categoria_ids), especialidad_id (subconsulta como en _scope_plan) y genero (atributo3), y permitir marca opcional para poder analizar 'toda la categoría'.
  - *Ref*: `app/templates/vistas/modulo_reportes/inteligencia_compra.html:96-106 y app/views_inteligencia_compra.py:166-169`
- [media] **Lead time del proveedor por match de string de artículo** — Compras_Producto.objects.filter(nombre__in=articulos) donde articulos son códigos de artículo truncados a 500. Si el código cambió de mayúsculas/formato (el proyecto ya tuvo que normalizarlos), el proveedor no matchea y cae al default de 21 días sin avisar.
  - *Fix*: Usar la FK real vía Compras_Producto_Talla.producto_talla__producto o, si no está poblada, normalizar con la misma función normalizar_articulo del proyecto y exponer 'fuente: default' en la UI cuando no hubo match (hoy el campo existe pero el template no lo muestra).
  - *Ref*: `app/views_inteligencia_compra.py:401-414`
- [media] **Vista sin decorador de permiso** — @login_required solamente; el menú la oculta con permiso_reporte_existencias_marca (permiso de otro reporte, además). URL directa expone costos, márgenes y recomendaciones de compra.
  - *Fix*: @requiere_permiso('inteligencia_compra','puede_ver') y un permiso propio en el menú.
  - *Ref*: `app/views_inteligencia_compra.py:141-142 y app/templates/layout/menu.html:2600-2606`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Error del pronóstico (MAPE y sesgo del seasonal-naive) | backtest: reconstruir _forecast cortando la serie 12 meses atrás y comparar contra lo realmente vendido. MAPE = mean(\|pred−real\|/real); sesgo = mean((pred−real)/real). | la misma serie mensual ya construida (app/views_inteligencia_compra.py:245-258); no requiere datos nuevos | Hoy el reporte entrega un número de compra ('comprar: N pares') sin ninguna medida de cuánto se equivocó la última vez. Sin MAPE nadie puede decidir si confiar en él, y el veredicto de cobertura ya bloquea compras con esa proyección. | medio |
| Demanda neta (ventas − devoluciones) y tasa de devolución por marca | neta = Σ\|cantidad\| de CONCEPTOS_VENTA − Σ\|cantidad\| de CONCEPTOS_REINGRESO; tasa = reingresos / ventas brutas. | Movimientos_Producto + app/constants_kardex.py:15 y :42 | Toda la cadena (velocidad, cobertura, forecast, recomendación de compra) está calculada sobre venta bruta. Una marca con 12% de devolución está sobrecomprada en 12% de forma invisible, y la tasa por sí sola es un indicador de calidad del producto. | bajo |
| Punto de reorden real (cobertura en semanas vs lead time del proveedor) | ROP = velocidad_diaria × (lead_time_dias + días de tránsito interno CD→tienda) + stock de seguridad; alerta cuando stock_tiendas < ROP. | v90/90 ya calculado (línea 271), Empresa.lead_time_dias, y el P90 de tránsito del Dashboard de Despachos | Hoy el reporte muestra lead_dias en la recomendación pero no lo usa para nada: la decisión se toma sólo con 'cobertura en meses'. Cruzarlos convierte un veredicto cualitativo en una fecha de pedido. | medio |
| Sell-through a semana N de la temporada (curva vs benchmark de la marca) | unidades vendidas acumuladas / unidades ingresadas, por semana desde el primer ingreso de la temporada, comparado con la mediana histórica de la misma marca. | StockInicialTemporada.fecha_primer_ingreso y stock_total_ingresado (app/models/predicciones.py:346-356) + ventas del kardex | El ST% anual actual es un número de cierre inútil para decidir a mitad de temporada. La curva permite detectar en la semana 6 que la marca va lenta y aún hay tiempo de reaccionar (rebaja o freno de recompra). | alto |
| Costo del quiebre por talla | para cada talla con flag 'quebrada': demanda estimada perdida = venta_pct de la talla × demanda total de la marca en el período de quiebre; valorizada al margen real unitario. | la curva de tallas ya calculada (línea 292-301) + margen real desde Ticket_Productos | El reporte ya identifica tallas quebradas pero no las valoriza, así que compiten en igualdad con el sobre-stock. Ponerles precio ordena la prioridad de la recompra de relleno. | medio |

### Predicción de Compras (dashboard + APIs) — `MEJORABLE`

- **Ruta**: /app/prediccion/ — vista app/views_prediccion_compras.py:78 dashboard_prediccion; APIs :93 resumen, :239 clasificacion, :276 sugerencias, :320 alertas_velocidad, :372 alertas_quiebre, :407 producto_detalle, :600 recalcular, :619 graficos, :914 analisis_categoria, :1155 analisis_marca, :1343 analisis_proveedor; template app/templates/vistas/modulo_dashboards/prediccion_compras_dashboard.html
- **Mide**: Salida del motor batch de predicción (app/services/prediccion_compras.py): clasificación ABC-XYZ con corrección por demanda censurada, velocidades históricas por marca/categoría/género, curvas de tallas, predicciones de demanda por artículo/temporada, sugerencias de compra por talle (con stock actual y en tránsito), alertas de velocidad y de quiebre de talla, más rankings por categoría, marca y proveedor.
- **Filtros**: El scope de sucursal NO viene de la UI sino de request.session['idSucursalActual'] (_get_sucursal_context, 45-60): si la sucursal activa es CD o no hay, se agrega sobre todas las VENDEDORA/MIXTA. Los rankings aceptan ?categoria, ?temporada, ?anio server-side; sugerencias acepta ?origen. Problema serio: temporada y año por defecto NO son el período actual sino el que tiene MÁS filas en PrediccionDemanda (_prediccion_default_temporada_anio, 783-788), o sea, el último batch grande — puede ser un año viejo y nada en la UI lo dice.
- **Fidelidad**: Es el módulo con más riesgo de esta familia. (a) Sin scoping por empresa en ningún endpoint: _q_sucursal_productos filtra por sucursal__tipo_sucursal__in=['VENDEDORA','MIXTA'] a nivel global, e importa EmpresaUser sin usarlo nunca — un usuario de una empresa ve alertas, sugerencias y costos de las otras. (b) Sin indicador de frescura: ni fecha_calculo de ClasificacionABC ni fecha_generacion de PrediccionDemanda se muestran en el dashboard (grep en el template sólo encuentra el botón 'Recalcular'), así que datos de un batch de hace meses se presentan como vigentes. (c) 'Precisión del modelo' NO mide precisión: el docstring dice 'MAPE por método' y devuelve el promedio de PrediccionDemanda.confianza, que es una autoevaluación del método, no error contra la realidad. (d) 'variacion_vs_anterior_pct' del ranking de marcas compara unidades_a_pedir contra ventas del año anterior, no ventas actuales contra anteriores — la columna al lado de 'ventas_temporada_actual/anterior' miente. (e) _bulk_ventas no filtra por fecha: agrupa por producto.temporada/anio_temporada, así que 'ventas temporada actual vs anterior' compara la venta ACUMULADA DE POR VIDA de productos etiquetados 2025 (dos años de venta) contra los etiquetados 2026 (unos meses). (f) Tampoco excluye excluir_de_analitica en las ventas de esos rankings.
- **Rendimiento**: Dos N+1 confirmados. En api_prediccion_resumen (endpoint de KPIs, se carga al abrir el dashboard) se recorre TODA la lista de sugerencias no aprobadas sin límite y por cada una se ejecuta Compras_Producto.objects.filter(nombre=producto.articulo).select_related('compras__empresa').order_by('-fecha').first() — 1 query por sugerencia. Con 2.000 sugerencias son 2.000 queries y el KPI principal se cae. En api_prediccion_analisis_marca hay 3 queries por marca dentro del bucle (vh_base.filter, ClasificacionABC.aggregate, SugerenciaCompra.aggregate): ~300 queries con 100 marcas. Y api_prediccion_recalcular ejecuta el pipeline completo de forma SÍNCRONA dentro de la request HTTP.

**Problemas:**

- [alta] **api_prediccion_recalcular ejecuta el batch completo dentro de la request, sin permiso y por POST desde un botón** — @login_required + @require_POST y adentro ejecutar_pipeline_completo(). Es un job que escribe ClasificacionABC, VelocidadHistorica, CurvaTalles, PrediccionDemanda, SugerenciaCompra y alertas. Cualquier usuario autenticado lo dispara desde el botón 'Recalcular' del dashboard; el request se cuelga hasta el timeout del proxy y el pipeline puede quedar a medias.
  - *Fix*: Restringir con @requiere_permiso('prediccion_compras','puede_recalcular'), mover la ejecución a Celery (ya está en retailmind/requirements.txt) o a un management command agendado, y que el botón sólo encole y muestre el estado del último run.
  - *Ref*: `app/views_prediccion_compras.py:598-610 y app/templates/vistas/modulo_dashboards/prediccion_compras_dashboard.html:1626-1638`
- [alta] **N+1 de una query por sugerencia en el endpoint de KPIs del dashboard** — El bucle for sugerencia in sugerencias (124-153) hace una consulta a Compras_Producto por cada sugerencia para resolver el proveedor, además de matchear por nombre de artículo (string) en vez de por FK. El queryset de sugerencias no tiene límite.
  - *Fix*: Precalcular un mapa articulo→proveedor con una sola query agregada (values('nombre').annotate(Max('fecha'))) antes del bucle, o mejor, denormalizar proveedor_id en SugerenciaCompra al generarla en el pipeline.
  - *Ref*: `app/views_prediccion_compras.py:124-153 (query en 138-140)`
- [alta] **Ningún endpoint filtra por empresa del usuario** — _q_sucursal_productos sólo mira tipo_sucursal. EmpresaUser se importa (línea 18) y no se usa en todo el archivo. Alertas, sugerencias, costos estimados y rankings de proveedor cruzan empresas.
  - *Fix*: Construir el scope como Sucursal.objects.filter(empresa_id__in=EmpresaUser...) e intersectarlo con el tipo VENDEDORA/MIXTA, igual que hace _sucursales_usuario en app/views_inteligencia_compra.py:47-51.
  - *Ref*: `app/views_prediccion_compras.py:63-70 (helper) y :18 (import sin uso)`
- [alta] **El dashboard no muestra la antigüedad del batch y por defecto elige la temporada con más filas, no la actual** — _prediccion_default_temporada_anio ordena por Count('id') descendente. Si el batch grande fue el de 2025, el dashboard de 2026 muestra 2025 en silencio. Ni fecha_calculo ni fecha_generacion aparecen en el template.
  - *Fix*: Default = temporada/año vigentes por calendario, con fallback al último batch y un banner 'Datos calculados el DD/MM/YYYY (N días de antigüedad)' usando Max(fecha_calculo) de ClasificacionABC y Max(fecha_generacion) de PrediccionDemanda.
  - *Ref*: `app/views_prediccion_compras.py:783-788`
- [alta] **'Precisión del modelo' no mide precisión** — El gráfico se alimenta de Avg('confianza') de PrediccionDemanda y se rotula como MAPE en el docstring. confianza es un parámetro que el propio método se autoasigna; no hay ninguna comparación contra la demanda real.
  - *Fix*: Calcular error real: para cada (articulo, temporada, anio) cerrado, comparar unidades_predichas contra Σ|cantidad| de ventas de esa temporada en el kardex, y publicar MAPE y sesgo por metodo_usado. Los datos existen; sólo falta el join.
  - *Ref*: `app/views_prediccion_compras.py:620-625 (docstring) y 653-669 (implementación)`
- [alta] **variacion_vs_anterior_pct compara 'a pedir' contra ventas del año anterior** — variacion_pct = (u_pedir − ventas_ant)/ventas_ant, expuesto junto a ventas_temporada_actual y ventas_temporada_anterior. El usuario lo lee como crecimiento de venta y en realidad es cuánto se desvía la sugerencia de compra respecto a la venta pasada.
  - *Fix*: variacion_pct = (ventas_act − ventas_ant)/ventas_ant y, si el otro ratio interesa, exponerlo aparte como 'cobertura de la sugerencia vs venta histórica'.
  - *Ref*: `app/views_prediccion_compras.py:1279-1281`
- [alta] **'Ventas de la temporada' no tienen filtro de fecha: comparan vidas útiles distintas** — _bulk_ventas filtra por producto__temporada y producto__anio_temporada pero no por fecha del movimiento. Un producto etiquetado 2025 aporta toda su venta acumulada; uno de 2026, sólo la de meses. La comparación YoY es inválida por construcción. Además no excluye excluir_de_analitica.
  - *Fix*: Acotar Movimientos_Producto.fecha a la ventana calendario de cada temporada/año, y añadir ProductoTalla__producto__excluir_de_analitica=False.
  - *Ref*: `app/views_prediccion_compras.py:1198-1214`
- [media] **3 queries por marca dentro del bucle del ranking** — vh_base.filter(marca_id=mid), ClasificacionABC.aggregate y SugerenciaCompra.aggregate, uno por marca.
  - *Fix*: Tres agregaciones por lotes con .values('marca_id') / .values('articulo__atributo1_id') fuera del bucle y merge por dict, patrón que el mismo archivo ya aplica en _bulk_ventas y alertas_map.
  - *Ref*: `app/views_prediccion_compras.py:1228-1271`
- [alta] **Solape no reconciliado con Inteligencia de Compra: dos motores dan dos números de compra** — Inteligencia de Compra calcula 'comprar N pares' con seasonal-naive en vivo y una curva de tallas propia; Predicción de Compras calcula 'unidades_a_pedir' por talle con el pipeline batch, ABC-XYZ y CurvaTalles. Nada los compara ni los concilia, y para la misma marca pueden diferir sustancialmente. Además el bloque de liquidación de Inteligencia de Compra (dead180) duplica el Plan de Liquidación acotado a una marca.
  - *Fix*: Definir un dueño por decisión: el batch para la sugerencia por talle (que Inteligencia de Compra debería consumir en vez de recalcular su propia curva) e Inteligencia de Compra para el marco de marca/temporada. Como mínimo, mostrar en Inteligencia de Compra el total de SugerenciaCompra de esa marca junto a su propio 'comprar' y marcar la divergencia.
  - *Ref*: `app/views_inteligencia_compra.py:303-341 vs app/views_prediccion_compras.py:276-311`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| MAPE y sesgo del modelo por método y por marca (backtest de temporadas cerradas) | para cada PrediccionDemanda de una temporada ya cerrada: real = Σ\|cantidad\| de ventas de ese artículo en la ventana de la temporada; MAPE = mean(\|unidades_predichas − real\| / real); sesgo = mean((unidades_predichas − real)/real). | PrediccionDemanda.unidades_predichas, temporada, anio, metodo_usado (app/models/predicciones.py:144-165) + Movimientos_Producto | Es el KPI ausente más grave del módulo: hoy el dashboard muestra 'confianza' autodeclarada. Sin error medido no se puede elegir entre métodos, ni saber si el motor mejora, ni justificar comprar según sus sugerencias. El sesgo además dice si el motor sobre o subcompra sistemáticamente. | medio |
| Frescura y cobertura del motor | días desde Max(fecha_calculo de ClasificacionABC) y Max(fecha_generacion de PrediccionDemanda); cobertura = SKUs con predicción vigente / SKUs con venta en los últimos 90 días. | ClasificacionABC.fecha_calculo, PrediccionDemanda.fecha_generacion, Producto_Talla + Movimientos_Producto | Con datos batch sin fecha visible, el mayor riesgo no es equivocarse sino decidir con datos viejos creyendo que son de hoy. La cobertura además revela cuántos SKUs que sí venden el motor simplemente no está mirando. | bajo |
| Tasa de adopción y antigüedad de las sugerencias | aprobadas / generadas por período y por origen; edad mediana de las sugerencias con aprobada=False y unidades_a_pedir>0. | SugerenciaCompra.aprobada, fecha_aprobacion, fecha_generacion, origen (app/models/predicciones.py:176-201) | Mide si el módulo se usa o se ignora. Un backlog de sugerencias envejecidas con 0% de aprobación es la señal de que el motor no es creíble, y hoy nadie lo puede ver. Nota: la conversión sugerencia→orden de compra real NO es calculable, porque SugerenciaCompra no tiene FK a Compras — habría que agregarla si se quiere cerrar ese circuito. | bajo |
| Costo del quiebre valorizado | Σ AlertaQuiebreTalle.unidades_perdidas_semana_estimadas × margen unitario real, por marca y categoría; y semanas acumuladas en quiebre. | AlertaQuiebreTalle.unidades_perdidas_semana_estimadas y fecha_deteccion (app/models/predicciones.py:282-305) + Ticket_Productos para el margen real | El módulo ya estima las unidades perdidas y las deja como un número suelto por alerta. Agregarlas y valorizarlas produce el único KPI que compite de igual a igual con el capital inmovilizado del Plan de Liquidación, y permite priorizar entre 'comprar más' y 'liquidar'. | medio |
| Salud de la corrección por censura | % de artículos con demanda_censurada=True y factor_correccion_censura promedio, por marca y categoría. | ClasificacionABC.demanda_censurada y factor_correccion_censura (app/models/predicciones.py:60-63) | Es el supuesto más frágil del motor: si un alto porcentaje de la demanda está censurada por quiebre, las velocidades y las predicciones son extrapolaciones. Exponerlo convierte un parámetro oculto en un nivel de confianza que el comprador puede juzgar. | bajo |

### Trazabilidad de Producto — `MEJORABLE`

- **Ruta**: /app/trazabilidad-producto/ — vista app/views_modulo_existencias_nuevo.py:845 trazabilidad_producto; API :852 api_trazabilidad_producto; template app/templates/vistas/modulo_existencias/trazabilidad_producto.html
- **Mide**: Ficha de historia completa de un SKU: últimos 200 movimientos de kardex, 50 lotes FIFO, 50 traspasos (Traspaso_Detalle), 50 cambios de precio, 20 pendientes de despacho y una timeline unificada de los tres primeros.
- **Filtros**: UN solo parámetro: sku exacto (858-860). No se puede buscar por código de artículo, descripción, marca ni sucursal; no hay filtro por rango de fechas ni por concepto de movimiento, y los límites (200/50/50/50/20) son fijos y no paginables. Para investigar un descuadre de un artículo con 12 tallas hay que consultar 12 veces.
- **Fidelidad**: Correcta en el contenido (une kardex, lotes FIFO, traspasos, precios y pendientes, que es exactamente lo que se necesita para auditar un descuadre), pero con tres defectos. (1) La timeline se ordena por STRING en formato DD/MM/YYYY (línea 1034), así que ordena por día del mes antes que por mes y año: el orden cronológico de la vista principal es incorrecto. (2) Cuando el SKU está duplicado (dato legacy conocido) y no hay sucursal en sesión, se elige con .first() SIN order_by — no determinista, la misma consulta puede devolver la historia de otra sucursal en otra ejecución; es exactamente el patrón que ya causó el bug de comuna por filter(rut).first(). (3) Sin scoping: cualquier usuario autenticado consulta cualquier SKU de cualquier empresa, incluyendo costos.
- **Rendimiento**: Bien: 6 queries acotadas con select_related y slices. No hay N+1. El único costo es que los límites fijos hacen que un SKU con historia larga muestre una ventana arbitraria sin decir que hay más.

**Problemas:**

- [alta] **La timeline se ordena alfabéticamente sobre fechas DD/MM/YYYY** — timeline.sort(key=lambda x: x.get('fecha',''), reverse=True) sobre strings ya formateados. '31/01/2024' queda antes que '01/12/2026'. La vista principal de la herramienta de auditoría presenta los hechos fuera de orden.
  - *Fix*: Construir la timeline con objetos date/datetime y ordenar por ellos, formateando sólo al serializar (o usar una clave ISO auxiliar).
  - *Ref*: `app/views_modulo_existencias_nuevo.py:1034`
- [media] **.first() sin order_by al resolver SKUs duplicados** — Si _n_sku>1 y no hay idSucursalActual en sesión, producto_talla = _qs_pt.first() sin orden explícito: Postgres no garantiza cuál devuelve. Se avisa 'sku_duplicado' pero no cuál se eligió ni se ofrece elegir.
  - *Fix*: Añadir .order_by('producto__sucursal__alias','id') para determinismo y devolver la lista de ocurrencias (sucursal + stock) para que el usuario seleccione.
  - *Ref*: `app/views_modulo_existencias_nuevo.py:874-879`
- [media] **Sólo se busca por SKU exacto** — No hay búsqueda por artículo, descripción o marca, ni selección de talla. Para auditar un producto completo hay que conocer y tipear cada SKU.
  - *Fix*: Aceptar q= (artículo/descripción/marca) y devolver un selector de SKUs coincidentes, reutilizando el patrón de búsqueda de _detalle_query (app/views_inteligencia_compra.py:843-846).
  - *Ref*: `app/views_modulo_existencias_nuevo.py:858-860`
- [media] **Sin scoping por empresa ni permiso propio** — @login_required nada más; devuelve costo, sobreprecio y precio de venta de cualquier SKU del sistema.
  - *Fix*: Filtrar _qs_pt por producto__sucursal__empresa_id__in=EmpresaUser del usuario y aplicar @requiere_permiso.
  - *Ref*: `app/views_modulo_existencias_nuevo.py:843-869`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Discrepancia stock plano vs lotes FIFO del SKU | Producto_Talla.stock − Σ LoteProducto.cantidad_disponible (activo=True, agotado=False). Semáforo si ≠ 0. | Producto_Talla.stock y LoteProducto.cantidad_disponible (ya cargados en la ficha) | El drift entre stock plano y lotes es un problema conocido de la casa y hoy sólo se detecta con un management command a ciegas. Ponerlo en la ficha del SKU convierte la trazabilidad en una herramienta de diagnóstico, no sólo de lectura, y con cero queries adicionales. | bajo |
| Saldo acumulado (running balance) por movimiento | columna con Σ acumulada de cantidad ordenada por fecha/hora ascendente, comparada contra el stock actual al final. | Movimientos_Producto.cantidad (ya cargados) | Es el kardex de verdad: permite ver EN QUÉ movimiento se rompió el saldo. Hoy la ficha lista movimientos sueltos y el auditor tiene que sumar a mano. | bajo |
| Tiempo de permanencia y edad del lote vivo | días desde LoteProducto.fecha_ingreso del lote más antiguo con cantidad_disponible>0; y días entre entrada y primera venta del SKU. | LoteProducto.fecha_ingreso + primer movimiento de venta posterior | Conecta la ficha con el Plan de Liquidación: al abrir un SKU se ve de inmediato si es candidato a markdown, usando la misma definición de antigüedad FIFO (app/views_inteligencia_compra.py:848-851) para que ambos módulos no discrepen. | bajo |

### Trazabilidad de DTE (árbol padre/hijos) — `UTIL`

- **Ruta**: /app/api/dte/<id>/trazabilidad/ — app/views.py:15430 api_dte_trazabilidad (se consume desde los modales de Gestión DTE / Recepción DTE)
- **Mide**: Árbol de un documento: para traspasos, las compras origen que aportaron los Producto_Talla (padre); todos los DTEs que lo referencian como documento_afectado (hijos: NC, ND, ajustes), con una bandera stock_pendiente que detecta NCs que no generaron los movimientos de reversa esperados; y los movimientos de kardex agrupados por sucursal.
- **Filtros**: Ninguno: recibe un dte_id por URL y devuelve el árbol completo. No es un reporte navegable — no hay pantalla que permita buscar 'todos los DTEs con NC sin stock revertido', que es justamente el uso analítico valioso.
- **Fidelidad**: El contenido es correcto y valioso: la reconstrucción del padre vía Compras_Producto_Talla es la única forma de saber a qué compra pertenece la mercadería de un traspaso, y stock_pendiente reutiliza el mismo diagnóstico que api_ncs_sin_stock, con try/except para no romper la respuesta. Dos observaciones: no valida que el DTE pertenezca a una empresa del usuario (Dte.objects.get(id=dte_id) sin scoping), y el padre sólo se calcula para tipo_transaccion='TRASPASO' — para una venta no se puede rastrear el origen de compra aunque el dato existiría por el mismo camino.
- **Rendimiento**: Aceptable para un modal: 1 query del DTE, 1 de líneas, 1 de compras, 1 de hijos, más una llamada a _diagnostico_nc por cada hijo (15542-15556). Como un DTE rara vez tiene más de 2-3 NCs, no es un N+1 problemático; sí lo sería si se intentara usar en lote.

**Problemas:**

- [alta] **No existe una vista agregada: la trazabilidad sólo se puede consultar de a un documento** — El endpoint calcula stock_pendiente (NCs que no revirtieron stock), que es un hallazgo de auditoría de primer orden, pero sólo lo revela si alguien abre ese DTE puntual. No hay listado, ni conteo, ni alerta.
  - *Fix*: Crear un reporte 'Integridad DTE' que corra el mismo _diagnostico_nc por lote sobre las NCs de un rango y liste las que tienen stock pendiente, con total de unidades y $ afectados.
  - *Ref*: `app/views.py:15542-15556`
- [media] **Sin validación de pertenencia del DTE al usuario** — Dte.objects.select_related(...).get(id=dte_id) sin filtrar por empresa/sucursal del usuario: enumerando ids se accede a montos, emisor, receptor y compras de otras empresas.
  - *Fix*: Filtrar por sucursal__empresa_id__in de EmpresaUser o reutilizar el helper puede_ver_sucursal que ya usan otros endpoints de DTE del proyecto.
  - *Ref*: `app/views.py:15464-15471`
- [baja] **El padre sólo se reconstruye para traspasos** — El bloque de compras origen está condicionado a tipo_transaccion=='TRASPASO'. Para una venta o una NC no se ve de qué compra vino la mercadería, aunque el camino Compras_Producto_Talla→producto_talla es el mismo.
  - *Fix*: Quitar la condición y resolver el padre siempre que el DTE tenga líneas con productoTalla.
  - *Ref*: `app/views.py:15499-15532`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| NCs sin reversa de stock: cantidad, unidades y $ pendientes | conteo y suma de total_faltantes de _diagnostico_nc sobre todas las NC emitidas en el período que no están 'ya_reparado', valorizadas a costo. | Dte (es_nota_credito) + el diagnóstico ya implementado (app/views.py:15551) + Producto.costo | Es un descuadre de inventario silencioso que hoy sólo se descubre por casualidad al abrir un documento. Convertirlo en un KPI de portada del módulo de documentos hace visible cuánto stock fantasma hay. | medio |
| Ciclo de vida del traspaso: días emisión→recepción→cierre, y % cerrado sin regularización | por DTE de traspaso: fecha_recepcion − fecha_emision; % de DTEs que terminan en RECEPCIONADO_COMPLETO sin pasar por EN_REGULARIZACION. | Dte.fecha_emision, fecha_recepcion, estado_dte | Es la métrica de salud del proceso documental completo, complementaria a la de unidades del Dashboard de Despachos; hoy sólo existe el promedio truncado a días enteros de ese dashboard. | bajo |
| Rastreo compra→traspaso→venta (lead time comercial total) | días entre la fecha de la compra origen (padre) y la primera venta del SKU en la tienda destino. | Compras.fecha vía Compras_Producto_Talla (el camino ya implementado en 15507-15514) + Movimientos_Producto de venta | Mide el tiempo total que el capital pasa inmovilizado antes de generar el primer peso, cruzando compra, logística y venta. Es el número que hoy nadie puede producir porque cada módulo ve sólo su tramo. | alto |

### Diagnóstico Cuadratura vs Reporte de Ventas — `UTIL`

- **Ruta**: /app/api/reportes/diagnostico-cuadratura/ — app/views_modulo_reportes.py:1464 api_diagnostico_cuadratura_vs_reporte; se consume desde app/templates/vistas/modulo_reportes/reporte_ventas_sucursal.html:1867
- **Mide**: Herramienta de auditoría que, para UNA sucursal y UN día, compara el total de la cuadratura de caja contra el total del reporte de ventas-sucursal y lista documento por documento los que están en disputa, con el motivo exacto de exclusión (tipo_transaccion, estado, descartado), más los tickets pagados sin DTE.
- **Filtros**: fecha (obligatoria, un solo día) y sucursal_id (obligatoria). Ambos server-side. No admite rango de fechas ni consulta multi-sucursal, así que reconciliar un mes son 30 consultas manuales por sucursal.
- **Fidelidad**: Es la pieza más honesta del módulo: no reimplementa la cuadratura sino que llama al helper real _calcular_cuadratura_data y reconstruye los filtros del reporte para compararlos dte por dte, documentando en el propio código la asimetría conocida (el reporte resta todas las NC, la cuadratura sólo las de DEVOLUCION). Dos problemas: (1) usa Ticket.fecha para listar los tickets pagados del día (1599-1602), y Ticket.fecha es auto_now=True (app/models/ventas.py:183) — se reescribe en cada save, así que la lista de 'tickets sin DTE' puede incluir tickets de otro día que se tocaron hoy y omitir los del día que no se han vuelto a guardar; es exactamente el caso que motivó la regla de usar created_at. (2) el emparejamiento ticket↔DTE es por número de folio dentro de un set global de folios del día, sin considerar el tipo de documento: una boleta folio 100 y una factura folio 100 el mismo día se cruzan.
- **Rendimiento**: Correcto para su alcance: una llamada a la cuadratura, una query de DTEs del día con select_related y un .only() sobre tickets. Sin N+1. La limitación es funcional, no de performance.

**Problemas:**

- [alta] **Los tickets se filtran por Ticket.fecha, que es auto_now y se reescribe en cada guardado** — Ticket.objects.filter(sucursal=..., fecha=fecha_obj, estado='PAGADO'). Con fecha=models.DateField(auto_now=True), cualquier edición posterior (registro de pago, anulación parcial, sincronización) mueve el ticket al día de la edición. La lista 'tickets_sin_dte' y su total son por tanto inestables y contradicen la regla del proyecto.
  - *Fix*: Filtrar por created_at__date=fecha_obj (con timezone America/Santiago) manteniendo fecha sólo como dato informativo; verificar que _calcular_cuadratura_data haga lo mismo para que la comparación sea coherente.
  - *Ref*: `app/views_modulo_reportes.py:1599-1603 y app/models/ventas.py:183`
- [media] **El match ticket↔DTE por folio ignora el tipo de documento** — folios_emitidos es un set plano de numero_documento del día; un ticket cuyo folio_dte coincida con el folio de OTRO tipo de documento se descarta como 'ya facturado' y desaparece del diagnóstico.
  - *Fix*: Comparar por la tupla (tipo_documento, numero_documento), o mejor por la FK del ticket al DTE si existe.
  - *Ref*: `app/views_modulo_reportes.py:1605-1611`
- [alta] **No valida que la sucursal consultada pertenezca al usuario** — Sucursal.objects.get(id=int(sucursal_id)) sin cruzar con EmpresaUser ni con permiso de sucursal. Devuelve ventas totales, folios y montos de cualquier sucursal del sistema.
  - *Fix*: Aplicar el mismo control de sucursal que ya se corrigió en kardex y en ventas-global (filtro por EmpresaUser + puede_ver_sucursal).
  - *Ref*: `app/views_modulo_reportes.py:1524-1530`
- [media] **Sólo funciona día a día: no sirve para cerrar un mes** — Ambos parámetros son obligatorios y singulares. Auditar un mes de 6 sucursales son 180 llamadas manuales.
  - *Fix*: Aceptar fecha_desde/fecha_hasta y una lista de sucursales, devolviendo primero un ranking de días con diferencia ≠ 0 y permitiendo entrar al detalle sólo en esos.
  - *Ref*: `app/views_modulo_reportes.py:1502-1522`

**KPIs propuestos:**

| KPI | Fórmula | Fuente | Para qué | Esf. |
|---|---|---|---|---|
| Días descuadrados del mes y monto absoluto acumulado de diferencia | por sucursal: nº de días con \|cuadratura_total − reporte_total\| > umbral, y Σ\|diferencia\| del período. | la misma comparación ya implementada, ejecutada sobre un rango | Convierte la herramienta de un lupa puntual en un semáforo de cierre contable: hoy nadie sabe cuántos días del mes están descuadrados hasta que los revisa uno por uno. | medio |
| Descomposición de la diferencia por causa | agrupar los DTEs en disputa por motivo (TIPO_TRANSACCION, ESTADO, DESCARTADO, NC de ANULACION restada sólo en el reporte) y por tickets sin DTE, con el monto que aporta cada causa. | motivos_exclusion_reporte y entra_cuadratura ya calculados por documento (app/views_modulo_reportes.py:1555-1595) | La diferencia hoy es un número único que hay que interpretar leyendo la lista. Atribuirla a causas dice si el problema es de proceso (tickets sin facturar), de datos (estados raros) o de definición (NC de anulación), que son tres acciones distintas. | bajo |
| Ventas de público sin documento tributario | Σ total de tickets PAGADOS sin DTE asociado, por sucursal y día, usando created_at. | Ticket.created_at (app/models/ventas.py:219), estado, folio_dte | Es riesgo tributario directo y ya está a medio calcular en el endpoint (tickets_sin_dte_total). Elevarlo a KPI mensual por sucursal lo saca del terreno del hallazgo casual. Ojo: hay que usar tipo_transaccion='VENTA_PUBLICO' y no el genérico 'VENTA' para no mezclar facturas por concepto o despacho. | bajo |

