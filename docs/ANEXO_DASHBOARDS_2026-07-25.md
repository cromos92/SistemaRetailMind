# Anexo — Ficha de cada dashboard (2026-07-25)

## Dashboard HOME + dashboards de VENTAS

### Dashboard Home / "Centro de Control" (pantalla nº1 tras login) — `MEJORABLE`

- **Ruta**: URL: /app/home/ (name=verHome, LOGIN_REDIRECT_URL) y alias /app/dashboard/ + /app/ruta_a_check_session/ · Vista: retailmind/app/views_dashboard_home.py:192 dashboard_home() (+ helpers calcular_kpis_ventas:333, calcular_kpis_stock:426, calcular_kpis_compras:571, calcular_kpis_requerimientos:633, calcular_kpis_operaciones:677, calcular_kpis_caja_depositos:727, calcular_kpis_precios_pendientes:770, calcular_kpis_dte_problemas:549, calcular_kpis_salud_inventario:149, obtener_top_productos:921, generar_alertas_criticas:797) · Template: retailmind/app/templates/vistas/dashboard_home.html · Fallback sin permiso: bienvenida() → vistas/bienvenida.html · APIs declaradas: /app/dashboard/api/ventas-tiempo-real/ y /app/dashboard/api/stock-alertas/ (views_dashboard_home.py:998 y :1029)
- **Audiencia**: Todo el mundo: es la pantalla de aterrizaje obligada (settings.py:309 LOGIN_REDIRECT_URL='verHome'). Quien no tenga el permiso 'dashboard_general' cae en /app/bienvenida/ (solo accesos directos). El dueño/administración la usa como semáforo del día; jefe de local para ver pendientes de su tienda.
- **Muestra**: 4 tarjetas KPI (Ventas Hoy con $ + tickets + unidades + ticket promedio; Quiebres que Rotan + SKUs c/stock + unidades + rotación; DTEs por Recepcionar + monto + compras del mes; Requerimientos pendientes + esperando + tasa aprobación). 3 tarjetas de Salud de Inventario (cobertura en días, % stock >180d vía lotes FIFO, sell-through 30d). Lista de alertas priorizadas. Grilla "Acciones Pendientes" de 9 contadores enlazados (DTEs por recepcionar, regularizaciones, DTEs con problemas, cambios/devoluciones, requerimientos, precios por regularizar, arqueos abiertos, depósitos sin verificar, ajustes de inventario). 3 paneles (Cuadraturas y Caja, Precios Pendientes, Operaciones con monto por cobrar y valor de inventario). Gráfico ApexCharts de venta general últimos 30 días. Top 10 productos del mes y Top 8 quiebres que rotan. Tabla de 5 DTEs de compra pendientes más antiguos. Accesos a 3 dashboards y 4 acciones rápidas.
- **Filtros**: NINGUNO. No hay selector de fechas, ni de sucursal, ni de empresa: todo es fijo (hoy / semana en curso / mes en curso) y se scopea con request.session['idSucursalActual'] / ['idEmpresaActual'] (views_dashboard_home.py:199-200). La sucursal se cambia fuera del dashboard, en seleccionar_empresa_sucursal (views.py:27108), que además cierra las otras sesiones del usuario. Consecuencia: el dueño NO puede ver la cadena completa desde el home; ve la tienda que tenga activa y, si esa tienda es una bodega/CD (EDEL, IMP, PA00), lo que ve es el bodegón. Si el EmpresaUser no tiene sucursal, idSucursalActual queda None y TODOS los KPIs pasan a ser de todas las empresas sin ningún filtro (no hay fallback a EmpresaUser).
- **Fidelidad**: MIXTA, con aciertos y errores graves. Aciertos: ventas usan created_at (no Ticket.fecha auto_now) en todo el render server-side (líneas 342, 352, 359, 366, 382, 392); salud de inventario usa Movimientos_Producto con CONCEPTOS_VENTA y excluye excluir_de_analitica (149-188); quiebres accionables bien planteados. Errores: (1) la venta se mide SOLO sobre Ticket estado='PAGADO' — las ventas emitidas como DTE sin ticket (facturas por concepto, mayorista, emisionDTE, ecommerce facturado) no existen para el home, mientras el reporte ventas-global sí las suma (views_modulo_reportes.py:7402-7460); (2) NO excluye modulo_origen='CAMBIO_DEVOLUCION', así que los tickets de diferencia de cambio se cuentan como venta (el POS sí los excluye: views_modulo_ventas.py:1854); (3) no resta notas de crédito, la "venta" es bruta; (4) no excluye excluir_de_analitica en ventas ni en stock/valor de inventario; (5) compras: cuenta Dte COMPRA EMITIDO sin límite de fecha, sin excluir descartado=True y sin excluir NOTA DE CREDITO, y Compras sin excluir estado='ELIMINADA' (571-606); (6) 'productos_pendientes' (585-589) no filtra por sucursal ni empresa: es un conteo global de toda la base; (7) cobertura/sell-through de "Salud de Inventario" no excluyen centros de distribución cuando no hay sucursal activa, a diferencia del indicador de compra del dashboard de ventas que sí los excluye — los dos números se contradicen; (8) la variación del mes compara mes-a-la-fecha contra el mes anterior COMPLETO (365-374), así que los primeros días del mes el semáforo siempre marca caída fuerte; (9) el badge de tendencia con esa variación mensual está pegado a la tarjeta "Ventas Hoy", que es de otro período. No repite el error de 'ROI = IVA' del reporte de compras (aquí no hay cálculo de margen).
- **Rendimiento**: Pesado para ser la pantalla de entrada de todos los usuarios: ~40-50 queries por carga, varias sobre Producto_Talla (100k+ filas) sin caché — total_skus.count(), con-stock.count(), sin-stock.count(), críticos.count() + slice, aggregate de stock×costo, más el subquery de quiebres (Producto_Talla IN Ticket_Productos de 30d) y el top de quiebres (426-520). calcular_kpis_requerimientos (658-662) trae a Python todos los requerimientos pendientes para promediar días. No hay ninguna caché (el resto del módulo ventas sí usa @cache_ventas_json). El template además no usa las APIs de refresco: el botón "Actualizar" hace location.reload(), o sea recalcula todo.

**Problemas:**

- [alta] **Dos enlaces del home apuntan a URLs que no existen (404)** — La alerta de quiebres y el pie del panel "Quiebres que Rotan" enlazan a /app/resumen-existencias/, pero la ruta real es /app/reportes/resumen-existencias/ (urls.py:1319). La alerta de cambios enlaza a /app/cambios-devoluciones/, cuando la ruta real es /app/ventas/cambios-devoluciones/ (urls.py:1008). Son justamente los enlaces de las dos alertas más accionables del tablero.
  - *Fix*: Reemplazar las URLs hardcodeadas por {% url 'ver_resumen_existencias' %} y {% url 'gestion_cambios_devoluciones' %} (el propio template ya las usa bien en otros bloques) o corregir las cadenas en generar_alertas_criticas.
  - *Ref*: `retailmind/app/views_dashboard_home.py:809 y :857; retailmind/app/templates/vistas/dashboard_home.html:562 vs retailmind/app/urls.py:1319 y :1008`
- [alta] **Los tickets de cambio/devolución se suman como venta** — tickets_base solo filtra estado='PAGADO'; no excluye modulo_origen='CAMBIO_DEVOLUCION'. Esos tickets se crean en el flujo de cambios (views_modulo_ventas.py:13904 y :15726) por la diferencia a cobrar, e inflan Ventas Hoy / Semana / Mes, el ticket promedio, el gráfico de 30 días y el Top Productos. El dashboard del POS sí los excluye explícitamente, y el reporte ventas-global filtra modulo_origen__in=['VENTA_PUBLICO','POS','ECOMMERCE'].
  - *Fix*: Añadir .exclude(modulo_origen='CAMBIO_DEVOLUCION') (o filtrar modulo_origen__in=['VENTA_PUBLICO','POS','ECOMMERCE']) en calcular_kpis_ventas y en obtener_top_productos.
  - *Ref*: `retailmind/app/views_dashboard_home.py:336-338 y :923-930 vs retailmind/app/views_modulo_ventas.py:1854 y retailmind/app/views_modulo_reportes.py:7411-7417`
- [alta] **La "venta" del home no cuadra con el reporte de ventas: faltan los DTEs sin ticket y no se restan las NC** — El home mide venta únicamente contando Tickets. El reporte ventas-global (ya corregido) suma Tickets con dte_generado=False MÁS los Dte de venta, y resta las notas de crédito emitidas con tipo_transaccion DEVOLUCION/ANULACION. Resultado: el home ignora facturas por concepto/despacho, ventas mayoristas y cualquier DTE emitido sin pasar por ticket, y nunca descuenta devoluciones. El dueño ve dos cifras distintas de "ventas del mes" según qué pantalla abra.
  - *Fix*: Extraer la lógica de _sumar_periodo de obtener_ventas_global_por_empresa a un servicio compartido (p.ej. app/services/ventas_metrics.py) y consumirlo desde el home; mostrar Venta bruta, Devoluciones (NC) y Venta neta como tres cifras separadas.
  - *Ref*: `retailmind/app/views_dashboard_home.py:333-423 vs retailmind/app/views_modulo_reportes.py:7402-7480`
- [media] **La API de "ventas en tiempo real" usa Ticket.fecha (auto_now) y además está muerta** — api_dashboard_ventas_tiempo_real filtra Ticket.objects.filter(fecha=hoy) y ordena por '-hora' para sacar la "última venta": ambos campos son auto_now (models/ventas.py:183-184), así que devuelve los tickets MODIFICADOS hoy (una boleta de marzo reimpresa aparece como venta de hoy) y la "última venta" es en realidad el último ticket guardado. Peor: ninguna plantilla ni JS del proyecto llama a esta API ni a api_dashboard_stock_alertas — son endpoints huérfanos que además quedan fuera del control de permisos porque /app/dashboard/ está en URLS_SIN_VERIFICACION.
  - *Fix*: O borrar ambos endpoints (y su ruta), o reescribir el filtro a created_at__date=hoy con order_by('-created_at') y cablearlos a un refresco parcial del header en vez del location.reload() actual.
  - *Ref*: `retailmind/app/views_dashboard_home.py:1005 y :1013; retailmind/app/models/ventas.py:183-184; retailmind/app/middleware_permisos.py:117-119`
- [alta] **KPIs de compras contaminados: sin fecha, sin excluir descartados, sin excluir compras ELIMINADA y sin scoping** — dtes_pendientes cuenta TODOS los Dte de compra en estado EMITIDO desde el inicio de los tiempos, sin excluir descartado=True ni tipo_documento='NOTA DE CREDITO'; el monto pendiente arrastra ese backlog histórico completo. compras_mes cuenta Compras sin excluir estado='ELIMINADA'. Y productos_pendientes (Compras_Producto_Talla no recepcionadas) no filtra por empresa ni sucursal: es un número global de todo el holding metido en la tarjeta de una tienda.
  - *Fix*: Añadir descartado=False y exclude(tipo_documento='NOTA DE CREDITO') a los DTE de compra, un límite de antigüedad (o un desglose por tramos de días), exclude(estado='ELIMINADA') en Compras y scoping por empresa/sucursal en productos_pendientes.
  - *Ref*: `retailmind/app/views_dashboard_home.py:574-606; retailmind/app/models/compras.py:12 y :39; retailmind/app/models/dte.py:160`
- [media] **Scoping por sesión sin validar contra EmpresaUser** — dashboard_home confía en request.session['idSucursalActual'] / ['idEmpresaActual']. El middleware los rellena desde EmpresaUser (status=True) cuando faltan, pero si el EmpresaUser no tiene sucursal asociada la sesión queda con idSucursalActual=None y entonces ventas, stock, requerimientos, operaciones, caja y precios se calculan sobre TODAS las sucursales de TODAS las empresas, sin filtro alguno.
  - *Fix*: Resolver el universo con obtener_sucursales_usuario(request.user) y usar sucursal_id__in=<visibles> cuando no haya sucursal activa, en vez de "sin filtro".
  - *Ref*: `retailmind/app/views_dashboard_home.py:199-200; retailmind/app/middleware_permisos.py:170-186; retailmind/app/views.py:27205-27210`
- [media] **Cobertura y sell-through del home incluyen bodegas/CD y contradicen al dashboard de ventas** — calcular_kpis_salud_inventario, sin sucursal activa, agrega el stock de toda la cadena incluyendo centros de distribución (EDEL/IMP/PA00), cuyo stock es mayorista y no vende a público, disparando la cobertura en días. El indicador de compra del dashboard de ventas hace exactamente lo contrario (excluye es_centro_distribucion=True). Dos pantallas dan cobertura distinta para el mismo día.
  - *Fix*: Excluir producto__sucursal__es_centro_distribucion=True cuando no hay sucursal específica, igual que obtener_indicador_compra_categoria, o mostrar dos líneas separadas (tiendas / CD).
  - *Ref*: `retailmind/app/views_dashboard_home.py:168-171 vs retailmind/app/views_modulo_ventas.py:18760-18766`
- [media] **La variación del mes compara peras con manzanas y se pinta sobre la tarjeta equivocada** — variacion_mes = (ventas del mes a la fecha vs mes anterior COMPLETO). El día 3 del mes eso siempre da -85%/-90% y dispara el badge rojo. Además ese badge se renderiza dentro de la tarjeta "Ventas Hoy", que muestra un valor de otro período.
  - *Fix*: Comparar MTD contra los mismos días del mes anterior (y opcionalmente contra el mismo mes del año anterior); mover el badge a una tarjeta "Ventas Mes" o etiquetarlo explícitamente "vs mes anterior (MTD)".
  - *Ref*: `retailmind/app/views_dashboard_home.py:365-374; retailmind/app/templates/vistas/dashboard_home.html:167-173`
- [media] **No excluye excluir_de_analitica en ventas, top productos ni valor de inventario** — La bandera excluir_de_analitica solo se respeta en calcular_kpis_salud_inventario. Ventas, unidades, top productos, total_skus, sin_stock, quiebres y valor de inventario la ignoran, así que los artículos marcados para no analizar siguen moviendo las cifras del tablero principal.
  - *Fix*: Propagar .exclude(...producto__excluir_de_analitica=True) a calcular_kpis_stock, calcular_kpis_ventas (vía Ticket_Productos) y obtener_top_productos.
  - *Ref*: `retailmind/app/views_dashboard_home.py:426-546 y :921-952 vs :159-167`
- [media] **Sin caché y con ~40-50 queries sobre tablas de 100k+ filas en la pantalla de login de todos** — Cada login y cada "Actualizar" (location.reload) recalcula todo: cinco conteos completos sobre Producto_Talla, el subquery de quiebres, el top de quiebres, el agregado stock×costo, más el bucle Python sobre requerimientos pendientes. El resto del módulo ventas ya tiene @cache_ventas_json con invalidación por sucursal y aquí no se usa.
  - *Fix*: Cachear el bloque de stock/salud de inventario 5-15 min con cache_ventas_json/make_cache_key por sucursal (se invalida con los signals ya existentes) y reemplazar el count() de sin_stock por total_skus - skus_con_stock.
  - *Ref*: `retailmind/app/views_dashboard_home.py:426-546 y :658-662; retailmind/app/cache_utils.py:94-156`
- [baja] **El home no lleva a la mitad de los dashboards y su grilla queda descuadrada** — El bloque "Ir a Dashboards" usa una grilla de 4 columnas con solo 3 enlaces (Ventas, Productos, Compras). Faltan Dashboard Documentos, Despachos, Requerimientos, FIFO y Predicción de Compras, que existen y están en el menú.
  - *Fix*: Completar la grilla con los dashboards restantes, condicionados por {% puede_ver_opcion_tag %} como hace menu.html.
  - *Ref*: `retailmind/app/templates/vistas/dashboard_home.html:613-626 vs retailmind/app/templates/layout/menu.html:2004-2070`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Venta neta del día y del mes (bruta − notas de crédito) | (Σ Ticket.total con dte_generado=False, modulo_origen∈[VENTA_PUBLICO,POS,ECOMMERCE] + Σ Dte.monto_con_iva de venta) − Σ NC (tipo_documento='NOTA DE CREDITO' y tipo_transaccion∈[VENTA,VENTA_PUBLICO,DEVOLUCION,ANULACION]) | Ticket.created_at + Dte.fecha_emision, reutilizando _sumar_periodo de views_modulo_reportes.py:7402 | Es la única cifra que cuadra con el reporte de ventas y con contabilidad; hoy el home muestra venta bruta parcial y el dueño no sabe cuál creer. | medio |
| Venta comparable MTD vs MTD del mes anterior y vs mismo mes año anterior | venta(1..día_actual del mes) / venta(1..día_actual del mes anterior) − 1 | Ticket.created_at / Dte.fecha_emision | Elimina el falso -90% de los primeros días del mes y hace el semáforo utilizable a diario. | bajo |
| Margen bruto del día ($ y %) | Σ(Ticket_Productos.subtotal) − Σ(stock × costo_fifo), ambos netos de IVA; excluyendo líneas con costo_fifo=0 y reportando aparte el % de líneas sin costo | Ticket_Productos.subtotal / costo_fifo | Hoy el home solo muestra facturación; sin margen no se detecta que la venta creció liquidando. El % de líneas sin costo actúa además como semáforo de calidad del FIFO. | medio |
| Tasa de quiebre de los SKUs que rotan (%) | quiebres_rotantes / SKUs con al menos una venta en 30 días × 100 | Ticket_Productos + Producto_Talla.stock (mismos querysets de calcular_kpis_stock) | El número absoluto ("312 quiebres") no dice si es mucho o poco; el porcentaje sí es comparable entre tiendas y en el tiempo. | bajo |
| Venta perdida estimada por quiebre ($) | Σ (unidades vendidas/día del SKU en los 30d previos al quiebre × días en cero × precio de venta) | Ticket_Productos + Movimientos_Producto (fecha del último egreso) + Producto_Talla.precioventa | Convierte la alerta de quiebres en pesos y prioriza qué reponer primero; es el argumento que mueve una compra. | alto |
| Cumplimiento de meta del día y del mes (%) | venta acumulada / meta de la sucursal para el período | Nueva tabla MetaVenta(sucursal, período, monto) + venta neta | Un centro de control sin meta no dice si el día fue bueno; es el KPI que un jefe de local mira primero. | alto |
| Efectivo pendiente de depositar por antigüedad (0-2 / 3-7 / >7 días) | Σ DepositoBancario.monto con verificado=False agrupado por tramo de (hoy − fecha_deposito) | DepositoBancario + ArqueoCaja | Hoy solo se ve el total sin verificar; el riesgo real está en la antigüedad, no en el monto. | bajo |
| Tasa de anulación de tickets del día (%) | tickets ANULADO / tickets del día × 100 | Ticket.estado + created_at | Indicador temprano de error operativo o de fuga en caja; el dashboard de ventas ya lo calcula por período y el home no lo muestra. | bajo |

### Home legacy / "Dashboard General" (verHome antiguo) — `REDUNDANTE`

- **Ruta**: URL: /app/home-legacy/ (name=verHomeLegacy, urls.py:470) · Vista: retailmind/app/views.py:8201 verHome() · Template: retailmind/app/templates/vistas/dashboard_general.html
- **Audiencia**: Nadie. No está en el menú, no lo enlaza ninguna plantilla y la URL name 'verHome' fue reasignada al dashboard nuevo (urls.py:463), así que ni siquiera los {% url 'verHome' %} llegan aquí.
- **Muestra**: Cambios y devoluciones del mes por tipo y tendencia, requerimientos por tipo/estado, DTEs emitidos y recibidos pendientes, regularizaciones, discrepancias de precios entre sucursales y cambios de precio pendientes con lista de 10.
- **Filtros**: Ninguno; período fijo (mes en curso) y sin selector de sucursal.
- **Fidelidad**: Irrelevante para operación (código muerto), pero el bloque de discrepancias de precios es directamente peligroso: recorre en Python un values('articulo','atributo1','atributo2') sobre TODA la tabla Producto (100k+ filas) anotando Min/Max de precioventa y luego itera el resultado completo. Además todo su contenido está duplicado —y mejor resuelto— en el home nuevo (paneles de Precios Pendientes, DTEs con problemas, Requerimientos, Regularizaciones) y en el Dashboard Documentos.
- **Rendimiento**: Malo: la sección de discrepancias de precios es un full scan agrupado de Producto materializado en memoria. Si alguien abre la URL en producción, cuelga el worker.

**Problemas:**

- [media] **Dashboard zombie: 400+ líneas de vista y un template completo que nadie puede alcanzar** — La ruta /app/home-legacy/ existe pero no está enlazada desde ningún template ni desde el menú; el nombre 'verHome' apunta al dashboard nuevo. Es superficie muerta que confunde al mantener (dos funciones distintas llamadas verHome/dashboard_home) y que sigue siendo accesible tecleando la URL.
  - *Fix*: Borrar la ruta, views.verHome y vistas/dashboard_general.html; lo poco que aporta (discrepancias de precios entre sucursales) migrarlo como panel del módulo de precios, no como dashboard.
  - *Ref*: `retailmind/app/urls.py:470; retailmind/app/views.py:8201 y :8548; sin referencias en templates (grep de dashboard_general.html solo devuelve views.py)`
- [alta] **Cálculo de discrepancias de precios en Python sobre todo el catálogo** — Producto.objects.values('articulo','atributo1','atributo2').annotate(count_sucursales, precio_min, precio_max).filter(count_sucursales__gt=1) y luego un for sobre el queryset completo para contar diferencias y calcular la variación máxima. Sin límite ni paginación.
  - *Fix*: Si se conserva el indicador, resolverlo en SQL con HAVING Max(precioventa) > Min(precioventa) y devolver solo el conteo + top N, no el queryset entero.
  - *Ref*: `retailmind/app/views.py:8467-8500`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Ninguno: consolidar en el home nuevo | n/a | n/a | Todo lo que muestra ya está (mejor calculado) en el Dashboard Home y en el Dashboard Documentos; mantenerlo vivo solo genera cifras divergentes. | bajo |
| Dispersión de precio del mismo artículo entre sucursales (% máx-mín) | (Max(precioventa) − Min(precioventa)) / Min(precioventa) × 100 por (articulo, marca, color), con HAVING en SQL | Producto.precioventa | Es el único indicador propio que vale la pena rescatar, pero pertenece al módulo de Gestión de Precios, no a un dashboard general. | medio |

### Dashboard de Ventas NEXO ("Dashboard Ventas" del menú) — `MEJORABLE`

- **Ruta**: URL: /app/ventas/dashboard-mejorado/ (name=dashboard_ventas_mejorado, urls.py:1076) · Vista: retailmind/app/views_modulo_ventas.py:17299 dashboard_ventas_mejorado() · Template: retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html · APIs: /app/api/ventas/{indicadores-globales, por-vendedor, por-sucursal, por-metodo-pago, analisis-cambios, estado-cuadraturas, productos-mas-vendidos, tendencias, indicadores-avanzados, estado-operacional, mix-por-sucursal, sucursales-dashboard, exportar-dashboard}/ (urls.py:1078-1096) · Enlazado desde el menú (menu.html:2013) y desde el home
- **Audiencia**: Dueño y administración (análisis comercial del período), jefe de local para su tienda. Decisiones: cómo va la venta vs período anterior, qué tienda/vendedor rinde, qué se vende, si hay fuga por cambios/devoluciones y si la caja cuadra.
- **Muestra**: 8 tarjetas KPI (Ventas Totales, Transacciones, Ticket Promedio/ATV, UPT, Unidades Vendidas, Margen Bruto, Sell-Through, Cambios/Devoluciones) con variación vs período comparativo; gráfico de evolución diaria/semanal; dona de métodos de pago; ventas por categoría y por especialidad v1.2; tabla de indicador de compra; comparativo por tienda (mix 100% apilado + tabla con variación); horas pico y día de la semana; Top 10 vendedores; Top productos por modelo con marca/categoría/género/especialidad; alertas del período; estado de cuadraturas; bloque de indicadores avanzados (GMROI, rotación, días de stock, descuento); control de fugas y detección de fraude (solo roles gerenciales); estado POS y depósitos bancarios; operaciones pendientes.
- **Filtros**: Barra con atajos (Hoy/Ayer/Semana/Mes/Mes anterior/Últimos 30/Año), fecha inicio-fin, comparación (período anterior / mismo mes año anterior / sin comparación), Empresa, Sucursal (en cascada desde Empresa), Vendedor, Estado (Todos/Pagado/Pendiente/Anulado), Categoría y Especialidad. PERO se aplican de forma muy desigual: Empresa y Sucursal sí llegan al backend (_scope_suc_emp) aunque SIN validar contra los permisos del usuario; Vendedor solo lo honra indicadores-globales (la tabla de vendedores, métodos de pago, tendencias, avanzados y operacional lo ignoran); Categoría y Especialidad NO afectan a ninguno de los 8 KPIs principales ni a evolución, métodos de pago, tendencias, margen ni cuadraturas — solo a los charts de categoría/especialidad, al top de productos y al mix por tienda; Estado tiene defaults distintos según endpoint (unos fuerzan PAGADO, otros dejan pasar ANULADO y PENDIENTE).
- **Fidelidad**: Es el dashboard con más errores de fecha y de coherencia del grupo. (1) Ticket.fecha es auto_now: solo indicadores-globales, productos-más-vendidos, mix-por-sucursal y los charts v1.2 usan created_at; por-vendedor (17525), por-sucursal (17696), por-método-pago (17769), analisis-cambios/ratio (17865), tendencias (18959), indicadores-avanzados (19025), estado-operacional (19132) y el export a Excel (19353) filtran por 'fecha', es decir por fecha de última modificación. (2) Dentro del MISMO endpoint de KPIs, los totales usan created_at pero la evolución diaria agrupa por values('fecha') (17441): el gráfico no cuadra con la tarjeta y puede pintar días fuera del rango pedido. (3) La tarjeta "Unidades Vendidas" y el UPT se calculan en el navegador sumando SOLO el top-20 de productos (agg[:limite] en 18488, JS en 1849-1855): son cifras estructuralmente falsas y muy por debajo de la realidad, cuando el propio endpoint de indicadores-avanzados ya devuelve unidades_vendidas real. (4) "Margen Bruto" mezcla unidades: ingresos = Σ(stock × precio) — precio BRUTO con IVA y sin restar descuento_unitario — contra costo = Σ(stock × costo_fifo) que es costo NETO; el margen sale inflado ~19 puntos por IVA más lo que sobra por ignorar descuentos, y cualquier línea con costo_fifo=0 (fallback de venta sin FIFO, drift conocido) aporta 100% de margen. Es el mismo tipo de defecto que el "ROI = IVA" del reporte de compras, aunque el mecanismo sea distinto. GMROI hereda el problema. (5) Igual que el home, cuenta como venta los tickets modulo_origen='CAMBIO_DEVOLUCION', no resta notas de crédito y no ve las ventas emitidas solo como DTE. (6) Sell-through y días de stock comparan unidades vendidas del período contra stock de TODA la cadena incluyendo centros de distribución y sin excluir excluir_de_analitica (19052-19060). (7) estado-operacional entrega "DTEs pendientes" y "Regularizaciones pendientes" SIN ningún filtro de sucursal/empresa ni de fecha (19251-19261) y los depósitos no se filtran por empresa. (8) estado-cuadraturas calcula "pendientes" como días_del_período − nº de arqueos, lo que en multi-sucursal no significa nada (con 6 tiendas y 30 días siempre da 0) y recalcula la diferencia contando billetes solo de efectivo, ignorando diferencia_transbank. (9) El contador "Cambios Pendientes" del panel de cuadraturas lee response.pendientes de /analisis-cambios/, campo que ese endpoint nunca devuelve: siempre muestra 0. (10) No hay filtro por notas de crédito mal construido (no se consultan NC en absoluto), así que el bug de 'venta + NOTA DE CREDITO' del reporte no está replicado aquí — pero el efecto práctico es peor: las devoluciones simplemente no se restan.
- **Rendimiento**: Aceptable en agregados (14 llamadas en paralelo, la mayoría con @cache_ventas_json de 60-300s e invalidación por versión de sucursal), con dos excepciones caras: obtener_tendencias_ventas itera en Python TODOS los tickets del período para armar el histograma por hora y por día (18969-18988) — con un año seleccionado son cientos de miles de filas materializadas; y obtener_estado_cuadraturas recorre los arqueos uno a uno en Python. Riesgos adicionales: Chart.js se carga desde https://cdn.jsdelivr.net/npm/chart.js (línea 1180) pese a que el proyecto tiene sus libs locales — sin internet o con CSP el dashboard queda en blanco; y Ticket.Meta.ordering=['-fecha','-hora'] ordena por campos auto_now, lo que degrada los planes de las consultas que no reordenan explícitamente.

**Problemas:**

- [alta] **La mitad de los endpoints filtra por Ticket.fecha, que es auto_now** — por-vendedor, por-sucursal, por-método-pago, el ratio de analisis-cambios, tendencias, indicadores-avanzados, estado-operacional y el export a Excel usan fecha__gte/__lte. Ticket.fecha se reescribe en cada save (reimpresión, cambio de estado, generación de DTE, sync desktop), así que esos bloques miden "tickets tocados en el rango", no vendidos. Un mismo ticket puede aparecer en el ranking de vendedores de un mes y en los KPIs de otro.
  - *Fix*: Reemplazar todos los fecha__gte/__lte por created_at__date__gte/__lte (como ya hace build_queryset en 17359-17361) y usar el helper único _tickets_pagados_periodo/_rango_periodo en todos los endpoints.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:17525, 17696, 17769, 17865, 18959, 19025, 19132, 19353; retailmind/app/models/ventas.py:183`
- [alta] **El gráfico de evolución no usa la misma fecha que los KPIs del mismo endpoint** — obtener_indicadores_globales_ventas calcula los totales con created_at pero arma la serie diaria con .values('fecha'). Como 'fecha' es auto_now, la suma del gráfico no coincide con la tarjeta "Ventas Totales" y el relleno de días (todas_fechas[item['fecha']] = ...) inserta claves fuera del rango solicitado, dibujando días que el usuario no pidió.
  - *Fix*: Agrupar con TruncDate('created_at') (igual que el home en views_dashboard_home.py:394) y descartar/loggear cualquier día fuera de rango en vez de insertarlo.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:17441-17455 vs :17359-17361`
- [alta] **"Unidades Vendidas" y "UPT" salen del top-20 de productos, no del total** — El JS suma response.productos (top 20 modelos, agg[:limite] con limite=20 por defecto) y con eso pinta la tarjeta de unidades y calcula UPT = esas unidades / total de transacciones. Ambos KPIs quedan groseramente subestimados y el UPT — que se usa para medir venta cruzada — es directamente inventado.
  - *Fix*: Usar response.unidades_vendidas de /api/ventas/indicadores-avanzados/ (ya calculado con Sum('stock') sobre todas las líneas) para #kpiUnidades y UPT = unidades_vendidas / cantidad_ventas.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18486-18489; retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html:1846-1856 y :19040-19047 (unidades_vendidas ya disponible)`
- [alta] **Margen Bruto y GMROI mezclan ingreso con IVA contra costo neto, ignoran descuentos y se caen a 100% cuando falta costo FIFO** — ingresos = Σ(stock × precio) usa el precio bruto de la línea sin restar descuento_unitario (el propio módulo ya usa Sum('subtotal') en los charts v1.2), mientras costo_ventas = Σ(stock × costo_fifo) es costo neto. El margen se infla por el IVA (~19 pts) y por los descuentos no descontados; y toda línea con costo_fifo=0 —conocidas por el drift de lotes FIFO y el fallback de venta— aporta margen 100%. GMROI y margen_pct heredan el error. Es el equivalente en ventas al "ROI que en realidad era el IVA" del reporte de compras.
  - *Fix*: ingresos = Sum('subtotal') / 1.19 (o el neto real de la línea), costo = Sum(stock × costo_fifo) excluyendo líneas con costo_fifo=0, y exponer un KPI de cobertura de costeo (% de unidades con costo FIFO válido) para que el margen se lea con su margen de error.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:19035-19052 vs :18482-18485 (los charts sí usan subtotal)`
- [alta] **Los filtros Categoría y Especialidad no afectan a ningún KPI principal** — cargarDashboard() envía categoria_id y especialidad_id a los 14 endpoints, pero solo los leen por-categoria, por-especialidad, productos-mas-vendidos y mix-por-sucursal. indicadores-globales, indicadores-avanzados, tendencias, por-vendedor, por-método-pago, estado-operacional y el indicador de compra los ignoran. El usuario filtra "Fútbol" y las tarjetas Ventas Totales, Margen, Ticket Promedio y Sell-Through siguen mostrando el total del negocio: es una lectura falsa, no una limitación evidente.
  - *Fix*: Propagar categoria_id/especialidad_id a los endpoints que agregan por línea (indicadores-globales sobre Ticket_Productos, avanzados, tendencias, indicador-compra) o, como mínimo, deshabilitar visualmente y marcar con un aviso las tarjetas que no responden al filtro.
  - *Ref*: `retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html:1436-1445; retailmind/app/views_modulo_ventas.py:17318 (indicadores-globales nunca lee categoria_id) y :18670 (indicador-compra usa _tickets_pagados_periodo sin categoría)`
- [alta] **Los endpoints del dashboard no validan permisos ni el sucursal_id/empresa_id recibido** — _scope_suc_emp toma sucursal_id/empresa_id crudos del GET y filtra por ellos sin comprobar EmpresaUser ni obtener_sucursales_usuario; sin parámetros no aplica ningún filtro ("todas", no "todas las visibles"). Además el middleware de permisos solo mapea la URL de la página ('/app/ventas/dashboard'), no las rutas /app/api/ventas/..., y cuando no hay match devuelve None = acceso permitido. Resultado: cualquier usuario autenticado (un cajero) puede pedir /app/api/ventas/indicadores-globales/?sucursal_id=<otra tienda> o sin parámetros y obtener la venta de todo el holding.
  - *Fix*: En _scope_suc_emp resolver el universo con obtener_sucursales_usuario(request.user), rechazar (403) un sucursal_id/empresa_id fuera de ese universo y usar sucursal_id__in=<visibles> como default; añadir el prefijo '/app/api/ventas/' a URL_PERMISO_MAP con el código 'dashboard_ventas'. El endpoint mix-por-sucursal ya lo hace bien y sirve de patrón.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18562-18581 vs :18885-18889; retailmind/app/middleware_permisos.py:222-228`
- [media] **El filtro Estado tiene defaults contradictorios entre endpoints** — Con Estado="Todos" (valor vacío), indicadores-globales y tendencias fuerzan estado='PAGADO', pero por-vendedor, por-sucursal, por-método-pago y estado-operacional no filtran nada e incluyen ANULADO y PENDIENTE. La suma de la tabla de vendedores no cuadra nunca con la tarjeta de Ventas Totales, y el usuario no tiene forma de saber por qué.
  - *Fix*: Centralizar el default en un helper único (PAGADO salvo que se pida otro estado) y usarlo en todos los endpoints, como ya hace _tickets_pagados_periodo.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:17355-17358 y :18962-18966 vs :17529-17531, :17700-17702, :17772-17774, :19137`
- [alta] **Los atajos de fecha se calculan con toISOString(): en Chile "Hoy" se corre al día siguiente después de las 20:00** — aplicarPeriodo construye la fecha con new Date() local y luego .toISOString().split('T')[0], que devuelve la fecha en UTC. Con America/Santiago (UTC-4/-3), a partir de las 20:00-21:00 hora local el ISO ya es del día siguiente, así que "Hoy" y "Ayer" consultan un rango corrido un día y el dashboard aparece vacío justo en el horario de cierre de tienda.
  - *Fix*: Formatear con fecha local: `${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}` a partir de getFullYear/getMonth/getDate, en aplicarPeriodo y en calcularPeriodoComparativo.
  - *Ref*: `retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html:1335-1336 y :1389-1390`
- [media] **"Cambios Pendientes" del panel de cuadraturas siempre muestra 0** — El JS hace $('#cambiosPendientes').text(response.pendientes || 0) sobre la respuesta de /api/ventas/analisis-cambios/, que devuelve total_cambios, monto_total, ratio, por_motivo, por_tipo y por_estado — nunca 'pendientes'. El indicador está muerto en pantalla.
  - *Fix*: Usar cambios_devoluciones.pendientes_aprobacion de /estado-operacional/ (ya calculado) o añadir el campo 'pendientes' al endpoint de análisis de cambios.
  - *Ref*: `retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html:1909 vs retailmind/app/views_modulo_ventas.py:17920-17932 y :19231`
- [media] **"Cuadraturas pendientes" y la diferencia de caja están mal definidas en multi-sucursal** — pendientes = días del período − nº total de arqueos: con varias tiendas el resultado es siempre 0 (o negativo saturado a 0) y con una sola tienda castiga los domingos. La diferencia se recalcula sumando billetes y monedas contra total_efectivo_teorico e ignora diferencia_transbank, pese a que el modelo ya guarda ambas diferencias (el home sí usa los dos campos).
  - *Fix*: Contar pendientes como (días hábiles × sucursales en alcance) − arqueos cerrados, o directamente listar los pares (sucursal, día) sin arqueo; y usar los campos diferencia_efectivo/diferencia_transbank del modelo en vez de recontar.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18413-18421 vs retailmind/app/views_dashboard_home.py:736-742`
- [media] **DTEs pendientes, regularizaciones y depósitos del bloque operacional ignoran los filtros** — En estado-operacional, dtes_pendientes (Dte tipo_transaccion='TRASPASO' estado EMITIDO) y regularizaciones_pendientes se consultan sin sucursal, sin empresa y sin rango de fechas: son totales globales del sistema pintados dentro de un panel filtrado por tienda y período. Los depósitos se filtran por sucursal pero no por empresa.
  - *Fix*: Aplicar el mismo scoping (_scope_suc_emp o sucursal_id__in visibles) y, donde tenga sentido, el rango de fechas; o etiquetarlos explícitamente como "global, sin filtro".
  - *Ref*: `retailmind/app/views_modulo_ventas.py:19234-19261`
- [alta] **El Excel exportado no coincide con lo que se ve en pantalla** — exportar_dashboard_ventas_excel filtra por 'fecha' (auto_now), fuerza estado='PAGADO' ignorando el filtro de Estado, no aplica empresa_id, no excluye excluir_de_analitica y calcula el total por producto como stock × precio, mientras la tabla en pantalla usa Sum('subtotal') agrupado por modelo. El archivo que se manda a gerencia contradice al dashboard del que salió.
  - *Fix*: Reimplementar el export consumiendo los mismos helpers (_tickets_pagados_periodo, obtener_productos_mas_vendidos) en vez de rehacer las consultas a mano.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:19352-19445 vs :18455-18490`
- [alta] **tendencias itera todos los tickets del período en Python** — obtener_tendencias_ventas hace `for ticket in queryset` dos veces (una para el histograma por hora, otra por día de semana) materializando todos los tickets; además usa ticket.hora y ticket.fecha, ambos auto_now, de modo que el "peak hour" es la hora del último guardado, no la de la venta.
  - *Fix*: Una sola query con ExtractHour('created_at') y ExtractWeekDay('created_at') + Sum('total'), como ya hace el home en views_dashboard_home.py:381-388.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18969-18988 vs retailmind/app/views_dashboard_home.py:381-388`
- [media] **Chart.js se carga desde CDN externo** — <script src="https://cdn.jsdelivr.net/npm/chart.js"> — el proyecto tiene sus librerías en app/static/libs/ y el resto de dashboards no depende de internet. Sin conexión (o con CSP/bloqueo corporativo) todos los gráficos del dashboard de ventas desaparecen sin mensaje.
  - *Fix*: Servir Chart.js desde static/libs y referenciarlo con {% static %}.
  - *Ref*: `retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html:1180`
- [baja] **Con "Sin comparación" el dashboard igual muestra variaciones** — Cuando tipoComparacion='sin_comparacion' el JS cae al else y usa response.crecimiento_ventas/cantidad/ticket que el backend calculó por su cuenta contra 'mes_anterior' (default de periodo_comparacion, parámetro que el frontend nunca envía). El usuario pidió no comparar y sigue viendo flechas de tendencia.
  - *Fix*: Ocultar los badges de tendencia cuando se elige "Sin comparación", o enviar periodo_comparacion y respetarlo.
  - *Ref*: `retailmind/app/templates/vistas/modulo_dashboards/dashboard_ventas_nexo.html:1551-1556 vs retailmind/app/views_modulo_ventas.py:17337 y :17391-17400`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Venta neta y % de devolución sobre venta ($, no sobre nº de tickets) | venta bruta − Σ NC del período; % devolución = Σ NC / venta bruta × 100 | Dte (tipo_documento='NOTA DE CREDITO', tipo_transaccion∈[VENTA,VENTA_PUBLICO,DEVOLUCION,ANULACION]) + Ticket/Dte de venta | Hoy el ratio de cambios es nº de solicitudes / nº de ventas, que subestima el impacto real: una devolución de $300.000 pesa igual que una de $12.000. | medio |
| UPT real y unidades por día | Σ Ticket_Productos.stock del período / nº de tickets pagados | Ticket_Productos (todas las líneas, no el top-20) | Corrige el KPI hoy falso y es el indicador estándar para medir venta cruzada por vendedor y por tienda. | bajo |
| Margen bruto neto y cobertura de costeo FIFO (%) | margen = Σ(subtotal neto de IVA) − Σ(stock × costo_fifo) sobre líneas con costo_fifo>0; cobertura = unidades con costo_fifo>0 / unidades totales × 100 | Ticket_Productos.subtotal / costo_fifo | Da un margen creíble y, con la cobertura al lado, dice cuánto confiar en él; sin ese segundo número el margen actual es indefendible. | medio |
| Contribución por categoría y por especialidad ($ de margen, no solo ingresos) | por categoría v1.2: Σ(subtotal) − Σ(stock × costo_fifo) | Ticket_Productos + Producto.categoria / atributo Especialidad | Hoy los charts v1.2 solo muestran facturación; la decisión de qué comprar y qué liquidar depende del margen, no del ingreso. | medio |
| Venta y tickets por hora efectiva de apertura (ventas/hora, tickets/hora) | venta del tramo horario / horas de apertura del tramo, con ExtractHour('created_at') | Ticket.created_at + horario de la sucursal | El chart de horas pico actual usa Ticket.hora (auto_now) y sin normalizar por horas de apertura no sirve para dotación de personal. | medio |
| Productividad por vendedor: venta/hora y UPT por vendedor | venta del vendedor / horas trabajadas del período; unidades del vendedor / tickets del vendedor | Ticket.vendedor + Ticket_Productos (horas requieren datos de turno) | El ranking actual premia al que atiende en la tienda más grande; venta/hora y UPT comparan personas, no locales. | alto |
| Participación de venta por canal (POS / ecommerce / mayorista / cambios) | Σ total agrupado por modulo_origen, sobre venta neta | Ticket.modulo_origen (ya devuelto por estado-operacional pero no promovido a KPI) | Permite ver el crecimiento del ecommerce y aísla los tickets de cambio que hoy contaminan la venta. | bajo |
| % de venta identificada con cliente (RUT) y ticket promedio identificado vs anónimo | tickets con cliente_rut no vacío / tickets totales; y ATV de cada grupo | Ticket.cliente_rut / cliente_id | Es el KPI base del programa de fidelización ya implementado; hoy el dashboard de ventas no lo muestra en ninguna parte. | bajo |
| Descuento sobre venta (%) y % de tickets con descuento | Σ(stock × descuento_unitario) / (venta + descuentos) × 100; tickets con alguna línea descontada / tickets | Ticket_Productos.descuento_unitario (el endpoint ya calcula descuento_total y descuento_prom_pct) | El dashboard muestra el monto descontado pero no su peso relativo, que es lo que revela si el crecimiento vino de precio o de liquidación. | bajo |

### Ruta duplicada /app/ventas/dashboard/ ("Dashboard de Ventas" enlazado desde Cambios y Devoluciones) — `REDUNDANTE`

- **Ruta**: URL: /app/ventas/dashboard/ (name=dashboard_ventas, urls.py:1075) · Vista: retailmind/app/views_modulo_ventas.py:17293 dashboard_ventas() · Template: el MISMO vistas/modulo_dashboards/dashboard_ventas_nexo.html, pero renderizado SIN contexto · Enlazado desde app/templates/vistas/modulo_ventas/gestion_cambios_devoluciones.html:2169
- **Audiencia**: Quien llega desde el módulo de Cambios y Devoluciones (jefe de local, administración). El menú no la usa: apunta a dashboard_ventas_mejorado.
- **Muestra**: Exactamente lo mismo que el dashboard de ventas NEXO, salvo que los selectores de Categoría y Especialidad se renderizan vacíos (solo la opción "Todas"), porque esta vista no pasa categorias_filtro ni especialidades_filtro al template.
- **Filtros**: Los mismos controles en pantalla, pero Categoría y Especialidad quedan inutilizables (sin opciones). El resto se comporta igual — con los mismos problemas de scoping y de fechas descritos en el dashboard NEXO.
- **Fidelidad**: Idéntica al dashboard NEXO (comparte los 14 endpoints), con el agravante de que dos URLs distintas presentan la misma pantalla con capacidades distintas, sin que nada lo advierta.
- **Rendimiento**: Igual al NEXO; el único ahorro es no construir el árbol de categorías, que es despreciable.

**Problemas:**

- [media] **Dos rutas para el mismo dashboard y una de ellas está degradada** — dashboard_ventas() hace render del mismo template sin contexto; dashboard_ventas_mejorado() sí construye el árbol de categorías v1.2 y la lista de especialidades. Quien entra por el enlace de gestion_cambios_devoluciones.html cae en la versión sin filtros de categoría/especialidad y no tiene manera de saberlo.
  - *Fix*: Dejar una sola vista: hacer que dashboard_ventas() sea un redirect permanente a dashboard_ventas_mejorado (o unificar ambas en una sola función con la ruta antigua como alias) y actualizar el enlace del template de cambios/devoluciones.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:17293-17296 vs :17299-17317; retailmind/app/urls.py:1075-1076; retailmind/app/templates/vistas/modulo_ventas/gestion_cambios_devoluciones.html:2169`
- [baja] **Dos plantillas huérfanas de ~3.000 líneas siguen en el repo** — vistas/modulo_dashboards/dashboard_ventas.html (1.452 líneas) y dashboard_ventas_mejorado.html (1.570 líneas) no las renderiza ninguna vista: el único template en uso es dashboard_ventas_nexo.html. Son versiones anteriores del mismo tablero que confunden cualquier búsqueda de "por qué el dashboard muestra X".
  - *Fix*: Eliminarlas del repositorio (el historial de git conserva la versión anterior).
  - *Ref*: `grep de render(...) en app/views_modulo_ventas.py solo devuelve dashboard_ventas_nexo.html (líneas 17295 y 17316)`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Ninguno propio: consolidar en el Dashboard de Ventas NEXO | n/a | n/a | Es la misma pantalla; agregarle indicadores solo duplicaría el mantenimiento y las divergencias. | bajo |

### Ventas por Categoría / Especialidad v1.2 + Indicador de Compra (sección del Dashboard de Ventas) — `UTIL`

- **Ruta**: URLs: /app/api/ventas/por-categoria/, /app/api/ventas/por-especialidad/, /app/api/ventas/indicador-compra/ (urls.py:1087-1089) · Vistas: retailmind/app/views_modulo_ventas.py:18597 obtener_ventas_por_categoria, :18636 obtener_ventas_por_especialidad, :18670 obtener_indicador_compra_categoria (+ helpers _tickets_pagados_periodo:18584 y _rango_periodo:18550) · Render: bloque "Ventas por categoría / especialidad" y tabla "Indicador de compra por categoría" en dashboard_ventas_nexo.html:308-402, JS en :1610-1843
- **Audiencia**: Comprador / dueño: decide qué categoría reponer, qué está en quiebre y qué hay que liquidar. Es la sección con mayor valor de decisión de todo el dashboard de ventas.
- **Muestra**: Dos gráficos de barras horizontales (ventas $ por categoría v1.2 Padre › Hijo, top 12; ventas $ por especialidad/deporte, top 12) y una tabla con Categoría, Vendidas, Stock, Cobertura en días, Sell-through, $ Inmovilizado y un veredicto accionable (QUIEBRE <15d, BAJO <30d, SANO, SOBRE-STOCK >120d, SIN ROTACIÓN), ordenada por urgencia de compra.
- **Filtros**: Respeta fecha, empresa, sucursal y estado a través de _tickets_pagados_periodo (que sí usa created_at). El chart de categoría acepta especialidad_id y el de especialidad acepta categoria_id (filtro cruzado real). El indicador de compra respeta fecha/sucursal/empresa/estado pero NO respeta categoria_id ni especialidad_id: seleccionar una categoría en la barra de filtros no acota la tabla. La exclusión de centros de distribución cuando no hay sucursal específica es correcta y está documentada en el código.
- **Fidelidad**: La mejor del grupo. Usa created_at (no auto_now), excluye excluir_de_analitica, usa Sum('subtotal') como ingreso real, resuelve las categorías con el árbol v1.2 Padre › Hijo, trata la especialidad como atribución multi-etiqueta y lo advierte explícitamente en el docstring (la suma puede exceder la venta total), excluye los CD del stock para que la cobertura refleje la tienda y no el bodegón, y complementa la cobertura con sell-through porque en un catálogo sobre-stockeado la cobertura satura. Puntos débiles: (1) hereda de _tickets_pagados_periodo la NO exclusión de los tickets modulo_origen='CAMBIO_DEVOLUCION' y la no resta de notas de crédito; (2) el stock del indicador se lee de Producto_Talla.stock (stock plano) y no de los lotes FIFO, con el drift conocido entre ambos; (3) las unidades vendidas salen de Ticket_Productos, o sea de tickets: una venta emitida solo como DTE no cuenta y su categoría aparece con menos rotación de la real; (4) el $ Inmovilizado usa Producto.costo (costo actual del maestro), no el costo del lote, así que no es exactamente el capital pagado; (5) la cobertura se calcula con la venta del período elegido: si el usuario elige "Hoy" o "Ayer", la venta diaria se estima con 1 día y las coberturas se vuelven ruido puro sin ningún aviso.
- **Rendimiento**: Bueno: 2-3 queries agregadas por endpoint, sin N+1 ni bucles en Python. No están cacheados (a diferencia de sus vecinos con @cache_ventas_json), pero el costo es bajo. El único agregado pesado es el de stock por categoría sobre Producto_Talla, acotado por categoria__padre__isnull=False.

**Problemas:**

- [media] **El filtro Categoría/Especialidad de la barra superior no acota la tabla de indicador de compra** — obtener_indicador_compra_categoria lee fecha, sucursal, empresa y estado pero nunca categoria_id ni especialidad_id, aunque el frontend se los envía en el mismo objeto params. Al filtrar por una categoría, los dos gráficos de arriba reaccionan y la tabla de abajo no: parece un desfase de datos.
  - *Fix*: Aplicar categoria_id (padre incluye hijas, como hace obtener_ventas_por_especialidad) y especialidad_id (con Exists, como productos-mas-vendidos) al queryset de ventas y al de stock.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18670-18700 vs :18648-18653 y :18473-18479`
- [media] **Con períodos cortos la cobertura en días es ruido y no se advierte** — venta_diaria = unidades / días del período. Con el atajo "Hoy" o "Ayer" (1 día), una categoría que vendió 2 unidades y tiene 400 de stock sale con 200 días de cobertura; otra que vendió 0 sale "SIN ROTACIÓN". El veredicto (QUIEBRE / SOBRE-STOCK) se pinta igual de rotundo que con 90 días de historia.
  - *Fix*: Exigir un mínimo de días (p.ej. 28) para calcular cobertura, o mostrar el veredicto en gris con un aviso "muestra insuficiente (N días)" cuando el período sea corto o las unidades vendidas sean menores a un umbral.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18676-18677 y :18730-18748`
- [media] **El stock del indicador viene del stock plano, no de los lotes FIFO** — stock_qs agrega Producto_Talla.stock. El proyecto tiene un drift documentado entre el stock plano y LoteProducto (comandos reconciliar_stock_lotes / reconciliar_lotes_a_stock), y el propio home calcula su "stock viejo" desde lotes. Cobertura, sell-through y $ inmovilizado quedan sujetos a ese desfase.
  - *Fix*: Ofrecer el mismo indicador calculado sobre LoteProducto.cantidad_disponible (o mostrar la diferencia plano-vs-lotes como control de calidad) para que el comprador sepa si el número es confiable.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18755-18775 vs retailmind/app/views_dashboard_home.py:164-177`
- [media] **Hereda de _tickets_pagados_periodo los tickets de cambio/devolución y la ausencia de NC** — El helper filtra Ticket estado='PAGADO' del período sin excluir modulo_origen='CAMBIO_DEVOLUCION' ni restar devoluciones; las líneas de esos tickets se cuentan como venta de la categoría, inflando la rotación aparente de las categorías con más cambios (calzado deportivo, típicamente).
  - *Fix*: Añadir .exclude(modulo_origen='CAMBIO_DEVOLUCION') en _tickets_pagados_periodo (lo arregla de una vez para categoría, especialidad, indicador de compra y top de productos) y restar las unidades de las NC del período.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18584-18592`
- [baja] **$ Inmovilizado usa el costo actual del maestro, no el costo del lote** — dinero = Sum(stock × producto__costo). Producto.costo es el costo vigente del maestro de la sucursal, no el costo al que entró cada lote; para stock antiguo (el grueso del capital parado) puede diferir bastante del desembolso real.
  - *Fix*: Calcular el capital parado con LoteProducto (cantidad_disponible × costo_unitario del lote) y dejar producto.costo como fallback cuando no haya lote.
  - *Ref*: `retailmind/app/views_modulo_ventas.py:18770-18774`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Índice de sobre/sub-representación: mix de venta vs mix de stock por categoría | (% de la categoría en la venta del período) / (% de la categoría en el stock actual) | Ticket_Productos.subtotal + Producto_Talla.stock × costo | En un catálogo sobre-stockeado, este ratio separa de inmediato lo que hay que comprar (índice >1,2) de lo que sobra (<0,8), sin depender de la cobertura que satura. | bajo |
| Rate of sale semanal por categoría (unidades/semana/tienda) | unidades vendidas / (semanas del período × nº de tiendas con stock de la categoría) | Ticket_Productos + Producto_Talla | Normaliza el ritmo de venta por punto de venta y es lo que permite comparar categorías entre tiendas grandes y chicas y dimensionar una orden de compra. | medio |
| Quiebre de talla core (% de tallas centrales sin stock por categoría) | SKUs sin stock dentro de las tallas que concentran el 80% de la venta de la categoría / total de esas tallas | Producto_Talla.talla + Ticket_Productos | Una categoría puede tener cobertura "sana" y estar quebrada en las tallas que se venden; ese es el quiebre que realmente pierde venta. | alto |
| Margen por categoría y por especialidad ($ y %) | Σ(subtotal neto) − Σ(stock × costo_fifo) por categoría v1.2 / especialidad | Ticket_Productos.subtotal y costo_fifo | Convierte el gráfico de ingresos en un ranking de rentabilidad: la categoría más vendida no siempre es la que más aporta. | medio |
| Antigüedad del stock por categoría (% >180 y >365 días) junto a la cobertura | Σ cantidad_disponible de lotes con fecha_ingreso mayor a 180/365 días / stock total de la categoría | LoteProducto.fecha_ingreso / cantidad_disponible | El home ya calcula esto a nivel global; bajarlo a categoría en la misma tabla del indicador de compra cierra el círculo "qué comprar / qué liquidar" en una sola pantalla. | medio |
| Semáforo de confiabilidad del dato (días del período, unidades de la muestra, % SKUs con costo) | metadatos del propio cálculo expuestos junto a cada veredicto | parámetros del request + agregados ya calculados | El veredicto QUIEBRE/SOBRE-STOCK se presenta hoy con la misma rotundidad con 1 día o con 90 días de datos; explicitar la muestra evita decisiones de compra sobre ruido. | bajo |

## Dashboards de COMPRAS y PREDICCIÓN

### Dashboard de Compras Mejorado (el único vivo del módulo Compras) — `MEJORABLE`

- **Ruta**: URL: /app/verDashboardComprasMejorado/ (alias /app/verDashboardCompras/ y redirect desde /app/dashboard_compras_estrategico/). Vista: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\views_modulo_compras.py:2877 verDashboardComprasMejorado. API: /app/dashboard_compras_mejorado_api/ -> views_modulo_compras.py:2884 dashboard_compras_mejorado_api (+ ~14 funciones auxiliares, líneas 3094-4340). Export: views_modulo_compras.py:1374 exportar_dashboard_compras. Template: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\templates\vistas\modulo_dashboards\dashboard_compras_mejorado.html (2618 líneas). Menú: layout/menu.html:2020 (permiso 'dashboard_compras_estrategico').
- **Audiencia**: Jefatura de compras / gerencia del centro de distribución. Decide cuánto invertir por temporada y proveedor, a quién exigir cumplimiento de entregas, y qué margen aplica el CD a las tiendas.
- **Muestra**: 7 tarjetas KPI (Total Compras, Inversión Total, Cumplimiento Recepciones, ROI Promedio, Unidades Esperadas/Recepcionadas, Margen Esperado, Compras no inventariables) + Evolución mensual Inversión vs Ventas + Pareto de proveedores + Top 10 proveedores y Top 10 productos + Inversión por categoría v1.2 (Padre›Hija) y por marca con sell-through 90d + Comparativa anual mes a mes + ROI por temporada con YoY + bloque 'Centro de Distribución' (unidades compradas/despachadas, stock CD, eficiencia, flujo mensual, despachos por sucursal, rendimiento de sucursales destino) + bloque 'Márgenes CD' (margen total, margen %, costo proveedor vs costo sucursales, comparativa de costos, 2 tablas de rentabilidad por tipo de sucursal) + cumplimiento por proveedor + alertas + insights + tabla de rendimiento por compra (20 filas).
- **Filtros**: Ofrece 4: Año (dinámico, año actual hasta 2023 — ya corregido), Período (Año/Trimestre/Mes/Semana), Temporada (Invierno/Verano/Otoño/Primavera, con normalización de acentos contra temporada_familia) y Proveedor (poblado desde /app/empresas_proveedoras/). SE APLICAN DE VERDAD sólo al bloque principal (compras_query, líneas 2914-2937): KPIs, Pareto, Top proveedores/productos, cumplimiento, ROI por temporada, tabla de rendimiento y categoría/marca. NO SE APLICAN a: evolución mensual (3182), comparativa anual (3263), tendencias YoY (3139), flujo de distribución (3972), despachos por sucursal (3841), rendimiento de sucursales destino (3893), márgenes CD (4025), comparativa de costos (4171) ni rentabilidad por tipo (4234) — esas funciones sólo reciben `anio`. Resultado: al filtrar un proveedor o 'Última Semana', la mitad inferior de la página y los dos gráficos grandes siguen mostrando el año completo de TODAS las compras. NO hay filtro por empresa ni sucursal (el modelo Compras no tiene FK al comprador).
- **Fidelidad**: Fechas: usa Compras.fecha (DateField editable, correcto — NO es auto_now) y Ticket.created_at para ventas (correcto). Doble conteo: no aplica (no cruza Ticket con Dte). NC de venta: no aplica (no toca notas de crédito). Exclusiones: el query principal SÍ excluye ELIMINADA/CANCELADA (2914), pero los gráficos de evolución y comparativa anual y las tendencias NO -> el total de la barra no cuadra con la tarjeta. NO excluye Compras.es_historica=True (compras retroactivas que por diseño nunca se recepcionan) -> hunde el Cumplimiento. Scoping: NULO a nivel empresa; la única tarjeta con scoping ('Compras no inventariables', 3035) usa request.session['idEmpresaActual'] sin validar contra EmpresaUser, y suma monto_con_iva (bruto) mientras la Inversión Total es costo neto -> dos bases distintas en la misma fila de tarjetas. KPI inventado: 'ROI Promedio' y 'Margen Esperado' NO son ROI — son markup teórico (precioSugerido − costo)/costo sobre unidades ORDENADAS (nunca vendidas ni recepcionadas). VERIFICADO respecto del antecedente: aquí NO se repite el bug del IVA del reporte de compras (no hay monto_con_iva − monto_neto en ninguna función del dashboard); el problema es distinto pero también infla: mezcla precio público (IVA incluido, convención del POS) contra costo neto. Traspasos: sólo concepto='TRASPASO_SALIDA', ignora CONCEPTOS_TRASPASO_LEGACY y los movimientos migrados con sucursal_destino NULL. Género/atributo4: no se usan aquí.
- **Rendimiento**: Malo. N+1 sistemático: Pareto (3233) 2 queries por proveedor, Top proveedores (3437) 3 por proveedor, Cumplimiento (3507) 3 por proveedor, tabla de rendimiento (3636) 4 queries × 20 compras, evolución mensual 12×3, comparativa anual 12×4, ROI por temporada 4 por rubro + 4 del año anterior. Además calcular_comparativa_costos_cd_vs_sucursales (4208) itera en Python TODOS los LoteProducto del año por cada una de 10 sucursales, y calcular_margenes_centro_distribucion (4070/4113) itera fila a fila todos los Traspaso_Detalle o todos los Movimientos del año. Con ~50-100 proveedores son fácilmente 500-1.000 consultas + varios cientos de miles de filas materializadas por carga. `compras_ids` se materializa como lista Python y se inyecta en `__in` (miles de IDs).

**Problemas:**

- [alta] **Los gráficos no comparten el filtro de estado con las tarjetas (ELIMINADA/CANCELADA)** — El query principal excluye ELIMINADA/CANCELADA, pero calcular_evolucion_mensual_mejorado, calcular_comparativa_anual_mejorado y calcular_flujo_distribucion_mensual construyen su propio Compras.objects.filter(fecha__year=..., fecha__month=...) sin exclusión. La suma de las 12 barras del gráfico 'Evolución Mensual' es MAYOR que la tarjeta 'Inversión Total' de la misma pantalla, sin explicación visible.
  - *Fix*: Extraer un helper `_compras_base(anio)` con exclude(estado__in=['ELIMINADA','CANCELADA']) + exclude(es_historica=True) y pasar el MISMO queryset (o los mismos filtros) a las tres funciones mensuales en vez de reconstruirlo.
  - *Ref*: `views_modulo_compras.py:2914 (con exclusión) vs :3193, :3292 y :3986 (sin exclusión)`
- [alta] **Las tendencias vs año anterior comparan filtrado contra sin filtrar** — calcular_metricas_principales_mejorado arma `compras_anterior = Compras.objects.filter(fecha__year=anio-1)` sin exclude de estado y sin aplicar temporada ni proveedor. Si el usuario filtra el proveedor X, las flechas trend_compras / trend_inversion / trend_roi comparan las compras de X en 2026 contra TODAS las compras de TODOS los proveedores en 2025 -> siempre saldrá una caída catastrófica y falsa.
  - *Fix*: Aplicar exactamente los mismos filtros (estado, temporada/temporada_familia, proveedor) al queryset del año anterior; reutilizar la función _aplicar_filtros que ya existe en calcular_comparativa_anual_mejorado (3274).
  - *Ref*: `views_modulo_compras.py:3139-3162`
- [alta] **Dos paneles completos del bloque CD están MUERTOS por FieldError silenciado** — calcular_rentabilidad_por_tipo_sucursal hace `Compras.objects.filter(responsable__sucursales=suc_cd, ...)` pero Compras.responsable es un CharField(max_length=50) -> Django lanza FieldError en el filter(). La excepción la captura el try genérico de la vista y devuelve el dict vacío por defecto. Las tablas 'Rentabilidad Centros de Distribución' y 'Rentabilidad Sucursales Vendedoras' están vacías desde siempre. Aparte, el bloque de vendedoras usa `Ticket_Productos.objects.filter(ticket__in=ventas)` y `Sum(F('costo')*F('cantidad'))` con TRES campos inexistentes: el FK real es idTicket, la cantidad es `stock` y el costo es `costo_fifo` (ya está bien resuelto 300 líneas más arriba, en calcular_rendimiento_sucursales_destino:3943).
  - *Fix*: Reemplazar `responsable__sucursales=suc_cd` por la vía real (Compras_Producto.sucursal_destino o Productos_Recepcionados.sucursal); corregir el agregado a `Ticket_Productos.objects.filter(idTicket__in=ventas).aggregate(Sum(F('stock')*F('costo_fifo')))`; y dejar de tragar la excepción (loguear y mostrar 'sin datos' en el panel en vez de una tabla vacía indistinguible de 'no hubo movimiento').
  - *Ref*: `views_modulo_compras.py:4261-4262 (responsable__sucursales), :4317-4321 (ticket__in / costo / cantidad), :3024-3028 (except que lo tapa), template dashboard_compras_mejorado.html:1119 y :1147`
- [alta] **'ROI Promedio' y 'Margen Esperado' no son ROI y están inflados por el IVA** — roi = (precioSugerido − costo) / costo sobre las unidades ORDENADAS (Compras_Producto_Talla.stock), no sobre lo recepcionado ni lo vendido: es un markup de lista, no un retorno. Además precioSugerido es precio público (convención del POS: el total del ticket es monto_con_iva, ver models/dte.py:332 y el cálculo monto_neto=total/1.19) mientras `costo` es neto de compra. Ejemplo: costo 10.000 y PVP 25.000 -> el dashboard muestra ROI 150%, cuando el markup real sobre neto es (21.008−10.000)/10.000 = 110%. Verificado: NO se repite aquí el bug del reporte de compras (ROI = monto_con_iva − monto_neto del DTE de compra); es un cálculo distinto, pero igualmente engañoso, y alimenta las alertas ('ROI bajo por temporada', umbral 15%) y los insights ('ROI Excelente', umbral 25%).
  - *Fix*: Renombrar la tarjeta a 'Markup teórico de lista' y calcularla neteando el IVA: (precioSugerido/1.19 − costo). Añadir aparte el margen REALIZADO (ver KPIs propuestos). Recalibrar los umbrales de alerta/insight después del cambio de base.
  - *Ref*: `views_modulo_compras.py:3129-3136 y :3315-3356; template dashboard_compras_mejorado.html:1428 y :1436`
- [alta] **'Eficiencia de Distribución' mezcla numerador global con denominador filtrado y suma egresos sin abs()** — calcular_metricas_distribucion recibe compras_ids (filtrado) para las unidades compradas, pero calcula las despachadas con Movimientos_Producto.filter(fecha__year=anio) global. Al filtrar un proveedor, la eficiencia explota a cientos de %. Peor: `Sum('cantidad')` sobre TRASPASO_SALIDA devuelve un número NEGATIVO (los egresos son negativos por convención del kardex) — sólo lo salva el `max()` contra Traspaso_Detalle; si esa tabla no tiene filas (datos legacy), unidades_despachadas queda negativo, la eficiencia sale negativa y stock_centro = compradas + |despachadas|, es decir, el stock del CD se INFLA. La función hermana calcular_despachos_por_sucursal sí usa abs() (3865), así que las dos cifras de la misma pantalla no cuadran entre sí.
  - *Fix*: Envolver la cantidad en Abs() y filtrar los despachos por el mismo período/temporada/proveedor que las compras (o, si no es posible, etiquetar explícitamente el bloque como 'Distribución — año completo, sin filtros' para que nadie lo lea como filtrado).
  - *Ref*: `views_modulo_compras.py:3811-3831 vs :3865`
- [media] **Traspasos legacy invisibles en todo el bloque de distribución** — Todas las funciones de despacho filtran únicamente concepto='TRASPASO_SALIDA'/'TRASPASO_ENTRADA' y varias exigen sucursal_destino__isnull=False. Los traspasos migrados del legacy viven en TRASPASO_SUCURSAL / TRASPASO_BODEGA / TRASPASO_VITRINA (constants_kardex.CONCEPTOS_TRASPASO_LEGACY) y llegaron en una sola pierna con destino NULL. Todo ese histórico no aparece: los despachos salen subestimados y el 'stock en centro de distribución' sobreestimado.
  - *Fix*: Usar CONCEPTOS_TRASPASO_SALIDA + CONCEPTOS_TRASPASO_LEGACY desde app/constants_kardex.py (el propio archivo dice que ningún reporte debe declarar listas inline) y resolver la sucursal con COALESCE(sucursal_destino, sucursal_origen) según el signo de la cantidad.
  - *Ref*: `views_modulo_compras.py:3811-3815, :3846-3850, :3907-3911, :4106-4110 vs app/constants_kardex.py:33-40`
- [media] **No excluye compras marcadas es_historica -> el Cumplimiento General está artificialmente bajo** — Compras.es_historica documenta 'cargada retroactivamente, solo para reportes (no toca stock)'. Esas órdenes suman unidades_esperadas pero nunca generan Productos_Recepcionados, así que arrastran hacia abajo el KPI 'Cumplimiento Recepciones' y disparan la alerta automática 'Cumplimiento Bajo (<80%)' y 'Recepciones Pendientes'. Ningún dashboard del proyecto filtra este flag (sólo views_modulo_reportes.py:6104 lo usa, y sobre recepciones).
  - *Fix*: Añadir .exclude(es_historica=True) al queryset base del dashboard, o mejor: mostrarlas en una tarjeta aparte ('inversión histórica cargada, sin recepción esperada') para no perder el monto.
  - *Ref*: `app/models/compras.py:66-69; ausencia de es_historica en views_modulo_compras.py (grep sin resultados)`
- [media] **La página y su API quedan fuera del control de permisos fino** — URL_PERMISO_MAP mapea '/app/verDashboardCompras/' -> 'dashboard_compras_estrategico', pero el menú y el Dashboard Home enlazan a '/app/verDashboardComprasMejorado/', que NO es substring de la clave (la barra final lo impide) y no está mapeado. Tampoco lo está '/app/dashboard_compras_mejorado_api/'. El middleware es una lista de opt-in: lo que no está mapeado pasa. Cualquier usuario autenticado (vendedor incluido) puede abrir la URL o pedir el JSON y ver costos de compra, márgenes del CD y precios de proveedor. Es exactamente el mismo patrón que la sesión paralela acaba de documentar y tapar para los reportes (comentario nuevo en middleware_permisos.py:89-95), pero los dashboards quedaron fuera de ese barrido.
  - *Fix*: Agregar a URL_PERMISO_MAP: '/app/verDashboardComprasMejorado/' y '/app/dashboard_compras_mejorado_api/' -> 'dashboard_compras_estrategico' (y de paso '/app/exportar_dashboard_compras/').
  - *Ref*: `app/middleware_permisos.py:19 y :89-95; app/urls.py:487-488; layout/menu.html:2020`
- [media] **Las tarjetas 'Inversión Total' y 'Compras no inventariables' no son comparables** — Están una al lado de la otra pero: (a) Inversión Total es costo NETO y global a todas las empresas; Compras no inventariables es monto_con_iva (bruto) y filtrado por request.session['idEmpresaActual']; (b) ese session id se usa sin validar contra EmpresaUser(status=True), contra la regla del proyecto; (c) el bucket por concepto no distingue tipo_documento, así que una NC de compra por concepto sumaría en vez de restar.
  - *Fix*: Resolver la empresa con EmpresaUser(user=request.user, status=True); expresar ambas tarjetas en la misma base (neto); y separar/negar las notas de crédito dentro del bucket por concepto.
  - *Ref*: `views_modulo_compras.py:3035-3048 vs :3124-3126`
- [media] **Rendimiento: N+1 masivo e iteración en Python sobre tablas grandes** — Ver campo 'rendimiento'. El caso más grave es calcular_comparativa_costos_cd_vs_sucursales, que hace `for lote in LoteProducto.objects.filter(sucursal=..., fecha_ingreso__year=anio)` por cada una de 10 sucursales, sumando costo_unitario*cantidad_inicial en Python: son cientos de miles de objetos instanciados por carga de página.
  - *Fix*: Reemplazar los loops por agregados agrupados (values('empresa').annotate(...)) — el patrón correcto ya está implementado en el dashboard estratégico para cumplimiento por proveedor (views_modulo_compras.py:1100-1119) y en calcular_compras_por_categoria_marca (3546). Para los lotes: un solo aggregate con Sum(F('costo_unitario')*F('cantidad_inicial')) agrupado por sucursal.
  - *Ref*: `views_modulo_compras.py:3233-3260, :3437-3480, :3507-3543, :3641-3689, :4197-4231`
- [media] **La línea 'Ventas' del gráfico Inversión vs Ventas no está saneada** — Ticket.objects.filter(created_at__year, created_at__month, estado='PAGADO').Sum('total'): usa created_at (correcto), pero no filtra modulo_origen -> incluye tickets de CAMBIO_DEVOLUCION y ECOMMERCE junto con la venta de público; no acota empresa ni sucursal; y compara ventas a PVP con IVA contra inversión a costo neto en el mismo eje, lo que hace que la 'brecha' visual no signifique nada.
  - *Fix*: Filtrar modulo_origen__in=['VENTA_PUBLICO','POS','VENTA_MAYORISTA'] (o el equivalente por tipo_transaccion en Dte) y graficar la venta a NETO (total/1.19) para que las dos series compartan base. Etiquetar el eje explícitamente.
  - *Ref*: `views_modulo_compras.py:3213-3220 y :4002-4010; app/models/ventas.py:220-226 (modulo_origen)`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Margen bruto realizado de la compra (no teórico) | Σ (precio_venta_neto − costo_fifo) × unidades vendidas de los SKU de la OC, en los 90/180 días posteriores a la recepción. Contra Σ costo × unidades recepcionadas. | Compras_Producto_Talla.producto_talla -> Ticket_Productos (idTicket, stock, costo_fifo) filtrando Ticket.created_at; Productos_Recepcionados.stockArribado | Es el ROI que hoy la tarjeta finge mostrar. Responde '¿esta compra se pagó sola?' en vez de '¿qué margen le puse en la lista de precios?'. El enlace OC->producto ya existe (producto_talla) y el dashboard ya reporta su cobertura (pct_enlace). | medio |
| Sell-through de la compra a 90 días | unidades vendidas de los SKU de la OC en 90 días desde la primera recepción / unidades recepcionadas de esa OC × 100 | Compras_Producto_Talla.producto_talla, Productos_Recepcionados.fecha, Ticket_Productos + Ticket.created_at | Es el discriminador que la auditoría de reportes ya validó como útil (línea base ST90 ≈12%). Permite rankear proveedores y temporadas por resultado, no por promesa. Hoy el dashboard sólo tiene sell-through por MARCA global (3616), no por orden de compra. | medio |
| Capital inmovilizado de lo comprado y su antigüedad | Σ stock actual × costo, de los SKU originados en las OC del período, segmentado en 0-90 / 91-180 / 181-365 / >365 días desde la recepción | Compras_Producto_Talla.producto_talla -> Producto_Talla.stock, Productos_Recepcionados.fecha, Producto.costo | Cierra el ciclo: inversión -> venta -> lo que quedó atrapado. Es la métrica que la auditoría de dashboards FIFO ya mostró que sí discrimina ($794M >1 año). Hoy el dashboard te dice cuánto compraste pero nunca cuánto de eso sigue en bodega. | medio |
| OTIF por proveedor (On Time In Full) | % de OC del proveedor recibidas al 100% de unidades Y dentro de la fecha_entrega_estimada. Complementarlo con lead time real = fecha primera recepción − fecha_envio_proveedor, contra Empresa.lead_time_dias. | Compras.fecha_envio_proveedor / fecha_entrega_estimada / fecha_entrega_real (ya existen en el modelo), Productos_Recepcionados.fecha | El 'Cumplimiento' actual sólo mide cantidad, no puntualidad: un proveedor que entrega el 100% con 3 meses de atraso sale con 100%. Los campos de fecha ya están en Compras y hoy NO se usan en ningún dashboard. Además alimenta el lead_time del motor de predicción. | bajo |
| Cobertura de la inversión clasificable (subir pct_enlace a tarjeta) | inversión de líneas de OC con producto_talla enlazado / inversión total × 100 | calcular_compras_por_categoria_marca ya lo calcula (views_modulo_compras.py:3632) | Está calculado pero escondido en un badge de una tabla secundaria. Si el enlace OC->catálogo es del 40%, TODOS los cortes por categoría y marca del dashboard son sobre menos de la mitad del dinero, y hoy nadie lo ve. Es una tarjeta de honestidad del dato. | bajo |
| Gasto no inventariable como % del gasto total de compras | monto neto de Dte(tipo_transaccion='COMPRA', es_por_concepto=True) / (ese monto + inversión inventariable) × 100 | Dte.es_por_concepto + Compras_Producto (ya se consultan ambos) | Ya se trae el monto pero como cifra suelta y en otra base (bruta). Como porcentaje del gasto revela cuánto del dinero de 'compras' no es mercadería (fletes, servicios, arriendos) — hoy invisible en la gestión de compras. | bajo |

### Dashboard de Compras Estratégico (RETIRADO hoy, con restos de código vivo) — `REDUNDANTE`

- **Ruta**: URL: /app/dashboard_compras_estrategico/ -> desde HOY (2026-07-25) es un RedirectView a verDashboardComprasMejorado (app/urls.py:482-484). Vista original (ahora INALCANZABLE): C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\views_modulo_compras.py:1006 dashboard_compras_estrategico (~360 líneas, devolvía JSON, no HTML). Duplicado muerto adicional en app/views.py:23136. Template HUÉRFANO: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\templates\vistas\modulo_dashboards\dashboard_compras_estrategico.html (1168 líneas) — sólo lo renderiza app/views.py:23579 verDashboardCompras, que está sombreado en urls.py por la versión de views_modulo_compras (que renderiza el template MEJORADO).
- **Audiencia**: Era la misma que el mejorado (jefatura de compras). Hoy: nadie — la ruta redirige.
- **Muestra**: Ya no muestra nada. Su contenido era: 4 tarjetas (Cumplimiento General, ROI Promedio, Rotación de Inventario, Precisión de Pronóstico), cumplimiento por proveedor, ROI por temporada (llamando a la MISMA función del mejorado), tabla de rendimiento por compra (10 filas), alertas, recomendaciones, compras por mes, top 5 proveedores y top 10 productos.
- **Filtros**: Ofrecía Año, Temporada, Proveedor y Responsable. El Año estaba HARDCODEADO a 2023/2024/2025 en el template (líneas 137-139): 2026 no existía en el selector — exactamente el bug que ya se corrigió en el mejorado y que nunca se corrigió aquí. El filtro Responsable es el único que el mejorado no tiene, y su selector caía a datos de ejemplo inventados ('Juan Pérez', 'María González', 'Carlos López') cuando la API de compras devolvía vacío.
- **Fidelidad**: Compartía los mismos defectos del mejorado (mismo ROI markup con IVA, mismo Cumplimiento sin excluir es_historica) y agregaba dos KPIs directamente inventados. Sus tendencias contra el año anterior no excluían ELIMINADA/CANCELADA y filtraban temporada sólo por texto libre (temporada__icontains), ignorando temporada_familia, así que la comparativa YoY era asimétrica respecto del año actual.
- **Rendimiento**: Irrelevante ya (inalcanzable). Tenía el bloque de cumplimiento por proveedor BIEN optimizado (2 queries agrupadas, líneas 1100-1119) — ironía: es el único lugar del módulo donde ese N+1 está resuelto, y es el código que quedó muerto. Vale la pena portar ese patrón al mejorado antes de borrarlo.

**Problemas:**

- [alta] **Confirmado: es un subconjunto estricto del Dashboard de Compras Mejorado** — Cumplimiento, ROI, inversión, valor de venta esperado, top proveedores, top productos, alertas y tabla de rendimiento se calculan con las MISMAS fórmulas que el mejorado, y el ROI por temporada literalmente invoca calcular_roi_temporadas_mejorado. No aporta un solo indicador que el mejorado no tenga, salvo el filtro por Responsable.
  - *Fix*: Ya está redirigido. Falta cerrar la retirada: borrar views_modulo_compras.py:1006-1369 (inalcanzable), el duplicado app/views.py:23136 y app/views.py:23575, y el template dashboard_compras_estrategico.html. ANTES de borrar, portar al mejorado: (a) el patrón de 2 queries agrupadas de cumplimiento por proveedor (1100-1119) y (b) el filtro por Responsable si el usuario lo quiere.
  - *Ref*: `views_modulo_compras.py:1135 (llama a calcular_roi_temporadas_mejorado); app/urls.py:478-484 (comentario de retiro de hoy)`
- [alta] **2 de sus 4 KPIs eran inventados o duplicados** — 'Rotación de Inventario' = productos_con_recepcion / total_productos — eso no es rotación (no interviene ninguna venta ni inventario promedio); es la cobertura de recepción del catálogo de la OC. 'Precisión de Pronóstico' es literalmente `precision_pronostico = cumplimiento_general` (con el comentario '# Similar al cumplimiento'): la misma cifra de la tarjeta de al lado con otro nombre. Peor: alimentaban recomendaciones automáticas ('optimizar gestión de inventario para aumentar rotación' si rotación <0.5).
  - *Fix*: No reimplementar ninguno de los dos en el mejorado. La rotación real requiere ventas (ver KPIs propuestos del mejorado); la precisión del pronóstico pertenece al dashboard de Predicción, no al de Compras.
  - *Ref*: `views_modulo_compras.py:1090-1094`
- [media] **Años hardcodeados en el selector (2026 no existía)** — El template fija <option>2025/2024/2023</option> a mano. Es el mismo bug que la auditoría anterior detectó y arregló en el dashboard de compras mejorado (que ahora genera los años desde new Date().getFullYear() hacia atrás hasta 2023), y que aquí nunca se tocó.
  - *Fix*: Se resuelve solo al borrar el template. Si se reutilizara algo, copiar el bucle de dashboard_compras_mejorado.html:1287-1292.
  - *Ref*: `dashboard_compras_estrategico.html:136-140 vs dashboard_compras_mejorado.html:1287-1292`
- [baja] **Datos de ejemplo inventados en producción** — Cuando /app/obtener_compras/ no devolvía datos, el front rellenaba el selector de Responsable con nombres ficticios ('Juan Pérez', 'María González', 'Carlos López'), y existía un banner #alertaDatosEjemplo. Un usuario podía filtrar por un responsable que no existe y obtener una pantalla en cero sin entender por qué.
  - *Fix*: Se elimina con el template. Regla general: ningún dashboard debe fabricar opciones de filtro; mostrar 'sin datos' es la respuesta correcta.
  - *Ref*: `dashboard_compras_estrategico.html:530-533 y :578-582`

### Dashboard de Predicción de Compras — `MEJORABLE`

- **Ruta**: URL: /app/prediccion/ + 15 endpoints bajo /app/api/prediccion/*. Vista: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\views_prediccion_compras.py:78 dashboard_prediccion (1636 líneas en total). Template: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\templates\vistas\modulo_dashboards\prediccion_compras_dashboard.html (1692 líneas). Motor batch: app/services/prediccion_compras.py (comando `calcular_predicciones`). Menú: layout/menu.html:2393 (permiso 'prediccion_compras'), bajo Compras.
- **Audiencia**: Comprador / planner. Decide qué SKU y qué talla reponer ya, en qué marcas conviene invertir la próxima temporada y a qué proveedor emitir la OC.
- **Muestra**: 6 tarjetas (Alertas Críticas, Alertas Velocidad, Quiebres de Talle, Sugerencias Pendientes, Unidades a Pedir, Productos Clase A) + un bloque 'Plan de compra' (unidades, costo estimado, productos, talles, líneas urgentes + desglose por proveedor) + 6 pestañas: Urgente (alertas de velocidad y quiebre fusionadas), Por Marca (ranking por score de rotación + scatter velocidad vs volumen + drill-down de artículos), Por Proveedor (ranking de OC), Sugerencias (tabla con aprobación masiva), ABC-XYZ (clasificación) y Gráficos (tendencia semanal 12 sem, 'Precisión del Modelo', 'Comparativa vs Temporada Anterior', heatmap de alertas por categoría). Modal de configuración del motor.
- **Filtros**: Contexto de sucursal implícito desde la sesión (si la sucursal activa es CD -> vista agregada de todas las VENDEDORA/MIXTA; si es tienda -> sólo esa). Por pestaña: Urgente tiene urgencia y tipo (client-side); Sugerencias tiene origen (server-side, OK) y búsqueda (client-side); Por Marca tiene categoría (server-side, OK), temporada y año (inputs de TEXTO LIBRE); Por Proveedor tiene categoría, temporada y año. FALLAS REALES: (1) en Por Proveedor el filtro de AÑO es un no-op — el queryset principal no filtra por fecha; (2) en Por Proveedor el filtro de CATEGORÍA se envía desde el front pero el backend nunca lo lee -> no-op silencioso; (3) el contexto de sucursal SÓLO se aplica a las tarjetas y a las pestañas Urgente/Sugerencias/ABC — las pestañas Por Marca, Por Proveedor y el gráfico de Precisión NO lo aplican, así que el encabezado dice 'Sucursal NICK1' y las tablas muestran la cadena completa.
- **Fidelidad**: Fechas: usa Movimientos_Producto.fecha (correcto, no es auto_now) y conceptos de venta explícitos ['VENTA_PUBLICO','VENTA_MAYORISTA','VENTA_DIRECTA','VENTA_TICKET'] — pero inline, no desde constants_kardex (que además incluye 'VENTA'). No cuenta el genérico 'VENTA', lo cual está BIEN. Doble conteo / NC de venta: no aplica (trabaja sobre kardex, no sobre Dte). Exclusiones: aplica excluir_de_analitica en sugerencias, clasificación y alertas de velocidad, pero NO en AlertaQuiebreTalle (línea 375) — inconsistencia. Compras ELIMINADAS: sí las excluye (estado__in=['ACTIVA','COMPLETADA']) en el cruce con OC. Scoping: usa request.session['idSucursalActual'] SIN validar contra EmpresaUser(status=True) — viola la regla del proyecto; y no hay ningún filtro de empresa: la vista 'agregada' suma TODAS las sucursales vendedoras del sistema, incluidas las de otras empresas. Género: usa atributo3 correctamente (líneas 977, 1000, 1017). KPI engañoso: 'Precisión del Modelo' no mide precisión (es la auto-confianza declarada por el motor). 'Comparativa vs Temporada Anterior' no compara períodos (compara añadas de producto). El proveedor sugerido del plan se resuelve ordenando por un campo auto_now.
- **Rendimiento**: Malo en la portada, aceptable en las pestañas. api_prediccion_resumen (que alimenta las 6 tarjetas y el plan de compra, y se carga al abrir la página) itera TODAS las SugerenciaCompra pendientes sin límite y, dentro del loop, ejecuta una consulta a Compras_Producto por cada sugerencia para adivinar el proveedor: N+1 puro sobre potencialmente miles de filas. Las tabs Marca/Categoría hacen ~7 queries por marca dentro de un loop (líneas 960-1087), aceptable con pocas marcas pero degrada linealmente. El resto ya tiene los N+1 resueltos con conditional aggregation (comentarios '# fix N+1' en 879 y 979).

**Problemas:**

- [alta] **La pestaña 'Por Proveedor' ignora el año: suma TODAS las temporadas de TODOS los años** — api_prediccion_analisis_proveedor arma `ocs_base = Compras.objects.filter(temporada__iexact=temporada, estado__in=['ACTIVA','COMPLETADA'])` — sin fecha__year. El parámetro `anio` se acepta, se devuelve en el JSON y se usa SÓLO para calcular la columna 'vs Año ant.' (línea 1393). Resultado: 'unidades ordenadas' es el acumulado histórico completo del proveedor y la variación YoY compara todo-el-histórico contra un solo año -> siempre da un porcentaje enorme y sin sentido. Encima, la descripción del panel dice 'Agrupa las sugerencias de compra pendientes por proveedor', pero los datos vienen de las órdenes de compra históricas: el texto no corresponde a la tabla.
  - *Fix*: Añadir fecha__year=anio a ocs_base; leer y aplicar el parámetro `categoria` (el front ya lo manda) con _q_categoria_canonical(prefix='compra_producto__compras__...' o vía Compras_Producto_Talla); y corregir el texto del panel en el template.
  - *Ref*: `views_prediccion_compras.py:1358-1361 (sin year) y :1391-1396 (year sólo para YoY); template prediccion_compras_dashboard.html:1044-1050 (manda categoria) y :648-650 (texto incorrecto)`
- [alta] **El proveedor del plan de compra se elige ordenando por un campo auto_now** — Para asignar proveedor a cada sugerencia: `Compras_Producto.objects.filter(nombre=producto.articulo).order_by('-fecha').first()`. Compras_Producto.fecha es `models.DateField(auto_now=True)` — se reescribe cada vez que la fila se guarda, igual que Ticket.fecha. El 'último proveedor' es en realidad el último registro editado, no la última compra. Además cruza Producto.articulo (que es el CÓDIGO del artículo) contra Compras_Producto.nombre (texto libre escrito en la OC): match frágil, y basta una diferencia de mayúsculas/espacios para caer a 'Sin proveedor asignado'.
  - *Fix*: Ordenar por `compras__fecha` (la fecha real de la orden, editable y confiable) en vez de por Compras_Producto.fecha; y hacer el match por la FK real Compras_Producto_Talla.producto_talla__producto_id en vez de por texto (ese enlace ya existe y ya se usa en el dashboard de compras). Como fallback, comparar con normalizar_articulo() e iexact.
  - *Ref*: `views_prediccion_compras.py:138-143; app/models/compras.py:89 (fecha = DateField(auto_now=True))`
- [alta] **El endpoint que dibuja la portada tiene un N+1 sin límite** — api_prediccion_resumen recorre el queryset completo de sugerencias no aprobadas (sin slice) y dentro del for ejecuta la consulta de proveedor descrita arriba. Con unos miles de SKU con sugerencia pendiente son miles de round-trips a Postgres sólo para pintar 6 tarjetas y una tabla de 8 filas de proveedores.
  - *Fix*: Resolver el proveedor en UNA query previa: un values('producto_id').annotate(...) o un Subquery sobre Compras_Producto_Talla agrupado, y luego un dict lookup en el loop. Alternativamente, precalcular proveedor_sugerido en el modelo SugerenciaCompra durante el pipeline batch.
  - *Ref*: `views_prediccion_compras.py:100-153`
- [media] **El scoping por sucursal se aplica a medias y no se valida contra EmpresaUser** — _get_sucursal_context lee request.session['idSucursalActual'] y hace Sucursal.objects.get(id=...) sin comprobar que el usuario tenga acceso a esa sucursal vía EmpresaUser(status=True). Y las pestañas Por Marca (api_prediccion_analisis_categoria/marca) y Por Proveedor, además del gráfico 'Precisión del Modelo', no reciben ningún filtro de sucursal. El encabezado del dashboard imprime 'sucursal_nombre' (línea 82), así que el usuario cree que toda la pantalla está acotada a su tienda cuando la mitad no lo está.
  - *Fix*: Resolver la sucursal contra EmpresaUser(user=request.user, status=True) y propagar q_suc a los tres endpoints faltantes; o, si el análisis por marca es deliberadamente global, rotularlo en la UI ('ranking a nivel cadena').
  - *Ref*: `views_prediccion_compras.py:45-60 (sin EmpresaUser), :914 y :1343 y :653-661 (sin q_suc), template :82`
- [media] **'Precisión del Modelo por Método' no mide precisión** — El docstring anuncia 'MAPE por método' pero el cálculo es `Avg('confianza')` de PrediccionDemanda: es el nivel de confianza que el propio motor se auto-asigna, no el error contra la venta real. Un modelo sistemáticamente equivocado con confianza alta sale como el más 'preciso'. La etiqueta del eje en el gráfico dice 'Confianza promedio %', pero el título de la tarjeta dice 'Precisión del Modelo', que es lo que el usuario lee.
  - *Fix*: Renombrar a 'Confianza declarada por método' mientras no exista backtesting; e implementar el MAPE real (ver KPIs propuestos).
  - *Ref*: `views_prediccion_compras.py:619-669 (docstring línea 623 vs Avg('confianza') línea 659); template :1288-1291 y sección 'Precision del Modelo por Metodo'`
- [media] **'Comparativa vs Temporada Anterior' compara añadas de producto, no períodos** — _ventas_por_cat filtra `ProductoTalla__producto__anio_temporada=anio` sin ninguna restricción de fecha. Es decir: suma toda la venta histórica (desde siempre) de los productos etiquetados como temporada 2026 y la compara con toda la venta histórica de los de 2025. Los productos de 2025 llevan un año más vendiendo, así que la barra 'anterior' gana casi siempre, y el gráfico se lee como una caída de ventas que no existe.
  - *Fix*: Acotar por fecha del movimiento a ventanas equivalentes (ej. mismo rango de meses del año actual vs del anterior) o dejar el corte por añada pero rotularlo 'venta acumulada por añada de producto', que es otra pregunta legítima.
  - *Ref*: `views_prediccion_compras.py:675-696`
- [media] **'Unidades compradas año anterior' queda casi siempre en 0** — El cruce con las OC del año anterior usa `compra_producto__compras__temporada__iexact=temporada`, donde `temporada` viene del motor en minúscula ('verano') mientras Compras.temporada es texto libre que en la práctica trae cosas como 'VERANO 2025' o 'Invierno'. Además ignora los campos normalizados temporada_familia / temporada_anio que existen justamente para esto. Consecuencia: la columna 'vs Año ant.' del ranking de marcas sale vacía (variacion_pct=None) para casi todo, sin decir por qué.
  - *Fix*: Filtrar por temporada_familia + temporada_anio (con el fold de acentos que ya implementa views_modulo_compras.py:2928-2933) y mantener temporada__icontains sólo como fallback legacy.
  - *Ref*: `views_prediccion_compras.py:1041-1053; app/models/compras.py:56-65 (campos normalizados)`
- [media] **No hay indicador de frescura del motor** — El dashboard consume tablas precalculadas (PrediccionDemanda, ClasificacionABC, VelocidadHistorica, SugerenciaCompra, AlertaVelocidad). Si `calcular_predicciones` no corre por semanas, la pantalla se ve exactamente igual: mismas alertas 'CRÍTICAS', mismas sugerencias, cero señal de que los números son viejos. El comprador puede emitir una OC basada en un stock de hace un mes.
  - *Fix*: Tarjeta con la fecha del último pipeline (max de fecha_generacion / fecha_deteccion) y badge rojo si supera N días. Es una query trivial y probablemente el mayor retorno por esfuerzo de toda la pantalla.
  - *Ref*: `views_prediccion_compras.py:600-610 (recalcular manual) y ausencia de cualquier lectura de fecha de pipeline en api_prediccion_resumen`
- [baja] **Inconsistencias menores de filtrado y UX de filtros** — (a) AlertaQuiebreTalle no excluye productos con excluir_de_analitica mientras AlertaVelocidad sí -> las dos tarjetas de alertas no cubren el mismo universo. (b) Temporada y Año son inputs de texto/número libres: escribir 'Verano' o 'VERANO 2026' devuelve una tabla vacía sin explicar que el motor espera exactamente 'verano'. (c) Los endpoints /app/api/prediccion/* no están en URL_PERMISO_MAP (sólo lo está la página /app/prediccion/), así que el JSON con sugerencias, costos y clasificación ABC es accesible para cualquier usuario autenticado.
  - *Fix*: (a) añadir articulo__excluir_de_analitica=False en :375. (b) reemplazar los inputs por selects poblados desde _prediccion_default_temporada_anio / valores distintos existentes. (c) mapear '/app/api/prediccion/' -> 'prediccion_compras' en middleware_permisos.py.
  - *Ref*: `views_prediccion_compras.py:372-379 vs :323-325; template :615-617 y :652-654; app/middleware_permisos.py:24`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Error real de la predicción (MAPE / sesgo) por método y por clase ABC | MAPE = promedio( /unidades_predichas − unidades_vendidas_reales/ / unidades_vendidas_reales ) sobre temporadas ya cerradas. Acompañarlo del sesgo: Σ(predicho − real)/Σreal, con signo, para saber si el motor sobre o sub-pide. | PrediccionDemanda (unidades_predichas, temporada, anio, metodo_usado) vs Movimientos_Producto con CONCEPTOS_VENTA en la ventana de esa temporada | Es el único KPI que responde '¿le puedo creer a este motor?'. Hoy la pantalla presenta la auto-confianza como si fuera precisión. El sesgo separado importa más que el MAPE: un motor que se equivoca siempre hacia arriba genera sobre-stock, que es exactamente el problema estructural que la auditoría ya documentó (cobertura 641 días). | alto |
| Frescura y cobertura del motor | Días desde el último recálculo (max fecha_generacion) + % de SKU vendedores con predicción vigente sobre el total de SKU con stock > 0 | PrediccionDemanda.fecha_generacion, SugerenciaCompra.fecha_generacion, Producto_Talla | Sin esto no se sabe si la pantalla está viva o congelada, ni sobre qué porción del catálogo opina el motor. Es la diferencia entre 'no hay alertas' y 'el motor no corrió'. Costo: dos aggregates. | bajo |
| Plan de compra valorizado a costo Y a venta, con margen implícito | Σ unidades_a_pedir × Producto.costo (ya existe) y Σ unidades_a_pedir × precioventa/1.19; margen implícito = (venta_neta − costo)/venta_neta | SugerenciaCompra.unidades_a_pedir, Producto.costo, Producto.precioventa | Hoy sólo se muestra 'Costo estimado'. El comprador necesita saber qué margen trae el plan antes de aprobarlo, y el backend YA calcula un monto a precio de venta (línea 1032-1038) que nunca se muestra en el template — es dato muerto en el JSON. | bajo |
| Tasa de conversión sugerencia -> orden de compra | unidades aprobadas / unidades sugeridas, y % de OC del período que se originaron en el motor (Compras.generada_por_sistema=True) | SugerenciaCompra.aprobada, Compras.generada_por_sistema (el campo ya existe en el modelo) | Mide la adopción real del motor. Si el comprador aprueba el 5% de lo sugerido, el problema no es el dashboard sino la calidad de las sugerencias, y hoy nadie lo puede ver. El campo generada_por_sistema existe y no se usa en ningún dashboard. | bajo |
| Venta perdida estimada por quiebre de talla ($) | Σ AlertaQuiebreTalle.unidades_perdidas_semana_estimadas × precio de venta neto del artículo, agregado por marca y categoría | AlertaQuiebreTalle (el campo ya se calcula y se muestra por fila) + Producto.precioventa | Convierte una lista de alertas en una cifra de dinero que prioriza sola. Es la contracara del capital inmovilizado: hoy el sistema mide muy bien el exceso y casi nada la falta. El dato bruto ya está calculado por fila y sólo se muestra en la tabla, sin totalizar. | bajo |

### Inteligencia de Compra (NO es un dashboard: es un reporte del módulo Reportes) — `UTIL`

- **Ruta**: URL: /app/reportes/inteligencia-compra/ + API /app/api/inteligencia-compra/. Vista: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\views_inteligencia_compra.py:142 ver_inteligencia_compra y :163 obtener_inteligencia_compra. Template: C:\Users\cromo\Documents\DjangoProyects\SistemaRetailMind\retailmind\app\templates\vistas\modulo_reportes\inteligencia_compra.html. Menú: layout/menu.html:2602, bajo REPORTES (no bajo Dashboard).
- **Audiencia**: Comprador por marca. Decide cuántos pares comprar de una marca y con qué curva de tallas.
- **Muestra**: Selector de marca + tienda. Venta pública por año (unidades y monto), venta por tienda, distribución en bodegas/CD, stock actual por sucursal, serie mensual de 36 meses, pronóstico 12 meses (seasonal-naive con tendencia acotada, TTM y YoY), sell-through anual (vendido/ingresado), velocidad 90 días vs mismo período del año anterior, curva de tallas EU normalizada con flags (quebrada / sobre-stock / gap / ok), y una recomendación de compra con veredicto textual por cobertura en meses + curva de compra sugerida por talla.
- **Filtros**: Marca (obligatorio) y Sucursal ('todas' o una tienda). AMBOS se aplican server-side de verdad. El sucursal_id recibido SE VALIDA: sólo se acepta si está en tiendas_ids, que sale de EmpresaUser(status=True) — es el único de los cuatro que cumple la regla de scoping del proyecto.
- **Fidelidad**: La mejor del grupo, por bastante margen. Usa CONCEPTOS_VENTA y CONCEPTOS_ABASTECIMIENTO desde constants_kardex (no listas inline); usa Movimientos_Producto.fecha (correcto); suma Σ|cantidad| para ser robusto a los signos de la migración; distingue tienda vendedora de bodega/CD por es_centro_distribucion (regla correcta según la auditoría de sucursales); excluye excluir_de_analitica; resuelve las sucursales del usuario contra EmpresaUser(status=True); marca = atributo1 (correcto). Debilidades: el monto de venta se calcula |cantidad| × Movimientos_Producto.precio (precio grabado en el kardex, con IVA) y no descuenta devoluciones/NC — CONCEPTOS_VENTA no incluye los reingresos, así que una venta luego devuelta sigue contando como vendida; el pronóstico no expone intervalo de confianza.
- **Rendimiento**: Bueno. Agregados en DB con Sum/Abs y ExtractYear/ExtractMonth, sin loops de queries. El único punto pesado es la serie mensual de 36 meses, que se arma con una sola consulta agrupada y se rellena en Python. Está acotado a una marca por vez, lo que lo mantiene barato.

**Problemas:**

- [media] **Solapamiento de tres motores para la misma pregunta** — '¿Cuánto compro de esta marca?' se responde hoy en TRES lugares con lógicas distintas: (1) aquí, con seasonal-naive + cobertura en meses + curva de tallas; (2) en el Dashboard de Predicción, pestaña Por Marca, con score de rotación (sell-through / semanas mediana × factor ABC) y sugerencias del motor ABC-XYZ; (3) en el Dashboard de Compras Mejorado, tabla 'Inversión por marca' con sell-through 90d. Los tres pueden dar recomendaciones contradictorias para la misma marca y ninguno referencia a los otros.
  - *Fix*: Definir cuál es la fuente de verdad por marca (recomiendo ésta: es la que respeta el scoping y separa tienda de bodega) y en las otras dos pantallas dejar sólo el número más un enlace 'ver análisis completo' hacia acá. Como mínimo, unificar la definición de sell-through: aquí es anual vendido/ingresado, en compras mejorado es venta90/(venta90+stock) — dos fórmulas distintas con el mismo nombre.
  - *Ref*: `views_inteligencia_compra.py:258-263 (sell-through anual) vs views_modulo_compras.py:3616 (sell_through_90d) vs views_prediccion_compras.py:1090 (score_rotacion)`
- [media] **El monto de venta no neta devoluciones ni IVA** — monto = Σ |cantidad| × Movimientos_Producto.precio. Como CONCEPTOS_VENTA no incluye los conceptos de reingreso (DEVOLUCION_CLIENTE, DEVOLUCION_NC, ANULACION...), una venta posteriormente devuelta sigue sumando al monto y a las unidades. Y el precio del kardex es bruto (IVA incluido), lo que no se advierte en la UI.
  - *Fix*: Restar los reingresos usando CONCEPTOS_REINGRESO de constants_kardex (ya está definido), o al menos mostrar la cifra de devoluciones al lado. Rotular el monto como 'venta bruta (IVA incl.)'.
  - *Ref*: `views_inteligencia_compra.py:~209-212 (monto=Sum(Abs(cantidad)*precio)); app/constants_kardex.py:43-51 (CONCEPTOS_REINGRESO, no usado aquí)`
- [baja] **El pronóstico no comunica su incertidumbre** — _forecast usa índices estacionales de años COMPLETOS previos; si una marca tiene menos de 2 años completos de historia cae a índices planos de 1/12 y el 'forward_annual' pasa a ser esencialmente el TTM. La recomendación de compra ('comprar N pares') y el veredicto de cobertura salen con la misma contundencia visual en ambos casos.
  - *Fix*: Devolver y mostrar cuántos años completos alimentaron la estacionalidad, y degradar el veredicto a 'estimación preliminar' cuando sean 0 o 1.
  - *Ref*: `views_inteligencia_compra.py:89-104 (shares/seas con fallback 1/12) y :~280-300 (veredicto)`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Margen $ y % realizado por marca | Σ (precio_neto − costo_fifo) × unidades vendidas, y margen % sobre venta neta, por marca y por año | Ticket_Productos (stock, precio, costo_fifo) + Ticket.created_at, o Movimientos_Producto con costo | El reporte responde muy bien cuánto se vende y a qué velocidad, pero no si esa marca deja plata. Dos marcas con la misma rotación y distinto margen exigen decisiones de compra opuestas, y hoy el reporte las trata igual. | medio |
| Capital inmovilizado y aging por marca | Σ stock actual × costo, segmentado por antigüedad del lote (0-90 / 91-180 / 181-365 / >365 días) | Producto_Talla.stock, LoteProducto.fecha_ingreso y costo_unitario | La recomendación actual se basa en cobertura en meses (unidades). Poner el peso en pesos, con antigüedad, es lo que hizo que la auditoría de dashboards FIFO pasara de 'cobertura satura' a un ranking accionable ($490M viejos concentrados en 5 marcas). Encaja natural al lado del veredicto de sobre-stock. | medio |
| Precio promedio de venta efectivo vs precio de lista (profundidad de descuento) | promedio ponderado de Ticket_Productos.precio neto / promedio de Producto.precioventa neto, por marca y por año | Ticket_Productos (precio, precio_original, descuento_unitario, porcentaje_descuento) + Producto.precioventa | Un sell-through alto logrado a punta de liquidación no es la misma señal de compra que uno a precio pleno, y hoy el reporte no los distingue. Los campos de descuento ya existen por línea de ticket y no se usan en ningún dashboard. | medio |

## Dashboards de INVENTARIO y OPERACIONES (FIFO/lotes, despachos, existencias, DTE/documentos, requerimientos, caja/arqueos, fidelización)

### Dashboard FIFO / Lotes — `MEJORABLE`

- **Ruta**: URL /app/dashboard_fifo/ (urls.py:695) → app/views.py:22580 dashboard_fifo; API app/views.py:22601 obtener_datos_dashboard_fifo (urls.py:700); export app/views.py:22989 exportar_dashboard_fifo. Template app/templates/vistas/modulo_dashboards/dashboard_fifo.html. Menú: Dashboard › Dashboard Existencias › FIFO (layout/menu.html:2040-2044).
- **Audiencia**: Control de gestión y jefatura de bodega. Decisión declarada: detectar descalce lotes-vs-stock y priorizar liquidación del capital viejo (>180d / >1 año) antes de recomprar.
- **Muestra**: 8 tarjetas KPI: Valor Total Inventario FIFO, Capital >1 año a costo, Capital 6-12 meses, % Capital Viejo (>180d), Capital Fresco <90d, Productos con Diferencias, Diferencia Total (FIFO vs Sistema) y Antigüedad Promedio Stock. Panel de diferencias (positivas/negativas/cero), bloque de aging del capital en 4 tramos (0-90/90-180/180-365/>365) con unidades y $, tablas 'Capital >180d por Categoría' (taxonomía v1.2 padre›hijo) y 'por Marca' (atributo1), panel de alertas accionables con $ y tabla completa SKU-talla (stock, valor FIFO, costo promedio, diferencia, %, antigüedad, n° lotes) agrupable por artículo, con exportación CSV.
- **Filtros**: Período (hoy/semana/mes/trimestre/semestre/año/todo), fecha desde-hasta, antigüedad (fresco/normal/antiguo/crítico), producto, talla, tipo de diferencia, tramo de stock, orden y 'solo diferencias'. Server-side REAL solo producto/talla y el rango de fecha sobre lotes.fecha_ingreso (views.py:22662-22699). Antigüedad, diferencia y stock se aplican en Python fila a fila (views.py:22760-22791). NO existe filtro de sucursal: se toma request.session['idSucursalActual'] (views.py:22607) sin validar con puede_ver_sucursal(); no hay selector en pantalla, el usuario no sabe qué sucursal está mirando.
- **Fidelidad**: Excluye correctamente excluir_de_analitica (views.py:22679, 22874) y usa el mismo criterio de lote vivo que los helpers FIFO. NO aplica el bug de 'ROI = IVA' del reporte de compras (aquí no se toca monto_con_iva). PERO el KPI insignia 'Diferencia Total (FIFO vs Sistema)' no mide lo que dice: compara Σ(cantidad_disponible × costo_unitario del lote) contra stock_plano × producto.costo (views.py:22729-22732), o sea mezcla el descalce de UNIDADES con el drift de COSTEO (costo histórico del lote vs costo actual del maestro). Tras la reconciliación el descalce de unidades quedó ≈0, pero el drift de costeo nunca será 0 → el dashboard seguirá reportando 'diferencias' masivas irreconciliables y su alerta recomienda correr reconciliar_stock_lotes (views.py:22917), que no las va a arreglar. Además 'Antigüedad Promedio' es un promedio SIN ponderar del lote más antiguo por SKU-talla, 'Rotación estimada'=365/antigüedad no usa ventas y 'Eficiencia FIFO' no mide adherencia FIFO. Confirmado: el dashboard perdió su razón de ser como herramienta de reconciliación; lo único vivo y valioso es el bloque de aging de capital.
- **Rendimiento**: Aceptable tras la optimización de julio (1 query anotada, 600s→~39s; views.py:22671-22688) + 4 agregaciones de aging en DB. Riesgos que quedan: devuelve TODAS las filas SKU-talla en un solo JSON sin paginar y las pinta en un único DOM (dashboard_fifo.html:1662-1690); el orden y 3 filtros se resuelven en Python. OJO: el reporte que tarda ~9,9 min y tumba la conexión NO es este dashboard sino reporte_fifo_general (app/views.py:22532), N+1 puro (obtener_valor_inventario_fifo + obtener_costo_promedio_fifo por cada SKU-talla, views.py:22551-22553), que se invoca desde modulo_existencias/verGestionProductos.html:14547 y :14666.

**Problemas:**

- [alta] **El KPI principal ('Diferencia FIFO vs Sistema') no mide descalce de stock sino drift de costeo** — valor_sistema = producto_talla.stock × producto.costo (costo ACTUAL del maestro) contra valor FIFO = Σ(disponible × costo_unitario del lote). Aunque las unidades cuadren perfecto, cualquier cambio de costo en la ficha genera 'diferencia'. Tras la reconciliación de lotes, este KPI mide ruido de costeo y no un problema de inventario; la alerta asociada manda a correr un comando que no lo corrige.
  - *Fix*: Separar en dos métricas: (1) DESCALCE DE UNIDADES = Σ|stock_plano − Σ cantidad_disponible de lotes| (el verdadero KPI de reconciliación, hoy inexistente); (2) DRIFT DE COSTEO = valor FIFO vs valor a costo maestro, presentado como 'diferencia de valorización' y no como error. Cambiar el texto de la alerta.
  - *Ref*: `app/views.py:22726-22737 (cálculo) y app/views.py:22911-22918 (alerta que sugiere reconciliar_stock_lotes)`
- [alta] **Sin scoping validado: la sucursal sale de la sesión y no se comprueba contra EmpresaUser** — sucursal_id = request.session.get('idSucursalActual') y se usa tal cual en el filtro; no hay puede_ver_sucursal() ni obtener_empresas_usuario(). Tampoco hay selector de sucursal, así que el mismo usuario ve números distintos según cómo quedó su sesión, sin ninguna indicación en pantalla. Si no hay sucursal en sesión, la vista devuelve un JSON crudo en el navegador en vez de una página.
  - *Fix*: Usar app/utils_permisos.py:208 puede_ver_sucursal() + obtener_sucursales_usuario() para poblar un selector de sucursal (y una opción 'consolidado empresa' para quien tenga permiso multi-sucursal), y mostrar en el header la sucursal activa. Reemplazar el JsonResponse de error por un render con mensaje.
  - *Ref*: `app/views.py:22585-22594 y app/views.py:22607-22609; helpers disponibles en app/utils_permisos.py:185-225`
- [media] **'Antigüedad Promedio Stock' y 'Rotación estimada' son métricas engañosas** — antiguedad_promedio es el promedio aritmético de la antigüedad del lote MÁS ANTIGUO de cada SKU-talla, sin ponderar por unidades ni por capital: una talla con 1 par pesa igual que una con 800. Sobre ese número se calcula rotacion_estimada = 365/antigüedad, que no usa ventas ni COGS, y dias_inventario = la misma antigüedad. La tarjeta se pinta como 'Fresco/Normal/Antiguo'.
  - *Fix*: Ponderar por capital: antigüedad_ponderada = Σ(días × cantidad × costo)/Σ(cantidad × costo). Eliminar 'rotación estimada' o reemplazarla por rotación real = COGS del período / inventario promedio a costo.
  - *Ref*: `app/views.py:22743-22757 y app/views.py:22854-22862`
- [media] **'Eficiencia FIFO' no mide FIFO** — eficiencia_fifo = (lotes_frescos + lotes_normales)/total × 100, es decir el % de SKUs cuyo lote más antiguo tiene menos de 180 días. No verifica en ningún momento que los consumos hayan tomado el lote más antiguo primero, que es lo que significaría 'eficiencia FIFO'.
  - *Fix*: Renombrar a '% SKUs con inventario fresco (<180d)' y, si se quiere adherencia FIFO real, medirla sobre los consumos de lote: % de unidades consumidas que salieron del lote más antiguo disponible al momento del movimiento.
  - *Ref*: `app/views.py:22864-22865`
- [media] **Dos universos de datos en la misma pantalla (el aging ignora el filtro de período, el resto no)** — El bloque de aging/capital es a propósito un snapshot completo que ignora el período (comentado en el código), pero el KPI 'Valor Total Inventario FIFO', 'Diferencia Total' y la tabla SÍ respetan el período filtrando lotes por fecha_ingreso. Al elegir 'Últimos 30 días' el valor de inventario se desploma (solo cuenta lotes ingresados en 30 días) mientras las tarjetas de capital viejo no cambian: parece un error de datos.
  - *Fix*: Etiquetar visualmente cada bloque ('Snapshot de inventario' vs 'Ingresos del período') o quitar el filtro de período del KPI de valorización. El período debería filtrar ANÁLISIS DE INGRESOS, nunca la foto de inventario.
  - *Ref*: `app/views.py:22665-22669 (filtro por fecha_ingreso) vs app/views.py:22867-22874 (snapshot que lo ignora)`
- [media] **El panel 'Análisis de diferencias / distribución de antigüedad' no coincide con la tabla filtrada** — Los contadores lotes_frescos/normales/antiguos/criticos y total_lotes_analizados se incrementan ANTES de aplicar filtro_antiguedad (que hace `continue`), mientras diferencias_positivas/negativas, stock_bajo/medio/alto y los totales se calculan DESPUÉS. Con el filtro de antigüedad activo, la distribución mostrada corresponde al universo completo y la tabla a un subconjunto.
  - *Fix*: Calcular todos los contadores después de aplicar todos los filtros, o exponer dos bloques explícitos ('universo total' vs 'selección actual').
  - *Ref*: `app/views.py:22746-22757 (contadores) vs app/views.py:22759-22794 (filtros con continue)`
- [media] **Sin paginación: devuelve y renderiza todo el catálogo SKU-talla de la sucursal** — productos_data se serializa completo en el JSON y el front lo pinta en una sola tabla agrupada. En un CD con decenas de miles de SKU-talla el payload y el render son el cuello de botella que queda tras la optimización de queries.
  - *Fix*: Paginar server-side (mismo patrón ya usado en el drill-down de antigüedad del Plan de Liquidación) y dejar el CSV para el detalle completo.
  - *Ref*: `app/views.py:22939-22945 y dashboard_fifo.html:1662-1690`
- [alta] **reporte_fifo_general (el de 9,9 minutos) sigue vivo y sin optimizar** — No lo usa el dashboard, pero es el mismo dominio y es el que tumba la conexión: itera todos los Producto_Talla con lotes y por cada uno llama obtener_valor_inventario_fifo() y obtener_costo_promedio_fifo() (2+ queries por fila). Se invoca desde el módulo de existencias.
  - *Fix*: Reescribirlo con la misma query anotada del dashboard (Sum(cantidad_disponible*costo_unitario) con filter=) y paginarlo, o eliminarlo y apuntar los dos botones de verGestionProductos.html al endpoint del dashboard.
  - *Ref*: `app/views.py:22532-22573; consumidores en app/templates/vistas/modulo_existencias/verGestionProductos.html:14547 y :14666`
- [baja] **Redundancia con el reporte de resumen de existencias y con el Plan de Liquidación** — El bloque 'capital >180d por categoría/marca' replica lo que ya entrega /app/reportes/plan-liquidacion/ (4 dimensiones + drill-down de antigüedad FIFO paginado + Excel) y la valorización replica /app/reportes/resumen-existencias/.
  - *Fix*: Al reorientar el dashboard a 'Salud de Inventario', dejar el aging como resumen ejecutivo con link directo al Plan de Liquidación (no duplicar el detalle) y quitar la tabla SKU-talla completa.
  - *Ref*: `app/urls.py:1279-1285 (plan-liquidacion) y app/urls.py:1308-1315 (resumen-existencias)`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Unidades descalzadas lotes vs stock plano (y n° de SKU-talla afectados) | Σ /Producto_Talla.stock − Σ LoteProducto.cantidad_disponible (activo, no agotado)/ por sucursal | Producto_Talla.stock + LoteProducto | Es EL indicador de reconciliación que hoy falta; si se mantiene ≈0 confirma que el FIFO está sano y justifica jubilar el KPI de 'diferencia de valor'. | bajo |
| Cobertura en días por sucursal (línea base histórica ~641 días) | stock_actual_unidades / (unidades vendidas últimos 90 días / 90) | Producto_Talla.stock + Ticket_Productos con Ticket.created_at y estado PAGADO | Es la métrica que decide comprar o liquidar; hoy el dashboard de inventario no mira ventas en absoluto. | medio |
| Antigüedad ponderada por capital | Σ(días_lote × cantidad_disponible × costo_unitario) / Σ(cantidad_disponible × costo_unitario) | LoteProducto | Reemplaza el promedio simple actual, que está dominado por la cola de SKUs con 1 unidad. | bajo |
| % de SKU-talla con stock y sin venta en 90/180 días, y su capital a costo | count(SKU con stock>0 sin Ticket_Productos en el período) / count(SKU con stock>0); valor = Σ(stock × costo) | Producto_Talla + Ticket_Productos (created_at) | Traduce 'stock muerto' a plata a costo (comparable con el aging), no a precio de venta como hace el dashboard de productos. | medio |
| Adherencia FIFO real | unidades consumidas desde el lote más antiguo disponible / unidades totales consumidas del período | movimientos de consumo de lote (LoteProducto / kardex) | Si es alta, el sistema costea bien y el dashboard puede dejar de vigilar lotes; si es baja, hay un bug de consumo que sí hay que perseguir. | alto |
| Capital viejo por sucursal y su variación mes a mes | Σ(cantidad_disponible × costo_unitario) con fecha_ingreso ≤ hoy-180d, comparado con el cierre del mes anterior | LoteProducto | Convierte el aging (hoy estático) en un indicador de gestión: ¿estamos drenando el capital viejo o creciendo? | medio |

### Dashboard Despachos y Recepciones — `MEJORABLE`

- **Ruta**: URL /app/dashboard-despachos/ (urls.py:1505) → app/views_dashboards_kpi.py:417; APIs /app/api/dashboard-despachos/datos/ (views_dashboards_kpi.py:588) y /app/api/dashboard-despachos/flujo/ (views_dashboards_kpi.py:431). Template app/templates/vistas/modulo_dashboards/dashboard_despachos.html. Menú: layout/menu.html:2058-2062.
- **Audiencia**: Jefatura de logística/CD y jefes de tienda. Decide qué guías están en tránsito sin recepcionar, qué hay que regularizar y qué origen genera errores.
- **Muestra**: Dos zonas. ZONA DESPACHO (endpoint /flujo/): 5 KPIs (unidades despachadas, valorizado a costo, guías, CD activos, sucursales atendidas), Sankey CD→sucursal, matriz CD × sucursal en unidades, barras de marca despachada por sucursal, serie temporal (día/semana/mes) y detalle por sucursal destino con último despacho. ZONA RECEPCIÓN (endpoint /datos/): KPIs 'DTEs por recibir' y 'por regularizar' (sin filtro de fecha), total traspasos, tasa de éxito, días promedio de recepción, pendientes y sobrantes; pipeline por estado_dte; tabla por sucursal con % OK/faltantes/dañados/sobrantes; tendencia mensual de problemas; lista de regularizaciones pendientes; matriz origen→destino; atribución de errores por origen vía NC; y detalle de los 30 DTEs incompletos más antiguos con acción directa a /app/recepcion-dte/.
- **Filtros**: Fecha inicio/fin, checkbox 'Todas las sucursales', selector de CD y granularidad (día/semana/mes). Server-side: el rango y el CD sí se aplican; el checkbox 'todas' SOLO lo respeta /datos/ y además exige permiso (views_dashboards_kpi.py:596-600). El endpoint /flujo/ ignora el parámetro 'todas' y tampoco filtra por sucursal en ningún caso: siempre muestra toda la empresa. La empresa sale de request.session['idEmpresaActual'] sin validar contra EmpresaUser.
- **Fidelidad**: La zona de despacho es la mejor pieza analítica del grupo y sigue las reglas duras del kardex: detecta el traspaso por concepto='TRASPASO_SALIDA' (no por tipo_movimiento), cuenta solo la pierna de salida para no duplicar con TRASPASO_ENTRADA, usa abs() sobre cantidades negativas, exige sucursal_destino no nulo y define 'es CD' con un OR entre es_centro_distribucion y tipo_sucursal (views_dashboards_kpi.py:421-467). Marca = atributo1 (correcto). No repite el bug 'ROI = IVA'. Los problemas de fidelidad son de scoping, de mezcla de unidades de medida (DTEs vs líneas) y de que las dos zonas usan bases de fecha distintas (Movimientos_Producto.fecha vs Dte.fecha_emision), por lo que sus totales no reconcilian con el mismo rango.
- **Rendimiento**: Bien: agregaciones en DB, un solo group-by para árbol+matriz, tops acotados (10-40 filas). El único costo apreciable es el .distinct() que se agrega al filtrar por sucursal sobre el join dte_movimientos (views_dashboards_kpi.py:612-615 y 634-637). No hay N+1.

**Problemas:**

- [alta] **El endpoint /flujo/ no aplica ningún filtro de sucursal ni valida el permiso multi-sucursal** — api_despachos_flujo filtra solo por empresa de sesión y CD; nunca usa suc_id ni comprueba PermisoUsuario.usuario_ve_todas_sucursales. Cualquier usuario con login (un vendedor de tienda) ve las unidades y el VALORIZADO A COSTO de todos los CD de la empresa. Es incoherente con /datos/, que sí exige permiso para ver todas las sucursales.
  - *Fix*: Aplicar el mismo gate que /datos/: sin permiso multi-sucursal, restringir a sucursal_destino = sucursal de sesión (la tienda ve lo que le llega); y ocultar la columna 'valorizado a costo' a quien no tenga permiso de ver costos.
  - *Ref*: `app/views_dashboards_kpi.py:446-467 (sin gate) vs app/views_dashboards_kpi.py:595-601 (gate en /datos/)`
- [alta] **La empresa se toma de la sesión sin validar contra EmpresaUser** — _get_sucursal_empresa() devuelve request.session['idEmpresaActual'] crudo y ambos endpoints hacen `if emp_id:`; si la sesión no trae empresa, NO se aplica ningún filtro y el dashboard muestra datos de todas las empresas del sistema.
  - *Fix*: Resolver la empresa con obtener_empresas_usuario(request.user) y, si el resultado es vacío, devolver queryset vacío en vez de 'sin filtro'. Nunca dejar que la ausencia de sesión signifique 'ver todo'.
  - *Ref*: `app/views_dashboards_kpi.py:35-38 y usos en :458-459, :608-611; helper en app/utils_permisos.py:185-205`
- [media] **Las dos zonas del dashboard usan bases de fecha distintas y no reconcilian** — La zona de despacho filtra Movimientos_Producto.fecha (fecha del movimiento físico) y la zona de recepción filtra Dte.fecha_emision / Productos_Recepcionados.fecha_recepcion. Con el mismo rango, 'unidades despachadas' y 'total traspasos' pueden diferir estructuralmente y el usuario lo lee como error de datos.
  - *Fix*: Anclar ambas zonas al mismo eje (fecha del movimiento de salida) o rotular explícitamente cada zona con la fecha que usa.
  - *Ref*: `app/views_dashboards_kpi.py:452-456 vs app/views_dashboards_kpi.py:603-607 y :639-642`
- [media] **KPIs que mezclan unidades de medida: 'Total Traspasos' son DTEs y 'Tasa de Éxito' son líneas** — total_traspasos = traspasos_qs.count() (documentos), pero total_recepciones/total_ok/tasa_exito se calculan sobre filas de Productos_Recepcionados (líneas de producto). La 'tasa de éxito' que el usuario lee como '% de guías bien recibidas' es en realidad '% de líneas OK'.
  - *Fix*: Rotular explícitamente ('líneas recepcionadas OK') y agregar una tasa de éxito a nivel de guía = DTEs en RECEPCIONADO_COMPLETO / DTEs recepcionados.
  - *Ref*: `app/views_dashboards_kpi.py:617 vs app/views_dashboards_kpi.py:644-646`
- [media] **Unidades y valorizado usan abs(SUMA) en vez de SUMA(abs)** — kpis['unidades'] = abs(Sum('cantidad')) y kpis['valor'] = abs(Sum(cantidad*costo)). Si el histórico migrado trae alguna TRASPASO_SALIDA con cantidad positiva (dato conocido como sucio en el kardex legacy), esas filas restan silenciosamente en vez de sumar.
  - *Fix*: Usar Sum(Abs('cantidad')) / Sum(Abs(F('cantidad')*F('costo'))) o excluir explícitamente cantidad__gte=0 y reportar aparte cuántas filas anómalas hay.
  - *Ref*: `app/views_dashboards_kpi.py:465-484`
- [media] **Los traspasos tienda→tienda quedan fuera de toda la zona de despacho** — El qs base exige que el origen sea CD (_es_cd_q('sucursal_origen')). Los movimientos entre tiendas no aparecen en KPIs, Sankey, matriz, marca ni serie temporal; solo asoman en la matriz origen→destino de la zona de recepción. Un jefe de tienda que despacha a otra tienda no ve su operación.
  - *Fix*: Añadir un toggle 'Solo desde CD / Toda la red' que quite el predicado _es_cd_q, manteniendo CD como default.
  - *Ref*: `app/views_dashboards_kpi.py:452-457`
- [baja] **El checkbox 'Todas las sucursales' no afecta la mitad superior de la pantalla** — cargarDatos() añade todas=1 a ambos fetch, pero /flujo/ nunca lee ese parámetro. El usuario marca/desmarca y la zona de despacho no cambia, lo que hace desconfiar de todo el tablero.
  - *Fix*: O bien /flujo/ respeta el parámetro (junto con el gate de permiso del punto 1), o se mueve el checkbox al bloque de recepción con una etiqueta que aclare su alcance.
  - *Ref*: `dashboard_despachos.html:512-518 y :553-560 vs app/views_dashboards_kpi.py:446-451`
- [baja] **'Días Prom. Recepción' se trunca a días enteros** — avg_dias = round(avg_dias_raw.days, 1): .days descarta la parte fraccionaria del timedelta, así que un promedio de 0,8 días se muestra como 0. Además solo considera DTEs con fecha_recepcion seteada (las recepciones parciales sin cierre no entran).
  - *Fix*: avg_dias = round(avg_dias_raw.total_seconds()/86400, 1) y agregar el conteo de DTEs sin fecha de recepción como nota al pie.
  - *Ref*: `app/views_dashboards_kpi.py:658-670`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Fill rate del CD (nivel de servicio a tienda) | unidades efectivamente despachadas / unidades solicitadas (requerimiento o pedido de reposición) × 100 | Movimientos_Producto TRASPASO_SALIDA vs la solicitud de origen (Requerimiento / pedido de reposición) | Es el KPI universal de un CD y hoy no existe: solo se ve lo que salió, nunca lo que se pidió y no se envió. | alto |
| Lead time CD→tienda (mediana y percentil 90) | percentil de (fecha_recepcion − fecha del movimiento de salida) en días, por par CD→sucursal | Movimientos_Producto.fecha + Productos_Recepcionados.fecha_recepcion | El promedio actual esconde la cola larga; el p90 es lo que define si una tienda queda quebrada esperando reposición. | medio |
| Backlog en tránsito: unidades y $ a costo con más de N días sin recepcionar | Σ(unidades × costo) de guías en estado EMITIDO con antigüedad > 7/15/30 días, por sucursal destino | Dte (estado EMITIDO) + Movimientos_Producto asociados | Hoy solo se cuentan documentos ('DTEs por recibir'); ponerle unidades y plata prioriza la cobranza operativa. | bajo |
| Merma en tránsito | (unidades faltantes + dañadas) / unidades despachadas × 100, por CD de origen | Productos_Recepcionados (cantidad_danada, faltantes) vs unidades despachadas | Convierte el conteo de líneas problemáticas en una tasa comparable entre CD, que es como se audita a una bodega. | medio |
| Cobertura post-despacho de la tienda destino | días de cobertura de la tienda después de recibir el traspaso = stock resultante / venta diaria 90d | Producto_Talla.stock + Ticket_Productos (created_at) | Permite detectar el patrón real del negocio (sobre-stock: cobertura 230-800 días) y frenar despachos que empeoran el problema. | alto |
| Reincidencia de error por origen | n° de guías con NC o regularización / n° de guías emitidas por ese origen, últimos 90 días, con tendencia | Dte (es_nota_credito + documento_afectado) y Productos_Recepcionados | Ya existe la atribución de errores por origen pero sin serie temporal: no se sabe si una bodega está mejorando o empeorando. | bajo |

### Dashboard Documentos / DTE — `MEJORABLE`

- **Ruta**: URL /app/dashboard-documentos/ (urls.py:1501) → app/views_dashboards_kpi.py:67; API /app/api/dashboard-documentos/datos/ (views_dashboards_kpi.py:73). Template app/templates/vistas/modulo_dashboards/dashboard_documentos.html. Menú: layout/menu.html:2051-2055.
- **Audiencia**: Administración/finanzas y encargado de facturación electrónica. En teoría decide sobre deuda, recepción de DTEs de proveedor e incidencias SII.
- **Muestra**: 4 KPIs grandes (Total Documentos con variación, Monto Facturado, Pendientes Recepción, Deuda Pendiente), tira de 10 mini-KPIs (facturas, boletas, NC, guías, pagos vencidos, % aceptados SII, ticket promedio, incidencias, días de crédito promedio, tasa NC/facturas), pipeline de traspasos con % por estado y tarjetas por sucursal origen, tabla de top proveedores con % pendiente de pago y 7 gráficos (evolución mensual cantidad+monto, por tipo de documento, compras vs ventas, estado de pago, estado DTE, top emisores, por sucursal).
- **Filtros**: Solo fecha inicio/fin (default: mes en curso). NO hay filtro de sucursal, de empresa, de tipo de transacción ni de tipo de documento en la UI, pero el backend SÍ filtra en silencio por request.session['idSucursalActual'] y por idEmpresaActual: el usuario no tiene forma de saber si está viendo su tienda o la empresa completa. Si la sesión no trae empresa, no se aplica ningún filtro (datos de todas las empresas).
- **Fidelidad**: Es el dashboard con más KPIs inventados o mal etiquetados del grupo. Excluye descartado=False (bien), pero NO excluye es_manual ni es_por_concepto, no distingue compra de venta en los montos y suma las notas de crédito en positivo. Verificado que NO repite el bug 'ROI = IVA' (no hay monto_con_iva − monto_neto aquí). Es en gran medida redundante: el pipeline de traspasos duplica lo que hace mejor el Dashboard de Despachos, y el desglose por tipo/estado duplica el reporte de documentos emitidos del módulo de documentos.
- **Rendimiento**: Bien. ~20 agregaciones en DB sobre un mismo queryset con tops acotados a 10 filas y sin N+1. El costo crece con el rango de fechas, pero el default es el mes en curso.

**Problemas:**

- [alta] **'Monto Facturado' y 'Ticket Promedio' suman compras + ventas + traspasos + notas de crédito en positivo** — monto_total = Sum('monto_con_iva') sobre TODOS los DTEs del período sin filtrar tipo_transaccion ni signo. Mezcla facturas de proveedor (COMPRA), ventas, guías de traspaso interno (valorizadas a costo/precio) y notas de crédito, que deberían restar. El número que preside el dashboard no corresponde a ninguna magnitud contable, y el 'ticket promedio' que se deriva de él (monto_total/total_dtes) tampoco.
  - *Fix*: Separar en tres tarjetas: Ventas netas (tipo_transaccion VENTA_PUBLICO/VENTA, menos NC de venta que van con tipo_transaccion DEVOLUCION/ANULACION), Compras (COMPRA, menos NC de compra) y Traspasos (unidades, no $). Excluir siempre TRASPASO del monto facturado.
  - *Ref*: `app/views_dashboards_kpi.py:87-90 (monto_total) y :148 (ticket_promedio)`
- [alta] **'% Aceptados SII' no mide aceptación del SII** — pct_aceptados usa estado_dte in ('ACEPTADO','RECEPCIONADO_COMPLETO'), que son estados INTERNOS del flujo de recepción de mercadería, no el acuse del SII. Las boletas electrónicas del POS quedan en EMITIDO para siempre, así que el indicador baja mecánicamente con el volumen de boletas y sugiere un problema tributario inexistente.
  - *Fix*: Renombrar a '% documentos cerrados en el flujo interno' o conectarlo al estado real de acuse (trazabilidad DTE / Acepta) si existe el dato; si no existe, quitar la tarjeta.
  - *Ref*: `app/views_dashboards_kpi.py:106-109 y el rótulo en dashboard_documentos.html:417-423`
- [alta] **'Deuda Pendiente' mezcla cuentas por pagar y cuentas por cobrar** — monto_pendiente_pago = Sum(monto_con_iva) con estado_pago='PENDIENTE' sobre el queryset completo, que incluye tanto facturas de compra (lo que debemos) como facturas de venta a crédito (lo que nos deben). Sumarlas en una sola tarjeta 'Deuda Pendiente' es incorrecto en cualquier lectura financiera.
  - *Fix*: Dividir en 'Por pagar (compras)' y 'Por cobrar (ventas)', ambas con aging 0-30/31-60/61-90/>90 días sobre fecha_vencimiento.
  - *Ref*: `app/views_dashboards_kpi.py:123-127`
- [alta] **'Pendientes Recepción' está inflado permanentemente por las compras por concepto** — Cuenta receptor=empresa con estado_dte='EMITIDO'. Las compras por concepto (es_por_concepto=True) son stock-neutral y NUNCA se recepcionan, igual que los DTEs manuales de cuadratura (es_manual=True); ambas se quedan en EMITIDO para siempre y engordan el KPI mes a mes.
  - *Fix*: Excluir es_por_concepto=True y es_manual=True del KPI, y añadir un desglose 'antigüedad del pendiente' (>7/>30 días) para que sea accionable.
  - *Ref*: `app/views_dashboards_kpi.py:117-121; campos en app/models/dte.py:188-202`
- [media] **'Tasa NC / Facturas' mezcla notas de crédito de compra y de venta** — tasa_nc = notas_credito/facturas donde ambos conteos son por tipo_documento sobre el universo completo (compras + ventas). Una NC de proveedor sobre una factura de compra contamina el indicador que el usuario lee como 'calidad de mi facturación de venta'.
  - *Fix*: Calcular por tipo_transaccion: tasa NC de venta = NC con tipo_transaccion DEVOLUCION/ANULACION sobre boletas+facturas de venta; y tasa NC de compra aparte.
  - *Ref*: `app/views_dashboards_kpi.py:111-112 y :149`
- [alta] **Filtro de sucursal invisible: se aplica desde la sesión sin decirlo y sin validar permisos** — base_qs se filtra por sucursal_id de sesión y por empresa de sesión, pero la UI solo ofrece fechas. Dos usuarios ven cifras distintas para el mismo período sin explicación, y si la sesión no trae empresa el dashboard cruza empresas.
  - *Fix*: Agregar selector de empresa/sucursal poblado con obtener_empresas_usuario()/obtener_sucursales_usuario(), mostrar el alcance activo en el header y no permitir el caso 'sin filtro'.
  - *Ref*: `app/views_dashboards_kpi.py:74-83; UI en dashboard_documentos.html:334-338`
- [media] **La variación 'vs mes anterior' es inválida para cualquier rango que no sea el mes en curso** — El comparativo siempre toma el mes calendario anterior al día de inicio del rango (mes_anterior_fin = inicio − 1 día; mes_anterior_inicio = ese día con day=1). Si el usuario pide 3 días, o un año completo, se compara contra un mes calendario entero: la flecha de tendencia miente.
  - *Fix*: Comparar contra un período inmediatamente anterior de la MISMA duración (fin_prev = inicio-1d; inicio_prev = fin_prev − (fin-inicio)).
  - *Ref*: `app/views_dashboards_kpi.py:254-263`
- [media] **Redundancia: el pipeline de traspasos duplica (peor) al Dashboard de Despachos** — Los bloques 'flujo' y 'flujo_por_origen' recalculan el pipeline de traspasos a partir de Dte, mientras el Dashboard de Despachos hace lo mismo con más detalle, atribución de errores y desde el kardex. Además aquí 'unidades_despachadas' sale de Dte.unidades_productos (cabecera), que es un campo conocido por venir en 0 en parte del histórico de pedidos ecommerce.
  - *Fix*: Eliminar la sección de traspasos de este dashboard y dejar un enlace a /app/dashboard-despachos/.
  - *Ref*: `app/views_dashboards_kpi.py:225-252 vs app/views_dashboards_kpi.py:431-583`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Cuentas por pagar y por cobrar con aging real | Σ monto_con_iva por tramo de días vencidos (0-30/31-60/61-90/>90) sobre fecha_vencimiento, separando COMPRA de VENTA | Dte.estado_pago + fecha_vencimiento + tipo_transaccion | Reemplaza la tarjeta 'Deuda Pendiente' por lo que realmente se usa para gestionar caja. | bajo |
| Ventas netas de notas de crédito | Σ ventas (VENTA_PUBLICO) − Σ NC de venta (tipo_transaccion DEVOLUCION/ANULACION), por mes | Dte + Ticket (created_at, dte_generado para no duplicar POS→DTE) | Hoy las NC suman en positivo; el neto es la única cifra comparable con el reporte ventas-global ya corregido. | medio |
| DSO / DPO (días de cobro y de pago) | DSO = (cuentas por cobrar / ventas a crédito del período) × días; DPO análogo con compras | Dte + estado_pago + diasCredito | El 'días de crédito promedio' actual es el promedio del campo pactado, no el comportamiento real de pago. | medio |
| Antigüedad del pendiente de recepción | conteo y $ de DTEs receptor=empresa en EMITIDO agrupados por >7 / >15 / >30 días desde fecha_emision, excluyendo es_por_concepto y es_manual | Dte | Convierte un contador que solo crece en una cola de trabajo priorizada. | bajo |
| Incidencias DTE abiertas por antigüedad y responsable | Dte_Incidencia en PENDIENTE/EN_GESTION agrupadas por días abiertos y por sucursal emisora | Dte_Incidencia | Hoy solo se muestra el total de incidencias abiertas, sin ninguna pista de dónde ni desde cuándo. | bajo |
| Continuidad de folios por tipo de documento y sucursal | detección de saltos y de números repetidos en numero_documento por (tipo_documento, emisor, sucursal) en el período | Dte.numero_documento | Riesgo tributario ya materializado en la casa (PAO4 reusó 368 folios de PAO3); ningún tablero lo vigila. | medio |

### Dashboard Existencias / Productos (mejorado) — `ROTO`

- **Ruta**: URL /app/dashboard_productos_mejorado/ y /app/dashboard_productos/ (urls.py:704-706) → app/views.py:24623 dashboard_productos_mejorado; API app/views.py:24630 dashboard_productos_mejorado_api. Template app/templates/vistas/modulo_dashboards/dashboard_productos_mejorado.html. Menú: Dashboard › Dashboard Existencias (layout/menu.html:2033-2038).
- **Audiencia**: Gerencia/compras: valor del inventario, rotación, stock muerto, ABC y productos por reponer.
- **Muestra**: KPIs de catálogo (productos, SKUs, con stock, agotados), valorización a costo (FIFO desde lotes) y a precio de venta, margen potencial y %, rotación, días de cobertura, velocidad de venta, flujo del período (compras recepcionadas, ventas, traspasos in/out, ajustes), clasificación ABC, stock muerto (conteo, $ y %), flujo mensual de 6 meses, distribución por categoría, estado del stock, top 10 vendidos, bajo stock con días de cobertura, lenta rotación, traspasos recientes, valorización por sucursal, alertas y tabla de 50 productos con margen y rotación.
- **Filtros**: UI con categoría, sucursal, estado de stock y período (días). Los cuatro se envían al backend, PERO el filtro de SUCURSAL no se aplica al queryset principal: solo se usa para la valorización a costo desde lotes. Categoría, estado de stock y período sí se aplican. No hay ningún filtro de empresa en toda la vista.
- **Fidelidad**: El más comprometido del grupo. Excluye excluir_de_analitica en el queryset de tallas y usa correctamente Ticket.created_at + estado PAGADO + subtotal para ventas (views.py:24711-24721, ya corregido en la cirugía de julio). Pero el scoping está roto de raíz: los KPIs de unidades/valor de venta/ABC/stock muerto son de TODAS las empresas y sucursales, mientras el valor a costo sí se filtra por sucursal, lo que produce un 'margen potencial' sin sentido. No repite el bug 'ROI = IVA', pero comete uno equivalente en gravedad: restar dos magnitudes con alcances distintos.
- **Rendimiento**: Malo en dos puntos: (1) carga en memoria el set completo de ProductoTalla_id con movimiento en 90 días y luego hace exclude(id__in=set) — set de cientos de miles de ids convertido en SQL gigante, usado DOS veces; (2) el ABC trae a Python todas las filas con stock>0 para acumular el 80/95%. En un catálogo de este tamaño son los dos cuellos de botella del endpoint. El resto son agregaciones sanas.

**Problemas:**

- [alta] **El filtro de sucursal se recibe y no se aplica: KPIs globales contra costo por sucursal** — sucursal_id se lee en views.py:24643 y solo se usa en lotes_filter (views.py:24690-24691). productos_talla_qs —del que salen total_skus, stock_total, valor_venta, ABC, estado de stock, stock muerto, bajo stock, lenta rotación y la tabla— nunca se filtra por sucursal. Resultado: al elegir una sucursal, el 'valor a costo' baja a esa sucursal pero el 'valor a precio de venta' sigue siendo el de toda la red, y margen_potencial = valor_venta_global − valor_costo_sucursal queda inflado sin límite.
  - *Fix*: Aplicar el filtro al queryset base: productos_talla_qs.filter(producto__sucursal_id=sucursal_id) (validando antes con puede_ver_sucursal), o quitar el selector si el dashboard se define como consolidado. El margen debe calcularse siempre sobre el mismo universo.
  - *Ref*: `app/views.py:24641-24666 (filtros), :24690-24696 (único uso de sucursal) y :24699-24708 (margen)`
- [alta] **Sin scoping de empresa: fuga de datos entre empresas del holding** — Ni productos_talla_qs, ni las ventas, ni los movimientos, ni la valorización por sucursal filtran por empresa. Además el selector de sucursal se puebla con Sucursal.objects.all(), exponiendo los nombres de sucursales de otras empresas, y total_productos = Producto.objects.count() cuenta el catálogo completo del sistema sin excluir siquiera excluir_de_analitica.
  - *Fix*: Filtrar todo por producto__sucursal__empresa_id in obtener_empresas_usuario(request.user) y poblar el selector con obtener_sucursales_usuario().
  - *Ref*: `app/views.py:24651-24653, :24680, :24713-24721, :24733-24742 y :25061-25064`
- [alta] **El botón 'Exportar' descarga el catálogo completo de todas las empresas, sin filtros** — exportarReporte() llama a /app/exportar_dashboard_productos/ sin parámetros y esa vista recorre Producto_Talla.objects.all() sin filtro de sucursal, empresa, categoría ni excluir_de_analitica, escribiendo un CSV con SKU, stock y precio de venta de todo el sistema. Es accesible para cualquier usuario autenticado.
  - *Fix*: Eliminar ese botón (ya existe 'exportarTabla' → exportar_productos_filtrado, que sí respeta filtros) o reescribir la vista para que reciba y aplique los mismos filtros + scoping.
  - *Ref*: `app/views.py:24506-24538 y dashboard_productos_mejorado.html:1569-1571 (vs :1573-1581 la versión filtrada)`
- [media] **'Rotación' no es rotación de inventario** — rotacion = unidades vendidas del período / stock_total actual. La rotación de inventario es COGS / inventario promedio (o unidades vendidas / stock promedio, anualizado). Tal como está, depende del largo del período elegido y compara ventas de una ventana contra un stock puntual; además la alerta 'rotación baja' se dispara con umbral fijo 0,5 sobre esa métrica.
  - *Fix*: Anualizar: rotación = (unidades vendidas / días del período) × 365 / stock promedio; o mostrar directamente días de cobertura, que es lo que el negocio ya usa.
  - *Ref*: `app/views.py:24726-24729 y la alerta en :25003-25008`
- [media] **'Stock muerto' se valoriza a precio de venta y no a costo** — stock_muerto_valor = Σ(stock × precioventa) y stock_muerto_porcentaje se calcula sobre valor_venta. El capital inmovilizado del negocio se mide a costo (como sí hace el dashboard FIFO), así que las dos pantallas dan cifras incomparables para el mismo fenómeno.
  - *Fix*: Valorizar a costo (lotes o producto.costo) y mostrar el precio de venta solo como 'venta potencial'.
  - *Ref*: `app/views.py:24757-24770`
- [media] **'Traspasos recientes' usa un modelo que en la práctica no se usa, con el error silenciado** — Lee el modelo Traspaso/Traspaso_Detalle dentro de un try/except Exception: pass. Los traspasos reales del sistema son DTE de tipo TRASPASO + Movimientos_Producto con concepto TRASPASO_SALIDA/ENTRADA, así que la tabla queda habitualmente vacía y cualquier fallo se traga sin log.
  - *Fix*: Reemplazar por la consulta canónica del kardex (concepto='TRASPASO_SALIDA', COALESCE(destino, origen) para la sucursal de entrada) y quitar el except mudo.
  - *Ref*: `app/views.py:24927-24950`
- [alta] **Dos consultas que materializan el catálogo entero en memoria** — productos_con_movimiento_ids es un set() de todos los ProductoTalla con movimiento en 90 días, usado en dos exclude(id__in=...); y el ABC hace list() de todas las filas con stock>0 para acumular porcentajes en Python.
  - *Fix*: Sustituir el set por un subquery/EXISTS anti-join en SQL, y el ABC por una window function (PERCENT_RANK / SUM OVER) o por un cálculo sobre el top-N valorizado.
  - *Ref*: `app/views.py:24750-24766 y :24772-24792`
- [media] **Redundante con el reporte de resumen de existencias, con el Plan de Liquidación y con el dashboard FIFO** — Valorización por sucursal, distribución por categoría, stock muerto y ABC ya están cubiertos —con mejor scoping— en /app/reportes/resumen-existencias/ y /app/reportes/plan-liquidacion/; el aging de capital está en el dashboard FIFO.
  - *Fix*: Consolidar: un solo tablero 'Salud de Inventario' (ver propuesta del FIFO) y dejar este dashboard como vista de catálogo/reposición (bajo stock, top vendidos, agotados) o retirarlo.
  - *Ref*: `app/urls.py:1279-1285 y :1308-1315`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Días de cobertura por sucursal y por categoría v1.2 | stock / (unidades vendidas 90d / 90), agregado por sucursal y por categoría padre›hijo | Producto_Talla + Ticket_Productos (created_at, PAGADO) + Categoria | A nivel empresa todo sale sobre-stock; la señal accionable aparece al abrir por sucursal y categoría. | medio |
| Quiebre de stock real (SKU con venta y sin stock) | count(SKU con ventas en 30d y stock=0 hoy) / count(SKU con ventas en 30d) por sucursal | Producto_Talla.stock + Ticket_Productos | 'Agotados' hoy cuenta SKUs muertos que nunca se venden; solo el quiebre de SKUs con demanda representa venta perdida. | medio |
| Curva de tallas completa (% de tallas core disponibles por modelo) | tallas con stock>0 / tallas del modelo, sobre los modelos que concentran el 80% de la venta | Producto_Talla agrupado por producto | En calzado la venta se pierde por curva rota, no por stock total; ninguna pantalla lo mide. | alto |
| Margen bruto real (no potencial) | (Σ subtotal de ventas − Σ costo FIFO de lo vendido) / Σ subtotal, por período | Ticket_Productos.subtotal + costo del lote consumido | Reemplaza el 'margen potencial' hoy roto por una cifra realizada; y evita repetir el error del reporte de compras donde el margen era el IVA. | alto |
| Capital inmovilizado a costo por antigüedad de ingreso | Σ(cantidad_disponible × costo_unitario) por tramo 0-90/90-180/180-365/>365 días | LoteProducto | Unifica la métrica con el dashboard FIFO para que ambas pantallas den el mismo número. | bajo |
| Reposición sugerida (SKU bajo punto de pedido con venta activa) | stock < venta_diaria_90d × lead_time_CD_p90, filtrando SKUs con venta > 0 en 90d | Producto_Talla + Ticket_Productos + lead time del dashboard de despachos | Convierte la tabla 'bajo stock' (hoy un simple stock<10, sin demanda ni lead time) en una lista accionable de reposición. | alto |

### Dashboard Productos (versión legacy) — `REDUNDANTE`

- **Ruta**: Template app/templates/vistas/modulo_dashboards/dashboard_productos.html (1.030 líneas) — SIN URL propia: /app/dashboard_productos/ está redirigido al mejorado (urls.py:704). La vista existe TRIPLICADA en app/views.py:24157, app/views_modulo_existencias.py:1177 y app/views_modulo_productos.py:1059, ninguna registrada. Sus APIs siguen expuestas: obtener_datos_dashboard_productos (urls.py:707, app/views.py:24165), filtrar_productos_dashboard (urls.py:708, app/views.py:24451).
- **Audiencia**: Ninguna — no es alcanzable desde el menú ni desde ninguna URL viva. Solo consume mantenimiento y confunde a quien busca el código del dashboard de productos.
- **Muestra**: Versión anterior del dashboard de productos (KPIs de catálogo, gráficos y tabla filtrable). Su único consumidor es el propio template huérfano.
- **Filtros**: Irrelevante: nadie llega a la pantalla. Los endpoints AJAX quedan accesibles vía URL directa para cualquier usuario autenticado, sin scoping de empresa.
- **Fidelidad**: No auditada en profundidad por estar muerta; el riesgo real es de superficie expuesta (endpoints sin consumidor pero accesibles) y de confusión: tres copias del mismo código en tres archivos distintos, ninguna es la que corre.
- **Rendimiento**: N/A.

**Problemas:**

- [media] **Template y vistas huérfanas triplicadas** — dashboard_productos.html no tiene ruta viva y la función dashboard_productos está copiada en views.py, views_modulo_existencias.py y views_modulo_productos.py. Cualquier corrección futura corre el riesgo de aplicarse a la copia equivocada.
  - *Fix*: Borrar el template y las tres copias de la vista; dejar únicamente dashboard_productos_mejorado.
  - *Ref*: `app/views.py:24157, app/views_modulo_existencias.py:1177, app/views_modulo_productos.py:1059 y app/urls.py:704`
- [media] **Endpoints AJAX sin consumidor pero registrados en urls.py** — obtener_datos_dashboard_productos y filtrar_productos_dashboard siguen ruteados; el único template que los llamaba es el huérfano. Quedan como superficie de ataque/consulta sin scoping de empresa.
  - *Fix*: Eliminar las rutas 707 y 708 junto con las vistas, siguiendo el mismo criterio ya aplicado en julio a obtener_metricas_fifo y obtener_analisis_fifo_detallado (comentado en urls.py:698-699).
  - *Ref*: `app/urls.py:707-708 y dashboard_productos.html:606, :916`

### Dashboard Requerimientos (garantías / devoluciones a proveedor) — `MEJORABLE`

- **Ruta**: URL /app/dashboard-requerimientos/ (urls.py:1509) → app/views_dashboards_kpi.py:305; API /app/api/dashboard-requerimientos/datos/ (views_dashboards_kpi.py:311). Template app/templates/vistas/modulo_dashboards/dashboard_requerimientos.html. Menú: layout/menu.html:2065-2069.
- **Audiencia**: Jefatura de tienda y postventa: seguimiento de productos fallados, garantías y devoluciones a proveedor.
- **Muestra**: 6 KPIs (total, pendientes, esperando respuesta, aprobados, rechazados, tiempo promedio en días), alerta de requerimientos sin respuesta del proveedor hace más de 7 días, y 4 gráficos: distribución por estado, evolución mensual, por tipo de requerimiento y por prioridad.
- **Filtros**: Solo fecha inicio/fin (default mes en curso). El scoping es implícito: si hay idSucursalActual en sesión filtra por esa sucursal, si no filtra por las sucursales de la empresa de sesión; sin validación EmpresaUser y sin selector en pantalla.
- **Fidelidad**: Los conteos por estado/tipo/prioridad son correctos y usan fecha_creacion (DateTimeField auto_now_add, correcto para analítica). Los problemas son de completitud: 3 de los 8 estados posibles no aparecen en ningún KPI, el tiempo de resolución excluye el estado de cierre real (COMPLETADO) y el dashboard no tiene ninguna magnitud en pesos, que es lo que interesa de una garantía.
- **Rendimiento**: Aceptable, salvo que el cálculo del tiempo promedio itera el queryset de resueltos en Python (sum sobre .duracion fila a fila) en vez de agregar en DB. Con volúmenes bajos es irrelevante; con años de histórico crece lineal.

**Problemas:**

- [media] **Los KPIs ignoran 3 de los 8 estados y no cuadran con el total** — ESTADO_REQUERIMIENTO_CHOICES incluye EN_REVISION, EN_PROCESO y COMPLETADO, que no tienen tarjeta. La suma de las tarjetas (pendientes + esperando + aprobados + rechazados) no da el 'Total Requerimientos', y el usuario no ve cuántos están efectivamente cerrados.
  - *Fix*: Agrupar en tres estados de gestión (Abiertos = PENDIENTE+EN_REVISION+ESPERANDO_RESPUESTA+EN_PROCESO; Resueltos = APROBADO+COMPLETADO; Rechazados/Cancelados) y que la suma cuadre con el total.
  - *Ref*: `app/models/requerimientos.py:35-44 vs app/views_dashboards_kpi.py:391-400 y dashboard_requerimientos.html:118-149`
- [media] **'Tiempo Prom. (días)' excluye el estado de cierre real** — Solo promedia requerimientos en APROBADO o RECHAZADO con fecha_resolucion no nula; los COMPLETADO (cierre efectivo) quedan fuera, así que el SLA mostrado no representa el ciclo real. Además el promedio se calcula iterando en Python.
  - *Fix*: Incluir COMPLETADO y calcular con Avg(ExpressionWrapper(...)) en DB; añadir mediana/p90, que es lo que revela los casos colgados.
  - *Ref*: `app/views_dashboards_kpi.py:343-359`
- [media] **'por_sucursal' y 'top_proveedores' se calculan y nunca se muestran** — El endpoint agrega y serializa ambos bloques, pero renderCharts() solo pinta estado, evolución, tipo y prioridad. Es trabajo de DB desperdiciado y, sobre todo, se pierde la información más accionable: qué proveedor concentra las garantías.
  - *Fix*: Renderizar ambos (ranking de proveedores por n° de requerimientos y tabla por sucursal) o quitarlos del payload.
  - *Ref*: `app/views_dashboards_kpi.py:376-388 y :404-408 vs dashboard_requerimientos.html:222-268`
- [alta] **Scoping por sesión sin validar EmpresaUser** — Filtra por request.session['idSucursalActual'] o, en su defecto, por las sucursales de request.session['idEmpresaActual']; si la sesión no trae ninguno, muestra los requerimientos de todas las empresas.
  - *Fix*: Resolver el alcance con obtener_empresas_usuario()/obtener_sucursales_usuario() y devolver vacío si el usuario no tiene asignaciones.
  - *Ref*: `app/views_dashboards_kpi.py:312-321`
- [media] **Sin ninguna métrica en pesos ni aging de la cartera abierta** — El módulo gestiona devoluciones y garantías con proveedor —dinero a recuperar— y el dashboard solo cuenta documentos. La única señal de antigüedad es la alerta binaria de 7 días sobre ESPERANDO_RESPUESTA.
  - *Fix*: Añadir $ comprometido (costo o precio del producto asociado) por estado y un aging 0-7/8-15/16-30/>30 días de los requerimientos abiertos.
  - *Ref*: `app/views_dashboards_kpi.py:370-374 (única señal de antigüedad) y app/models/requerimientos.py:87+ (el modelo tiene producto asociado)`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Monto en recuperación por estado | Σ(costo del producto × cantidad) de requerimientos abiertos, agrupado por estado y proveedor | Requerimiento + Producto_Talla/producto.costo | Es la razón de existir del módulo (recuperar plata del proveedor) y hoy no aparece en ninguna parte. | medio |
| Tasa de aprobación por proveedor | APROBADO / (APROBADO + RECHAZADO) por proveedor, últimos 12 meses | Requerimiento.proveedor + estado | Identifica a los proveedores que sistemáticamente rechazan garantías, insumo directo para la negociación de compra. | bajo |
| Aging de requerimientos abiertos | conteo y $ por tramo de días desde fecha_creacion (0-7/8-15/16-30/>30), por sucursal | Requerimiento.fecha_creacion | Reemplaza la alerta binaria de 7 días por una cola de trabajo priorizada. | bajo |
| Tasa de defecto por marca / modelo | requerimientos tipo PRODUCTO_FALLADO / unidades vendidas de esa marca en el período × 10.000 | Requerimiento + Ticket_Productos + atributo1 (marca) | Convierte reclamos aislados en una señal de calidad comparable entre marcas, útil para decidir recompra. | alto |
| Tiempo de respuesta del proveedor (mediana y p90) | percentiles de (fecha_respuesta_proveedor − fecha_envio_proveedor) | Requerimiento.fecha_envio_proveedor / fecha_respuesta_proveedor | El promedio actual mezcla el tiempo interno con el del proveedor; separarlos dice a quién hay que apurar. | bajo |

### Dashboard Caja / Arqueos — NO EXISTE (código muerto que lo anuncia) — `ROTO`

- **Ruta**: No hay URL, ni vista, ni template. El archivo app/views_dashboards_kpi.py:1-4 declara en su docstring 'API views for KPI dashboards: Documentos/DTE, Caja/Arqueos, Requerimientos, and CRM' y sigue importando ArqueoCaja, Cliente, Proveedor, ContactoEmpresa y Empresa (líneas 22-30) sin usarlos en ninguna función. La analítica de caja vive solo como reporte en app/templates/vistas/modulo_ventas/cuadraturaCaja_v2.html.
- **Audiencia**: Sería el cajero/jefe de local y administración (diferencias de arqueo, faltantes/sobrantes por caja y por usuario). Hoy nadie tiene esa vista consolidada: solo existe la cuadratura documento a documento.
- **Muestra**: Nada. Los dashboards de Caja/Arqueos y de CRM fueron removidos o nunca terminados y quedaron los imports huérfanos como única huella.
- **Filtros**: N/A.
- **Fidelidad**: N/A. Nota relevante para cuando se construya: la cuadratura de caja ya excluye tipo_transaccion='VENTA' y cuenta solo VENTA_PUBLICO, mientras el reporte de ventas todavía incluye 'VENTA' — un dashboard nuevo debe adoptar el criterio de la cuadratura (VENTA_PUBLICO) o replicará la divergencia ya detectada entre ambos.
- **Rendimiento**: N/A.

**Problemas:**

- [baja] **El módulo promete dashboards de Caja/Arqueos y CRM que no existen** — Docstring e imports (ArqueoCaja, Cliente, Proveedor, ContactoEmpresa) sugieren funcionalidad implementada; no hay ninguna URL, vista ni template. Quien lea el archivo o el menú asume que existe.
  - *Fix*: Limpiar el docstring y los imports muertos; decidir explícitamente si se construye el dashboard de caja.
  - *Ref*: `app/views_dashboards_kpi.py:1-4 y :22-30 (imports sin ninguna referencia en el resto del archivo)`
- [media] **No hay ninguna vista consolidada de descuadres de caja** — Las diferencias de arqueo solo se ven arqueo por arqueo desde la cuadratura; no existe agregación por sucursal, por cajero ni por período, ni tendencia de faltantes/sobrantes.
  - *Fix*: Construir un dashboard mínimo de caja sobre ArqueoCaja: diferencia acumulada, % de arqueos con descuadre, ranking por sucursal/usuario y serie mensual, respetando VENTA_PUBLICO y excluyendo excluir_de_analitica.
  - *Ref*: `Modelo ArqueoCaja disponible (importado en app/views_dashboards_kpi.py:24) y reporte existente en app/templates/vistas/modulo_ventas/cuadraturaCaja_v2.html`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Diferencia de arqueo acumulada y % de arqueos descuadrados | Σ(monto_declarado − monto_esperado) y count(/diferencia/ > umbral)/count(arqueos), por sucursal y mes | ArqueoCaja | Es el control interno básico de un retail con caja física y hoy no se mide en ninguna pantalla. | medio |
| Descuadre por cajero | diferencia promedio y máxima por usuario responsable del arqueo, últimos 90 días | ArqueoCaja + usuario | Distingue el error sistémico (todos descuadran igual) del problema individual. | bajo |
| Mix de medios de pago vs efectivo declarado | Σ pagos por medio (efectivo/tarjeta/transferencia) contra lo declarado en el arqueo del día | Dte_Detalle_Pago / pagos de ticket + ArqueoCaja | Detecta ventas cobradas y no declaradas, y cuadra el POS con Transbank. | alto |
| Ventas sin arqueo / días sin cierre de caja | días con ventas VENTA_PUBLICO en una sucursal sin ArqueoCaja asociado | Ticket (created_at) + ArqueoCaja | El agujero de control más barato de cerrar: días operados sin cierre formal. | bajo |

### Fidelización — reporte operativo de puntos (hace de dashboard) — `UTIL`

- **Ruta**: URL /app/fidelizacion/reporte/ (urls.py:1386) → app/views_modulo_fidelizacion.py:73 reporte_fidelizacion_vista; API /app/api/fidelizacion/reporte/ (urls.py:1396 → views_modulo_fidelizacion.py:802 api_reporte_fidelizacion, lógica en :452 construir_reporte_fidelizacion). Template app/templates/vistas/modulo_fidelizacion/reporte.html. NO está en el menú de Dashboards: cuelga del módulo Fidelización.
- **Audiencia**: Administración del programa de puntos y control interno (detección de abuso por vendedor).
- **Muestra**: KPIs de puntos (saldo total vigente, acumulados, bienvenida, canjeados, expirados, todos convertidos a pesos con valor_punto_en_pesos), lista de puntos por vencer en N días por cliente con su próximo vencimiento, últimos 20 canjes con vendedor y ticket, tendencia mensual de 12 meses (ganados/canjeados/expirados), ranking de clientes del mes y un bloque de señales de abuso: concentración de acumulaciones de un mismo cliente en un mismo vendedor, uso de RUT genérico/inválido, acumulación multi-sucursal el mismo día y canje dentro de 48h de la acumulación.
- **Filtros**: desde/hasta (default últimos 30 días), 'vencen_en' (días) y mes para el ranking. Se aplican server-side con conversión tz-aware correcta (make_aware con time.min/time.max en America/Santiago).
- **Fidelidad**: Es el único del grupo con scoping CORRECTO: resuelve las empresas del usuario contra EmpresaUser(status=True) y aplica el mismo alcance a cuentas y movimientos; usa Ticket.created_at (no Ticket.fecha) para el barrido de RUTs sospechosos. Un solo defecto de fidelidad: la señal de concentración cuenta 'días distintos' con Count('ticket__fecha', distinct=True), y Ticket.fecha es DateField(auto_now=True) — se reescribe en cada guardado, así que ese conteo mide la fecha de última modificación, no el día de la venta.
- **Rendimiento**: Bueno en general (agregaciones en DB, tops acotados a 20-30 filas). Un punto a vigilar: los lotes por vencer se recorren en Python fila a fila leyendo lote.saldo_lote, lo que puede volverse N+1 si el programa escala en clientes.

**Problemas:**

- [media] **La señal de fraude 'días distintos' se calcula sobre Ticket.fecha (auto_now)** — dias=Count('ticket__fecha', distinct=True) dentro de las alertas de concentración: Ticket.fecha es auto_now, se reescribe con cada save del ticket, de modo que dos ventas de días distintos pueden colapsar en la misma fecha (o desplazarse a la fecha de un reproceso). La severidad de la alerta ('ALTA' si tickets>=10 o participación>=50%) se apoya parcialmente en ese conteo.
  - *Fix*: Cambiar a Count(TruncDate('ticket__created_at'), distinct=True) — el mismo criterio que ya usa detectar_senales_rut_sospechoso en la línea 388 de este archivo.
  - *Ref*: `app/views_modulo_fidelizacion.py:550 (bug) vs :387-388 (uso correcto de created_at); modelo en app/models/ventas.py:183 fecha = models.DateField(auto_now=True)`
- [baja] **No existe como dashboard: está escondido dentro del módulo Fidelización** — El menú Dashboard (layout/menu.html:2004-2070) no lo lista, así que la gerencia no lo encuentra donde busca los tableros; funcionalmente es un dashboard completo (KPIs + tendencia + ranking + alertas).
  - *Fix*: Añadir el enlace en el submenú de Dashboards (respetando el permiso 'fidelizacion_reporte') o al menos un acceso desde el Dashboard General.
  - *Ref*: `app/urls.py:1386 y app/templates/layout/menu.html:2004-2070`
- [baja] **Los lotes por vencer se recorren en Python** — Se itera lotes_por_vencer y por cada movimiento se lee lote.saldo_lote y lote.cuenta.cliente, agrupando en un dict. Con select_related parcial, el acceso a saldo_lote (propiedad del modelo) puede disparar consultas por fila.
  - *Fix*: Anotar el saldo del lote en la consulta y agrupar por cuenta en DB; limitar a los N clientes con más puntos por vencer.
  - *Ref*: `app/views_modulo_fidelizacion.py:486-514`
- [media] **Sin KPI de impacto económico del programa** — Todo está expresado en puntos y en su equivalente en pesos, pero no hay ninguna medida de si el programa vende más: ni ticket promedio de cliente con puntos vs sin puntos, ni tasa de recompra, ni pasivo de puntos como % de la venta.
  - *Fix*: Añadir el bloque de impacto (ver KPIs propuestos), que es lo que justifica o mata el programa ante gerencia.
  - *Ref*: `app/views_modulo_fidelizacion.py:452-484 (el reporte se agota en conteos de puntos)`

**KPIs propuestos:**

| KPI | Formula | Fuente | Para que | Esf |
|---|---|---|---|---|
| Pasivo de puntos y su cobertura | saldo_puntos total × valor_punto_en_pesos, y ese monto como % de la venta neta de los últimos 12 meses | CuentaPuntos.saldo_puntos + ProgramaFidelizacion.valor_punto_en_pesos + ventas | Es el compromiso contable del programa; hoy se muestra el saldo en puntos pero sin contexto de tamaño respecto al negocio. | bajo |
| Tasa de redención | puntos canjeados / puntos emitidos, acumulado 12 meses | MovimientoPuntos (CANJE vs ACUMULACION+BIENVENIDA+CUMPLEANOS) | Indicador estándar de salud de un programa de fidelidad: una redención muy baja significa que el cliente no percibe valor. | bajo |
| Ticket promedio y frecuencia: cliente identificado vs anónimo | Σ total/ n° tickets con cliente con cuenta de puntos, contra los tickets sin cliente identificado, mismo período | Ticket (created_at, cliente) + CuentaPuntos | Es la única forma de demostrar (o refutar) que el programa levanta la venta. | medio |
| Tasa de recompra a 90 días de los inscritos | % de clientes con cuenta que compran de nuevo dentro de 90 días de su primera compra | Ticket (created_at) + Cliente/CuentaPuntos | Mide retención real, que es el objetivo declarado del programa. | medio |
| Puntos por vencer en 30/60/90 días y su valor en pesos | Σ saldo de lotes con fecha_expiracion en cada tramo × valor_punto | MovimientoPuntos.fecha_expiracion | Hoy hay un solo umbral configurable; los tres tramos permiten planificar campañas de canje antes de que expiren. | bajo |

